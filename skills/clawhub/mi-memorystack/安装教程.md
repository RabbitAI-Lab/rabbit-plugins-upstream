第一步：切换到指定路径
cd ~/.openclaw/workspace/skills/Mi-MemoryStack/xiugai

第二步：执行安装程序
执行 bash install.sh 安装即可

第三步：配置API_KEY
cd ~/.openclaw/workspace/skills/Mi-MemoryStack/scripts

向memory_add.py添加API_KEY
向memory_search添加API_KEY

然后 openclaw gateway restart && openclaw skills list 即可
