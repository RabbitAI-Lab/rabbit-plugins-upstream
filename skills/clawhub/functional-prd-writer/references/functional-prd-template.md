# 功能型 PRD 模板

> 版本：v1.0 | 适用范围：功能需求文档（面向开发实现）

---

## 文档信息

| 字段 | 内容 |
|------|------|
| 功能名称 | |
| 所属模块 | |
| 作者 | |
| 版本 | |
| 状态 | 草稿 / 评审中 / 已确认 |
| 最后更新 | |

---

## 一、功能概述

### 1.1 一句话描述

> [功能] 用于 [场景]，让 [用户] 能够 [完成什么操作]。

### 1.2 要解决的问题

> 具体痛点是什么？用户当前怎么做的？有什么不便？

### 1.3 适用范围

- 目标用户：
- 使用场景：
- 前置条件：

---

## 二、功能需求

### 2.1 功能点列表

| 编号 | 功能点 | 优先级 | 说明 |
|------|--------|--------|------|
| F01 | | P0 | |
| F02 | | P0 | |
| F03 | | P1 | |

### 2.2 功能点详细描述

#### F01：[功能名称]

**行为描述：**
> 用户执行 [操作] 后，系统 [做什么]，展示 [什么结果]。

**输入：**
| 字段 | 类型 | 必填 | 校验规则 | 默认值 |
|------|------|------|----------|--------|
| | | | | |

**输出/展示：**
> 成功后展示什么，失败后展示什么。

**规则：**
- 规则1：
- 规则2：

---

## 三、用户流程

### 3.1 主流程

```
用户进入页面
  → 输入 [字段A]
  → 点击 [按钮B]
  → 系统校验 [规则C]
    → 校验通过 → 执行 [操作D] → 展示 [结果E]
    → 校验失败 → 提示 [错误信息F] → 用户修正
```

### 3.2 异常/分支流程

**分支1：[场景描述]**
```
...
  → 条件判断
    → 情况A → 处理逻辑A
    → 情况B → 处理逻辑B
```

**分支2：[场景描述]**
```
...
```

---

## 四、交互与界面

### 4.1 页面布局

```
┌─────────────────────────────┐
│         页面标题             │
├─────────────────────────────┤
│                             │
│  [区域1] 描述               │
│  ┌───────────────────────┐  │
│  │ 输入框 / 列表 / 卡片   │  │
│  └───────────────────────┘  │
│                             │
│  [区域2] 描述               │
│  ┌───────────┐ ┌─────────┐  │
│  │ 按钮 A    │ │ 按钮 B  │  │
│  └───────────┘ └─────────┘  │
│                             │
└─────────────────────────────┘
```

### 4.2 表单字段

| 字段名 | 标签 | 类型 | 占位提示 | 校验规则 | 必填 |
|--------|------|------|----------|----------|------|
| | | text/password/select/... | | | |

### 4.3 操作反馈

| 操作 | 成功提示 | 失败提示 | 加载状态 |
|------|----------|----------|----------|
| | | | |

### 4.4 特殊状态

| 状态 | 展示方式 |
|------|----------|
| 空数据（无记录） | |
| 加载中 | |
| 网络错误 | |
| 无权限 | |
| 数据过期 | |

---

## 五、数据与规则

### 5.1 数据实体

```
实体名
├── 字段1: 类型 (说明)
├── 字段2: 类型 (说明)
├── 字段3: 类型 (说明)
└── 字段4: 类型 (说明)
```

### 5.2 校验规则

| 规则编号 | 校验对象 | 规则描述 | 错误提示 |
|----------|----------|----------|----------|
| V01 | | | |
| V02 | | | |

### 5.3 业务规则

| 规则编号 | 触发条件 | 执行逻辑 | 优先级 |
|----------|----------|----------|--------|
| B01 | | | |
| B02 | | | |

### 5.4 状态流转

```
[初始态] ──(触发条件A)──→ [状态1]
[状态1]   ──(触发条件B)──→ [状态2]
[状态1]   ──(触发条件C)──→ [终态-成功]
[状态2]   ──(触发条件D)──→ [终态-失败]
```

### 5.5 权限控制

| 角色 | 查看权限 | 操作权限 | 限制 |
|------|----------|----------|------|
| 普通用户 | | | |
| 管理员 | | | |

---

## 六、接口设计

### 6.1 接口列表

| 方法 | 路径 | 用途 | 认证 |
|------|------|------|------|
| GET | | | 需要/不需要 |
| POST | | | 需要/不需要 |

### 6.2 接口详细设计

#### `[METHOD] /api/xxx`

**请求参数：**
```json
{
  "field1": "类型 - 说明",
  "field2": "类型 - 说明"
}
```

**成功响应：**
```json
{
  "code": 0,
  "message": "success",
  "data": {
    "field1": "类型 - 说明"
  }
}
```

**错误码：**

| 错误码 | 含义 | 触发条件 |
|--------|------|----------|
| 40001 | | |
| 40002 | | |

---

## 七、边界与异常

| 场景 | 处理方式 |
|------|----------|
| 输入为空/格式错误 | |
| 网络超时 | |
| 服务器错误 | |
| 并发操作（重复提交） | |
| 数据不存在 | |
| 数据已变更（乐观锁） | |
| 权限不足 | |

---

## 八、非功能性需求

| 类别 | 要求 |
|------|------|
| 响应时间 | < XX ms |
| 并发量 | XX QPS |
| 认证方式 | |
| 数据加密 | |
| 浏览器兼容 | |
| 移动端适配 | |
| 日志记录 | |

---

## 九、范围说明

### 本期包含
- ✅ 功能A
- ✅ 功能B

### 本期不包含
- ❌ 功能C（原因：xxx）
- ❌ 功能D（原因：xxx）

---

## 附录

- 原型图 / 设计稿链接
- 相关技术文档
- 历史版本变更记录
