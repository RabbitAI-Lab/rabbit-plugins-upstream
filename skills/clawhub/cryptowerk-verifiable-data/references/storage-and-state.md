# Storage and State

Recommended local sidecars:

- `<file>.cwseal` for retrieval id retrieved by register.py, provable seal retrieved by getseal.py, optionally more metadata

Credential storage:

- stored in skill directory
- restrict file permissions
- avoid storing credentials inside watched data trees
