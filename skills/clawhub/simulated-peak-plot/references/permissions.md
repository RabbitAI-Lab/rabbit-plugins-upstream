# 基于 skill-standardization 渐进式披露规范的权限说明

本文档由 `skill-standardization` 权限扫描器自动维护。

## 风险等级

**LOW**（实际权重: 0.0）

## 权限总览

| 权限类别 | 涉及项数 | 风险等级 |
|-----------|----------|----------|
| `subprocess_call` | 0 项 | ✅ LOW |
| `file_delete` | 0 项 | ✅ LOW |
| `network_access` | 0 项 | ✅ LOW |
| `sensitive_access` | 0 项 | ✅ LOW |
| `critical_write` | 0 项 | ✅ LOW |

## 高权限操作说明

- 无。所有文件操作均限制在技能独立数据目录内，不涉及系统关键目录、网络监听或外部请求。

## 权限详细说明

### 子进程调用（subprocess）

**无**。


### 文件删除

**无**。


### 网络访问

**无**。


### 敏感信息访问

**无**。


### 关键位置写入

**无**。


## 授权方式说明

- **immediate（即时授权）**：每次执行前需获得用户批准
- **unified（统一授权）**：首次执行前获得用户批准，后续不再询问
- **silent（静默授权）**：无需用户交互，自动执行并记录

<!-- fp:risk=LOW|sensitive=0|critical_write=0|network=0|delete=0|subprocess=0|issues=0 -->
---

# 权限权重说明（R-16）

本技能（simulated-peak-plot）的权限需求和风险评估如下：

## 权限清单

| 权限类型 | 需求 | 风险等级 | 说明 |
|---------|------|---------|------|
| **文件读取** | 需要 | 🟡 低 | 仅读取用户指定的 CSV 文件（`--import-csv` 参数） |
| **文件写入** | 需要 | 🟡 低 | 仅写入用户指定的输出文件（PNG 图像、CSV 数据） |
| **网络访问** | 不需要 | 🟢 无 | 完全离线运行 |
| **系统命令执行** | 不需要 | 🟢 无 | 仅调用 Python 解释器和 matplotlib 库 |
| **环境变量读取** | 不需要 | 🟢 无 | 不读取任何环境变量 |
| **敏感目录访问** | 不需要 | 🟢 无 | 不访问 ~/.ssh、~/.aws 等敏感目录 |

## 权重计算

根据 `permission_checker.py` 的计算规则：

| 权限 | 权重 | 说明 |
|------|------|------|
| 读取权限 | +1 | 仅读取用户指定文件 |
| 写入权限 | +1 | 仅写入用户指定文件 |
| 网络权限 | 0 | 无网络访问 |
| 命令执行 | 0 | 无命令执行 |

**总权重：2**（低风险）

## 风险缓解措施

1. **文件读取**：仅读取用户通过 `--import-csv` 参数明确指定的 CSV 文件，不读取其他文件
2. **文件写入**：输出文件路径由用户通过 `--output` 参数指定，默认使用 `simulated_peak.png` 和 `simulated_peak.csv`
3. **无网络访问**：完全离线运行，不发送任何数据
4. **无命令执行**：仅调用 Python 标准库和 matplotlib，不执行系统命令

## 授权方式

本技能采用 **unified** 授权方式（统一授权）：
- 用户在加载技能时一次性授权
- 后续所有操作（读取 CSV、生成图像、导出 CSV）不再重复询问
- 符合"只读用户指定文件、只写用户指定输出"的低风险场景

## 合规声明

本技能符合 skill-standardization v2.15.2 的所有安全要求：
- ✅ R-13：已声明敏感信息访问需求（仅读取用户指定 CSV）
- ✅ R-14：已声明关键位置写入需求（仅写入用户指定输出文件）
- ✅ R-15：低风险操作，无需额外授权检查
- ✅ R-16：已说明权限权重（总权重：2，低风险）
