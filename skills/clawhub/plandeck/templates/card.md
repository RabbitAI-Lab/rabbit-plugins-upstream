# <Card id> — <title>

Use a note only when a receipt is too big for the card in `plan.yaml`. Point the
card at it with `receipt: { note: cards/<id>-<slug>.md }`.

## What happened

<Findings, decision, or the change made.>

## Evidence

- <file paths, command output, links>

## Verification

- `<command>`: pass | fail
