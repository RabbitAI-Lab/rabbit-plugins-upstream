# Agent contract — lygo-sandcastle

1. Read `references/SECURITY.md` before running workflows.
2. Never claim permaweb anchor unless `workflow_orchestrator_planter` + `verify_kernel_eggs` → ALIGNED.
3. Prefer `validate` before `run` on untrusted YAML.
4. No autonomous publish to GitHub, HF, ClawHub, or social.