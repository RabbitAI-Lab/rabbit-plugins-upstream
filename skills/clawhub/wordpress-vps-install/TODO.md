# TODO

## Current status

- [x] Rename the skill to `wordpress-vps-install`
- [x] Rewrite `SKILL.md` for fresh VPS WordPress bootstrap
- [x] Update `README.md` to match the new skill name
- [x] Add the skill to the repo index README

## Follow-up items

- [ ] Add a short example command block with the `aqua` SSH alias workflow
- [ ] Add a compact troubleshooting section for the three most common failures:
  - wrong DB host
  - wrong volume mount
  - proxy/router mismatch
- [ ] Decide whether to add `CHANGELOG.md` and `LICENSE` for this skill
- [ ] If needed, add `references/` with the live deployment notes from the AquaMiracle install
- [ ] Keep the skill focused on new VPS installs; use a separate skill for live-site recovery if that becomes a distinct workflow
