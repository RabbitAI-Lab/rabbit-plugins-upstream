interface:
  display_name: "Dataify Facebook Events"
  short_description: "Collect Facebook Events data and return results"
  icon_small: "./assets/dataify-facebook-events-small.svg"
  icon_large: "./assets/dataify-facebook-events-large.svg"
  default_prompt: "Use $dataify-facebook-events to complete the requested Dataify collection, wait for the asynchronous task, and return the final collected result. Stop at task submission only when I explicitly request no-wait behavior."

policy:
  allow_implicit_invocation: true
