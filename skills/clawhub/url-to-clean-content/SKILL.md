---
name: url-to-clean-content
description: >-
  Turn any web page into clean, readable content an agent can use — fetch a URL and
  get the main text/markdown back without ads, nav, or boilerplate. Use whenever you
  need to read, summarize, research, or extract facts from a web page and don't want
  to run your own scraper or fight anti-bot/CORS. ~$0.005/call (cheaper than Firecrawl/
  Exa), USDC on Base via x402, no API key, no signup.
metadata:
  tags: [web-scraping, extraction, markdown, research, crawl, url, content, x402, agent-tools]
---
# URL → Clean Content (x402, ~$0.005/call)

Get the usable content out of any page in one authenticated GET, settled ~$0.005 USDC on Base via your x402 client. No key, no signup, no proxy headaches.
```
GET https://store.agentexchange.work/web/scrape?url=<PAGE_URL>
```
Returns the page's main content cleaned of nav/ads/boilerplate — ready to summarize or extract from. Undercuts Firecrawl/Exa ($0.02–0.025). Free sample: GET https://store.agentexchange.work/samples. Price shown in the 402 before you pay.
