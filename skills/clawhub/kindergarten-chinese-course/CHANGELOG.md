# Changelog

- **1.0.3** — 精简 `description` 至 174 字，与同系列 `kindergarten-english-course`（155 字）等保持
  一致的篇幅；保留 Use when 触发词与英文关键词，去掉冗余的功能罗列。功能无变更。
- **1.0.2** — 完善 `description`，明确使用场景（识字/描红/组词/古诗填空/可打印页）与能力边界
  （不适用于小学中高年级阅读理解与写作技巧），提升触发准确性。功能无变更。
- **1.0.1** — 更名为「幼儿园识字与诗歌课程体系」（displayName 与文档标题同步更新）。功能无变更。
- **1.0.0** — 首次发布（ClawHub）。L1-L4 识字与诗歌练习生成器：看图认字、描红、组词、
  古诗带拼音、古诗填空；内置儿歌/五言/七言诗歌池（均带拼音）。页面顶部内置
  「🖨️ 打印 / 另存为 PDF」按钮与打印设置提示，打印时自动隐藏。零外网（仅 Python 标准库）、
  姓名 HTML 转义、seed 可复现、答案内嵌同一份 HTML（打印另起一页）；
  附 TRACE-E 红线自检 `preflight.py` 与回归 `test_skill.py`。
