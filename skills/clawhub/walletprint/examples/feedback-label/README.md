# Feedback Label Example

```bash
npm install
WALLETPRINT_API_KEY=your_key npm start
```

This scores a transaction and immediately submits a `confirmed_benign` label.

Use this pattern when your app learns the outcome of a previous advisory result.
