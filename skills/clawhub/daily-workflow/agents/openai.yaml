interface:
  display_name: "Daily Workflow"
  short_description: "Preserve evidence-backed project memory"
  default_prompt: "Use $daily-workflow to orient from the current project state and create an evidence-backed checkpoint, wrap-up, or handoff without overwriting governance or acceptance records."
policy:
  allow_implicit_invocation: true
