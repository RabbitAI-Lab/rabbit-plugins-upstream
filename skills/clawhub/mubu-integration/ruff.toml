line-length = 100
target-version = "py39"

[lint]
# 保守规则集：E(F/K)/F(pyflakes 真实 bug)/W(警告)/I(import 排序)
# 仅作为静态门禁的起步；后续可逐步收紧。本地 ruff 因沙箱 SOCKS 代理无法安装，
# CI 中的 lint job 当前 continue-on-error，待真实环境跑通后改为必过。
select = ["E", "F", "W", "I"]
ignore = ["E501"]
