#!/usr/bin/env python3
"""Disabled by security hardening.

旧版独立支付宝支付入口已禁用：必须先调用云端 create_order，并用返回 privateKey 在内存中生成 H5 支付。
"""
raise SystemExit('旧版独立支付宝支付入口已禁用：必须先调用云端 create_order，并用返回 privateKey 在内存中生成 H5 支付。')
