# Publish to GitHub

1. Create empty repo: `DeepSeekOracle/lyra-crypto-operator` (no README).
2. From this folder:

```powershell
cd "I:\E Drive\lyra-crypto-operator"
git remote add origin https://github.com/DeepSeekOracle/lyra-crypto-operator.git
git push -u origin main
```

Local commit ready: `fc368b3` on `main`.