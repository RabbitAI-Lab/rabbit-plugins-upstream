{
    "mcpServers": {
        "lexiang": {
            "enabled": true,
            "url": "${MCP_ENDPOINT}",
            "transportType": "streamable-http",
            "headers": {
                "Authorization": "Bearer ${LEXIANG_TOKEN}"
            }
        }
    }
}
