# 类型参考


## HttpServer

HttpServer


**HttpServer**

HttpServer是一个全局变量，用于开启和关闭Http服务。

HttpServer的属性有：

* **IsRunning**

  逻辑型，用于判断HppServer是否正在运行。
* **P****refixes**

  集合，包括所有定义好的Http服务。
* **WebPath**

  字符型，用于设置静态文件路径，参考：[去掉通用事件头](0276.htm)
* **Extensions**
  字符型集合，用于设置可自动发送的静态文件后缀名，参考：[去掉通用事件头](0276.htm)

HttpServer的方法有：

* **Start**

  启动Http服务。
* **Close**
  关闭Http服务。

**开启服务**

HttpServer.Prefixes.Add("http://\*/")
HttpServer.WebPath
= "d:\web"
'指定静态文件存储位置
HttpServer.Start()

默认端口为80，可以改端口，例如：

HttpServer.Prefixes.Add("http://\*:32177/")
HttpServer.WebPath
= "d:\web"
'指定静态文件存储位置
HttpServer.Start()

如果不是默认端口，访问的时候需要加上端口号：

http://192.168.100.1:32177/

关闭服务

HttpServer.Close()

## HttpRequest

HttpRequest


**HttpRequest事件**

HttpRequest的e参数属性有：

* Host
  字符型，返回客户端访问的服务器IP地址(或域名)。
* Port
  整数型，返回客户端访问的服务器端口号。
* Path
  字符型，返回客户端访问的路径，含文件名。

  假定用户输入的访问路径是"http://192.168.0.100:32188/fox/",这三个e参数的返回值分别是：

  Host: 192.168.0.100
  Port: 32188
  Path: fox

  假定用户输入访问路径是"http://192.168.0.100:32188/fox/china.htm"，这三个e参数的返回值分别是：

  Host: 192.168.0.100
  Port: 32188
  Path: fox\china.htm
* Values
  集合，返回接收到的数据，包括通过post和get方式提交的数据。
* PostValues
  集合，包括通过post方式提交的数据，参考：[Values、GetValue和PostValues](0113.htm)
* GetValues
  集合，包括通过get方式提交的数据，参考：[Values、GetValue和PostValues](0113.htm)
* Cookies
  集合，返回接收到的Cookie，参考：使用[Cookie](0042.htm)
* Files
  集合，返回接收到的文件，参考：[文件的上传和接收](0030.htm)
* PlainText
  字符型，返回收到的纯文本数据，参考：[sendAjaxText](0108.htm)
* ResponseEncoding
  字符型，用于设置向客户端发送文本内容时采用的编码，参考：[网页出现乱码啦](0022.htm)
* Request
  返回一个HttpListenerRequest对象，适合专业开发人员使用。
* Response
  返回一个HttpListenerResponse对象，适合专业人员使用。
* CacheTime
  设置缓存时间，单位为秒，在指定缓存时间内，将不再访问服务器重新下载内容。
* Headers
  用于获取客户端发来的头部信息，参考：[再谈头部信息](3279.htm)
* ResponseHeaders
  用于设置向客户端发送的头部信息，参考：[再谈头部信息](3279.htm)
* StatusCode
  设置返回给客户端的HTTP状态代码，一般无需设置。

HttpRequest的e参数方法有：

* WriteString
  向客户端发送文本内容，参考：[关于WriteString](0010.htm)
* WriteFile
  向客户端发送文件，参考：[我想显示一个图片](0015.htm)
* WriteDataTable
  向客户端发送DataTable，需要和HttpClient配合使用，参考：[接收DataTable](0162.htm)
* SaveFile
  保存接收到的文件，参考：[文件的上传与接收](0030.htm)
* AppendCookie
  增加Cookie，参考：使用：[使用Cookie](0156.htm)
* WriteBookAsHTML
  将Excel报表转换成网页并发送到客户端，参考： [用Excel报表生成网页](0288.htm)
* WriteBookAsPDF
  将Excel报表转换成PDF文件并发送到客户端，参考： [用Excel报表生成网页](0288.htm)
* Redirect
  跳转到指定页面，例如将HttpRequst事件代码设置为：

  Select
  Case e.Path
      Case "foxtable.htm"
          e.Redirect("http://www.foxtable.com")
      Case "sina.htm"
          e.Redirect("default.htm")
      Case "default.htm"
          e.WriteString("Hello
  World")
  End
  Select

  现在访问地址"
  http://127.0.0.1/foxtable.htm"，打开的是foxtable的官方网站，而访问地址"
  http://127.0.0.1/sina.htm"，显示的是页面default.htm的内容：Hello
  Word。

## BeforeHttpRequest

BeforeHttpRequest


**BeforeHttpRequest事件**

BeforeHttpRequest事件和HttpRequest事件都可以响应Http访问请求，向客户端发送网页和文件。

**重要提示：**

1、BeforeHttpRequest执行的时候，还没有接收客户端上传的表单数据和文件，所以在此事件中，PostValues、Files和SaveFiles三个成员无效。

2、BeforeHttpRequest事件代码执行完毕之后，默认会继续执行HttpRequest事件，除非在BeforeHttpRequest中将Cancel属性设置为True，或者使用了WriteString或WriteFile等方法向客户端发送了数据。

3、BeforeHttpRequest和HttpRequest使用的是同一个e参数，所以在BeforeHttpRequest进行的任何设置，都等同在HttpRequest事件中进行了同样的设置。

两个事件的本质区别在于：BeforeHttpRequest在接收数据和文件之前触发，HttpRequest在接收数据和文件之后触发，BeforeHttpRequest非常适合提前做
前置验证工作。

例如有人上传恶意文件（或大文件炸弹），BeforeHttpRequest可直接拒绝，而HttpRequest则将恶意文件接收并存储在一个临时目录后再触发，消耗了服务期资源接收，占用了硬盘空间存储，而且恶意文件（
或大文件炸弹）已经在服务器上，有可能会攻击得逞。

**示例**

假定做了一个名为upload.htm的上传页面，一切都按照原来设计，但是在BeforeHttpRequest事件中加上了代码：

Dim
Token As
String = e.Headers("token")
If
Token <>
"定期随机生成的token字符串" Then
    e.Cancel
= True
    'e.WriteString("可以根据需要返回信息，直接cancel对方会显示404错误")

End
If

现在只有通过token验证的访问请求，才会触发HttpRequest事件，也就是才能上传文件，避免非法访问占用服务器资源。

**BeforeHttpRequest的e参数属性有：**

* Host
  字符型，返回客户端访问的服务器IP地址(或域名)。
* Port
  整数型，返回客户端访问的服务器端口号。
* Path
  字符型，返回客户端访问的路径，含文件名。

  假定用户输入的访问路径是"http://192.168.0.100:32188/fox/",这三个e参数的返回值分别是：

  Host: 192.168.0.100
  Port: 32188
  Path: fox

  假定用户输入访问路径是"http://192.168.0.100:32188/fox/china.htm"，这三个e参数的返回值分别是：

  Host: 192.168.0.100
  Port: 32188
  Path: fox\china.htm
* GetValues
  集合，包括通过get方式提交的数据，参考：[Values、GetValue和PostValues](0113.htm)
* Cookies
  集合，返回接收到的Cookie，参考：使用[Cookie](0042.htm)
* ResponseEncoding
  字符型，用于设置向客户端发送文本内容时采用的编码，参考：[网页出现乱码啦](0022.htm)
* Request
  返回一个HttpListenerRequest对象，适合专业开发人员使用。
* Response
  返回一个HttpListenerResponse对象，适合专业人员使用。
* CacheTime
  设置缓存时间，单位为秒，在指定缓存时间内，将不再访问服务器重新下载内容。
* Headers
  用于获取客户端发来的头部信息，参考：[再谈头部信息](3279.htm)
* ResponseHeaders
  用于设置向客户端发送的头部信息，参考：[再谈头部信息](3279.htm)
* Cancel
  逻辑型，设置为True，接线来不会触发

HttpRequest的e参数方法有：

* WriteString
  向客户端发送文本内容，参考：[关于WriteString](0010.htm)
* WriteFile
  向客户端发送文件，参考：[我想显示一个图片](0015.htm)
* WriteDataTable
  向客户端发送DataTable，需要和HttpClient配合使用，参考：[接收DataTable](0162.htm)
* AppendCookie
  增加Cookie，参考：使用：[使用Cookie](0156.htm)
* WriteBookAsHTML
  将Excel报表转换成网页并发送到客户端，参考： [用Excel报表生成网页](0288.htm)
* WriteBookAsPDF
  将Excel报表转换成PDF文件并发送到客户端，参考： [用Excel报表生成网页](0288.htm)
* Redirect
  跳转到指定页面，例如将HttpRequst事件代码设置为：

## HttpClient

HttpClient


**HttpClient**

HttpClient的属性有：

|  |  |
| --- | --- |
| 属性 | 说明 |
| Content | 字符型，用于设置要发送到服务器的数据，参考：[发送数据](0165.htm) |
| ContentType | 字符型，用于设置数据类型，默认为"text/plain"，一般无需设置。 |
| FormData | 字典，用于添加需要发送到服务器的表单数据，参考：[模拟表单](0159.htm) |
| sCookies | 字典，用于添加需要发送到服务器的Cookie，参考：[收发Cookie](0158.htm) |
| rCookies | 字典，包括所有服务器返回的Cookies，参考：[收发Cookie](0158.htm) |
| Files | 字典，包括所有需要发送到服务器的文件，参考：[上传文件](0161.htm) |
| Method | 字符串性，用于设置请求方式，通常无需设置，由HttpClient根据提交的内容自行决定。 |
| TimeOut | 整数型，用于设置获取服务器响应的超时时限，单位为秒，默认是100秒。 |
| Headers | 字典，用于添加HTTP头，参考：[阿里云全国物流查询接口](0246.htm)。 |
| UserAgent | 获取或设置 User-agent HTTP 标头的值。 |
| Accept | 获取或设置 Accept 标头的值。 |
| ReadWriteTimeout | 整数型，用于获取读写服务器数据的超时时限，单位为秒，默认是300秒。 |
| RequestEncoding | 字符型，用于设置向服务器发送数据时所使用的字符编码，默认为"utf-8"，一般无需设置。 |
| ResponseEncoding | 字符型，用于设置解析收到的服务器数据时所采用的字符编码，默认为"utf-8"，一般无需设置。 |
| LastModified | 获取服务器返回内容的最近一次更新时间，参考：[获取内容信息](3276.htm) |
| StatusCode | 获取服务器返回的状态码，参考：[获取内容信息](3276.htm) |
| StatusDescription | 获取服务器返回的状态字符串，参考：[获取内容信息](3276.htm) |
| ResponseContentType | 获取服务器返回内容的类型，参考：[获取内容信息](3276.htm) |
| ResponseContentLength | 获取服务器返回内容的长度，参考：[获取内容信息](3276.htm) |
| ResponseHeaders | 回去服务器返回内容的头部信息，参考：[获取内容信息](3276.htm) |
| EncodeCookie | 逻辑型，用于设置Cookie是否编码发送，默认为True，如果需要原文发送Cookie，可将此属性设置为False |
| SkipError | 参考:[跳过错误提示](0294.htm) |

HttpClient的方法有：

|  |  |
| --- | --- |
| 方法 | 说明 |
| GetData | 用于接收从服务器返回的数据，参考：[接收数据](0157.htm) |
| GetFile | 用于接收从服务器返回的文件，参考：[接收文件](0160.htm) |
| GetDataTable | 用于接收从服务器返回的DataTable，参考：[接收DataTable](0162.htm) |

## WeUI

WeUI


**WeUI**

WeUI的属性有：

|  |  |
| --- | --- |
| 属性 | 说明 |
| Title | 字符型，用于设置网页标题 |
| WeUIPath | 字符型，用于指定WeUI库文件的路径，默认为"/weui"。 |

WeUI的方法有：

|  |  |
| --- | --- |
| 方法 | 说明 |
| AddActionSheet | 用于增加ActionSheet，参考：[使用ActionSheet](0084.htm) |
| AddArticle | 用于增加Article，参考：[使用Article](0079.htm) |
| AddButtonGroup | 用于增加按钮分组，参考：[定义按钮](0008.htm) |
| AddCheckGroup | 用于增加复选列表组，参考：[复选列表项](0053.htm) |
| AddDialog | 用于增加Dialog，参考：[使用Dialog](0080.htm) |
| AddForm | 用于增加表单，参考：[定义表单](0007.htm) |
| AddGallery | 用于增加Gallery，参考：[使用Gallery](0087.htm) |
| AddGrid | 用于增加Grid，参考：[使用Grid](0077.htm) |
| AddInputGroup | 用于增加输入框分组，参考：[文本输入框](0045.htm) [数值输入框](0047.htm) [日期输入框](0046.htm) [列表输入框](0048.htm) [InputCell](0051.htm) [逻辑开关](0049.htm) [多行文本框](0050.htm) |
| AddListGroup | 用于增加列表项组，参考：[使用List](0076.htm) |
| AddMsgPage | 用于增加MSgPage，参考：使用[MsgPage](0058.htm) |
| AddPage | 用于增加页面，参考：[使用Page](0109.htm) |
| AddPageFooter | 用于增加PageFooter，参考：[使用PageFooter](0086.htm) |
| AddPageTitle | 用于增加PageTitle，参考：[使用PageTitle](0075.htm) |
| AddPanelGroup | 用于增加Panel分组，参考：[使用Panel](0078.htm) |
| AddPreview | 用于增加Preview，参考：[使用Preview](0085.htm) |
| AddProgress | 用于增加Progress，参考：使用[Progress](0098.htm) |
| AddRadioGroup | 用于增加单选列表组，参考：[使用单选列表项](0052.htm) |
| AddTabBar | 用于增加TabBar，参考：[使用TabBar](0081.htm) |
| AddTable | 用于增加表格，参考：[生成表格](0063.htm) |
| AddToast | 用于增加Toast,参考：使用：[使用Toast](0082.htm) |
| AddToptips | 用于增加Toptoips，参考使用：使用[Toptoips](0083.htm) |
| AppendHTML | 用于在head或body的快结束位置附加内容，参考：[引用外部文件](0061.htm) |
| InsertHTML | 用于在当前位置插入HTML代码，参考[InsertHTML](0097.htm) |
| ImageToBase64 | 用于将图片转换为base64字符串，参考：[动态生成图表](0137.htm) |
| BuildHTML | 用于获得指定元素的HTML，例如：   Dim wb As New weui  wb.AddForm("","form1","test.htm")  With wb.AddInputGroup("form1","ipg1","登录")      .AddInput("xm","户名","text")      .AddInput("pw","密码","password")  End With  With wb.AddButtonGroup("form1","btg1",True)      .Add("btn1", "确定", "submit")  End With  Output.Show(wb.BuildHTML("form1"))  'BuildHTML的参数为元素ID |
| Save | 将WeUI的内容保存一个一个文件。 Dim wb As new   WeUI  With wb.AddMsgPage("","msgpage","操作完成","内容详情,可根据实际需要安排")      .AddButton("btn1","确定")         .AddButton("btn2","取消").kind = 1      .AddExtra("详细信息","http://www.foxtable.com/")  End  With  wb.Save("d:\web\tst.html") |
| Build | 根据WeUI的内容生成一个字符串，这是最常用的一个方法。 |
| AppendCookie | 用于添加Cookie，参考：[使用Cookie](0042.htm) |
| DeleteCookie | 用于删除Cookie，参考：[使用Cookie](0042.htm) |
| ClearCookie | 用于清除所有Cookie，参考：[使用Cookie](0042.htm) |

## WXBizCrypt

WXBizCrypt类


**WXBizCrypt类**

腾讯官方提供了微信消息加解密算法的通用代码，
我们已经将其编译成DLL，并整合在Foxtable中，命名空间为"Tencent.WXBizMsgCrypt"，如果你熟悉用.net进行微信开发，你可以直接调用。

但是对多数用户来说，直接使用封装在DLL中的函数进行加解密还是比较麻烦的，我个人就曾经因为一个参数顺序问题，浪费了一个多小时。

为此我们另外提供了一个类WXBizCrypt，在腾讯官方代码的基础上进行了简写，更适合非专业人员使用。

**定义WXBizCrypt类的语法为：**

Dim wbiz As New WXBizCrypt(AppId, Token, EncodingAESKey)

|  |  |
| --- | --- |
| AppId | 开发者ID，对于企业号来说，就是CorpID。 |
| Token | 设置回调URL时指定的Token。 |
| EncodingAESKey | 设置回调URL时指定的EncodingAESKey。 |

**WXBizCrypt包括四个函数：**

* **DecryptMsg**

  DecryptMsg函数用于解密消息，语法为：

  DecryptMsg(e)

  e为HttpRequest事件的e参数。

  返回值是对e.PlainText属性进行解密的结果，如果解密失败，则返回一个错误值。
* **EncryptMsg**

  EncryptMsg函数用于加密消息，语法为：

  EncryptMsg(e, Message)

  e为HttpRequest事件的e参数，Message为要加密的消息。

  返回值为对Message加密后的结果，如果加密失败，则返回一个错误值。
* **GetErorDesc**

  GetErorDesc函数用于获取错误值对应的错误描述，语法为：

  GetErorDesc(ErrorCode)

  ErrorCode是一个整数型的错误值。

  函数返回的是ErrorCode对应的错误描述。
* **CheckQYSignature**

  CheckQYSignature专门用于企业号的接入验证，语法为：

  CheckQYSignature(e)

  如果接入验证成功返回0，否则返回一个错误值。

## JObject

JObject


**JObject**

JObject用于生成和解析JSON数据。

参考：

[解析JSON](0140.htm)

[生成JSON](0167.htm)

## XObject

XObject


**XObject**

XObject用于生成和解析XML数据。

参考：

[解析XML](0171.htm)

[生成XML](0172.htm)

## 使用缓存

使用缓存


**使用缓存**

一些网页文件，例如css、js、图片文件，很少会频繁更新，为提高效率，可以使用缓存：在一定时间内将文件缓存在客户端，这样客户端再次使用这些文件时，可直接使用缓存文件，无需再次访问服务器下载，以提高效率。

HttpRequest时间有个CatcheTime属性，用于设置缓存时间，单位为秒。

**示例**

例如希望除html和htm之外的文件，全部在客户端缓存一个小时：

Dim
fl As
String =
"d:\web\" &
e.path
If
filesys.FileExists(fl)
    Dim idx
As Integer
= fl.LastIndexOf(".")
    Dim ext
As String
= fl.SubString(idx)
    Select Case
ext
        Case
".jpg",".gif",".png",".bmp",".wmf",".js",".css"
,".zip",".rar"
            e.CacheTime
= 3600 '缓存文件一个小时
            e.WriteFile(fl)
            Return
        Case ".html",".htm"
            e.WriteFile(fl)
            Return
    End
Select
End
If

提示：

1、这只是一个示例，实际上静态的网页也应该缓存的。

2、在缓存期内刷新页面(例如在地址栏再次按回车或单击刷新按钮)，依然会重新访问服务器下载文件。

## 常用函数

常用函数


**常用函数**

* **CompressJson**

  CompressJson用于压缩JSON数据，参考：[压缩JSON](0227.htm)
* **UrlEncode**

  UrlEncode用于对字符串进行URL编码，例如：

  Dim
  ul As
  String =
  "http://www.foxtable.com/?page=123"
  Output.Show(UrlEncode(ul))

  显示结果为：

  http%3a%2f%2fwww.foxtable.com%2f%3fpage%3d123
* **UrlDecode**

  UrlDecode用于对字符串进行URL解码，例如：

  Dim
  ul As
  String =

  "http%3a%2f%2fwww.foxtable.com%2f%3fpage%3d123"
  Output.Show(UrlDecode(ul))

  显示结果为：

  http://www.foxtable.com/?page=123
* HTMLEncode

  HTMLEncode用于对字符串进行HTML编码，例如：

  Dim
  v As
  String =
  "1 + 2 <> 3"
  Output.Show(HTMLEncode(v))

  显示结果为：

  1 + 2 &lt;&gt; 3
* **HTMLDecode**
  HTMLDecode用于对字符串进行HTML解码，例如：

  Dim
  v As
  String =
  "1 + 2 &lt;&gt; 3"
  Output.Show(HTMLDecode(v))