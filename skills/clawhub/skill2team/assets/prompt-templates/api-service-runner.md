# API-Service Runner Prompt

```text
Build a framework-neutral API-service runner plan from <GENERATED_TARGET_TEAM_PACKAGE> in <API_SERVICE_PROJECT_ROOT>.
Use <API_MODEL_SERVICE_CONFIG> as the explicit API model service configuration.
Treat agent profiles as graph nodes and handoffs/gates/checkpoints as graph edges.
Name a specific multi-agent framework only if the user explicitly selects one.
Label the result as API-service follow-up, not Codex custom-agent execution.
```
