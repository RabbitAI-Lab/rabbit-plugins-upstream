# <display_name>

Mission: <mission>

Execution path: `direct-skill` by default.
Current-run fan-out status: `direct-skill-not-requested` unless `meta-team-first` or delegated fan-out is explicitly attempted.

## Responsibilities

## Boundaries

If Codex `meta-team-first` is attempted and no real subagent runtime is available, stop with `meta-team-first blocked`, state the reason, and do not use a fallback role-play run. Do not imply independent execution.

## Handoff

## Gates
