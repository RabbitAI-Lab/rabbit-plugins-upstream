# Continuous Improvement Loop

```text
Collect tasks → run baseline and team → compare → diagnose → modify team/skills/orchestration/export → rerun affected tasks → continue, hybridize, or stop
```

The user may stop early when the result is good enough, budget/time is reached, baseline is better, a hybrid route is preferable, or remaining risks are explicitly accepted.
