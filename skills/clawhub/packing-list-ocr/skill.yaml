name: packing_list_ocr
version: 1.0.0
description: 支持识别货物品类、重量体积、收发货主体，提取单据编号等信息。
author: SCNet
license: MIT
tags:
  - ocr
  - 文字提取
required_env_vars:
  - SCNET_API_KEY

optional_env_vars:
  - SCNET_API_BASE

primary_credential: SCNET_API_KEY

# 增加项目主页，解决 missing homepage 警告
homepage: https://github.com/SCNet-sugon/packing_list_ocr.git  # 请替换为实际仓库地址
dependencies:
  - python3
  - requests