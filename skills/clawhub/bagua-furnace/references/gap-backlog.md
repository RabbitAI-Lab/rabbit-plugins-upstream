# 覆盖缺口 backlog（S4 审计产出）

> 隐性缺口在此登记，逐条可追踪。状态：🔲 待补 / 🔧 进行中 / ✅ 已补。

## G1 · 音视频 ASR 兜底端点未部署
- 描述：公司 GPU 服务器 `192.168.21.97` 当前仅跑 vLLM+qwen2.5-7b，未部署 `faster-whisper` / `whisper.cpp` 推理端点。
- 影响：YouTube/B站 无字幕、且 Whisper 未就绪时，音视频输入走降级提示（不卡死，但非全自动）。
- 状态：🔲 待补（独立子任务：在 192.168.21.97 部署 Whisper 端点 + 本炉加调用段）。
- 不阻塞 v1：字幕优先路径已覆盖 YouTube/B站；抖音/快手/小红书/视频号 走 douyin_copy_extract 已全自动。

## G2 · 微信/公众号文章登录墙
- 描述：部分微信文章需登录，WebFetch 抓不到正文。
- 影响：极小（用户可贴正文）；已写入 ingestion.md 降级提示。
- 状态：✅ 已用降级提示覆盖，不单列开发。

## G3 · 多语言原料
- 描述：当前透镜与卡模板以中文为主，外文原料可提炼但未做语言归一。
- 影响：低（提炼不依赖语言）；如需外文卡库分类可后续补。
- 状态：🔲 待补（低优先）。

## G4 · PDF 原料的读取方式
- 描述：少数环境下直接读取 PDF 正文会受限。`references/ingestion.md` 的 PDF 适配器已提供备用取文路径，照其执行即可正常提炼。
- 影响：低——用户无感，按 PDF 适配器指引走即可。
- 状态：✅ 已有备用取文路径（见 `references/ingestion.md`）。

## 不视为缺口的项（红队复核）
- 不产可执行 Agent Skill：是**差异化红线**，非缺口（仓颉地盘）。
- 不修改扫地僧源文件：是 v1 解耦决策，非缺口（深度注入留独立评审）。
