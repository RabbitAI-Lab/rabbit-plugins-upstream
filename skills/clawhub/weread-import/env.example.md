# 官方 Gateway API Key（默认后端需要；微信读书 App 最新版 -> 我的 -> 右上角设置按钮 -> 微信读书 Skill -> 快速配置第 2 步获取）
WEREAD_API_KEY=

# API 后端：gateway 或 web。默认 gateway。
WEREAD_API_BACKEND=gateway

# 官方 Gateway skill 版本
WEREAD_GATEWAY_SKILL_VERSION=1.0.3

# 手动 cookie（仅 web 后端需要，从浏览器开发者工具中复制）
WEREAD_COOKIE=

# 导出模式：api
WEREAD_IMPORT_MODE=api

# Chrome 远程调试地址（仅 web 后端 / 兜底链路用于浏览器 cookie 提取）
WEREAD_CDP_URL=http://127.0.0.1:9222

# 输出目录
WEREAD_OUTPUT=

# Frontmatter tags（逗号分隔）
WEREAD_TAGS=reading,weread

# 自定义 User-Agent（可选，默认 Chrome 146）
WEREAD_USER_AGENT=
