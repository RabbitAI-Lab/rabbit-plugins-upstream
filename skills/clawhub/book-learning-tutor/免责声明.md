# Book Learning Tutor · Disclaimer & Usage Agreement

> This file is the formal disclaimer and license terms for the "Book Learning Tutor" skill (including its bundled parsing / course-ification tools).
> Using this skill is deemed to mean you have read, understood, and accepted all of the following terms.

## 1. Purpose

"Book Learning Tutor" exists as an **Agent Skills open-standard skill** (installable into any compatible host such as WorkBuddy, Claude Code, Copilot CLI, Amp, OpenClaw). Its purpose is to help **the user themselves** "course-ify" their books (a **local file they supply**, or one **fetched from a public source under their explicit authorization**) and assist a human in learning chapter-by-chapter, lesson-by-lesson.

- We **provide no book content** and **provide no web content**.
- We **provide no website source code, book-source subscriptions, source rules, crawler programs, or proxy programs**.
- All raw files that get course-ified must be **supplied by the user** (a local file, or material lawfully saved locally).

## 2. Online acquisition (optional · authorized only)

This skill **by default does not go online or proxy on its own**. Only when **the user has no local book and explicitly authorizes** it may the agent drive the repo's existing acquisition pipeline to search for and fetch from **public sources the user is authorized to access**:

- No proxying, no VPN/circumvention, no bypassing access restrictions;
- **No bypass of DRM / paywalls / login limits**;
- The scope of automated acquisition is strictly limited to sources the user has confirmed.

Any acquisition is **authorized by and the sole responsibility of the user**, and is unrelated to this skill or its authors. The `search` / `download` / `discover` scripts retained in the repo (a repo-level, not bare-skill, capability) are used at the user's own compliance and copyright risk.

## 3. User compliance

The user must ensure on their own:

- The books / materials they provide have a **lawful** source;
- Their usage complies with the **laws of their jurisdiction** and the **terms of service** of the platforms used;
- This skill is not used for any purpose that infringes third-party rights (including but not limited to copyright, and the right of communication to the public via information networks).

The skill's authors **bear no responsibility for the content the user provides or how they use it**.

## 4. About the "acquisition pipeline" (optional · authorized sites only)

This repo's main path is local-file course-ification (`teach.py` / `all-local`, processing the local files the user provides). The `search` / `download` / `import_source` / `discover` scripts retained in the repo are **optional legacy capabilities**; their crawling design only borrows the *idea* of open-source reader (Legado)-style "book source" tools and keeps compatibility with that general rule format — we **re-implemented everything ourselves, did not embed or redistribute any project's source code, and are not a derivative work** of Legado (GPL-3.0) or any peer project, so our MIT license stays clean.

This repo **does not provide, bundle, or recommend** any specific book-source subscription address or preset rules; its optional acquisition pipeline may only be driven by the agent, after **explicit user authorization**, to fetch from public sources the user is authorized to access. If the user uses the acquisition pipeline:

- **Use only on URLs you are authorized for, or that the site explicitly allows such tools to access**;
- **No support for, and no encouragement of, crawling any unauthorized site (especially those explicitly forbidding such tools)**;
- Any acquisition is entirely the user's own doing, and they bear all compliance and copyright responsibility, unrelated to this repo or its authors.

By default this repo only parses and course-ifies **local files the user provides**; when explicitly authorized by the user, it may first fetch from a public source on their behalf and then course-ify.

## 5. License (consistent with LICENSE)

This repo (parse / course-ify engine, skill, scripts) is released under **MIT**, see root
[`LICENSE`](./LICENSE). This skill is provided **"as-is"**.

- Code: you may freely use, modify, distribute, and commercially use this repo's code and skill (must retain the MIT copyright notice).
- Content: this repo **contains no book / copyright-protected content**; the user must supply local files and **bear responsibility** for the use and copyright compliance of what they provide.

## 6. No warranty & limitation of liability

This skill is provided "as is", **with no express or implied warranty** (including but not limited to warranties of fitness, accuracy, reliability, non-infringement). Any consequence arising from the user's use of this skill (including but not limited to data loss, parse errors, course-structure deviations) is **borne by the user**, and the authors bear no direct or indirect liability.

## 7. About multimodal training-data extraction ("传道")

This repo also contains model-training-oriented data-extraction capabilities (e.g. image-text alignment unit extraction). That capability **likewise follows** the MIT license and user-responsibility principle above, and **ships no content or data** with this skill.

---

使用者因使用本仓库产生的任何后果自行承担；代码许可与权利以根目录 [`LICENSE`](./LICENSE) 为准。

# 中文版（以下法律中文原版具有同等说明效力）

# 授业 · 免责声明与使用协议

> 本文件是「授业」技能（含其附带的解析/课程化工具）的正式声明与许可条款。
> 使用本技能即视为已阅读、理解并接受以下全部条款。

## 1. 用途说明

「授业」以 **Agent Skills 开放标准技能**的形式存在（可装进 WorkBuddy、Claude Code、Copilot CLI、Amp、OpenClaw 等任意兼容宿主），目的是帮助**使用者本人**把其书籍（**自行提供的本地文件**，或**经其明确授权从公开来源获取**者）进行「课程化」，并辅助人类按章分课学习。

- 我们**不提供任何书籍内容**、**不提供任何网页内容**。
- 我们**不提供任何网站源码、书源订阅、书源规则、爬虫程序或代理程序**。
- 所有被课程化的原始文件，均须由**使用者自行提供**（本地文件或已合法保存在本地的资料）。

## 2. 联网获取（可选 · 仅限已授权）

本技能**默认不主动联网、不代理**。仅在**使用者无本地书且明确授权**时，agent 可代为驱动仓库内既有的获取管道，从**使用者有权访问的公开来源**检索并获取：

- 不进行任何代理（proxy）、翻墙、或绕过访问限制的行为；
- **不绕过 DRM / 付费墙 / 登录限制**；
- 自动获取的范围严格限制在使用者已确认的来源之内。

任何获取行为，**由使用者授权并自担全部责任**，与本技能及其作者无关。仓库中保留的 `search` / `download` / `discover` 等获取脚本（属仓库能力、非裸技能能力），使用者若使用须自担合规与版权责任。

## 3. 使用者自行遵守规范

使用者须自行确保：

- 其所提供的书籍 / 资料来源**合法**；
- 其使用方式符合**所在国家 / 地区的法律法规**以及所用平台的**服务条款**；
- 不将本技能用于任何侵犯第三方权利（包括但不限于著作权、信息网络传播权）的用途。

本技能作者**不对使用者提供的内容及其使用方式承担任何责任**。

## 4. 关于「获取管道」（可选 · 仅限已授权站点）

本仓库的主路径是本地文件课程化（`teach.py` / `all-local`，处理使用者已提供的本地文件）。仓库内保留的 `search` / `download` / `import_source` / `discover` 等脚本为**可选遗留能力**；其爬虫设计仅借鉴开源阅读（Legado）这类「书源」工具的**思路**并保持对通用规则格式的兼容——我们**全部自行重新实现，未内嵌或再分发任何项目的源码，也不构成 Legado（GPL-3.0）或其他项目的衍生作品**，故本项目 MIT 许可不受影响。

本仓库**不提供、不附带、不推荐**任何具体的书源订阅地址或预设规则；其包含的可选获取管道，仅在**使用者明确授权**后可由 agent 代为从使用者有权访问的公开来源获取。若使用者使用获取管道：

- **仅可在你已获授权、或该站点明确允许此类工具访问的网址上使用**；
- **不支持、不鼓励对任何未授权站点（尤其是明确禁止此类工具访问的站点）的爬取**；
- 任何获取行为完全由使用者自行完成，并自行承担全部合规与版权责任，与本仓库及作者无关。

本仓库默认仅对**使用者已提供的本地文件**做解析与课程化；经使用者明确授权时，可先代为从公开来源获取再课程化。

## 5. 许可（与 LICENSE 一致）

本仓库（解析 / 课程化引擎、技能、脚本）以 **MIT 许可**发布，见根目录
[`LICENSE`](./LICENSE)。本技能以 **「原样」（as-is）** 提供。

- 代码层面：你可自由使用、修改、分发、商用本仓库的代码与技能（须保留 MIT 版权声明）。
- 内容层面：本仓库**不包含任何书籍 / 受版权保护的内容**；使用者须自行提供本地文件，
  并对所提供内容的使用与版权合规**自行负责**。

## 6. 无担保与责任限制

本技能按「现状」提供，**不提供任何明示或暗示的担保**（包括但不限于对适用性、准确性、
可靠性、不侵权的担保）。使用者因使用本技能所产生的任何后果（包括但不限于数据丢失、
解析错误、课程结构偏差）**自行承担**，作者不承担任何直接或间接责任。

## 7. 关于多模态训练数据抽取（"传道"）

本仓库另含面向模型训练的数据抽取能力（如图文对齐单元抽取）。该能力**同样遵循**上述
MIT 许可与使用者自担责原则，且**不随本技能提供任何内容或数据**。

---

使用者因使用本仓库产生的任何后果自行承担；代码许可与权利以根目录 [`LICENSE`](./LICENSE) 为准。
