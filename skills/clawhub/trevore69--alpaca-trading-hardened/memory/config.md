# Memory Config

Created: 2026-07-15
Sync from built-in: yes (selected facts only; never credentials)

## Categories
- projects/ — full histories and current state per project/platform
- decisions/ — why choices were made, with alternatives considered
- knowledge/ — platform quirks, lessons, reference material
- people/ — contacts and community members with context

## Preferences
- Write immediately when something important happens; don't batch.
- Grep first for recall (`grep -r "keyword" ~/memory/`), then navigate indices.
- Built-in memory (MEMORY.md + workspace memory/) stays the quick layer; this system holds depth.
- Weekly: refresh INDEX.md files. Monthly: split categories >100 entries.

## Notes
- Trevor picked defaults proposed by the agent on 2026-07-15; reshape categories anytime.
- Never store secrets/credentials here — store pointers to where they live.
