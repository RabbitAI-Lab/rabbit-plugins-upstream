# 三方交易 - 接口对接流程

URL: https://developer.meituan.com/docs/biz/biz_rms_0e077fa0-07e2-4198-b361-40ce496c69eb

搜 索热门搜索：入驻流程接口调用规则授权接入签名规则外卖接单菜品映射业务文档平台介绍开发指南生活服务到店餐饮外卖餐饮服务零售餐饮系统智能版业务介绍入驻/购买流程开发指南解决方案堂食扫码点餐自营外卖下单第三方会员CRM对接第三方财务对接第三方数据中台第三方供应链对接三方交易三方交易接口白皮书接口对接流程三方开台接口查询桌台订单接口三方下单接口供应链旗舰版对接三方系统-说明文档API文档快驴配送美团智能商用终端免费试美团平台直播酒店SaaS充电宝酒旅门店基础信息直连客满满门票直连美团收单企业版美团技术服务合作中心>生活服务>餐饮系统>智能版>解决方案>三方交易>接口对接流程>接口对接流程更新时间：2026-03-10AI生成摘要一、场景说明      为三方系统提供直连美团收银系统的能力，本地收银系统提供三方可直接调用的业务接口，三方通过这些接口，与美团收银系统进行打通，进行开台、下单等操作，因为是直连本地收银pos机进行接口调用的，所以要求三方应用要与收银pos在同一个局域网内      注：“三方交易”目录下的接口，和普通的开放平台接口对接逻辑不是同一套，如果三方想调这个目录中的接口，一定要按照完全按照这个文档的流程来，示例代码中有token的授权逻辑以及headers中的参数示例二、接口调用流程前提条件：三方已注册开发者账号，与开放平台对接完成整体流程核心包括两个流程：应用授权(换取本地authToken)和业务操作(接口调用)1、应用授权(换取本地authToken)流程先调用/rms/base/v1/auth/resources/get 接口获取当前开发者所被授权的资源列表，接口说明：https://developer.meituan.com/docs/api/rms-base-v1-auth-resources-get再调用/rms/base/api/v1/thirdparty/appauth 进行应用授权，核心是换取本地authToken，后续所有本接口调用时，都要携带本地authToken进行权限校验，接口说明：https://developer.meituan.com/docs/api/rms-base-api-v1-thirdparty-appauth授权代码示例Plain Textxxxxxxxxxxpublic class CloudOpenService {​    // 固定写死    private static final String BIZ = "{}";    // 固定写死    private static final String VERSION = "2";    // 固定写死    private static final String CHARSET = "UTF-8";​    private final CloudApi cloudApi;    // JSON 序列化工具，业务根据语言和框架可以自行解决    private final Gson mGson = new Gson();    private String localServerAuthToken = null;​​​    public CloudOpenService() {        this.cloudApi = new CloudApiProvider().getApi();    }​​    private Map<String, String> genRequest() {        Map<String, String> param = new HashMap<>();        param.put("timestamp", String.valueOf(new Date().getTime() / 1000));        param.put("developerId", Properties.DEVELOPER_ID);        param.put("biz", BIZ);        param.put("version", VERSION);        param.put("charset", CHARSET);        param.put("appAuthToken", Properties.ACCESS_TOKEN);        // 访问开放平台 sign 生成        param.put("sign", CryptoUtil.getSign(Properties.SIGN_KEY, param));        return param;    }​    private Map<String, String> genRequest(String biz) {        Map<String, String> param = new HashMap<>();        param.put("timestamp", String.valueOf(new Date().getTime() / 1000));        param.put("developerId", Properties.DEVELOPER_ID);        param.put("biz", biz);        param.put("version", VERSION);        param.put("charset", CHARSET);        param.put("appAuthToken", Properties.ACCESS_TOKEN);        // 访问开放平台 sign 生成        param.put("sign", CryptoUtil.getSign(Properties.SIGN_KEY, param));        return param;    }​    // 请求 LocalServer 明确报鉴权不过的时候失效缓存    public void invalidLocalServerAuthToken() {        this.localServerAuthToken = null;    }​    // 获取本地服务器访问 Token    // 获取（刷新）时机 1.进程启动 2.每隔 1 个星期刷新一次（如果程序一直不退出） 3.访问本地服务报鉴权错误    // 业务使用的时候可以缓存改函数返回的 token，在后续和本地服务器交互的时候重复使用，如果本地服务返回错误再进行重新请求    public String getLocalServerAuthToken() {        // 简单缓存        if (this.localServerAuthToken != null) {            System.out.println("use cache " + this.localServerAuthToken);            return this.localServerAuthToken;        }​        try {            // 请求开放平台云端获取必要信息 ORG_ID            DevelopAuthResResp resResp = cloudApi.getResources(genRequest()).toFuture().get();            if (!resResp.getCode().equals("OP_SUCCESS") ||                    resResp.getData().getResources() == null ||                    resResp.getData().getResources().isEmpty()) {                // 业务输出异常日志，便于后续排查。此步报错检查网络以及 genRequest 的各项参数                System.out.println("failed to get org_id " + resResp);                return null;            }​            // 请求开放平台云端获取必要信息 ORG_ID 获取本地服务器访问授权            AppAuthReq appAuthReq = new AppAuthReq();            appAuthReq.setAuthedOrgIds(resResp.getOrgIds());            appAuthReq.setCode(Properties.APP_CODE);            appAuthReq.setSn(Properties.SN);            appAuthReq.setMerchantNo(Properties.MERCHANT_NO);​            Map<String, String> request = genRequest(mGson.toJson(appAuthReq));            AppAuthResp appAuthResp = cloudApi.appAuth(request).toFuture().get();            if (!appAuthResp.getCode().equals("OP_SUCCESS") ||                    StringUtils.isEmpty(appAuthResp.getData().getAuthToken())) {                // 业务输出异常日志，便于后续排查。此步报错检查网络以及 genRequest 和 AppAuthReq 的各项参数                // code: 200002 -> appCode 非法，确认填写正确                // code: 200003/200004 -> merchantNo 非法，确认填写正确或者确认对应开发者账号DEVELOPER_ID是否授权该门店                System.out.println("failed to get auth token " + appAuthResp);                return null;            }​​            // 业务可以考虑多种缓存方案 authToken，不要频发请求云端            this.localServerAuthToken = appAuthResp.getData().getAuthToken();            System.out.println("new localserver auth token is " + this.localServerAuthToken);​            return this.localServerAuthToken;        } catch (Exception e) {            // 业务输出异常日志，便于后续排查。此步报错检查网络以及 genRequest 和 AppAuthReq 的各项参数            throw new RuntimeException("应用授权失败");        }​    }}​public class CloudApiProvider extends AbstractConfigProvider {​    private static final String UNIQUE_KEY = "THIRD_CLOUD_API_PROVIDER";​    public CloudApi getApi() {        NetworkManager.addNetworkFactory(UNIQUE_KEY, new DefaultNetworkFactory(this));        return NetworkManager.getService(UNIQUE_KEY, CloudApi.class);    }​    public CloudApiProvider() {    }​    @Override    public String getHost() {        return "https://api-open-cater.meituan.com";    }​    @Override    public HashMap<String, String> getHeaders() {        return new HashMap<String, String>(){{            put("Content-Type", "application/x-www-form-urlencoded;charset=utf-8");        }};    }​    @Override    public final RawCall.Factory getCallFactory() {        return OkHttp3CallFactory.create(HttpBuilder.createOkHttp3());    }​    @Override    public List<CallAdapter.Factory> getCallAdapterFactoryList() {        return Collections.<CallAdapter.Factory>singletonList(                RxJava2CallAdapterFactory.create()        );    }​    @Override    public long getCurrentTime() {        return new Date().getTime();    }}​​public interface CloudApi {​    // 应用认证    @FormUrlEncoded    @POST("/rms/base/api/v1/thirdparty/appauth")    Observable<AppAuthResp> appAuth(@FieldMap Map<String, String> req);​    // 开发者授权资源列表    @FormUrlEncoded    @POST("/rms/base/v1/auth/resources/get")    Observable<DevelopAuthResResp> getResources(@FieldMap Map<String, String> req);​}​public class DevelopAuthResResp {    private String code;    private String msg;    private DataBean data;​​    @Data    public static class DataBean {        private List<Resource> resources;    }​    @Data    public static class Resource {        private Integer orgId;    }​    public List<Integer> getOrgIds() {        if (this.data == null || this.data.getResources() == null || this.data.getResources().isEmpty()) {            return new ArrayList<>();        }        List<Integer> orgIds = new ArrayList<>();        for (Resource resource : this.data.getResources()) {            orgIds.add(resource.getOrgId());        }        return orgIds;    }​}​public class AppAuthReq {    private int code;    private String sn;    private String merchantNo;    private List<Integer> authedOrgIds;}​public class AppAuthResp {    private String code;    private String msg;    private DataBean data;​​    @Data    public static class DataBean {        private String authToken;    }}​public class Properties {    // 开放平台分配    public static final String DEVELOPER_ID = "123";    // 开放平台分配    public static final String SIGN_KEY = "123456";    // 开放平台分配，以上三个参数如何获取见文档，建议不要写死在代码中（权限敏感），考虑动态传入    // 具体见文档 https://developer.meituan.com/docs/biz/biz_2023243_b9c16a0b-93ba-49af-a570-39eb4861b92c    public static final String ACCESS_TOKEN = "V2-123456";​    // 餐饮 SaaS 分配，咨询对接人员，后续可固定写死在代码中    public static final int APP_CODE = 101;​    // 唯一标识本机物理设备，业务方自行生成，确保每个服务器该值不同即可，程序启动时根据特定逻辑获取生成    public static final String SN = "123412xad9uwj9sajxajs9x";​    // 本地服务器对应门店号，部署不同门店时确保该值正确，有限考虑外部动态传入    public static final String MERCHANT_NO = "51679303";}核心是CloudOpenService.getLocalServerAuthToken()方法，会通过上面两个方法，来获取本地authToken，并把这个这个authToken保存到本地，但authToken有有效期，一个星期后过期，所以建议三方系统在系统启动时刷新，同时用定时任务进行周期性刷新，另外防止有边界场景未刷新到，建议在业务接口调用时，如果发现返回的是未授权的异常code=42时，也再触发一次刷新，示例代码如下：接口调用失败刷新authToken示例Plain Textxxxxxxxxxxpublic class ThirdClientTest {​​    private static final int UNAUTHORIZED_ACCESS = 402;​    public static void main(String[] args) throws Exception {​        // step 1，初始化填充 Properties 各个字段        // step 2，初始化 CloudOpenService        CloudOpenService cloudOpenService = new CloudOpenService();        // step 3, 初始化 LsAPI。本地服务器 IP 自行指定（可以从主 POS UI 上获知）        LsApi lsAPI = new LsApiProvider("127.0.0.1", cloudOpenService).getApi();​        // 根据业务需求，请求本地服务 LocalServer        while (true) {            try {                // 业务侧可以考虑每 1 分钟请求一次心跳(频率不要太高），根据返回值考虑是否失效本地 Token                AliveResp resp = lsAPI.alive().toFuture().get();                System.out.println(resp);​                if (resp != null && resp.getCode() == UNAUTHORIZED_ACCESS) {                    // 明确鉴权不通过，失效当前本地鉴权 Token                    cloudOpenService.invalidLocalServerAuthToken();                }​                QueryTableOrderResp callOrderResp = lsAPI.queryTableOrder(new QueryTableOrderReq(1L)).toFuture().get();                System.out.println(callOrderResp);​            } catch (Exception e) {                System.out.println("failed to ping localserver. try later");                e.printStackTrace();            }​            Thread.sleep(11000);        }    }}2、业务操作(接口调用)流程      因为是直连本地收银pos机进行接口调用的，所以要求三方应用要与收银pos在同一个局域网内，而且要能知道主pos的ip地址，当主pos的ip变化时，三方系统也要能及时感知到，所以一般建议三方系统能有动态发现主pos ip地址的逻辑，或者有一个简易的页面，当主pos发生变化时，能手动更改这个ip地址，不影响后续接口调用。业务接口请求参数示例：业务接口请求参数示例Plain Textxxxxxxxxxxpublic class LsApiProvider extends AbstractConfigProvider {​    private static final String UNIQUE_KEY = "THIRD_LS_API_PROVIDER";​    private final String localServerIP;    private final CloudOpenService cloudOpenService;​    public LsApi getApi() {        NetworkManager.addNetworkFactory(UNIQUE_KEY, new DefaultNetworkFactory(this));        return NetworkManager.getService(UNIQUE_KEY, LsApi.class);    }​    public LsApiProvider(String localServerIP, CloudOpenService cloudOpenService) {        this.localServerIP = localServerIP;        this.cloudOpenService = cloudOpenService;    }​    @Override    public String getHost() {        return "http://" + localServerIP + ":3028";    }​    @Override    public HashMap<String, String> getHeaders() {        // 在请求本地服务器 LocalServer 时，以下 http request header 必须填入        return new HashMap<String, String>(){{            put("Content-Type", "application/json;charset=UTF-8");            put("Accept", "application/json");            put("appCode", String.valueOf(Properties.APP_CODE));            put("sn", Properties.SN);            put("merchantNo", Properties.MERCHANT_NO);            put("appToken", cloudOpenService.getLocalServerAuthToken());        }};    }​    @Override    public final RawCall.Factory getCallFactory() {        return OkHttp3CallFactory.create(HttpBuilder.createOkHttp3());    }​    @Override    public List<CallAdapter.Factory> getCallAdapterFactoryList() {        return Collections.<CallAdapter.Factory>singletonList(                RxJava2CallAdapterFactory.create()        );    }​    @Override    public long getCurrentTime() {        return new Date().getTime();    }}​public interface LsApi {​    // 探活    @GET("/api/monitor/alive")    Observable<AliveResp> alive();​    @GET("/api/v1/kds/third-party/query-order")    Observable<ThirdPartyCallOrderResp> queryThirdPartyCallOrder();​    @POST("/api/v1/third/table/order/query")    Observable<QueryTableOrderResp> queryTableOrder(@Body QueryTableOrderReq queryTableOrderReq);​​}​​其中headers中的appToken就是上一步中获取的本地authToken，其中localServerIP就是主pos的ip地址，端口是3028。实际接口调用可参考上面的“接口调用失败刷新authToken示例”中的ThirdClientTest，里面有刷新authToken的逻辑目前已提供接口列表接口url接口说明接口文档/api/v1/third/table/order/query查询指定桌台订单信息https://developer.meituan.com/docs/biz/biz_rms_0242f666-e9a5-431a-83e8-c95c333cfa5f/api/v1/third/tables/order/open开台，指定桌台进行开台https://developer.meituan.com/docs/biz/biz_rms_4be69aed-bd16-471b-affa-07815589867d/api/v1/third/purchase/order/submitOrder下单，指定订单id进行加菜下单https://developer.meituan.com/docs/biz/biz_rms_8398585a-c69d-4482-8a34-f363743815e7一、场景说明二、接口调用流程1、应用授权(换取本地authToken)流程2、业务操作(接口调用)流程新手入门业务文档API文档学习中心热门能力外卖餐饮到店餐饮餐饮系统配送业务帮助中心舆情反馈常见问题公告中心变更日志联系我们邮箱: mtdeveloper@meituan.com©2024美团技术服务合作中心京ICP备10211739号-1京公网安备 11000002002052号