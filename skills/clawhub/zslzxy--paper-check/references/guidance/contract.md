# guidance lane 合约

- 读取 `/api/paper/check/public/pwp/config`、各产品 `/public/config` 获取实时价格、格式和支付渠道。
- 推荐、答疑和验真入口是 Skill 内部知识和固定页面，不创建额外订单或交接 token。
- 检索范围、产品定位和报告指标只使用对应 reference 中列出的厂商官方资料；具体订单以报告显示范围为准。
- 回答算法问题时只讲厂商公开描述和行业级概念链路，不猜内部阈值。

推荐和答疑只返回知识/入口，不代替产品 lane 的提交；有具体文件与执行意图时必须转到相应产品模块。
