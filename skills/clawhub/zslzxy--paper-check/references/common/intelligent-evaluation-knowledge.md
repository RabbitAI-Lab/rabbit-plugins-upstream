# 智评、论文答疑与推荐

综合 Skill 的“智评”不是自动替用户作出学校或期刊的最终决定，主要包含：

1. 根据用途、语言、是否指定品牌和文件状态推荐产品；
2. 解释格式、支付、时效、报告、隐私和字符口径；
3. 将需要人工确认或官方页面操作的事项交给用户端。

回答事实时优先调用 `guidance` 的推荐/答疑接口，使用接口返回的 FAQ 和商品事实。价格、支持格式、报告时效、退款与保存期限都是动态信息，Skill 不用模型记忆补写。

“帮我判断是否能毕业/是否一定通过”这类问题只能给出风险提示和核对清单，不能替代学校规定、导师意见或检测机构报告。
