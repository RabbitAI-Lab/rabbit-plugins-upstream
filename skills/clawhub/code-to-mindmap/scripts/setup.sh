#!/bin/bash
# Setup for code-to-mindmap scripts/
# No Python dependencies required — uses mmdc CLI or mermaid.live
echo "No external dependencies needed."
echo "For PNG/SVG rendering, install mmdc: npm install -g @mermaid-js/mermaid-cli"
