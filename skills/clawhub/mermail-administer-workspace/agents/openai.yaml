interface:
  display_name: "Administer Mermail Workspace"
  short_description: "Manage Mermail workspaces and mailboxes"
  default_prompt: "Use $mermail-administer-workspace to manage this Mermail workspace safely."
dependencies:
  tools:
    - type: "mcp"
      value: "mermail"
      description: "Mermail workspace and mailbox MCP server"
      transport: "streamable_http"
      url: "https://console.mermail.app/mcp"
