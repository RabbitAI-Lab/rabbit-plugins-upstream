# Changelog

## 0.1.0 - 2026-07-31

- Initial ClawHub-ready release of `xhs-knowledge-retriever`.
- Retrieves semantically relevant Xiaohongshu competitor-note chunks from a local RAG index.
- Adds `--check-only` to verify local index readiness without requiring a query.
- Declares runtime requirements in `metadata.openclaw`: `python3` and optional `XHS_KNOWLEDGE_ROOT`.
- Documents security posture, dependency list, and publishing flow.
