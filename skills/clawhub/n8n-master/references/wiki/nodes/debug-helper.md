# Debug Helper

## 何时读取

当用户要在 n8n 中使用、配置、排查或替代 `Debug Helper` 时读取。该卡片由官方节点文档编译，完整细节以 source 为准。

## 节点标识

- n8n node id: `n8n-nodes-base.debughelper`
- node group: `core-nodes`

## 核心要点

- Documentation for the Debug Helper node in n8n, a workflow automation platform. Includes guidance on usage, and links to examples.

## 关键操作 / 参数线索

- **Do Nothing**: Don't do anything.
- **Throw Error**: Throw an error with the specified type and message.
- **Out Of Memory**: Generate a specific memory size to simulate being out of memory.
- **Generate Random Data**: Generate some random data in a selected format.

## n8n 使用建议

- 需要精确字段、选项枚举、认证方式或错误解释时，回读 source 文件，不凭记忆补全。

