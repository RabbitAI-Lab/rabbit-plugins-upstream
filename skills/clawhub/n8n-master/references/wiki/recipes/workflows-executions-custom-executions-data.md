# Custom executions data

## 何时读取

当用户的问题涉及 n8n 文档 `workflows/executions/custom-executions-data.md` 的主题、配置、概念或操作步骤时读取。

## 核心要点

- Add custom data to your workflow executions using the Code node. You can then filter executions by this data.

## 快速定位

- Set and access custom data using the Code node
- Set custom executions data
- Access the custom data object during execution

