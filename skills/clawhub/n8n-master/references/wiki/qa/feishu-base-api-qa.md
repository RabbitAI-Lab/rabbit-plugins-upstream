# Feishu Base API Q&A

## 何时读取

当用户在 n8n + 飞书多维表格场景里遇到鉴权、字段格式、权限或接口组合疑问时读取。

## 核心要点

- **第 1 步**：调用上传素材或分片上传素材接口将附件上传至多维表格，获取文件的 file_token。 **第 2 步**：调用新增记录或更新记录接口，将 file_token 的值传入多维表格中。请求示例如下所示： file_token 仅支持在当前多维表格中使用，要上传附件至其他多维表格，你仍需要重新上传素材获取新的 file_token。 ```json , ] } }, , ] } } ] } ```

## 快速定位

- 1. 如何在多维表格中上传附件？
- 2. 如何下载多维表格中的附件？
- 3. 调用多维表格相关接口，返回 1254040、1254041、1254042、1254043、1254044 等 NotFound 错误码，该如何解决？
- 4. 调用查询记录接口成功，数据返回为空，但实际多维表格存在数据，该如何解决？
- 5. 调用查询记录接口，复选框返回数据为空，如何解决？
- 6. 如何筛选自定义编号类型的自动编号字段？
- 7. 调用查询记录接口，如何筛选人员字段？
- 8. 查询记录接口是否支持查询特定行，如数据表第 10 行~20 行的数据？
- 9. 如何获取多维表格指定数据表的总记录数（或总行数）？

