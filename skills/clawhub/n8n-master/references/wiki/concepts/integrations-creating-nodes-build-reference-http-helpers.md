# HTTP request helper for node builders

## 何时读取

当用户的问题涉及 n8n 文档 `integrations/creating-nodes/build/reference/http-helpers.md` 的主题、配置、概念或操作步骤时读取。

## 核心要点

- n8n provides a flexible helper for making HTTP requests, which abstracts away most of the complexity. Call the helper inside the `execute` function.

## 快速定位

- Usage
- Example
- Deprecation of the previous helper
- Migration guide to the new helper

