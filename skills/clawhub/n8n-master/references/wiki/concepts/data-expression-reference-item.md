# Item

## 何时读取

当用户的问题涉及 n8n 文档 `data/expression-reference/item.md` 的主题、配置、概念或操作步骤时读取。

## 核心要点

- **Description:** Returns any binary data the item contains **Syntax:** `$item`.`$item`.**`binary`**

## 快速定位

- `$item`.**`binary`**
- `$item`.**`json`**

