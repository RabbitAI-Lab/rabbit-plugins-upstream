# NodeOutputData

## 何时读取

当用户的问题涉及 n8n 文档 `data/expression-reference/nodeoutputdata.md` 的主题、配置、概念或操作步骤时读取。

## 核心要点

- **Description:** Returns an array of the node’s output items **Syntax:** `$()`.all(branchIndex?, runIndex?)

## 快速定位

- `$()`.**`all()`**
- `$()`.**`first()`**
- `$()`.**`isExecuted`**
- `$()`.**`item`**
- `$()`.**`itemMatching()`**
- `$()`.**`last()`**
- `$()`.**`params`**

