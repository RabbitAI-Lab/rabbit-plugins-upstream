# Root

## 何时读取

当用户的问题涉及 n8n 文档 `data/expression-reference/root.md` 的主题、配置、概念或操作步骤时读取。

## 核心要点

- **Description:** Returns the data of the specified node **Syntax:** $(nodeName)

## 快速定位

- **`$()`**
- **`$binary`**
- **`$execution`**
- **`$fromAI()`**
- **`$if()`**
- **`$ifEmpty()`**
- **`$input`**
- **`$itemIndex`**
- **`$jmespath()`**
- **`$json`**
- **`$max()`**
- **`$min()`**
- **`$nodeVersion`**
- **`$now`**
- **`$pageCount`**
- **`$parameter`**
- **`$prevNode`**
- **`$request`**

