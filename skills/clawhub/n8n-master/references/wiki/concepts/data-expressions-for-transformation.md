# Expressions for data transformation

## 何时读取

当用户的问题涉及 n8n 文档 `data/expressions-for-transformation.md` 的主题、配置、概念或操作步骤时读取。

## 核心要点

- You can use expression transformation functions anywhere expressions are supported in n8n. However, if your main goal is to transform data using expressions without performing any other operations, use the **Edit Fields (Set)** node. This node is designed specifically for data transformation, providing a clean interface to:

## 快速定位

- Example: Get data from webhook body
- Using expressions in credentials
- Example: Writing longer JavaScript as expressions
- Common issues
- The 'JSON Output' in item 0 contains invalid JSON
- Can't get data for expression
- Invalid syntax

