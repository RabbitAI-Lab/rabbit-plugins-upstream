# `("<node-name>").all(branchIndex?: number, runIndex?: number)`

## 何时读取

当用户的问题涉及 n8n 文档 `code/cookbook/builtin/all.md` 的主题、配置、概念或操作步骤时读取。

## 核心要点

- This gives access to all the items of the current or parent nodes. If you don't supply any parameters, it returns all the items of the current node. === "JavaScript" ```js // Returns all the items of the given node and current run let allItems = $("").all();

## 快速定位

- Getting items
- Accessing item data

