# Boolean

## 何时读取

当用户的问题涉及 n8n 文档 `data/expression-reference/boolean.md` 的主题、配置、概念或操作步骤时读取。

## 核心要点

- **Description:** Returns false for all booleans. Returns true for null. **Syntax:** _`Boolean`_.isEmpty()

## 快速定位

- _`Boolean`_.**`isEmpty()`**
- _`Boolean`_.**`toNumber()`**
- _`Boolean`_.**`toString()`**

