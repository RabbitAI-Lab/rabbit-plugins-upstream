# Date

## 何时读取

当用户的问题涉及 n8n 文档 `data/expression-reference/date.md` 的主题、配置、概念或操作步骤时读取。

## 核心要点

- **Description:** Converts a JavaScript Date to a Luxon DateTime. The DateTime contains the same information, but is easier to manipulate. **Syntax:** _`Date`_.toDateTime()

## 快速定位

- _`Date`_.**`toDateTime()`**

