# WorkflowData

## 何时读取

当用户的问题涉及 n8n 文档 `data/expression-reference/workflowdata.md` 的主题、配置、概念或操作步骤时读取。

## 核心要点

- **Description:** Whether the workflow is active **Syntax:** `$workflow`.`$workflow`.**`active`**

## 快速定位

- `$workflow`.**`active`**
- `$workflow`.**`id`**
- `$workflow`.**`name`**

