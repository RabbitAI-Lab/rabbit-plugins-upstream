interface:
  display_name: "Dataify YouTube Product By ID"
  short_description: "Collect YouTube Product By ID data and return results"
  icon_small: "./assets/dataify-youtube-product-by-id-small.svg"
  icon_large: "./assets/dataify-youtube-product-by-id-large.svg"
  default_prompt: "Use $dataify-youtube-product-by-id to complete the requested Dataify collection, wait for the asynchronous task, and return the final collected result. Stop at task submission only when I explicitly request no-wait behavior."

policy:
  allow_implicit_invocation: true
