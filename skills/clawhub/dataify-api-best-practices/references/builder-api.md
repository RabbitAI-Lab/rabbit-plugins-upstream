# Builder API

Prefer an existing platform scraper for structured records. Validate inputs before submission and persist the returned task ID before doing any other work. Submission is not completion. Never automatically repeat a submission when the client cannot tell whether the first request reached the server.
