# Web Unlocker

POST JSON to `https://webunlocker.dataify.com/request` with a validated public HTTP(S) URL. Choose HTML/clean content/screenshot according to the requested output. Decode UTF-8 with replacement, follow redirects deliberately, cap response time, and detect block/error pages before treating a 200 response as usable content.
