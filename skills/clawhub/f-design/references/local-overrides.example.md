# Local Overrides Example

Copy this to `~/.f-design/preferences.md` for machine-local preferences. Do not commit personal preferences to a public skill repository.

```text
Default language: en or zh-CN
Preferred frontend stack:
Preferred package manager:
Preferred icon library:
Default screenshot devices:
Default quality threshold:
Default review artifact:
Default artifact presentation:
Default approval behavior:
Forbidden visual patterns:
Preferred output format:
Local commands:
```

Example:

```text
Default language: zh-CN
Preferred frontend stack: React, Next.js, Vite
Preferred package manager: follow existing lockfile
Default screenshot devices: 1440x900, 1024x768, 390x844
Default quality threshold: no score below 8
Default review artifact: standalone responsive HTML
Default artifact presentation: auto-open only on a shared desktop, otherwise attach screenshots
Default approval behavior: pause for Level 2 design confirmation
Forbidden visual patterns: generic AI gradients, decorative nested cards
Preferred output format: concise implementation summary with file paths and checks
```
