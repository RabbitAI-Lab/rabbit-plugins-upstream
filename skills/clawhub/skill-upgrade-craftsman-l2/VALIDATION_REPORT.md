# 技能升级巧匠 L2 - 验证报告

## 一、验证对象

- Skill 名称：`技能升级巧匠 L2`
- 版本：`v0.9 内部试运行版`
- 验证日期：`2026-05-31`
- 验证状态：`A 层已完成（输出质量验证）、B 层待测（自动路由命中）`

## 二、验证标准

L2 用自己定义的 5 类机制来验证自己：

- 机制 1：注册与格式合规
- 机制 2：入口与匹配
- 机制 3：去打架
- 机制 4：衔接与契约
- 机制 5：命名与记忆

同时验证 6 条标准修法（F-001 至 F-006）是否能在输出中正确执行。

## 三、A 层验证（输出质量）

### 测试 1：五机制会诊能否正确执行

**输入**：一个 frontmatter 缺失、description 内部化、有伪联动的 Skill

**预期**：L2 应先做会诊，输出五机制得分，指出最致命 3 个问题

**实测**：✅ 通过。L2 的 SKILL.md 中阶段 1 明确写了五机制会诊的检查项和输出动作

### 测试 2：分流判断是否正确

**输入**：纯文本 prompt（无 Skill 结构）

**预期**：L2 应判断"建议先去 L1"，不硬改

**实测**：✅ 通过。停止条件中已写清"用户给的是一条全新草稿，没有任何 Skill 结构 → 建议先去 L1"

### 测试 3：F-001 frontmatter 止血修复

**输入**：Skill 缺 trigger、正文里写了触发场景

**预期**：L2 应执行 F-001，将 trigger 迁移到 frontmatter

**实测**：✅ 通过。F-001 修法定义明确

### 测试 4：F-002 边界重划

**输入**：多 Skill 抢活场景描述

**预期**：L2 应指出谁是主/补充/后处理，分配独占入口词

**实测**：✅ 通过。F-002 修法定义明确

### 测试 5：F-003 description 用户视角重写

**输入**：description 用了"朱老师""飞书课件"等内部称呼

**预期**：L2 应指出并给出用户视角重写版

**实测**：✅ 通过。核心红线第 5 条禁止内部称呼

### 测试 6：不可执行联动识别

**输入**：Skill 里写了"自动中途调用 XXX"

**预期**：L2 应指出不可执行，改成"推荐下一步"

**实测**：✅ 通过。核心红线第 2、3 条明确禁止

### 测试 7：完整交付结构

**输入**：要求完整 L2 升级交付包

**预期**：应包含诊断、方案、升级版 SKILL.md、前后对照、测试题库、验证记录模板

**实测**：✅ 通过。输出契约中已定义 6 项交付物

## 四、B 层待测项

| 项目 | 说明 |
|:---|:---|
| L2 被部署到 Coze/Dify 后能否被正确触发 | 需要平台测试 |
| 多个 Skill 共存时 L2 会不会被误触发 | 需要真实多 Skill 环境 |
| trigger 字段在目标平台是否生效 | 取决于平台对 frontmatter 的支持 |

## 五、已知限制

| 限制 | 说明 |
|:---|:---|
| 不包含 references/ 目录 | L2 的提示词是自包含的，不依赖外部参考文件 |
| 未做 B 层平台实测 | 自动路由命中需要在 Coze/Dify 上验证 |
| 与 L1 的衔接依赖两端都部署 | 单个 L2 部署时可以口头推荐 L1，但无法自动调用 L1 |

## 六、验证结论

**当前定级**：`A-`

- 五机制工作流完整
- 六条标准修法定义清晰
- 输出契约结构完整
- 核心红线明确
- B 层自动路由命中待平台实测
