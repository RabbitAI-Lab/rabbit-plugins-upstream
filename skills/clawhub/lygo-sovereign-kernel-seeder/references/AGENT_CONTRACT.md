# Agent contract — Sovereign Kernel Seeder

1. Consent before seed.  
2. Seed → verify → only then load modules.  
3. Exit 3 / QUARANTINE → stop, report, do not decode inline_b64 for execution.  
4. Never auto-publish.  
5. Never invent egg_ids as “aligned” without running `verify_seed.py`.  
6. Prefer this skill for **local modular seed**; use `lygo-kernel-egg-planter` for full stack surfaces.  
7. After success, quote `registry_merkle_root` and egg `content_sha256`.  
