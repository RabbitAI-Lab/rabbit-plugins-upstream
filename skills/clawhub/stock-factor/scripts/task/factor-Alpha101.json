{
  "tool_name": "batch_factor_analysis",
  "feature_days": 5,
  "factor_dict": {
    "Alpha#1":"ac = d['close'] * d['adj_factor']; ret = ac / ac.shift(1) - 1; cond_val = ret.rolling(20).std().where(ret < 0, ac); power_val = cond_val ** 2; argmax_days = rolling_imax(power_val, 5) * 5; out = row_rank(argmax_days)-0.5",
    "Alpha#2":"ac = d['close'] * d['adj_factor']; ao = d['open'] * d['adj_factor']; vol = d['vol']; out = -1 * (row_rank(np.log(vol + 1e-12).diff(2))).rolling(6).corr(row_rank((ac - ao) / ao))",
    "Alpha#3": "ac = d['close'] * d['adj_factor']; ao = d['open'] * d['adj_factor']; vol = d['vol']; out = -1 * row_rank(ao).rolling(10).corr(row_rank(vol))",
    "Alpha#4": "al = d['low'] * d['adj_factor']; out = -1 * rolling_rank(row_rank(al), 9)",
    "Alpha#5": "ac = d['close'] * d['adj_factor']; ao = d['open'] * d['adj_factor']; avwap = (d['amount'] / (d['vol'] + 1e-12)) * d['adj_factor']; out = row_rank(ao - avwap.rolling(10).sum()/10) * (-1 * np.abs(row_rank(ac - avwap)))",
    "Alpha#6": "ao = d['open'] * d['adj_factor']; vol = d['vol']; out = -1 * ao.rolling(10).corr(vol)",
    "Alpha#7": "ac = d['close'] * d['adj_factor']; vol = d['vol']; adv20 = d['amount'].rolling(20).mean(); delta7 = ac - ac.shift(7); ts_rank_abs = delta7.abs().rolling(60).rank(pct=False); sign_delta = np.sign(delta7); out = ((-1 * ts_rank_abs) * sign_delta).where(adv20 < vol, -1)",
    "Alpha#8": "ac = d['close'] * d['adj_factor']; ao = d['open'] * d['adj_factor']; ret = ac / ac.shift(1) - 1; prod = ao.rolling(5).sum() * ret.rolling(5).sum(); out = -1 * row_rank(prod - prod.shift(10))",
    "Alpha#9": "ac = d['close'] * d['adj_factor']; delta1 = ac - ac.shift(1); tsmin5 = delta1.rolling(5).min(); tsmax5 = delta1.rolling(5).max(); out = delta1.where(0 < tsmin5, delta1.where(tsmax5 < 0, -delta1))",
    "Alpha#10": "ac = d['close'] * d['adj_factor']; delta1 = ac - ac.shift(1); tsmin4 = delta1.rolling(4).min(); tsmax4 = delta1.rolling(4).max(); tmp = delta1.where(0 < tsmin4, delta1.where(tsmax4 < 0, -delta1)); out = row_rank(tmp)",
    "Alpha#11": "ac = d['close'] * d['adj_factor']; avwap = (d['amount'] / (d['vol'] + 1e-12)) * d['adj_factor']; vol = d['vol']; ts_max = (avwap - ac).rolling(3).max(); ts_min = (avwap - ac).rolling(3).min(); delta_vol = vol - vol.shift(3); out = (row_rank(ts_max) + row_rank(ts_min)) * row_rank(delta_vol)",
    "Alpha#12": "ac = d['close'] * d['adj_factor']; vol = d['vol']; out = np.sign(vol - vol.shift(1)) * (-1 * (ac - ac.shift(1)))",
    "Alpha#13": "ac = d['close'] * d['adj_factor']; vol = d['vol']; out = -1 * row_rank(row_rank(ac).rolling(5).cov(row_rank(vol)))",
    "Alpha#14": "ac = d['close'] * d['adj_factor']; ao = d['open'] * d['adj_factor']; vol = d['vol']; ret = ac / ac.shift(1) - 1; out = (-1 * row_rank(ret - ret.shift(3))) * ao.rolling(10).corr(vol)",
    "Alpha#15": "ah = d['high'] * d['adj_factor']; vol = d['vol']; corr3 = row_rank(ah).rolling(3).corr(row_rank(vol)); out = -1 * row_rank(corr3).rolling(3).sum()",
    "Alpha#16": "ah = d['high'] * d['adj_factor']; vol = d['vol']; out = -1 * row_rank(row_rank(ah).rolling(5).cov(row_rank(vol)))",
    "Alpha#17": "ac = d['close'] * d['adj_factor']; vol = d['vol']; adv20 = d['amount'].rolling(20).mean(); ts_rank10 = rolling_rank(ac, 10); delta_delta = ac - 2*ac.shift(1) + ac.shift(2); ts_rank_vol_adv = rolling_rank(vol / adv20, 5); out = ((-1 * row_rank(ts_rank10)) * row_rank(delta_delta)) * row_rank(ts_rank_vol_adv)",
    "Alpha#18": "ac = d['close'] * d['adj_factor']; ao = d['open'] * d['adj_factor']; abs_diff = (ac - ao).abs(); std5 = abs_diff.rolling(5).std(); corr10 = ac.rolling(10).corr(ao); out = -1 * row_rank(std5 + (ac - ao) + corr10)",
    "Alpha#19": "ac = d['close'] * d['adj_factor']; ret = ac / ac.shift(1) - 1; diff7 = ac - ac.shift(7); sign_val = np.sign(2 * diff7); out = (-1 * sign_val) * (1 + row_rank(1 + ret.rolling(250).sum()))",
    "Alpha#20": "ac = d['close'] * d['adj_factor']; ao = d['open'] * d['adj_factor']; ah = d['high'] * d['adj_factor']; al = d['low'] * d['adj_factor']; out = (-1 * row_rank(ao - ah.shift(1))) * row_rank(ao - ac.shift(1)) * row_rank(ao - al.shift(1))",
    "Alpha#21": "ac = d['close'] * d['adj_factor']; vol = d['vol']; adv20 = d['amount'].rolling(20).mean(); sum8 = ac.rolling(8).mean(); std8 = ac.rolling(8).std(); sum2 = ac.rolling(2).mean(); cond1 = (sum8 + std8) < sum2; cond2 = sum2 < (sum8 - std8); cond3 = (vol / adv20) >= 1; out = np.where(cond1, -1, np.where(cond2, 1, np.where(cond3, 1, -1))); out=pd.DataFrame(out)",
    "Alpha#22": "ac = d['close'] * d['adj_factor']; ah = d['high'] * d['adj_factor']; vol = d['vol']; corr5 = ah.rolling(5).corr(vol); corr5_delta = corr5 - corr5.shift(5); rank_std = row_rank(ac.rolling(20).std()); out = -1 * (corr5_delta * rank_std)",
    "Alpha#23": "ah = d['high'] * d['adj_factor']; high_ma20 = ah.rolling(20).mean(); out = np.where(high_ma20 < ah, -1 * (ah - ah.shift(2)), 0); out=pd.DataFrame(out)",
    "Alpha#24": "ac = d['close'] * d['adj_factor']; ma100 = ac.rolling(100).mean(); delta_ma100 = ma100 - ma100.shift(100); ratio = delta_ma100 / ac.shift(100); cond = ratio <= 0.05; out = np.where(cond, -1 * (ac - ac.rolling(100).min()), -1 * (ac - ac.shift(3))); out=pd.DataFrame(out)",
    "Alpha#25": "ac = d['close'] * d['adj_factor']; ah = d['high'] * d['adj_factor']; avwap = (d['amount'] / (d['vol'] + 1e-12)) * d['adj_factor']; vol = d['vol']; adv20 = d['amount'].rolling(20).mean(); ret = ac / ac.shift(1) - 1; out = row_rank((-1 * ret) * adv20 * avwap * (ah - ac))",
    "Alpha#26": "vol = d['vol']; ah = d['high'] * d['adj_factor']; ts_rank5_vol = rolling_rank(vol, 5); ts_rank5_high = rolling_rank(ah, 5); corr5 = ts_rank5_vol.rolling(5).corr(ts_rank5_high); out = -1 * corr5.rolling(3).max()",
    "Alpha#27": "vol = d['vol']; avwap = (d['amount'] / (d['vol'] + 1e-12)) * d['adj_factor']; corr6 = row_rank(vol).rolling(6).corr(row_rank(avwap)); sum2_corr = corr6.rolling(2).sum() / 2.0; rank_sum = row_rank(sum2_corr); out = np.where(0.5 < rank_sum, -1, 1); out=pd.DataFrame(out)",
    "Alpha#28": "adv20 = d['amount'].rolling(20).mean(); ah = d['high'] * d['adj_factor']; al = d['low'] * d['adj_factor']; ac = d['close'] * d['adj_factor']; corr = adv20.rolling(5).corr(al); expr = corr + (ah + al) / 2 - ac; out = row_scale(expr)",
    "Alpha#29": "ac = d['close'] * d['adj_factor']; ret = ac / ac.shift(1) - 1; delta5 = ac - ac.shift(5); rank_delta = row_rank(delta5); neg_rank = -1 * rank_delta; inner_rank1 = row_rank(neg_rank); ts_min_inner = inner_rank1.rolling(2).min(); log_val = np.log(ts_min_inner + 1e-12); scale_val = row_scale(log_val); rank_scale = row_rank(scale_val); rank_rank_scale = row_rank(rank_scale); min_part = rank_rank_scale.rolling(5).min(); part2 = rolling_rank(-1 * ret.shift(6), 5); out = min_part + part2",
    "Alpha#30": "ac = d['close'] * d['adj_factor']; vol = d['vol']; sign1 = np.sign(ac - ac.shift(1)); sign2 = np.sign(ac.shift(1) - ac.shift(2)); sign3 = np.sign(ac.shift(2) - ac.shift(3)); sum_sign = sign1 + sign2 + sign3; rank_sign = row_rank(sum_sign); out = (1 - rank_sign) * vol.rolling(5).sum() / vol.rolling(20).sum()",
    "Alpha#31": "ac = d['close'] * d['adj_factor']; al = d['low'] * d['adj_factor']; vol = d['vol']; adv20 = d['amount'].rolling(20).mean(); part1 = row_rank(row_rank(row_rank(rolling_decay_linear(-1 * row_rank(row_rank(ac - ac.shift(10))), 10)))); part2 = row_rank(-1 * (ac - ac.shift(3))); part3 = np.sign(row_scale(adv20.rolling(12).corr(al))); out = part1 + part2 + part3",
    "Alpha#32": "ac = d['close'] * d['adj_factor']; avwap = (d['amount'] / (d['vol'] + 1e-12)) * d['adj_factor']; part1 = row_scale(ac.rolling(7).mean() - ac); part2 = 20 * row_scale(avwap.rolling(230).corr(ac.shift(5))); out = part1 + part2",
    "Alpha#33": "ac = d['close'] * d['adj_factor']; ao = d['open'] * d['adj_factor']; out = row_rank(-1 * (1 - ao / ac))",
    "Alpha#34": "ac = d['close'] * d['adj_factor']; ret = ac / ac.shift(1) - 1; std2 = ret.rolling(2).std(); std5 = ret.rolling(5).std(); rank_ratio = row_rank(std2 / std5); rank_delta = row_rank(ac - ac.shift(1)); out = row_rank((1 - rank_ratio) + (1 - rank_delta))",
    "Alpha#35": "ac = d['close'] * d['adj_factor']; ah = d['high'] * d['adj_factor']; al = d['low'] * d['adj_factor']; vol = d['vol']; ret = ac / ac.shift(1) - 1; ts_rank_vol32 = rolling_rank(vol, 32); ts_rank_expr16 = rolling_rank((ac + ah) - al, 16); ts_rank_ret32 = rolling_rank(ret, 32); out = ts_rank_vol32 * (1 - ts_rank_expr16) * (1 - ts_rank_ret32)",
    "Alpha#36": "ac = d['close'] * d['adj_factor']; ao = d['open'] * d['adj_factor']; avwap = (d['amount'] / (d['vol'] + 1e-12)) * d['adj_factor']; vol = d['vol']; ret = ac / ac.shift(1) - 1; adv20 = d['amount'].rolling(20).mean(); part1 = 2.21 * row_rank((ac - ao).rolling(15).corr(vol.shift(1))); part2 = 0.7 * row_rank(ao - ac); part3 = 0.73 * row_rank(rolling_rank(-1 * ret.shift(6), 5)); part4 = row_rank(np.abs(avwap.rolling(6).corr(adv20))); part5 = 0.6 * row_rank(((ac.rolling(200).mean() - ao) * (ac - ao))); out = part1 + part2 + part3 + part4 + part5",
    "Alpha#37": "ac = d['close'] * d['adj_factor']; ao = d['open'] * d['adj_factor']; diff = ao - ac; corr = diff.shift(1).rolling(200).corr(ac); out = row_rank(corr) + row_rank(diff)",
    "Alpha#38": "ac = d['close'] * d['adj_factor']; ao = d['open'] * d['adj_factor']; ts_rank10 = rolling_rank(ac, 10); out = (-1 * row_rank(ts_rank10)) * row_rank(ac / ao)",
    "Alpha#39": "ac = d['close'] * d['adj_factor']; vol = d['vol']; adv20 = d['amount'].rolling(20).mean(); ret = ac / ac.shift(1) - 1; delta7 = ac - ac.shift(7); decay = rolling_decay_linear(vol / adv20, 9); rank_decay = row_rank(decay); expr = delta7 * (1 - rank_decay); rank_expr = row_rank(expr); sum_ret250 = ret.rolling(250).sum(); rank_sumret = row_rank(sum_ret250); out = (-1 * rank_expr) * (1 + rank_sumret)",
    "Alpha#40": "ah = d['high'] * d['adj_factor']; vol = d['vol']; std10 = ah.rolling(10).std(); rank_std = row_rank(std10); corr10 = ah.rolling(10).corr(vol); out = (-1 * rank_std) * corr10",
    "Alpha#41": "ah = d['high'] * d['adj_factor']; al = d['low'] * d['adj_factor']; avwap = (d['amount'] / (d['vol'] + 1e-12)) * d['adj_factor']; out = np.sqrt(ah * al) - avwap",
    "Alpha#42": "ac = d['close'] * d['adj_factor']; avwap = (d['amount'] / (d['vol'] + 1e-12)) * d['adj_factor']; out = row_rank(avwap - ac) / row_rank(avwap + ac)",
    "Alpha#43": "vol = d['vol']; adv20 = d['amount'].rolling(20).mean(); ac = d['close'] * d['adj_factor']; ts1 = rolling_rank(vol / adv20, 20); ts2 = rolling_rank(-1 * (ac - ac.shift(7)), 8); out = ts1 * ts2",
    "Alpha#44": "ah = d['high'] * d['adj_factor']; vol = d['vol']; out = -1 * ah.rolling(5).corr(row_rank(vol))",
    "Alpha#45": "ac = d['close'] * d['adj_factor']; vol = d['vol']; sum_close5 = ac.rolling(5).sum(); sum_close20 = ac.rolling(20).sum(); delay_close5_shift = ac.shift(5).rolling(20).sum() / 20; rank1 = row_rank(delay_close5_shift); corr2 = ac.rolling(2).corr(vol); corr2_sum = sum_close5.rolling(2).corr(sum_close20); rank_corr2 = row_rank(corr2_sum); out = -1 * ((rank1 * corr2) * rank_corr2)",
    "Alpha#46": "ac = d['close'] * d['adj_factor']; x = (ac.shift(20) - ac.shift(10)) / 10 - (ac.shift(10) - ac) / 10; cond1 = 0.25 < x; cond2 = x < 0; out = pd.DataFrame(np.where(cond1, -1, np.where(cond2, 1, -1 * (ac - ac.shift(1)))), index=ac.index, columns=ac.columns)",
    "Alpha#47": "ac = d['close'] * d['adj_factor']; ah = d['high'] * d['adj_factor']; avwap = (d['amount'] / (d['vol'] + 1e-12)) * d['adj_factor']; vol = d['vol']; adv20 = d['amount'].rolling(20).mean(); part1 = ((row_rank(1 / ac) * vol) / adv20) * ((ah * row_rank(ah - ac)) / (ah.rolling(5).sum() / 5)); part2 = row_rank(avwap - avwap.shift(5)); out = part1 - part2",
    "Alpha#48": "ac = d['close'] * d['adj_factor']; delta1 = ac - ac.shift(1); corr = delta1.rolling(250).corr(ac.shift(1) - ac.shift(2)); numerator = (corr * delta1) / ac; numerator_neutral = row_indneutralize(numerator, col_attrs['所属行业']); denominator = ((delta1 / ac.shift(1)) ** 2).rolling(250).sum(); out = numerator_neutral / denominator",
    "Alpha#49": "ac = d['close'] * d['adj_factor']; x = (ac.shift(20) - ac.shift(10)) / 10 - (ac.shift(10) - ac) / 10; out = pd.DataFrame(np.where(x < -0.1, 1, -1 * (ac - ac.shift(1))), index=ac.index, columns=ac.columns)",
    "Alpha#50": "vol = d['vol']; avwap = (d['amount'] / (d['vol'] + 1e-12)) * d['adj_factor']; corr5 = row_rank(vol).rolling(5).corr(row_rank(avwap)); rank_corr = row_rank(corr5); out = -1 * rank_corr.rolling(5).max()",
    "Alpha#51": "ac = d['close'] * d['adj_factor']; x = (ac.shift(20) - ac.shift(10)) / 10 - (ac.shift(10) - ac) / 10; out = pd.DataFrame(np.where(x < -0.05, 1, -1 * (ac - ac.shift(1))), index=ac.index, columns=ac.columns)",
    "Alpha#52": "al = d['low'] * d['adj_factor']; vol = d['vol']; ac = d['close'] * d['adj_factor']; ret = ac / ac.shift(1) - 1; ts_min5 = al.rolling(5).min(); ts_min5_delay = ts_min5.shift(5); expr = (-1 * ts_min5 + ts_min5_delay) * row_rank((ret.rolling(240).sum() - ret.rolling(20).sum()) / 220) * rolling_rank(vol, 5); out = expr",
    "Alpha#53": "ac = d['close'] * d['adj_factor']; ah = d['high'] * d['adj_factor']; al = d['low'] * d['adj_factor']; expr = ((ac - al) - (ah - ac)) / (ac - al); out = -1 * (expr - expr.shift(9))",
    "Alpha#54": "ac = d['close'] * d['adj_factor']; ao = d['open'] * d['adj_factor']; ah = d['high'] * d['adj_factor']; al = d['low'] * d['adj_factor']; out = -1 * ((al - ac) * (ao ** 5)) / ((al - ah) * (ac ** 5))",
    "Alpha#55": "ac = d['close'] * d['adj_factor']; ah = d['high'] * d['adj_factor']; al = d['low'] * d['adj_factor']; vol = d['vol']; ts_min_low12 = al.rolling(12).min(); ts_max_high12 = ah.rolling(12).max(); expr = (ac - ts_min_low12) / (ts_max_high12 - ts_min_low12); rank_expr = row_rank(expr); rank_vol = row_rank(vol); out = -1 * rank_expr.rolling(6).corr(rank_vol)",
    "Alpha#56": "ac = d['close'] * d['adj_factor']; ret = ac / ac.shift(1) - 1; cap = d['总市值']; sum_ret10 = ret.rolling(10).sum(); sum_ret2 = ret.rolling(2).sum(); sum_ret2_3 = sum_ret2.rolling(3).sum(); ratio = sum_ret10 / (sum_ret2_3 + 1e-12); rank1 = row_rank(ratio); rank2 = row_rank(ret * cap); out = -1 * (rank1 * rank2)",
    "Alpha#57": "ac = d['close'] * d['adj_factor']; avwap = (d['amount'] / (d['vol'] + 1e-12)) * d['adj_factor']; ts_argmax = rolling_imax(ac, 30) * 30; rank_argmax = row_rank(ts_argmax); decay = rolling_decay_linear(rank_argmax, 2); out = -1 * ((ac - avwap) / decay)",
    "Alpha#58": "avwap = (d['amount'] / (d['vol'] + 1e-12)) * d['adj_factor']; vol = d['vol']; vwap_neutral = row_indneutralize(avwap, col_attrs['所属行业']); corr = vwap_neutral.rolling(3).corr(vol); decay = rolling_decay_linear(corr, 7); out = -1 * rolling_rank(decay, 5)",
    "Alpha#59": "avwap = (d['amount'] / (d['vol'] + 1e-12)) * d['adj_factor']; vol = d['vol']; vwap_comb = avwap * 0.728317 + avwap * (1 - 0.728317); vwap_neutral = row_indneutralize(vwap_comb, col_attrs['所属行业']); corr = vwap_neutral.rolling(4).corr(vol); decay = rolling_decay_linear(corr, 16); out = -1 * rolling_rank(decay, 8)",
    "Alpha#60": "ac = d['close'] * d['adj_factor']; ah = d['high'] * d['adj_factor']; al = d['low'] * d['adj_factor']; vol = d['vol']; expr = ((ac - al) - (ah - ac)) / ((ah - al) + 1e-12) * vol; scale1 = row_scale(row_rank(expr)); ts_argmax = rolling_imax(ac, 10) * 10; scale2 = row_scale(row_rank(ts_argmax)); out = - (2 * scale1 - scale2)",
    "Alpha#61": "ac = d['close'] * d['adj_factor']; avwap = (d['amount'] / (d['vol'] + 1e-12)) * d['adj_factor']; vol = d['vol']; adv180 = d['amount'].rolling(180).mean(); rank1 = row_rank(avwap - avwap.rolling(16).min()); rank2 = row_rank(avwap.rolling(17).corr(adv180)); out = ((rank1 < rank2) * -1).astype(float)",
    "Alpha#62": "avwap = (d['amount'] / (d['vol'] + 1e-12)) * d['adj_factor']; vol = d['vol']; ao = d['open'] * d['adj_factor']; ah = d['high'] * d['adj_factor']; al = d['low'] * d['adj_factor']; rank1 = row_rank(avwap.rolling(9).corr(vol.rolling(22).sum())); rank2 = row_rank((row_rank(ao) + row_rank(ao)) < (row_rank((ah + al) / 2) + row_rank(ah))); out = ((rank1 < rank2) * -1).astype(float)",
    "Alpha#63": "ac = d['close'] * d['adj_factor']; ao = d['open'] * d['adj_factor']; avwap = (d['amount'] / (d['vol'] + 1e-12)) * d['adj_factor']; vol = d['vol']; adv180 = d['amount'].rolling(180).mean(); close_neutral = row_indneutralize(ac, col_attrs['所属行业']); delta_neutral = close_neutral - close_neutral.shift(2); decay1 = rolling_decay_linear(delta_neutral, 8); rank1 = row_rank(decay1); expr = avwap * 0.318108 + ao * (1 - 0.318108); corr_expr = expr.rolling(13).corr(adv180.rolling(37).sum()); decay2 = rolling_decay_linear(corr_expr, 12); rank2 = row_rank(decay2); out = (rank1 - rank2) * -1",
    "Alpha#64": "ao = d['open'] * d['adj_factor']; al = d['low'] * d['adj_factor']; avwap = (d['amount'] / (d['vol'] + 1e-12)) * d['adj_factor']; vol = d['vol']; ah = d['high'] * d['adj_factor']; adv120 = d['amount'].rolling(120).mean(); expr1 = ao * 0.178404 + al * (1 - 0.178404); sum_expr1 = expr1.rolling(12).sum(); sum_adv120 = adv120.rolling(12).sum(); corr = sum_expr1.rolling(16).corr(sum_adv120); rank_corr = row_rank(corr); expr2 = ((ah + al) / 2) * 0.178404 + avwap * (1 - 0.178404); delta_expr2 = expr2 - expr2.shift(3); rank_delta = row_rank(delta_expr2); out = ((rank_corr < rank_delta) * -1).astype(float)",
    "Alpha#65": "ao = d['open'] * d['adj_factor']; avwap = (d['amount'] / (d['vol'] + 1e-12)) * d['adj_factor']; vol = d['vol']; adv60 = d['amount'].rolling(60).mean(); expr1 = ao * 0.00817205 + avwap * (1 - 0.00817205); sum_adv60 = adv60.rolling(8).sum(); corr = expr1.rolling(6).corr(sum_adv60); rank_corr = row_rank(corr); ts_min_open = ao.rolling(13).min(); rank_min = row_rank(ao - ts_min_open); out = ((rank_corr < rank_min) * -1).astype(float)",
    "Alpha#66": "avwap = (d['amount'] / (d['vol'] + 1e-12)) * d['adj_factor']; al = d['low'] * d['adj_factor']; ao = d['open'] * d['adj_factor']; ah = d['high'] * d['adj_factor']; delta_vwap = avwap - avwap.shift(3); decay1 = rolling_decay_linear(delta_vwap, 7); rank_decay1 = row_rank(decay1); expr = (((al * 0.96633 + al * (1 - 0.96633)) - avwap) / (ao - (ah + al) / 2)); decay2 = rolling_decay_linear(expr, 11); ts_rank_decay = rolling_rank(decay2, 6); out = (rank_decay1 + ts_rank_decay) * -1",
    "Alpha#67": "ah = d['high'] * d['adj_factor']; avwap = (d['amount'] / (d['vol'] + 1e-12)) * d['adj_factor']; vol = d['vol']; adv20 = d['amount'].rolling(20).mean(); high_min = ah - ah.rolling(2).min(); rank_high = row_rank(high_min); vwap_neutral = row_indneutralize(avwap, col_attrs['所属行业']); adv20_neutral = row_indneutralize(adv20, col_attrs['所属行业']); corr = vwap_neutral.rolling(6).corr(adv20_neutral); rank_corr = row_rank(corr); out = (rank_high ** rank_corr) * -1",
    "Alpha#68": "ah = d['high'] * d['adj_factor']; vol = d['vol']; ac = d['close'] * d['adj_factor']; al = d['low'] * d['adj_factor']; adv15 = d['amount'].rolling(15).mean(); corr = row_rank(ah).rolling(8).corr(row_rank(adv15)); ts_rank_corr = rolling_rank(corr, 13); expr = ac * 0.518371 + al * (1 - 0.518371); delta_expr = expr - expr.shift(1); rank_delta = row_rank(delta_expr); out = ((ts_rank_corr < rank_delta) * -1).astype(float)",
    "Alpha#69": "ac = d['close'] * d['adj_factor']; avwap = (d['amount'] / (d['vol'] + 1e-12)) * d['adj_factor']; vol = d['vol']; adv20 = d['amount'].rolling(20).mean(); vwap_neutral = row_indneutralize(avwap, col_attrs['所属行业']); delta_vwap = vwap_neutral - vwap_neutral.shift(2); ts_max_delta = delta_vwap.rolling(4).max(); rank_tsmax = row_rank(ts_max_delta); expr = ac * 0.490655 + avwap * (1 - 0.490655); corr_expr = expr.rolling(4).corr(adv20); ts_rank_corr = rolling_rank(corr_expr, 9); out = (rank_tsmax ** ts_rank_corr) * -1",
    "Alpha#70": "avwap = (d['amount'] / (d['vol'] + 1e-12)) * d['adj_factor']; vol = d['vol']; adv50 = d['amount'].rolling(50).mean(); ac = d['close'] * d['adj_factor']; delta_vwap = avwap - avwap.shift(1); rank_delta = row_rank(delta_vwap); close_neutral = row_indneutralize(ac, col_attrs['所属行业']); corr = close_neutral.rolling(17).corr(adv50); ts_rank_corr = rolling_rank(corr, 17); out = (rank_delta ** ts_rank_corr) * -1",
    "Alpha#71": "ac = d['close'] * d['adj_factor']; ao = d['open'] * d['adj_factor']; al = d['low'] * d['adj_factor']; avwap = (d['amount'] / (d['vol'] + 1e-12)) * d['adj_factor']; adv180 = d['amount'].rolling(180).mean(); part1_ts1 = rolling_rank(ac, 3); part1_ts2 = rolling_rank(adv180, 12); part1_corr = part1_ts1.rolling(18).corr(part1_ts2); part1_decay = rolling_decay_linear(part1_corr, 4); part1_tsrank = rolling_rank(part1_decay, 15); part2_rank = row_rank((al + ao) - (avwap + avwap)); part2_sq = part2_rank ** 2; part2_decay = rolling_decay_linear(part2_sq, 16); part2_tsrank = rolling_rank(part2_decay, 4); out = np.maximum(part1_tsrank, part2_tsrank); out = pd.DataFrame(out)",
    "Alpha#72": "ah = d['high'] * d['adj_factor']; al = d['low'] * d['adj_factor']; avwap = (d['amount'] / (d['vol'] + 1e-12)) * d['adj_factor']; vol = d['vol']; adv40 = d['amount'].rolling(40).mean(); rank1 = row_rank(rolling_decay_linear(((ah + al) / 2).rolling(8).corr(adv40), 10)); rank2 = row_rank(rolling_decay_linear(rolling_rank(avwap, 3).rolling(6).corr(rolling_rank(vol, 18)), 2)); out = rank1 / rank2",
    "Alpha#73": "avwap = (d['amount'] / (d['vol'] + 1e-12)) * d['adj_factor']; ao = d['open'] * d['adj_factor']; al = d['low'] * d['adj_factor']; part1 = row_rank(rolling_decay_linear(avwap - avwap.shift(4), 2)); expr = ao * 0.147155 + al * (1 - 0.147155); delta_expr = expr - expr.shift(2); decay2 = rolling_decay_linear(-1 * delta_expr / expr, 3); ts_rank2 = rolling_rank(decay2, 16); out = np.maximum(part1, ts_rank2) * -1",
    "Alpha#74": "ac = d['close'] * d['adj_factor']; ah = d['high'] * d['adj_factor']; avwap = (d['amount'] / (d['vol'] + 1e-12)) * d['adj_factor']; vol = d['vol']; adv30 = d['amount'].rolling(30).mean(); sum_adv30 = adv30.rolling(37).sum(); corr1 = ac.rolling(15).corr(sum_adv30); rank_corr1 = row_rank(corr1); expr = ah * 0.0261661 + avwap * (1 - 0.0261661); rank_expr = row_rank(expr); rank_vol = row_rank(vol); corr2 = rank_expr.rolling(11).corr(rank_vol); rank_corr2 = row_rank(corr2); out = ((rank_corr1 < rank_corr2) * -1).astype(float)",
    "Alpha#75": "avwap = (d['amount'] / (d['vol'] + 1e-12)) * d['adj_factor']; vol = d['vol']; al = d['low'] * d['adj_factor']; adv50 = d['amount'].rolling(50).mean(); rank1 = row_rank(avwap.rolling(4).corr(vol)); rank2 = row_rank(row_rank(al).rolling(12).corr(row_rank(adv50))); out = (rank1 < rank2).astype(float)",
    "Alpha#76": "avwap = (d['amount'] / (d['vol'] + 1e-12)) * d['adj_factor']; vol = d['vol']; al = d['low'] * d['adj_factor']; adv81 = d['amount'].rolling(81).mean(); delta_vwap = avwap - avwap.shift(1); decay1 = rolling_decay_linear(delta_vwap, 11); rank1 = row_rank(decay1); low_neutral = row_indneutralize(al, col_attrs['所属行业']); corr = low_neutral.rolling(8).corr(adv81); ts_rank_corr = rolling_rank(corr, 19); decay2 = rolling_decay_linear(ts_rank_corr, 17); ts_rank_decay = rolling_rank(decay2, 19); rank2 = row_rank(ts_rank_decay); out = np.maximum(rank1, rank2) * -1",
    "Alpha#77": "ah = d['high'] * d['adj_factor']; al = d['low'] * d['adj_factor']; avwap = (d['amount'] / (d['vol'] + 1e-12)) * d['adj_factor']; vol = d['vol']; adv40 = d['amount'].rolling(40).mean(); part1 = row_rank(rolling_decay_linear(((ah + al) / 2 + ah) - (avwap + ah), 20)); part2 = row_rank(rolling_decay_linear(((ah + al) / 2).rolling(3).corr(adv40), 5)); out = np.minimum(part1, part2)",
    "Alpha#78": "al = d['low'] * d['adj_factor']; avwap = (d['amount'] / (d['vol'] + 1e-12)) * d['adj_factor']; vol = d['vol']; adv40 = d['amount'].rolling(40).mean(); expr = al * 0.352233 + avwap * (1 - 0.352233); sum_expr = expr.rolling(19).sum(); sum_adv40 = adv40.rolling(19).sum(); corr = sum_expr.rolling(6).corr(sum_adv40); rank_corr = row_rank(corr); rank_vwap = row_rank(avwap); rank_vol = row_rank(vol); corr2 = rank_vwap.rolling(5).corr(rank_vol); rank_corr2 = row_rank(corr2); out = rank_corr ** rank_corr2",
    "Alpha#79": "ac = d['close'] * d['adj_factor']; ao = d['open'] * d['adj_factor']; avwap = (d['amount'] / (d['vol'] + 1e-12)) * d['adj_factor']; vol = d['vol']; adv150 = d['amount'].rolling(150).mean(); expr = ac * 0.60733 + ao * (1 - 0.60733); expr_neutral = row_indneutralize(expr, col_attrs['所属行业']); delta_expr = expr_neutral - expr_neutral.shift(1); rank1 = row_rank(delta_expr); ts_rank_vwap = rolling_rank(avwap, 3); ts_rank_adv150 = rolling_rank(adv150, 9); corr = ts_rank_vwap.rolling(14).corr(ts_rank_adv150); rank2 = row_rank(corr); out = (rank1 < rank2).astype(float) * -1",
    "Alpha#80": "ao = d['open'] * d['adj_factor']; ah = d['high'] * d['adj_factor']; vol = d['vol']; adv10 = d['amount'].rolling(10).mean(); expr = ao * 0.868128 + ah * (1 - 0.868128); expr_neutral = row_indneutralize(expr, col_attrs['所属行业']); delta_expr = expr_neutral - expr_neutral.shift(4); rank1 = row_rank(np.sign(delta_expr)); corr = ah.rolling(5).corr(adv10); ts_rank_corr = rolling_rank(corr, 5); out = (rank1 ** ts_rank_corr) * -1",
    "Alpha#81": "avwap = (d['amount'] / (d['vol'] + 1e-12)) * d['adj_factor']; vol = d['vol']; adv10 = d['amount'].rolling(10).mean(); sum_adv10 = adv10.rolling(49).sum(); corr1 = avwap.rolling(8).corr(sum_adv10); rank_corr1 = row_rank(corr1); pow4 = rank_corr1 ** 4; rank_pow4 = row_rank(pow4); log_rank = np.log(rank_pow4 + 1e-12); sum_log = log_rank.rolling(14).sum(); rank_logprod = row_rank(sum_log); rank_vwap = row_rank(avwap); rank_vol = row_rank(vol); corr2 = rank_vwap.rolling(5).corr(rank_vol); rank_corr2 = row_rank(corr2); out = ((rank_logprod < rank_corr2) * -1).astype(float)",
    "Alpha#82": "ao = d['open'] * d['adj_factor']; vol = d['vol']; delta_open = ao - ao.shift(1); decay1 = rolling_decay_linear(delta_open, 14); rank1 = row_rank(decay1); vol_neutral = row_indneutralize(vol, col_attrs['所属行业']); expr = ao * 0.634196 + ao * (1 - 0.634196); corr = vol_neutral.rolling(17).corr(expr); decay2 = rolling_decay_linear(corr, 6); ts_rank_decay = rolling_rank(decay2, 13); rank2 = row_rank(ts_rank_decay); out = np.minimum(rank1, rank2) * -1",
    "Alpha#83": "ah = d['high'] * d['adj_factor']; al = d['low'] * d['adj_factor']; ac = d['close'] * d['adj_factor']; avwap = (d['amount'] / (d['vol'] + 1e-12)) * d['adj_factor']; vol = d['vol']; expr = (ah - al) / (ac.rolling(5).sum() / 5); rank_delay = row_rank(expr.shift(2)); rank_vol = row_rank(row_rank(vol)); denom = expr / (avwap - ac); out = (rank_delay * rank_vol) / denom",
    "Alpha#84": "avwap = (d['amount'] / (d['vol'] + 1e-12)) * d['adj_factor']; ac = d['close'] * d['adj_factor']; ts_rank_val = rolling_rank(avwap - avwap.rolling(15).max(), 20); power = ac - ac.shift(4); out = np.sign(ts_rank_val) * (np.abs(ts_rank_val) ** power)",
    "Alpha#85": "ah = d['high'] * d['adj_factor']; ac = d['close'] * d['adj_factor']; vol = d['vol']; adv30 = d['amount'].rolling(30).mean(); expr = ah * 0.876703 + ac * (1 - 0.876703); corr1 = expr.rolling(9).corr(adv30); rank_corr1 = row_rank(corr1); ts_rank_highlow = rolling_rank((ah + ac) / 2, 3); ts_rank_vol = rolling_rank(vol, 10); corr2 = ts_rank_highlow.rolling(7).corr(ts_rank_vol); rank_corr2 = row_rank(corr2); out = rank_corr1 ** rank_corr2",
    "Alpha#86": "ac = d['close'] * d['adj_factor']; ao = d['open'] * d['adj_factor']; avwap = (d['amount'] / (d['vol'] + 1e-12)) * d['adj_factor']; vol = d['vol']; adv20 = d['amount'].rolling(20).mean(); sum_adv20 = adv20.rolling(14).sum(); corr = ac.rolling(6).corr(sum_adv20); ts_rank_corr = rolling_rank(corr, 20); expr = (ao + ac) - (avwap + ao); rank_expr = row_rank(expr); out = ((ts_rank_corr < rank_expr) * -1).astype(float)",
    "Alpha#87": "ac = d['close'] * d['adj_factor']; avwap = (d['amount'] / (d['vol'] + 1e-12)) * d['adj_factor']; vol = d['vol']; adv81 = d['amount'].rolling(81).mean(); expr = ac * 0.369701 + avwap * (1 - 0.369701); delta_expr = expr - expr.shift(1); decay1 = rolling_decay_linear(delta_expr, 2); rank1 = row_rank(decay1); adv81_neutral = row_indneutralize(adv81, col_attrs['所属行业']); corr_abs = (adv81_neutral.rolling(13).corr(ac)).abs(); decay2 = rolling_decay_linear(corr_abs, 4); ts_rank_decay = rolling_rank(decay2, 14); rank2 = row_rank(ts_rank_decay); out = np.maximum(rank1, rank2) * -1",
    "Alpha#88": "ao = d['open'] * d['adj_factor']; al = d['low'] * d['adj_factor']; ah = d['high'] * d['adj_factor']; ac = d['close'] * d['adj_factor']; vol = d['vol']; adv60 = d['amount'].rolling(60).mean(); part1 = row_rank(rolling_decay_linear((row_rank(ao) + row_rank(al)) - (row_rank(ah) + row_rank(ac)), 8)); part2 = rolling_rank(rolling_decay_linear(rolling_rank(ac, 8).rolling(8).corr(rolling_rank(adv60, 20)), 6), 2); out = np.minimum(part1, part2)",
    "Alpha#89": "al = d['low'] * d['adj_factor']; vol = d['vol']; adv10 = d['amount'].rolling(10).mean(); avwap = (d['amount'] / (d['vol'] + 1e-12)) * d['adj_factor']; expr = al * 0.967285 + al * (1 - 0.967285); corr1 = expr.rolling(6).corr(adv10); decay1 = rolling_decay_linear(corr1, 5); ts_rank1 = rolling_rank(decay1, 3); vwap_neutral = row_indneutralize(avwap, col_attrs['所属行业']); delta_vwap = vwap_neutral - vwap_neutral.shift(3); decay2 = rolling_decay_linear(delta_vwap, 10); ts_rank2 = rolling_rank(decay2, 15); out = ts_rank1 - ts_rank2",
    "Alpha#90": "ac = d['close'] * d['adj_factor']; al = d['low'] * d['adj_factor']; vol = d['vol']; adv40 = d['amount'].rolling(40).mean(); close_max = ac - ac.rolling(4).max(); rank1 = row_rank(close_max); adv40_neutral = row_indneutralize(adv40, col_attrs['所属行业']); corr = adv40_neutral.rolling(5).corr(al); ts_rank_corr = rolling_rank(corr, 3); out = (rank1 ** ts_rank_corr) * -1",
    "Alpha#91": "ac = d['close'] * d['adj_factor']; vol = d['vol']; avwap = (d['amount'] / (d['vol'] + 1e-12)) * d['adj_factor']; adv30 = d['amount'].rolling(30).mean(); close_neutral = row_indneutralize(ac, col_attrs['所属行业']); corr1 = close_neutral.rolling(9).corr(vol); decay1 = rolling_decay_linear(corr1, 16); decay2 = rolling_decay_linear(decay1, 3); ts_rank1 = rolling_rank(decay2, 4); corr2 = avwap.rolling(4).corr(adv30); decay3 = rolling_decay_linear(corr2, 2); rank2 = row_rank(decay3); out = (ts_rank1 - rank2) * -1",
    "Alpha#92": "ah = d['high'] * d['adj_factor']; al = d['low'] * d['adj_factor']; ac = d['close'] * d['adj_factor']; ao = d['open'] * d['adj_factor']; vol = d['vol']; adv30 = d['amount'].rolling(30).mean(); part1 = rolling_rank(rolling_decay_linear(((ah + al) / 2 + ac) < (al + ao), 14), 18); part2 = rolling_rank(rolling_decay_linear(row_rank(al).rolling(7).corr(row_rank(adv30)), 6), 6); out = np.minimum(part1, part2)",
    "Alpha#93": "avwap = (d['amount'] / (d['vol'] + 1e-12)) * d['adj_factor']; vol = d['vol']; ac = d['close'] * d['adj_factor']; adv81 = d['amount'].rolling(81).mean(); vwap_neutral = row_indneutralize(avwap, col_attrs['所属行业']); corr = vwap_neutral.rolling(17).corr(adv81); decay1 = rolling_decay_linear(corr, 19); ts_rank1 = rolling_rank(decay1, 7); expr = ac * 0.524434 + avwap * (1 - 0.524434); delta_expr = expr - expr.shift(2); decay2 = rolling_decay_linear(delta_expr, 16); rank2 = row_rank(decay2); out = ts_rank1 / rank2",
    "Alpha#94": "avwap = (d['amount'] / (d['vol'] + 1e-12)) * d['adj_factor']; vol = d['vol']; adv60 = d['amount'].rolling(60).mean(); rank1 = row_rank(avwap - avwap.rolling(11).min()); ts_rank_vwap = rolling_rank(avwap, 19); ts_rank_adv60 = rolling_rank(adv60, 4); corr = ts_rank_vwap.rolling(18).corr(ts_rank_adv60); ts_rank_corr = rolling_rank(corr, 2); out = (rank1 ** ts_rank_corr) * -1",
    "Alpha#95": "ao = d['open'] * d['adj_factor']; ah = d['high'] * d['adj_factor']; al = d['low'] * d['adj_factor']; vol = d['vol']; adv40 = d['amount'].rolling(40).mean(); rank_open = row_rank(ao - ao.rolling(12).min()); sum_highlow = ((ah + al) / 2).rolling(19).sum(); sum_adv40 = adv40.rolling(19).sum(); corr = sum_highlow.rolling(12).corr(sum_adv40); rank_corr = row_rank(corr); ts_rank_corr_pow5 = rolling_rank(rank_corr ** 5, 11); out = (rank_open < ts_rank_corr_pow5).astype(float)",
    "Alpha#96": "avwap = (d['amount'] / (d['vol'] + 1e-12)) * d['adj_factor']; vol = d['vol']; ac = d['close'] * d['adj_factor']; adv60 = d['amount'].rolling(60).mean(); part1 = rolling_rank(rolling_decay_linear(row_rank(avwap).rolling(3).corr(row_rank(vol)), 4), 8); ts_rank_close = rolling_rank(ac, 7); ts_rank_adv60 = rolling_rank(adv60, 4); corr = ts_rank_close.rolling(3).corr(ts_rank_adv60); argmax = rolling_imax(corr, 12) * 12; decay_argmax = rolling_decay_linear(argmax, 14); ts_rank_part2 = rolling_rank(decay_argmax, 13); out = np.maximum(part1, ts_rank_part2) * -1",
    "Alpha#97": "al = d['low'] * d['adj_factor']; avwap = (d['amount'] / (d['vol'] + 1e-12)) * d['adj_factor']; vol = d['vol']; adv60 = d['amount'].rolling(60).mean(); expr = al * 0.721001 + avwap * (1 - 0.721001); expr_neutral = row_indneutralize(expr, col_attrs['所属行业']); delta_expr = expr_neutral - expr_neutral.shift(3); decay1 = rolling_decay_linear(delta_expr, 20); rank1 = row_rank(decay1); ts_rank_low = rolling_rank(al, 7); ts_rank_adv60 = rolling_rank(adv60, 17); corr = ts_rank_low.rolling(4).corr(ts_rank_adv60); ts_rank_corr = rolling_rank(corr, 18); decay2 = rolling_decay_linear(ts_rank_corr, 15); rank2 = rolling_rank(decay2, 6); out = (rank1 - rank2) * -1",
    "Alpha#98": "avwap = (d['amount'] / (d['vol'] + 1e-12)) * d['adj_factor']; vol = d['vol']; adv5 = d['amount'].rolling(5).mean(); ao = d['open'] * d['adj_factor']; adv15 = d['amount'].rolling(15).mean(); part1 = row_rank(rolling_decay_linear(avwap.rolling(4).corr(adv5.rolling(26).sum()), 7)); corr_open_adv15 = row_rank(ao).rolling(20).corr(row_rank(adv15)); argmin = rolling_imin(corr_open_adv15, 8) * 8; ts_rank_argmin = rolling_rank(argmin, 6); decay_argmin = rolling_decay_linear(ts_rank_argmin, 8); part2 = row_rank(decay_argmin); out = part1 - part2",
    "Alpha#99": "ah = d['high'] * d['adj_factor']; al = d['low'] * d['adj_factor']; vol = d['vol']; adv60 = d['amount'].rolling(60).mean(); sum_highlow = ((ah + al) / 2).rolling(19).sum(); sum_adv60 = adv60.rolling(19).sum(); corr1 = sum_highlow.rolling(8).corr(sum_adv60); rank_corr1 = row_rank(corr1); corr2 = al.rolling(6).corr(vol); rank_corr2 = row_rank(corr2); out = ((rank_corr1 < rank_corr2) * -1).astype(float)",
    "Alpha#100": "ac = d['close'] * d['adj_factor']; ah = d['high'] * d['adj_factor']; al = d['low'] * d['adj_factor']; vol = d['vol']; adv20 = d['amount'].rolling(20).mean(); expr = ((ac - al) - (ah - ac)) / ((ah - al) + 1e-12) * vol; rank_expr = row_rank(expr); neutral1 = row_indneutralize(rank_expr, col_attrs['所属行业']); neutral2 = row_indneutralize(neutral1, col_attrs['所属行业']); scale1 = row_scale(neutral2); corr = ac.rolling(5).corr(row_rank(adv20)); ts_argmin = rolling_imin(ac, 30) * 30; rank_argmin = row_rank(ts_argmin); diff = corr - rank_argmin; diff_neutral = row_indneutralize(diff, col_attrs['所属行业']); scale2 = row_scale(diff_neutral); out = - ( (1.5 * scale1 - scale2) * (vol / adv20) )",
    "Alpha#101": "ac = d['close'] * d['adj_factor']; ao = d['open'] * d['adj_factor']; ah = d['high'] * d['adj_factor']; al = d['low'] * d['adj_factor']; out = (ac - ao) / ((ah - al) + 0.001)"
  },
  "save_path": "output\\factor-Alpha101.xlsx"
}