{
    "tool_name":"batch_factor_analysis",
    "feature_days": 5,
    "factor_dict": {
    "GTJA_Alpha1": "out = -1*row_rank(d['vol']/d['vol'].shift(1)).rolling(6).corr(row_rank((d['close'] - d['open']) / d['open']))",
    "GTJA_Alpha2": "x = ((d['close']-d['low']) - (d['high'] - d['close']))/(d['high']- d['low']); out = -1*(x - x.shift(1))",
    "GTJA_Alpha3": "ac = d['close'] * d['adj_factor']; al = d['low'] * d['adj_factor']; ah = d['high'] * d['adj_factor']; ac_s1 = ac.shift(1); inner = np.where(ac>ac_s1,ac - np.minimum(al, ac_s1), ac - np.maximum(ah, ac_s1)); daily_value = np.where(ac == ac_s1, 0, inner); daily_value_df = pd.DataFrame(daily_value); out = pd.DataFrame(daily_value); Alpha3 = daily_value_df.rolling(6).sum()",
    "GTJA_Alpha4": "ac = d['close'] * d['adj_factor']; volume = d['vol']; sma8 = ac.rolling(8).mean(); std8 = ac.rolling(8).std(); sma2 = ac.rolling(2).mean(); ratio = volume / volume.rolling(20).mean(); cond1 = (sma8 + std8) < sma2; cond2 = sma2 < (sma8 - std8); cond3 = ratio >= 1; result = np.where(cond1, -1, np.where(cond2, 1, np.where(cond3, 1, -1))); out = pd.DataFrame(result)",
    "GTJA_Alpha5": "ac = d['close'] * d['adj_factor']; ah = d['high'] * d['adj_factor']; volume = d['vol']; ts_rank_vol  = volume.rolling(5).rank(pct=True, method='max'); ts_rank_high = ah.rolling(5).rank(pct=True, method='max'); corr_5 = ts_rank_vol.rolling(5).corr(ts_rank_high); tsmax_3 = corr_5.rolling(3).max(); out = -1 * tsmax_3",
    "GTJA_Alpha6": "ao  = d['open'] * d['adj_factor']; ah  = d['high'] * d['adj_factor']; x = ao * 0.85 + ah * 0.15; delta_x = x - x.shift(4); sgn = np.sign(delta_x); out = -1 * row_rank(sgn)",
    "GTJA_Alpha7": "spread = d['amount'] / d['vol'] - d['close']; max_spread = spread.rolling(3).max(); min_spread = spread.rolling(3).min(); rank_delta = row_rank(d['vol'] - d['vol'].shift(3)); out = (row_rank(max_spread) + row_rank(min_spread)) * rank_delta",
    "GTJA_Alpha8": "vwap = d['amount'] / d['vol']; mid = ((d['high'] + d['low']) / 2) * 0.2 + vwap * 0.8; delta_mid = mid - mid.shift(4); out = row_rank(-1 * delta_mid)",
    "GTJA_Alpha9": "mid = (d['high'] + d['low']) / 2; delay_mid = (d['high'].shift(1) + d['low'].shift(1)) / 2; x = (mid - delay_mid) * (d['high'] - d['low']) / d['vol']; out = x.ewm(alpha=2/7, adjust=False, min_periods=1).mean()",
    "GTJA_Alpha10": "ac = d['close'] * d['adj_factor']; ret = ac / ac.shift(1) - 1; std_ret = ret.rolling(20).std(); val = std_ret.where(ret < 0, ac); max_val = (val ** 2).rolling(5).max(); out = row_rank(max_val)",
    "GTJA_Alpha11": "ac = d['close'] * d['adj_factor']; op = d['open'] * d['adj_factor']; hi = d['high'] * d['adj_factor']; lo = d['low'] * d['adj_factor']; vol = d['vol']; inner = ((ac - lo) - (hi - ac)) / (hi - lo).replace(0, np.nan) * vol; out = inner.rolling(6).sum()",
    "GTJA_Alpha12": "ac = d['close'] * d['adj_factor']; op = d['open'] * d['adj_factor']; vwap = d['amount'] / d['vol']; vwap = vwap.where(d['vol'] > 0, np.nan); mean_vwap_10 = vwap.rolling(10).mean(); out = row_rank(op - mean_vwap_10) * (-1 * row_rank(np.abs(ac - vwap)))",
    "GTJA_Alpha13": "hi = d['high'] * d['adj_factor']; lo = d['low'] * d['adj_factor']; vwap = d['amount'] / d['vol']; vwap = vwap.where(d['vol'] > 0, np.nan); out = np.sqrt(hi * lo) - vwap",
    "GTJA_Alpha14": "ac = d['close'] * d['adj_factor']; out = ac - ac.shift(5)",
    "GTJA_Alpha15": "ac = d['close'] * d['adj_factor']; op = d['open'] * d['adj_factor']; out = op / ac.shift(1) - 1",
    "GTJA_Alpha16": "vwap = d['amount'] / d['vol']; vwap = vwap.where(d['vol'] > 0, np.nan); rank_vol = row_rank(d['vol']); rank_vwap = row_rank(vwap); corr_5 = rank_vol.rolling(5).corr(rank_vwap); out = -1 * row_rank(corr_5).rolling(5).max()",
    "GTJA_Alpha17": "ac = d['close'] * d['adj_factor']; vwap = d['amount'] / d['vol']; vwap = vwap.where(d['vol'] > 0, np.nan); max_vwap_15 = vwap.rolling(15).max(); out = row_rank(vwap - max_vwap_15) ** (ac - ac.shift(5))",
    "GTJA_Alpha18": "ac = d['close'] * d['adj_factor']; out = ac / ac.shift(5)",
    "GTJA_Alpha19": "ac = d['close'] * d['adj_factor']; ac_sf5 = ac.shift(5); diff = ac - ac_sf5; out = pd.DataFrame(np.where(ac < ac_sf5, diff / ac_sf5,np.where(ac == ac_sf5, 0, diff / ac)))",
    "GTJA_Alpha20": "ac = d['close'] * d['adj_factor']; out = (ac - ac.shift(6)) / ac.shift(6) * 100",
    "GTJA_Alpha21": "ac = d['close'] * d['adj_factor']; ma6 = ac.rolling(6).mean(); out = rolling_slope(ma6, 6)",
    "GTJA_Alpha22": "ac = d['close'] * d['adj_factor']; ma6 = ac.rolling(6).mean(); x = (ac - ma6) / ma6; diff = x - x.shift(3); out = diff.ewm(alpha=1/12, adjust=False).mean()",
    "GTJA_Alpha23": "ac = d['close'] * d['adj_factor']; cond_up = ac > ac.shift(1); std20 = ac.rolling(20).std(); val_up = std20.where(cond_up, 0); val_down = std20.where(~cond_up, 0); sma_up = val_up.ewm(alpha=1/20, adjust=False).mean(); sma_down = val_down.ewm(alpha=1/20, adjust=False).mean(); out = sma_up / (sma_up + sma_down) * 100",
    "GTJA_Alpha24": "ac = d['close'] * d['adj_factor']; out = (ac - ac.shift(5)).ewm(alpha=1/5, adjust=False).mean()",
    "GTJA_Alpha25": "ac = d['close'] * d['adj_factor']; vol = d['vol']; vol_ratio = vol / vol.rolling(20).mean(); decay = rolling_decay_linear(vol_ratio, 9); rank_decay = row_rank(decay); delta_close = ac - ac.shift(7); inner = delta_close * (1 - rank_decay); term1 = -1 * row_rank(inner); ret = ac / ac.shift(1) - 1; sum_ret = ret.rolling(250).sum(); term2 = 1 + row_rank(sum_ret); out = term1 * term2",
    "GTJA_Alpha26": "ac = d['close'] * d['adj_factor']; vwap = d['amount'] / d['vol'].replace(0, np.nan); ma7 = ac.rolling(7).mean(); corr26 = vwap.rolling(230).corr(ac.shift(5)); out = (ma7 - ac) + corr26",
    "GTJA_Alpha27": "ac = d['close'] * d['adj_factor']; x = (ac - ac.shift(3)) / ac.shift(3) * 100 + (ac - ac.shift(6)) / ac.shift(6) * 100; out = rolling_decay_linear(x, 12)",
    "GTJA_Alpha28": "ac = d['close'] * d['adj_factor']; hi = d['high'] * d['adj_factor']; lo = d['low'] * d['adj_factor']; tsmin_low9 = lo.rolling(9).min(); max_high9 = hi.rolling(9).max(); tsmax_low9 = lo.rolling(9).max(); denom = max_high9 - tsmax_low9; denom = denom.replace(0, np.nan); val28 = (ac - tsmin_low9) / denom * 100; sma1 = val28.ewm(alpha=1/3, adjust=False).mean(); sma2 = sma1.ewm(alpha=1/3, adjust=False).mean(); out = 3 * sma1 - 2 * sma2",
    "GTJA_Alpha29": "ac = d['close'] * d['adj_factor']; out = (ac - ac.shift(6)) / ac.shift(6) * d['vol']",
    "GTJA_Alpha30": "ac = d['close'] * d['adj_factor']; ret = ac / ac.shift(1) - 1; bench = get_stock_index('000001', 'close', 'serie'); mkt = bench / bench.shift(1) - 1; mv = d['总市值']; bm = 1 / d['市净率']; year_ago = pd.Timestamp.now() - pd.DateOffset(years=1); row_mask = col_attrs['上市日期'] < year_ago; mv.loc[:, row_mask] = np.nan; bm.loc[:, row_mask] = np.nan; ret.loc[:, row_mask] = np.nan; rank_mv = row_rank(mv); rank_bm = row_rank(bm); S = rank_mv <= 0.5; B = rank_mv > 0.5; L = rank_bm <= 0.3; N = (rank_bm > 0.3) & (rank_bm <= 0.7); H = rank_bm > 0.7; r_SL = row_mean(ret.where(S & L, np.nan)); r_SN = row_mean(ret.where(S & N, np.nan)); r_SH = row_mean(ret.where(S & H, np.nan)); r_BL = row_mean(ret.where(B & L, np.nan)); r_BN = row_mean(ret.where(B & N, np.nan)); r_BH = row_mean(ret.where(B & H, np.nan)); smb = (r_SL + r_SN + r_SH) / 3 - (r_BL + r_BN + r_BH) / 3; hml = (r_SH + r_BH) / 2 - (r_SL + r_BL) / 2; smb.fillna(0,inplace=True); hml.fillna(0,inplace=True); resid = rolling_regresi(ret, [mkt, smb, hml], 60); out = rolling_decay_linear(resid ** 2, 20)", 
    
    "GTJA_Alpha31": "ac = d['close'] * d['adj_factor']; ma12 = ac.rolling(12).mean(); out = (ac - ma12) / ma12 * 100",
    "GTJA_Alpha32": "rank_high = row_rank(d['high'] * d['adj_factor']); rank_vol = row_rank(d['vol']); corr3 = rank_high.rolling(3).corr(rank_vol); out = -1 * corr3.rolling(3).sum()",
    "GTJA_Alpha33": "ac = d['close'] * d['adj_factor']; lo = d['low'] * d['adj_factor']; ret = ac / ac.shift(1) - 1; tsmin_low5 = lo.rolling(5).min(); sum_ret240 = ret.rolling(240).sum(); sum_ret20 = ret.rolling(20).sum(); rank_sumret = row_rank((sum_ret240 - sum_ret20) / 220); tsrank_vol5 = d['vol'].rolling(5).rank(pct=True, method='max'); out = ((-1 * tsmin_low5) + tsmin_low5.shift(5)) * rank_sumret * tsrank_vol5",
    "GTJA_Alpha34": "ac = d['close'] * d['adj_factor']; out = ac.rolling(12).mean() / ac",
    "GTJA_Alpha35": "op = d['open'] * d['adj_factor']; delta_open = op - op.shift(1); decay1 = rolling_decay_linear(delta_open, 15); rank_decay1 = row_rank(decay1); inner_corr = d['vol'].rolling(17).corr(op); decay2 = rolling_decay_linear(inner_corr, 7); rank_decay2 = row_rank(decay2); min_rank = np.minimum(rank_decay1, rank_decay2); out = -1 * pd.DataFrame(min_rank)",
    "GTJA_Alpha36": "rank_vol = row_rank(d['vol']); vwap = d['amount'] / d['vol'].replace(0, np.nan); rank_vwap = row_rank(vwap); corr6 = rank_vol.rolling(6).corr(rank_vwap); sum_corr = corr6.rolling(6).sum(); out = row_rank(sum_corr)",
    "GTJA_Alpha37": "ac = d['close'] * d['adj_factor']; ret = ac / ac.shift(1) - 1; prod = d['open'].rolling(5).sum() * ret.rolling(5).sum(); out = -1 * row_rank(prod - prod.shift(10))",
    "GTJA_Alpha38": "hi = d['high'] * d['adj_factor']; cond = (hi.rolling(20).mean() < hi); delta_high2 = hi - hi.shift(2); out = np.where(cond, -1 * delta_high2, 0); out = pd.DataFrame(out, index=hi.index, columns=hi.columns)",
    "GTJA_Alpha39": "ac = d['close'] * d['adj_factor']; vwap = d['amount'] / d['vol'].replace(0, np.nan); op = d['open'] * d['adj_factor']; delta_close2 = ac - ac.shift(2); decay1 = rolling_decay_linear(delta_close2, 8); rank_decay1 = row_rank(decay1); mean_vol180 = d['vol'].rolling(180).mean(); sum_mean_vol37 = mean_vol180.rolling(37).sum(); inner_corr = (vwap*0.3 + op*0.7).rolling(14).corr(sum_mean_vol37); decay2 = rolling_decay_linear(inner_corr, 12); rank_decay2 = row_rank(decay2); out = rank_decay2 - rank_decay1",
    "GTJA_Alpha40": "ac = d['close'] * d['adj_factor']; cond_up = ac > ac.shift(1); sum_vol_up = (cond_up * d['vol']).rolling(26).sum(); sum_vol_down = ((~cond_up) * d['vol']).rolling(26).sum(); out = sum_vol_up / sum_vol_down * 100",
    "GTJA_Alpha41": "vwap = d['amount'] / d['vol'].replace(0, np.nan); delta_vwap3 = vwap - vwap.shift(3); max_delta = delta_vwap3.rolling(5).max(); out = -1 * row_rank(max_delta)",
    "GTJA_Alpha42": "hi = d['high'] * d['adj_factor']; rank_std_high = row_rank(hi.rolling(10).std()); corr_high_vol = hi.rolling(10).corr(d['vol']); out = -1 * rank_std_high * corr_high_vol",
    "GTJA_Alpha43": "ac = d['close'] * d['adj_factor']; diff = ac - ac.shift(1); cond_up = diff > 0; cond_down = diff < 0; term = np.where(cond_up, d['vol'], np.where(cond_down, -d['vol'], 0)); out = pd.DataFrame(term).rolling(6).sum()",
    "GTJA_Alpha44": "lo = d['low'] * d['adj_factor']; vwap = d['amount'] / d['vol'].replace(0, np.nan); corr_low_meanvol = lo.rolling(7).corr(d['vol'].rolling(10).mean()); decay1 = rolling_decay_linear(corr_low_meanvol, 6); tsrank1 = decay1.rolling(4).rank(pct=True, method='max'); delta_vwap3 = vwap - vwap.shift(3); decay2 = rolling_decay_linear(delta_vwap3, 10); tsrank2 = decay2.rolling(15).rank(pct=True, method='max'); out = tsrank1 + tsrank2",
    "GTJA_Alpha45": "ac = d['close'] * d['adj_factor']; op = d['open'] * d['adj_factor']; vwap = d['amount'] / d['vol'].replace(0, np.nan); price = ac * 0.6 + op * 0.4; rank_delta = row_rank(price - price.shift(1)); corr_vwap_mean = vwap.rolling(15).corr(vwap.rolling(150).mean()); rank_corr = row_rank(corr_vwap_mean); out = rank_delta * rank_corr",
    "GTJA_Alpha46": "ac = d['close'] * d['adj_factor']; out = (ac.rolling(3).mean() + ac.rolling(6).mean()+ ac.rolling(12).mean() + ac.rolling(24).mean()) / (4 * ac)",
    "GTJA_Alpha47": "ac = d['close'] * d['adj_factor']; hi = d['high'] * d['adj_factor']; lo = d['low'] * d['adj_factor']; tsmax_high6 = hi.rolling(6).max(); tsmin_low6 = lo.rolling(6).min(); x = (tsmax_high6 - ac) / (tsmax_high6 - tsmin_low6) * 100; out = x.ewm(alpha=1/9, adjust=False).mean()",
    "GTJA_Alpha48": "ac = d['close'] * d['adj_factor']; s1 = np.sign(ac - ac.shift(1)); s2 = np.sign(ac.shift(1) - ac.shift(2)); s3 = np.sign(ac.shift(2) - ac.shift(3)); sum_sign = s1 + s2 + s3; out = -1 * (row_rank(sum_sign) * d['vol'].rolling(5).sum()) / d['vol'].rolling(20).sum()",
    "GTJA_Alpha49": "hi = d['high'] * d['adj_factor']; lo = d['low'] * d['adj_factor']; hi_shift1 = hi.shift(1); lo_shift1 = lo.shift(1); sum_hl = hi + lo; sum_hl_shift1 = hi_shift1 + lo_shift1; abs_hi = np.abs(hi - hi_shift1); abs_lo = np.abs(lo - lo_shift1); max_abs = np.maximum(abs_hi, abs_lo); cond_ge = sum_hl >= sum_hl_shift1; cond_le = sum_hl <= sum_hl_shift1; up = np.where(cond_ge, 0, max_abs); down = np.where(cond_le, 0, max_abs); sum_up = pd.DataFrame(up).rolling(12).sum(); sum_down = pd.DataFrame(down).rolling(12).sum(); out = sum_up / (sum_up + sum_down)",
    "GTJA_Alpha50": "hi = d['high'] * d['adj_factor']; lo = d['low'] * d['adj_factor']; hi_shift1 = hi.shift(1); lo_shift1 = lo.shift(1); sum_hl = hi + lo; sum_hl_shift1 = hi_shift1 + lo_shift1; abs_hi = np.abs(hi - hi_shift1); abs_lo = np.abs(lo - lo_shift1); max_abs = np.maximum(abs_hi, abs_lo); cond_ge = sum_hl >= sum_hl_shift1; cond_le = sum_hl <= sum_hl_shift1; up = np.where(cond_ge, 0, max_abs); down = np.where(cond_le, 0, max_abs); sum_up = pd.DataFrame(up).rolling(12).sum(); sum_down = pd.DataFrame(down).rolling(12).sum(); out = (sum_down - sum_up) / (sum_up + sum_down)",
    "GTJA_Alpha51": "hi = d['high'] * d['adj_factor']; lo = d['low'] * d['adj_factor']; sum_hl = hi + lo; sum_hl_s1 = hi.shift(1) + lo.shift(1); max_abs = np.maximum(np.abs(hi - hi.shift(1)), np.abs(lo - lo.shift(1))); up_move = max_abs.where(sum_hl > sum_hl_s1, 0); down_move = max_abs.where(sum_hl < sum_hl_s1, 0); sum_up = up_move.rolling(12).sum(); sum_down = down_move.rolling(12).sum(); out = sum_up / (sum_up + sum_down)",
    "GTJA_Alpha52": "ac = d['close'] * d['adj_factor']; hi = d['high'] * d['adj_factor']; lo = d['low'] * d['adj_factor']; tp_s1 = ((hi + lo + ac) / 3).shift(1); num = (hi - tp_s1).clip(lower=0).rolling(26).sum(); den = (tp_s1 - lo).clip(lower=0).rolling(26).sum(); out = num / den * 100",
    "GTJA_Alpha53": "ac = d['close'] * d['adj_factor']; cond_up = (ac > ac.shift(1)).astype(float); out = cond_up.rolling(12).sum() / 12 * 100",
    "GTJA_Alpha54": "ac = d['close'] * d['adj_factor']; op = d['open'] * d['adj_factor']; diff = ac - op; std_abs10 = np.abs(diff).rolling(10).std(); corr10 = ac.rolling(10).corr(op); out = -1 * row_rank(std_abs10 + diff + corr10)",
    "GTJA_Alpha55": "ac = d['close'] * d['adj_factor']; op = d['open'] * d['adj_factor']; hi = d['high'] * d['adj_factor']; lo = d['low'] * d['adj_factor']; c1 = ac.shift(1); o1 = op.shift(1); l1 = lo.shift(1); a = np.abs(hi - c1); b = np.abs(lo - c1); c = np.abs(hi - l1); e = np.abs(c1 - o1); cond1 = (a > b) & (a > c); cond2 = (b > c) & (b > a); den = (c + e / 4).where(~cond2, b + a / 2 + e / 4).where(~cond1, a + b / 2 + e / 4); num = 16 * (ac - c1 + (ac - op) / 2 + c1 - o1); out = (num / den * np.maximum(a, b)).rolling(20).sum()",
    "GTJA_Alpha56": "op = d['open'] * d['adj_factor']; hi = d['high'] * d['adj_factor']; lo = d['low'] * d['adj_factor']; rank_open = row_rank(op - op.rolling(12).min()); sum_mid19 = ((hi + lo) / 2).rolling(19).sum(); sum_meanvol19 = d['vol'].rolling(40).mean().rolling(19).sum(); corr13 = sum_mid19.rolling(13).corr(sum_meanvol19); rank_corr = row_rank(row_rank(corr13) ** 5); out = (rank_open < rank_corr).astype(float)",
    "GTJA_Alpha57": "ac = d['close'] * d['adj_factor']; hi = d['high'] * d['adj_factor']; lo = d['low'] * d['adj_factor']; tsmin_low9 = lo.rolling(9).min(); tsmax_high9 = hi.rolling(9).max(); x = (ac - tsmin_low9) / (tsmax_high9 - tsmin_low9) * 100; out = x.ewm(alpha=1/3, adjust=False).mean()",
    "GTJA_Alpha58": "ac = d['close'] * d['adj_factor']; cond_up = (ac > ac.shift(1)).astype(float); out = cond_up.rolling(20).sum() / 20 * 100",
    "GTJA_Alpha59": "ac = d['close'] * d['adj_factor']; hi = d['high'] * d['adj_factor']; lo = d['low'] * d['adj_factor']; c1 = ac.shift(1); inner = (ac - np.maximum(hi, c1)).where(ac <= c1, ac - np.minimum(lo, c1)); daily = inner.where(ac != c1, 0); out = daily.rolling(20).sum()",
    "GTJA_Alpha60": "ac = d['close'] * d['adj_factor']; hi = d['high'] * d['adj_factor']; lo = d['low'] * d['adj_factor']; inner = ((ac - lo) - (hi - ac)) / (hi - lo).replace(0, np.nan) * d['vol']; out = inner.rolling(20).sum()",
    "GTJA_Alpha61": "lo = d['low'] * d['adj_factor']; vwap = d['amount'] / d['vol'].replace(0, np.nan); rank1 = row_rank(rolling_decay_linear(vwap - vwap.shift(1), 12)); corr8 = lo.rolling(8).corr(d['vol'].rolling(80).mean()); rank2 = row_rank(rolling_decay_linear(row_rank(corr8), 17)); out = -1 * np.maximum(rank1, rank2)",
    "GTJA_Alpha62": "hi = d['high'] * d['adj_factor']; out = -1 * hi.rolling(5).corr(row_rank(d['vol']))",
    "GTJA_Alpha63": "ac = d['close'] * d['adj_factor']; diff = ac - ac.shift(1); sma_up = diff.clip(lower=0).ewm(alpha=1/6, adjust=False).mean(); sma_abs = np.abs(diff).ewm(alpha=1/6, adjust=False).mean(); out = sma_up / sma_abs * 100",
    "GTJA_Alpha64": "ac = d['close'] * d['adj_factor']; vwap = d['amount'] / d['vol'].replace(0, np.nan); corr1 = row_rank(vwap).rolling(4).corr(row_rank(d['vol'])); rank1 = row_rank(rolling_decay_linear(corr1, 4)); corr2 = row_rank(ac).rolling(4).corr(row_rank(d['vol'].rolling(60).mean())); rank2 = row_rank(rolling_decay_linear(corr2.rolling(13).max(), 14)); out = -1 * np.maximum(rank1, rank2)",
    "GTJA_Alpha65": "ac = d['close'] * d['adj_factor']; out = ac.rolling(6).mean() / ac",
    "GTJA_Alpha66": "ac = d['close'] * d['adj_factor']; ma6 = ac.rolling(6).mean(); out = (ac - ma6) / ma6 * 100",
    "GTJA_Alpha67": "ac = d['close'] * d['adj_factor']; diff = ac - ac.shift(1); sma_up = diff.clip(lower=0).ewm(alpha=1/24, adjust=False).mean(); sma_abs = np.abs(diff).ewm(alpha=1/24, adjust=False).mean(); out = sma_up / sma_abs * 100",
    "GTJA_Alpha68": "hi = d['high'] * d['adj_factor']; lo = d['low'] * d['adj_factor']; mid = (hi + lo) / 2; x = (mid - mid.shift(1)) * (hi - lo) / d['vol'].replace(0, np.nan); out = x.ewm(alpha=2/15, adjust=False).mean()",
    "GTJA_Alpha69": "op = d['open'] * d['adj_factor']; hi = d['high'] * d['adj_factor']; lo = d['low'] * d['adj_factor']; o1 = op.shift(1); dtm = np.maximum(hi - op, op - o1).where(op > o1, 0); dbm = np.maximum(op - lo, op - o1).where(op < o1, 0); sum_dtm = dtm.rolling(20).sum(); sum_dbm = dbm.rolling(20).sum(); out = ((sum_dtm - sum_dbm) / sum_dbm).where(sum_dtm != sum_dbm, 0).where(sum_dtm <= sum_dbm, (sum_dtm - sum_dbm) / sum_dtm)",
    "GTJA_Alpha70": "out = d['amount'].rolling(6).std()",
    "GTJA_Alpha71": "ac = d['close'] * d['adj_factor']; ma24 = ac.rolling(24).mean(); out = (ac - ma24) / ma24 * 100",
    "GTJA_Alpha72": "ac = d['close'] * d['adj_factor']; hi = d['high'] * d['adj_factor']; lo = d['low'] * d['adj_factor']; tsmax_high6 = hi.rolling(6).max(); tsmin_low6 = lo.rolling(6).min(); x = (tsmax_high6 - ac) / (tsmax_high6 - tsmin_low6) * 100; out = x.ewm(alpha=1/15, adjust=False).mean()",
    "GTJA_Alpha73": "ac = d['close'] * d['adj_factor']; vwap = d['amount'] / d['vol'].replace(0, np.nan); corr10 = ac.rolling(10).corr(d['vol']); decay_1 = rolling_decay_linear(rolling_decay_linear(corr10, 16), 4); tsrank1 = decay_1.rolling(5).rank(pct=True, method='max'); corr4 = vwap.rolling(4).corr(d['vol'].rolling(30).mean()); rank2 = row_rank(rolling_decay_linear(corr4, 3)); out = -1 * (tsrank1 - rank2)",
    "GTJA_Alpha74": "lo = d['low'] * d['adj_factor']; vwap = d['amount'] / d['vol'].replace(0, np.nan); sum_price20 = (lo * 0.35 + vwap * 0.65).rolling(20).sum(); sum_meanvol20 = d['vol'].rolling(40).mean().rolling(20).sum(); rank1 = row_rank(sum_price20.rolling(7).corr(sum_meanvol20)); rank2 = row_rank(row_rank(vwap).rolling(6).corr(row_rank(d['vol']))); out = rank1 + rank2",
    "GTJA_Alpha75": "ac = d['close'] * d['adj_factor']; op = d['open'] * d['adj_factor']; bench_close = get_stock_index('000001', 'close'); bench_open = get_stock_index('000001', 'open'); bench_down = bench_close < bench_open; cond = (ac > op) & bench_down; out = cond.rolling(50).sum() / bench_down.rolling(50).sum()",
    "GTJA_Alpha76": "ac = d['close'] * d['adj_factor']; x = np.abs(ac / ac.shift(1) - 1) / d['vol'].replace(0, np.nan); out = x.rolling(20).std() / x.rolling(20).mean()",
    "GTJA_Alpha77": "hi = d['high'] * d['adj_factor']; lo = d['low'] * d['adj_factor']; vwap = d['amount'] / d['vol'].replace(0, np.nan); mid = (hi + lo) / 2; rank1 = row_rank(rolling_decay_linear((mid + hi) - (vwap + hi), 20)); corr3 = mid.rolling(3).corr(d['vol'].rolling(40).mean()); rank2 = row_rank(rolling_decay_linear(corr3, 6)); out = np.minimum(rank1, rank2)",
    "GTJA_Alpha78": "ac = d['close'] * d['adj_factor']; hi = d['high'] * d['adj_factor']; lo = d['low'] * d['adj_factor']; tp = (hi + lo + ac) / 3; ma_tp12 = tp.rolling(12).mean(); out = (tp - ma_tp12) / (0.015 * np.abs(ac - ma_tp12).rolling(12).mean())",
    "GTJA_Alpha79": "ac = d['close'] * d['adj_factor']; diff = ac - ac.shift(1); sma_up = diff.clip(lower=0).ewm(alpha=1/12, adjust=False).mean(); sma_abs = np.abs(diff).ewm(alpha=1/12, adjust=False).mean(); out = sma_up / sma_abs * 100",
    "GTJA_Alpha80": "vol = d['vol']; out = (vol - vol.shift(5)) / vol.shift(5) * 100",
    "GTJA_Alpha81": "out = d['vol'].ewm(alpha=2/21, adjust=False).mean()",
    "GTJA_Alpha82": "ac = d['close'] * d['adj_factor']; hi = d['high'] * d['adj_factor']; lo = d['low'] * d['adj_factor']; tsmax_high6 = hi.rolling(6).max(); tsmin_low6 = lo.rolling(6).min(); x = (tsmax_high6 - ac) / (tsmax_high6 - tsmin_low6) * 100; out = x.ewm(alpha=1/20, adjust=False).mean()",
    "GTJA_Alpha83": "hi = d['high'] * d['adj_factor']; cov5 = row_rank(hi).rolling(5).cov(row_rank(d['vol'])); out = -1 * row_rank(cov5)",
    "GTJA_Alpha84": "ac = d['close'] * d['adj_factor']; diff = ac - ac.shift(1); signed_vol = d['vol'].where(diff > 0, (-d['vol']).where(diff < 0, 0)); out = signed_vol.rolling(20).sum()",
    "GTJA_Alpha85": "ac = d['close'] * d['adj_factor']; vol_ratio = d['vol'] / d['vol'].rolling(20).mean(); tsrank_vol20 = vol_ratio.rolling(20).rank(pct=True, method='max'); tsrank_delta8 = (-1 * (ac - ac.shift(7))).rolling(8).rank(pct=True, method='max'); out = tsrank_vol20 * tsrank_delta8",
    "GTJA_Alpha86": "ac = d['close'] * d['adj_factor']; slope_diff = (ac.shift(20) - ac.shift(10)) / 10 - (ac.shift(10) - ac) / 10; out = (-1 * (ac - ac.shift(1))).where(slope_diff >= 0, 1.0).where(slope_diff <= 0.25, -1.0)",
    "GTJA_Alpha87": "op = d['open'] * d['adj_factor']; hi = d['high'] * d['adj_factor']; lo = d['low'] * d['adj_factor']; vwap = d['amount'] / d['vol'].replace(0, np.nan); rank1 = row_rank(rolling_decay_linear(vwap - vwap.shift(4), 7)); x = ((lo * 0.9 + lo * 0.1) - vwap) / (op - (hi + lo) / 2); tsrank2 = rolling_decay_linear(x, 11).rolling(7).rank(pct=True, method='max'); out = -1 * (rank1 + tsrank2)",
    "GTJA_Alpha88": "ac = d['close'] * d['adj_factor']; out = (ac - ac.shift(20)) / ac.shift(20) * 100",
    "GTJA_Alpha89": "ac = d['close'] * d['adj_factor']; sma13 = ac.ewm(alpha=2/13, adjust=False).mean(); sma27 = ac.ewm(alpha=2/27, adjust=False).mean(); diff = sma13 - sma27; out = 2 * (diff - diff.ewm(alpha=2/10, adjust=False).mean())",
    "GTJA_Alpha90": "vwap = d['amount'] / d['vol'].replace(0, np.nan); corr5 = row_rank(vwap).rolling(5).corr(row_rank(d['vol'])); out = -1 * row_rank(corr5)",
    "GTJA_Alpha91": "ac = d['close'] * d['adj_factor']; lo = d['low'] * d['adj_factor']; rank1 = row_rank(ac - ac.rolling(5).max()); corr5 = d['vol'].rolling(40).mean().rolling(5).corr(lo); out = -1 * rank1 * row_rank(corr5)",
    "GTJA_Alpha92": "ac = d['close'] * d['adj_factor']; vwap = d['amount'] / d['vol'].replace(0, np.nan); price = ac * 0.35 + vwap * 0.65; rank1 = row_rank(rolling_decay_linear(price - price.shift(2), 3)); corr13 = d['vol'].rolling(180).mean().rolling(13).corr(ac); tsrank2 = rolling_decay_linear(np.abs(corr13), 5).rolling(15).rank(pct=True, method='max'); out = -1 * np.maximum(rank1, tsrank2)",
    "GTJA_Alpha93": "op = d['open'] * d['adj_factor']; lo = d['low'] * d['adj_factor']; o1 = op.shift(1); daily = np.maximum(op - lo, op - o1).where(op < o1, 0); out = daily.rolling(20).sum()",
    "GTJA_Alpha94": "ac = d['close'] * d['adj_factor']; diff = ac - ac.shift(1); signed_vol = d['vol'].where(diff > 0, (-d['vol']).where(diff < 0, 0)); out = signed_vol.rolling(30).sum()",
    "GTJA_Alpha95": "out = d['amount'].rolling(20).std()",
    "GTJA_Alpha96": "ac = d['close'] * d['adj_factor']; hi = d['high'] * d['adj_factor']; lo = d['low'] * d['adj_factor']; tsmin_low9 = lo.rolling(9).min(); tsmax_high9 = hi.rolling(9).max(); x = (ac - tsmin_low9) / (tsmax_high9 - tsmin_low9) * 100; out = x.ewm(alpha=1/3, adjust=False).mean().ewm(alpha=1/3, adjust=False).mean()",
    "GTJA_Alpha97": "out = d['vol'].rolling(10).std()",
    "GTJA_Alpha98": "ac = d['close'] * d['adj_factor']; ma100 = ac.rolling(100).mean(); ratio = (ma100 - ma100.shift(100)) / ac.shift(100); out = (-1 * (ac - ac.shift(3))).where(ratio > 0.05, -1 * (ac - ac.rolling(100).min()))",
    "GTJA_Alpha99": "ac = d['close'] * d['adj_factor']; cov5 = row_rank(ac).rolling(5).cov(row_rank(d['vol'])); out = -1 * row_rank(cov5)",
    "GTJA_Alpha100": "out = d['vol'].rolling(20).std()",
    "GTJA_Alpha101": "ac = d['close'] * d['adj_factor']; hi = d['high'] * d['adj_factor']; vwap = d['amount'] / d['vol'].replace(0, np.nan); sum_meanvol37 = d['vol'].rolling(30).mean().rolling(37).sum(); rank1 = row_rank(ac.rolling(15).corr(sum_meanvol37)); rank2 = row_rank(row_rank(hi * 0.1 + vwap * 0.9).rolling(11).corr(row_rank(d['vol']))); out = (rank1 < rank2).astype(float) * -1",
    "GTJA_Alpha102": "vol = d['vol']; diff = vol - vol.shift(1); sma_up = diff.clip(lower=0).ewm(alpha=1/6, adjust=False).mean(); sma_abs = np.abs(diff).ewm(alpha=1/6, adjust=False).mean(); out = sma_up / sma_abs * 100",
    "GTJA_Alpha103": "lo = d['low'] * d['adj_factor']; out = (1 - rolling_imin(lo, 20)) * 100",
    "GTJA_Alpha104": "ac = d['close'] * d['adj_factor']; hi = d['high'] * d['adj_factor']; corr5 = hi.rolling(5).corr(d['vol']); out = -1 * (corr5 - corr5.shift(5)) * row_rank(ac.rolling(20).std())",
    "GTJA_Alpha105": "op = d['open'] * d['adj_factor']; out = -1 * row_rank(op).rolling(10).corr(row_rank(d['vol']))",
    "GTJA_Alpha106": "ac = d['close'] * d['adj_factor']; out = ac - ac.shift(20)",
    "GTJA_Alpha107": "ac = d['close'] * d['adj_factor']; op = d['open'] * d['adj_factor']; hi = d['high'] * d['adj_factor']; lo = d['low'] * d['adj_factor']; out = -1 * row_rank(op - hi.shift(1)) * row_rank(op - ac.shift(1)) * row_rank(op - lo.shift(1))",
    "GTJA_Alpha108": "hi = d['high'] * d['adj_factor']; vwap = d['amount'] / d['vol'].replace(0, np.nan); rank1 = row_rank(hi - hi.rolling(2).min()); rank2 = row_rank(vwap.rolling(6).corr(d['vol'].rolling(120).mean())); out = -1 * (rank1 ** rank2)",
    "GTJA_Alpha109": "hi = d['high'] * d['adj_factor']; lo = d['low'] * d['adj_factor']; sma1 = (hi - lo).ewm(alpha=2/10, adjust=False).mean(); out = sma1 / sma1.ewm(alpha=2/10, adjust=False).mean()",
    "GTJA_Alpha110": "ac = d['close'] * d['adj_factor']; hi = d['high'] * d['adj_factor']; lo = d['low'] * d['adj_factor']; c1 = ac.shift(1); num = (hi - c1).clip(lower=0).rolling(20).sum(); den = (c1 - lo).clip(lower=0).rolling(20).sum(); out = num / den * 100",
    "GTJA_Alpha111": "ac = d['close'] * d['adj_factor']; hi = d['high'] * d['adj_factor']; lo = d['low'] * d['adj_factor']; x = d['vol'] * ((ac - lo) - (hi - ac)) / (hi - lo).replace(0, np.nan); out = x.ewm(alpha=2/11, adjust=False).mean() - x.ewm(alpha=2/4, adjust=False).mean()",
    "GTJA_Alpha112": "ac = d['close'] * d['adj_factor']; diff = ac - ac.shift(1); sum_up = diff.clip(lower=0).rolling(12).sum(); sum_down = (-diff).clip(lower=0).rolling(12).sum(); out = (sum_up - sum_down) / (sum_up + sum_down) * 100",
    "GTJA_Alpha113": "ac = d['close'] * d['adj_factor']; rank1 = row_rank(ac.shift(5).rolling(20).sum() / 20); corr2 = ac.rolling(2).corr(d['vol']); rank3 = row_rank(ac.rolling(5).sum().rolling(2).corr(ac.rolling(20).sum())); out = -1 * rank1 * corr2 * rank3",
    "GTJA_Alpha114": "ac = d['close'] * d['adj_factor']; hi = d['high'] * d['adj_factor']; lo = d['low'] * d['adj_factor']; vwap = d['amount'] / d['vol'].replace(0, np.nan); ratio = (hi - lo) / (ac.rolling(5).sum() / 5); out = row_rank(ratio.shift(2)) * row_rank(row_rank(d['vol'])) / (ratio / (vwap - ac))",
    "GTJA_Alpha115": "ac = d['close'] * d['adj_factor']; hi = d['high'] * d['adj_factor']; lo = d['low'] * d['adj_factor']; rank1 = row_rank((hi * 0.9 + ac * 0.1).rolling(10).corr(d['vol'].rolling(30).mean())); tsrank_mid = ((hi + lo) / 2).rolling(4).rank(pct=True, method='max'); tsrank_vol = d['vol'].rolling(10).rank(pct=True, method='max'); rank2 = row_rank(tsrank_mid.rolling(7).corr(tsrank_vol)); out = rank1 ** rank2",
    "GTJA_Alpha116": "ac = d['close'] * d['adj_factor']; out = rolling_slope(ac, 20)",
    "GTJA_Alpha117": "ac = d['close'] * d['adj_factor']; hi = d['high'] * d['adj_factor']; lo = d['low'] * d['adj_factor']; ret = ac / ac.shift(1) - 1; tsrank_vol32 = d['vol'].rolling(32).rank(pct=True, method='max'); tsrank_chl16 = ((ac + hi) - lo).rolling(16).rank(pct=True, method='max'); tsrank_ret32 = ret.rolling(32).rank(pct=True, method='max'); out = tsrank_vol32 * (1 - tsrank_chl16) * (1 - tsrank_ret32)",
    "GTJA_Alpha118": "op = d['open'] * d['adj_factor']; hi = d['high'] * d['adj_factor']; lo = d['low'] * d['adj_factor']; out = (hi - op).rolling(20).sum() / (op - lo).rolling(20).sum() * 100",
    "GTJA_Alpha119": "op = d['open'] * d['adj_factor']; vwap = d['amount'] / d['vol'].replace(0, np.nan); sum_meanvol26 = d['vol'].rolling(5).mean().rolling(26).sum(); rank1 = row_rank(rolling_decay_linear(vwap.rolling(5).corr(sum_meanvol26), 7)); corr21 = row_rank(op).rolling(21).corr(row_rank(d['vol'].rolling(15).mean())); tsrank_inner = corr21.rolling(9).min().rolling(7).rank(pct=True, method='max'); rank2 = row_rank(rolling_decay_linear(tsrank_inner, 8)); out = rank1 - rank2",
    "GTJA_Alpha120": "ac = d['close'] * d['adj_factor']; vwap = d['amount'] / d['vol'].replace(0, np.nan); out = row_rank(vwap - ac) / row_rank(vwap + ac)",
    "GTJA_Alpha121": "vwap = d['amount'] / d['vol'].replace(0, np.nan); rank1 = row_rank(vwap - vwap.rolling(12).min()); tsrank_vwap20 = vwap.rolling(20).rank(pct=True, method='max'); tsrank_vol2 = d['vol'].rolling(60).mean().rolling(2).rank(pct=True, method='max'); tsrank2 = tsrank_vwap20.rolling(18).corr(tsrank_vol2).rolling(3).rank(pct=True, method='max'); out = -1 * (rank1 ** tsrank2)",
    "GTJA_Alpha122": "ac = d['close'] * d['adj_factor']; log_c = np.log(ac); sma3 = log_c.ewm(alpha=2/13, adjust=False).mean().ewm(alpha=2/13, adjust=False).mean().ewm(alpha=2/13, adjust=False).mean(); out = (sma3 - sma3.shift(1)) / sma3.shift(1)",
    "GTJA_Alpha123": "hi = d['high'] * d['adj_factor']; lo = d['low'] * d['adj_factor']; sum_mid20 = ((hi + lo) / 2).rolling(20).sum(); sum_meanvol20 = d['vol'].rolling(60).mean().rolling(20).sum(); rank1 = row_rank(sum_mid20.rolling(9).corr(sum_meanvol20)); rank2 = row_rank(lo.rolling(6).corr(d['vol'])); out = (rank1 < rank2).astype(float) * -1",
    "GTJA_Alpha124": "ac = d['close'] * d['adj_factor']; vwap = d['amount'] / d['vol'].replace(0, np.nan); out = (ac - vwap) / rolling_decay_linear(row_rank(ac.rolling(30).max()), 2)",
    "GTJA_Alpha125": "ac = d['close'] * d['adj_factor']; vwap = d['amount'] / d['vol'].replace(0, np.nan); rank1 = row_rank(rolling_decay_linear(vwap.rolling(17).corr(d['vol'].rolling(80).mean()), 20)); price = ac * 0.5 + vwap * 0.5; rank2 = row_rank(rolling_decay_linear(price - price.shift(3), 16)); out = rank1 / rank2",
    "GTJA_Alpha126": "ac = d['close'] * d['adj_factor']; hi = d['high'] * d['adj_factor']; lo = d['low'] * d['adj_factor']; out = (ac + hi + lo) / 3",
    "GTJA_Alpha127": "ac = d['close'] * d['adj_factor']; max_c12 = ac.rolling(12).max(); x = (100 * (ac - max_c12) / max_c12) ** 2; out = np.sqrt(x.rolling(12).mean())",
    "GTJA_Alpha128": "ac = d['close'] * d['adj_factor']; hi = d['high'] * d['adj_factor']; lo = d['low'] * d['adj_factor']; tp = (hi + lo + ac) / 3; tp_vol = tp * d['vol']; pos = tp_vol.where(tp > tp.shift(1), 0).rolling(14).sum(); neg = tp_vol.where(tp < tp.shift(1), 0).rolling(14).sum(); out = 100 - 100 / (1 + pos / neg)",
    "GTJA_Alpha129": "ac = d['close'] * d['adj_factor']; diff = ac - ac.shift(1); out = (-diff).clip(lower=0).rolling(12).sum()",
    "GTJA_Alpha130": "hi = d['high'] * d['adj_factor']; lo = d['low'] * d['adj_factor']; vwap = d['amount'] / d['vol'].replace(0, np.nan); corr9 = ((hi + lo) / 2).rolling(9).corr(d['vol'].rolling(40).mean()); rank1 = row_rank(rolling_decay_linear(corr9, 10)); corr7 = row_rank(vwap).rolling(7).corr(row_rank(d['vol'])); rank2 = row_rank(rolling_decay_linear(corr7, 3)); out = rank1 / rank2",
    "GTJA_Alpha131": "ac = d['close'] * d['adj_factor']; vwap = d['amount'] / d['vol'].replace(0, np.nan); rank1 = row_rank(vwap - vwap.shift(1)); corr18 = ac.rolling(18).corr(d['vol'].rolling(50).mean()); tsrank2 = corr18.rolling(18).rank(pct=True, method='max'); out = rank1 ** tsrank2",
    "GTJA_Alpha132": "out = d['amount'].rolling(20).mean()",
    "GTJA_Alpha133": "hi = d['high'] * d['adj_factor']; lo = d['low'] * d['adj_factor']; out = (1 - rolling_imax(hi, 20)) * 100 - (1 - rolling_imin(lo, 20)) * 100",
    "GTJA_Alpha134": "ac = d['close'] * d['adj_factor']; out = (ac - ac.shift(12)) / ac.shift(12) * d['vol']",
    "GTJA_Alpha135": "ac = d['close'] * d['adj_factor']; x = (ac / ac.shift(20)).shift(1); out = x.ewm(alpha=1/20, adjust=False).mean()",
    "GTJA_Alpha136": "ac = d['close'] * d['adj_factor']; op = d['open'] * d['adj_factor']; ret = ac / ac.shift(1) - 1; rank1 = row_rank(ret - ret.shift(3)); out = -1 * rank1 * op.rolling(10).corr(d['vol'])",
    "GTJA_Alpha137": "ac = d['close'] * d['adj_factor']; op = d['open'] * d['adj_factor']; hi = d['high'] * d['adj_factor']; lo = d['low'] * d['adj_factor']; c1 = ac.shift(1); o1 = op.shift(1); l1 = lo.shift(1); a = np.abs(hi - c1); b = np.abs(lo - c1); c = np.abs(hi - l1); e = np.abs(c1 - o1); cond1 = (a > b) & (a > c); cond2 = (b > c) & (b > a); den = (c + e / 4).where(~cond2, b + a / 2 + e / 4).where(~cond1, a + b / 2 + e / 4); num = 16 * (ac - c1 + (ac - op) / 2 + c1 - o1); out = num / den * np.maximum(a, b)",
    "GTJA_Alpha138": "lo = d['low'] * d['adj_factor']; vwap = d['amount'] / d['vol'].replace(0, np.nan); price = lo * 0.7 + vwap * 0.3; rank1 = row_rank(rolling_decay_linear(price - price.shift(3), 20)); tsrank_low8 = lo.rolling(8).rank(pct=True, method='max'); tsrank_vol17 = d['vol'].rolling(60).mean().rolling(17).rank(pct=True, method='max'); corr5 = tsrank_low8.rolling(5).corr(tsrank_vol17); tsrank_inner = corr5.rolling(19).rank(pct=True, method='max'); tsrank2 = rolling_decay_linear(tsrank_inner, 16).rolling(7).rank(pct=True, method='max'); out = -1 * (rank1 - tsrank2)",
    "GTJA_Alpha139": "op = d['open'] * d['adj_factor']; out = -1 * op.rolling(10).corr(d['vol'])",
    "GTJA_Alpha140": "ac = d['close'] * d['adj_factor']; op = d['open'] * d['adj_factor']; hi = d['high'] * d['adj_factor']; lo = d['low'] * d['adj_factor']; spread = (row_rank(op) + row_rank(lo)) - (row_rank(hi) + row_rank(ac)); rank1 = row_rank(rolling_decay_linear(spread, 8)); tsrank_close8 = ac.rolling(8).rank(pct=True, method='max'); tsrank_vol20 = d['vol'].rolling(60).mean().rolling(20).rank(pct=True, method='max'); corr8 = tsrank_close8.rolling(8).corr(tsrank_vol20); tsrank2 = rolling_decay_linear(corr8, 7).rolling(3).rank(pct=True, method='max'); out = np.minimum(rank1, tsrank2)",
    "GTJA_Alpha141": "hi = d['high'] * d['adj_factor']; corr9 = row_rank(hi).rolling(9).corr(row_rank(d['vol'].rolling(15).mean())); out = -1 * row_rank(corr9)",
    "GTJA_Alpha142": "ac = d['close'] * d['adj_factor']; rank1 = row_rank(ac.rolling(10).rank(pct=True, method='max')); delta1 = ac - ac.shift(1); rank2 = row_rank(delta1 - delta1.shift(1)); vol_ratio = d['vol'] / d['vol'].rolling(20).mean(); rank3 = row_rank(vol_ratio.rolling(5).rank(pct=True, method='max')); out = -1 * rank1 * rank2 * rank3",
    "GTJA_Alpha143": "ac = d['close'] * d['adj_factor']; ret = ac / ac.shift(1) - 1; ret_ac = ret * ac; out = ret_ac.where(ac > ac.shift(1), ac)",
    "GTJA_Alpha144": "ac = d['close'] * d['adj_factor']; cond_down = ac < ac.shift(1); x = (np.abs(ac / ac.shift(1) - 1) / d['amount']).where(cond_down, 0); cnt = cond_down.astype(float).rolling(20).sum(); out = x.rolling(20).sum() / cnt",
    "GTJA_Alpha145": "vol = d['vol']; out = (vol.rolling(9).mean() - vol.rolling(26).mean()) / vol.rolling(12).mean() * 100",
    "GTJA_Alpha146": "ac = d['close'] * d['adj_factor']; ret = ac / ac.shift(1) - 1; sma61 = ret.ewm(alpha=2/61, adjust=False).mean(); dev = ret - sma61; out = dev.rolling(20).mean() * dev / ((ret - dev) ** 2).ewm(alpha=2/60, adjust=False).mean()",
    "GTJA_Alpha147": "ac = d['close'] * d['adj_factor']; out = rolling_slope(ac.rolling(12).mean(), 12)",
    "GTJA_Alpha148": "op = d['open'] * d['adj_factor']; sum_meanvol9 = d['vol'].rolling(60).mean().rolling(9).sum(); rank1 = row_rank(op.rolling(6).corr(sum_meanvol9)); rank2 = row_rank(op - op.rolling(14).min()); out = (rank1 < rank2).astype(float) * -1",
    "GTJA_Alpha149": "ac = d['close'] * d['adj_factor']; ret_stock = ac / ac.shift(1) - 1; bench_close = get_stock_index('000001', 'close'); ret_bench = bench_close / bench_close.shift(1) - 1; out = rolling_regbeta(ret_stock, ret_bench, 252)",
    "GTJA_Alpha150": "ac = d['close'] * d['adj_factor']; hi = d['high'] * d['adj_factor']; lo = d['low'] * d['adj_factor']; out = (ac + hi + lo) / 3 * d['vol']",
    "GTJA_Alpha151": "ac = d['close'] * d['adj_factor']; out = (ac - ac.shift(20)).ewm(alpha=1/20, adjust=False).mean()",
    "GTJA_Alpha152": "ac = d['close'] * d['adj_factor']; x = (ac / ac.shift(9)).shift(1); sma9 = x.ewm(alpha=1/9, adjust=False).mean().shift(1); out = (sma9.rolling(12).mean() - sma9.rolling(26).mean()).ewm(alpha=1/9, adjust=False).mean()",
    "GTJA_Alpha153": "ac = d['close'] * d['adj_factor']; out = (ac.rolling(3).mean() + ac.rolling(6).mean() + ac.rolling(12).mean() + ac.rolling(24).mean()) / 4",
    "GTJA_Alpha154": "vwap = d['amount'] / d['vol'].replace(0, np.nan); corr18 = vwap.rolling(18).corr(d['vol'].rolling(180).mean()); out = ((vwap - vwap.rolling(16).min()) < corr18).astype(float)",
    "GTJA_Alpha155": "vol = d['vol']; sma13 = vol.ewm(alpha=2/13, adjust=False).mean(); sma27 = vol.ewm(alpha=2/27, adjust=False).mean(); diff = sma13 - sma27; out = diff - diff.ewm(alpha=2/10, adjust=False).mean()",
    "GTJA_Alpha156": "op = d['open'] * d['adj_factor']; lo = d['low'] * d['adj_factor']; vwap = d['amount'] / d['vol'].replace(0, np.nan); rank1 = row_rank(rolling_decay_linear(vwap - vwap.shift(5), 3)); price = op * 0.15 + lo * 0.85; rank2 = row_rank(rolling_decay_linear((price - price.shift(2)) / price * -1, 3)); out = -1 * np.maximum(rank1, rank2)",
    "GTJA_Alpha157": "ac = d['close'] * d['adj_factor']; ret = ac / ac.shift(1) - 1; base = ac - 1; rank_delta = -1 * row_rank(base - base.shift(5)); tsmin2 = row_rank(row_rank(rank_delta)).rolling(2).min(); log_val = np.log(tsmin2.replace(0, np.nan)); term1 = row_rank(row_rank(log_val)).rolling(5).min(); term2 = (-1 * ret).shift(6).rolling(5).rank(pct=True, method='max'); out = term1 + term2",
    "GTJA_Alpha158": "ac = d['close'] * d['adj_factor']; hi = d['high'] * d['adj_factor']; lo = d['low'] * d['adj_factor']; sma15 = ac.ewm(alpha=2/15, adjust=False).mean(); out = ((hi - sma15) - (lo - sma15)) / ac",
    "GTJA_Alpha159": "ac = d['close'] * d['adj_factor']; hi = d['high'] * d['adj_factor']; lo = d['low'] * d['adj_factor']; c1 = ac.shift(1); min_lc = np.minimum(lo, c1); rng = np.maximum(hi, c1) - min_lc; a6 = (ac - min_lc.rolling(6).sum()) / rng.rolling(6).sum(); a12 = (ac - min_lc.rolling(12).sum()) / rng.rolling(12).sum(); a24 = (ac - min_lc.rolling(24).sum()) / rng.rolling(24).sum(); out = (a6 * 12 * 24 + a12 * 6 * 24 + a24 * 6 * 12) * 100 / (6 * 12 + 6 * 24 + 12 * 24)",
    "GTJA_Alpha160": "ac = d['close'] * d['adj_factor']; std20 = ac.rolling(20).std(); x = std20.where(ac <= ac.shift(1), 0); out = x.ewm(alpha=1/20, adjust=False).mean()",
    "GTJA_Alpha161": "ac = d['close'] * d['adj_factor']; hi = d['high'] * d['adj_factor']; lo = d['low'] * d['adj_factor']; c1 = ac.shift(1); tr = np.maximum(np.maximum(hi - lo, np.abs(c1 - hi)), np.abs(c1 - lo)); out = tr.rolling(12).mean()",
    "GTJA_Alpha162": "ac = d['close'] * d['adj_factor']; dclose = ac - ac.shift(1); up = dclose.clip(lower=0); absd = dclose.abs(); s_up = up.ewm(alpha=1/12, adjust=False).mean(); s_abs = absd.ewm(alpha=1/12, adjust=False).mean(); rs = s_up / s_abs * 100; out = (rs - rs.rolling(12).min()) / (rs.rolling(12).max() - rs.rolling(12).min())",
    "GTJA_Alpha163": "ac = d['close'] * d['adj_factor']; hi = d['high'] * d['adj_factor']; vwap = d['amount'] / d['vol'].replace(0, np.nan); ret = ac / ac.shift(1) - 1; out = -1 * ret * d['vol'].rolling(20).mean() * vwap * (hi - ac)",
    "GTJA_Alpha164": "ac = d['close'] * d['adj_factor']; hi = d['high'] * d['adj_factor']; lo = d['low'] * d['adj_factor']; dclose = ac - ac.shift(1); inv = (1 / np.maximum(dclose, 1e-9)).where(ac > ac.shift(1), 1.0); inv_min = inv.rolling(12).min(); x = (inv - inv_min) / ((hi - lo) * 100).replace(0, np.nan); out = x.ewm(alpha=2/13, adjust=False).mean()",
    "GTJA_Alpha165": "ac = d['close'] * d['adj_factor']; dev = ac - ac.rolling(48).mean(); cum = dev.cumsum(); rng = cum.cummax() - cum.cummin(); out = rng / ac.rolling(48).std()",
    "GTJA_Alpha166": "ac = d['close'] * d['adj_factor']; r = ac / ac.shift(1) - 1; mr = r.rolling(20).mean(); num = (r - mr) ** 3; den = (r - mr) ** 2; s3 = num.rolling(20).sum(); s2 = den.rolling(20).sum(); out = -20 * (19) ** 1.5 * s3 / (19 * 18 * s2 ** 1.5)",
    "GTJA_Alpha167": "ac = d['close'] * d['adj_factor']; out = (ac - ac.shift(1)).clip(lower=0).rolling(12).sum()",
    "GTJA_Alpha168": "vol = d['vol']; out = -1 * vol / vol.rolling(20).mean()",
    "GTJA_Alpha169": "ac = d['close'] * d['adj_factor']; ratio = ac / ac.shift(1); s9 = ratio.ewm(alpha=1/9, adjust=False).mean(); delayed = s9.shift(1); ma12 = delayed.rolling(12).mean(); ma26 = delayed.rolling(26).mean(); diff = ma12 - ma26; out = diff.ewm(alpha=1/10, adjust=False).mean()",
    "GTJA_Alpha170": "ac = d['close'] * d['adj_factor']; hi = d['high'] * d['adj_factor']; vwap = d['amount'] / d['vol'].replace(0, np.nan); r1 = row_rank(1 / ac); termA = r1 * d['vol'] / d['vol'].rolling(20).mean(); r2 = row_rank(hi - ac); termB = hi * r2 / (hi.rolling(5).sum() / 5); r3 = row_rank(vwap - vwap.shift(5)); out = termA * termB - r3",
    "GTJA_Alpha171": "ac = d['close'] * d['adj_factor']; op = d['open'] * d['adj_factor']; hi = d['high'] * d['adj_factor']; lo = d['low'] * d['adj_factor']; out = -1 * ((lo - ac) * (op ** 5)) / ((ac - hi) * (ac ** 5))",
    "GTJA_Alpha172": "ac = d['close'] * d['adj_factor']; hi = d['high'] * d['adj_factor']; lo = d['low'] * d['adj_factor']; prev_close = ac.shift(1); tr1 = hi - lo; tr2 = (hi - prev_close).abs(); tr3 = (lo - prev_close).abs(); TR = pd.DataFrame(np.maximum(tr1, np.maximum(tr2, tr3))); HD = hi - prev_close; LD = prev_close - lo; cond_LD = (LD > 0) & (LD > HD); cond_HD = (HD > 0) & (HD > LD); sum_LD = (LD * cond_LD).rolling(14).sum(); sum_HD = (HD * cond_HD).rolling(14).sum(); sum_TR = TR.rolling(14).sum(); pct_LD = sum_LD * 100 / (sum_TR + 1e-10); pct_HD = sum_HD * 100 / (sum_TR + 1e-10); numerator = (pct_LD - pct_HD).abs(); denominator = pct_LD + pct_HD + 1e-10; ratio = numerator / denominator * 100; mean6 = ratio.rolling(6).mean(); out = mean6",
    "GTJA_Alpha173": "ac = d['close'] * d['adj_factor']; s1 = ac.ewm(alpha=2/13, adjust=False).mean(); s2 = s1.ewm(alpha=2/13, adjust=False).mean(); logc = np.log(ac); s3 = logc.ewm(alpha=2/13, adjust=False).mean().ewm(alpha=2/13, adjust=False).mean().ewm(alpha=2/13, adjust=False).mean(); out = 3 * s1 - 2 * s2 + s3",
    "GTJA_Alpha174": "ac = d['close'] * d['adj_factor']; cond = ac > ac.shift(1); val = ac.rolling(20).std().where(cond, 0); out = val.ewm(alpha=1/20, adjust=False).mean()",
    "GTJA_Alpha175": "ac = d['close'] * d['adj_factor']; hi = d['high'] * d['adj_factor']; lo = d['low'] * d['adj_factor']; c1 = ac.shift(1); tr = np.maximum(np.maximum(hi - lo, np.abs(c1 - hi)), np.abs(c1 - lo)); out = tr.rolling(6).mean()",
    "GTJA_Alpha176": "ac = d['close'] * d['adj_factor']; hi = d['high'] * d['adj_factor']; lo = d['low'] * d['adj_factor']; wpr = (ac - lo.rolling(12).min()) / (hi.rolling(12).max() - lo.rolling(12).min()).replace(0, np.nan); out = row_rank(wpr).rolling(6).corr(row_rank(d['vol']))",
    "GTJA_Alpha177": "hi = d['high'] * d['adj_factor']; out = (20 - rolling_imax(hi, 20)) / 20 * 100",
    "GTJA_Alpha178": "ac = d['close'] * d['adj_factor']; out = (ac - ac.shift(1)) / ac.shift(1) * d['vol']",
    "GTJA_Alpha179": "vwap = d['amount'] / d['vol'].replace(0, np.nan); lo = d['low'] * d['adj_factor']; r1 = row_rank(vwap.rolling(4).corr(d['vol'])); corr12 = row_rank(lo).rolling(12).corr(row_rank(d['vol'].rolling(50).mean())); r2 = row_rank(corr12); out = r1 * r2",
    "GTJA_Alpha180": "ac = d['close'] * d['adj_factor']; hi = d['high'] * d['adj_factor']; delta = ac - ac.shift(7); cond = d['vol'].rolling(20).mean() < d['vol']; term_if = -1 * np.abs(delta).rolling(60).rank(pct=True, method='max') * np.sign(delta); term_else = -1 * d['vol']; out = term_if.where(cond, term_else)",
    "GTJA_Alpha181": "ac = d['close'] * d['adj_factor']; ret = ac / ac.shift(1) - 1; mean_ret20 = ret.rolling(20).mean(); bench_close = get_stock_index('000001', 'close'); mean_bench20 = bench_close.rolling(20).mean(); numerator = ((ret - mean_ret20) - (bench_close - mean_bench20) ** 2).rolling(20).sum(); denominator = ((bench_close - mean_bench20) ** 3).rolling(20).sum(); out = numerator / denominator",
    "GTJA_Alpha182": "op = d['open'] * d['adj_factor']; ac = d['close'] * d['adj_factor']; bench_close = get_stock_index('000001', 'close'); bench_open = get_stock_index('000001', 'open'); same_direction = ((ac > op) & (bench_close > bench_open)) | ((ac < op) & (bench_close < bench_open)); out = same_direction.rolling(20).sum() / 20",
    "GTJA_Alpha183": "ac = d['close'] * d['adj_factor']; dev = ac - ac.rolling(24).mean(); cum = dev.cumsum(); rng = cum.cummax() - cum.cummin(); out = rng / ac.rolling(24).std()",
    "GTJA_Alpha184": "ac = d['close'] * d['adj_factor']; op = d['open'] * d['adj_factor']; r1 = row_rank((op - ac).shift(1).rolling(200).corr(ac)); r2 = row_rank(op - ac); out = r1 + r2",
    "GTJA_Alpha185": "ac = d['close'] * d['adj_factor']; op = d['open'] * d['adj_factor']; out = -1 * ((1 - op / ac) ** 2)",
    "GTJA_Alpha186": "ac = d['close'] * d['adj_factor']; hi = d['high'] * d['adj_factor']; lo = d['low'] * d['adj_factor']; prev_close = ac.shift(1); tr1 = hi - lo; tr2 = (hi - prev_close).abs(); tr3 = (lo - prev_close).abs(); TR = pd.DataFrame(np.maximum(tr1, np.maximum(tr2, tr3))); HD = hi - prev_close; LD = prev_close - lo; cond_LD = (LD > 0) & (LD > HD); cond_HD = (HD > 0) & (HD > LD); sum_LD = (LD * cond_LD).rolling(14).sum(); sum_HD = (HD * cond_HD).rolling(14).sum(); sum_TR = TR.rolling(14).sum(); pct_LD = sum_LD * 100 / sum_TR; pct_HD = sum_HD * 100 / sum_TR; numerator = (pct_LD - pct_HD).abs(); denominator = pct_LD + pct_HD; ratio = numerator / denominator * 100; mean6 = ratio.rolling(6).mean(); out = (mean6 + mean6.shift(6)) / 2",
    "GTJA_Alpha187": "op = d['open'] * d['adj_factor']; hi = d['high'] * d['adj_factor']; o1 = op.shift(1); upmove = np.maximum(hi - op, op - o1); val = upmove.where(op > o1, 0); out = val.rolling(20).sum()",
    "GTJA_Alpha188": "hi = d['high'] * d['adj_factor']; lo = d['low'] * d['adj_factor']; hl = hi - lo; sma = hl.ewm(alpha=2/11, adjust=False).mean(); out = (hl - sma) / sma * 100",
    "GTJA_Alpha189": "ac = d['close'] * d['adj_factor']; out = np.abs(ac - ac.rolling(6).mean()).rolling(6).mean()",
    "GTJA_Alpha190": "ac = d['close'] * d['adj_factor']; ret = ac / ac.shift(1) - 1; thresh = (ac / ac.shift(19)) ** (1/20) - 1; high_cond = ret > thresh; low_cond = ret < thresh; resid_sq = (ret - thresh) ** 2; count_high = high_cond.rolling(20).sum(); count_low = low_cond.rolling(20).sum(); sum_resid_high = (resid_sq * high_cond).rolling(20).sum(); sum_resid_low = (resid_sq * low_cond).rolling(20).sum(); ratio = (count_high - 1) * sum_resid_low / (count_low * sum_resid_high); out = pd.DataFrame(np.log(ratio))",
    "GTJA_Alpha191": "ac = d['close'] * d['adj_factor']; hi = d['high'] * d['adj_factor']; lo = d['low'] * d['adj_factor']; out = d['vol'].rolling(20).mean().rolling(5).corr(lo) + (hi + lo) / 2 - ac"
    },
    "save_path":"output\\factor-GTJA_Alpha191.xlsx"
  }