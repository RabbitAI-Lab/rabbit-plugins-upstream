# Next.js 16 + React 19 项目实战

> 来源：ling-shu-2.0 项目
> 整理日期：2026-05-25
> 归属：full-stack-architect技能

---

## 项目配置

### package.json 依赖

```json
{
  "name": "ling-shu-2.0",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "eslint"
  },
  "dependencies": {
    "next": "16.2.4",
    "react": "19.2.4",
    "react-dom": "19.2.4"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4",
    "@types/node": "^20",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "eslint": "^9",
    "eslint-config-next": "16.2.4",
    "tailwindcss": "^4",
    "typescript": "^5"
  }
}
```

---

## 技术栈分析

### 核心技术

1. **Next.js 16.2.4**
   - 最新的Next.js版本
   - App Router作为默认
   - React Server Components优先
   - 更好的性能优化

2. **React 19.2.4**
   - 最新React版本
   - 新的编译器特性
   - 更好的错误处理
   - 性能优化

3. **Tailwind CSS 4**
   - 最新的Tailwind版本
   - PostCSS 4集成
   - 更好的开发体验

4. **TypeScript 5**
   - 类型安全
   - 更好的开发体验
   - IDE支持

---

## 项目结构

### 标准Next.js 16结构

```
ling-shu-2.0/
├── src/
│   └── app/
│       ├── layout.tsx
│       ├── page.tsx
│       └── globals.css
├── public/
│   ├── file.svg
│   ├── globe.svg
│   ├── next.svg
│   ├── vercel.svg
│   └── window.svg
├── ios/
│   └── App/
│       └── App/
│           ├── AppDelegate.swift
│           ├── ViewController.swift
│           └── zh-Hans.lproj/
│               └── InfoPlist.strings
├── .gitignore
├── README.md
├── capacitor.config.json
├── eslint.config.mjs
├── next-env.d.ts
├── next.config.ts
├── package-lock.json
├── package.json
├── postcss.config.mjs
└── tsconfig.json
```

---

## 根布局 (layout.tsx)

### 完整代码

```tsx
import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Create Next App",
  description: "Generated by create next app",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}
```

### 关键点分析

1. **Google Fonts集成**
   - 使用 `next/font/google` 自动优化字体加载
   - 定义CSS变量用于主题使用
   - subsets优化加载体积

2. **Metadata定义**
   - 导出 `metadata` 对象用于SEO
   - 支持静态和动态metadata
   - Next.js自动生成标签

3. **根布局结构**
   - `html` 标签设置语言和字体
   - `body` 标签使用flex布局
   - `antialiased` 优化字体渲染
   - `h-full` 和 `min-h-full` 确保全屏

4. **TypeScript类型**
   - 使用 `Readonly` 确保不可变性
   - `React.ReactNode` 接受任意React内容

---

## 移动端集成

### Capacitor配置

项目使用Capacitor进行移动端打包：

```json
{
  "appId": "com.example.app",
  "appName": "Ling Shu",
  "webDir": "out",
  "server": {
    "androidScheme": "https"
  }
}
```

### iOS项目

包含完整的iOS项目结构，支持Swift开发。

---

## 配置文件

### tsconfig.json

TypeScript配置，提供类型安全。

### next.config.ts

Next.js配置文件，自定义构建和运行时行为。

### eslint.config.mjs

ESLint配置，使用Next.js推荐规则。

### postcss.config.mjs

PostCSS配置，配合Tailwind CSS 4使用。

---

## 最佳实践

### 1. 字体优化

```tsx
// ✅ 正确做法
const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});
```

- 使用next/font自动优化
- subsets减少加载体积
- CSS变量便于主题使用

### 2. 布局基础

```tsx
// ✅ 正确做法
<html lang="en" className="...">
  <body className="min-h-full flex flex-col">
    {children}
  </body>
</html>
```

- 设置正确的语言属性
- flex布局便于页面结构
- min-h-full确保足够高度

### 3. 类型安全

```tsx
// ✅ 正确做法
export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  // ...
}
```

- 使用Readonly确保参数不可变
- 清晰的类型定义
- 更好的开发体验

---

## 项目特色

### 1. 跨平台支持
- Web端：Next.js 16
- 移动端：Capacitor + iOS
- 可扩展到Android

### 2. 最新技术栈
- React 19
- Next.js 16
- Tailwind CSS 4
- TypeScript 5

### 3. 开发体验
- 热重载
- TypeScript支持
- ESLint集成
- Tailwind CSS便捷

---

## 下一步扩展

### 功能建议

1. **路由系统**
   - 添加页面路由
   - 动态路由
   - 嵌套路由

2. **样式系统**
   - Tailwind主题配置
   - 组件库集成
   - 响应式设计

3. **状态管理**
   - Server Components使用
   - Client Components状态
   - Context API

4. **数据获取**
   - Server Actions
   - API Routes
   - 数据缓存

5. **移动端优化**
   - PWA支持
   - 原生功能集成
   - 性能优化

---

## 相关资源

- [Next.js 16文档](https://nextjs.org/docs)
- [React 19新特性](https://react.dev/blog/2024/04/25/react-19)
- [Tailwind CSS 4](https://tailwindcss.com)
- [Capacitor文档](https://capacitorjs.com)

