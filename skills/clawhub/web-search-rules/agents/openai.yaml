interface:
  display_name: "Web Search Rules"
  short_description: "Verify and govern web research intake"
  default_prompt: "Use $web-search-rules to research this question, open and verify supporting sources, classify claim evidence, and stage or archive only what the confirmed policy allows."
policy:
  allow_implicit_invocation: true
