# Quickstart

```bash
python scripts/self_check.py
python scripts/witness_cli.py doctrine
python scripts/witness_cli.py overlay --json
```

Open https://chatagent.ca/witness/

Local summary (optional):

```bash
python scripts/witness_cli.py ollama
```

`--i-consent` on `propose` still does **not** write the live Star Chart.
