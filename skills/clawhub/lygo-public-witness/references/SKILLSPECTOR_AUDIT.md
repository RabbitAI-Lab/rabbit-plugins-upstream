# SkillSpector audit response — lygo-public-witness v1.0.0

**Signature:** Delta9Phi963-PUBLIC-WITNESS-SKILLSPECTOR-v1.0.0

| Risk class | Mitigation |
|------------|------------|
| Excessive agency | No live chart; no publish |
| subprocess / shell | None — stdlib + urllib |
| Unrestricted network | Fixed HTTPS tables; Ollama localhost only on `ollama` |
| Invented sources | Unreachable = error + empty points |
| Tainted writes | Opt-in; reject `..` |
| Covert exfil | No env dump; no POST to public hosts |

## Can

1. Print doctrine
2. GET allowlisted public + lattice JSON
3. Emit labeled overlay
4. Dry-run proposal JSON
5. Optional local Ollama draft

## Cannot

- Submit Haven Star Chart
- git / HF / ClawHub / social
- Fetch operator-supplied URLs
- Upgrade REFERENCE → CANON

```bash
python scripts/self_check.py
```
