interface:
  display_name: "SurgeonEdit"
  short_description: "对既有文本做最小化局部修改，保持语气与篇幅，不展开、不强调、不标注改动。Surgeon-precise, tone-preserving edits to existing text."
  default_prompt: "使用 $minimal-edit 修改下面这段文字，只做最小必要改动，不要展开或强调。"
