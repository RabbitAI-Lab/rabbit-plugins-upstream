# 钉钉自动回复技能运行依赖
#
# 安装前提：必须用「装了 SDK 的 default venv python」执行，裸 managed python / anaconda 不含此包。
#   Windows: %USERPROFILE%\.workbuddy\binaries\python\envs\default\Scripts\python.exe -m pip install -r requirements.txt
#   macOS/Linux: $HOME/.workbuddy/binaries/python/envs/default/bin/python3 -m pip install -r requirements.txt
#
# 一键预检：python _validate.py --env  （会逐项核查并把缺失项修复命令直接打出来）
#
# ⚠️ 锁定版本（2026-08-13）：codebuddy-agent-sdk 属「行为敏感」依赖，不同版本在
# 注入方式（--append-system-prompt / system-prompt-file）、超时、异常语义上差异大，
# 未锁版本会在换机/升级时拉到 breaking 版本。升级须先跑 `_validate.py --env` + 集成验证。
codebuddy-agent-sdk==0.3.205
