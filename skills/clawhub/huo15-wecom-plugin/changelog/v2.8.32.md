# v2.8.32 (2026-05-05)

**ClawHub 幽灵占用版本号 bypass — 内容与 v2.8.31 完全一致**

## 背景

`clawhub publish huo15-wecom-plugin --version 2.8.31` 报 `Uncaught ConvexError: Version already exists`。`clawhub inspect` 显示 `Tags: latest=2.8.31, plugin=2.8.30`——上一次 publish 注册了 v2.8.31 的 slot 但 plugin tag 没刷新，触发 §7 #6 "幽灵占用" 规则：**不要重试同一版本号，立刻 +1 patch**。

## 修改

**没有新代码改动**。所有 v2.8.31 的修复（bot-ws 6 处 sendMessage `markdown_v2 → markdown`）原样保留。仅 bump：

- `package.json`: 2.8.31 → 2.8.32
- `SKILL.md`: 2.8.31 → 2.8.32

## v2.8.31 修复内容（继承）

P0 真根因：`@wecom/aibot-node-sdk` `SendMsgBody` 不支持 `msgtype: 'markdown_v2'`，30 个版本一直撞 errcode=40008 但 v2.8.26 的 catch 走 stderr 隐身。本版本继承全部修复：

- sdk-adapter.ts: sendMarkdown handler + sendMedia caption（2 处）
- reply.ts: 3 处 sendMessage 主动推送 fallback
- media.ts: share-fallback 链接推送（1 处）

详见 [v2.8.31.md](./v2.8.31.md)。

## 测试

- npm @huo15/wecom@2.8.31 已发布（不影响）；@huo15/wecom@2.8.32 与之内容等价
- ClawHub publish v2.8.32（绕过 v2.8.31 幽灵占用）
- 用户 `openclaw plugins install @huo15/wecom` 拉 npm latest=2.8.32 即拿到本修复
