# ClawHub Publishing Notes

Skill: `paper-deepread-comic-studio`

Display name: `Paper DeepRead Comic Studio`

中文名: `论文精读漫画工坊`

Display version: `v1.2.0`

ClawHub semver value: `1.2.0`

License: `MIT-0`

## Publish command

```bash
clawhub skill publish ./paper-deepread-comic-studio --version 1.2.0
```

## Package requirements checklist

- `SKILL.md` exists at the skill root.
- `SKILL.md` starts with YAML frontmatter containing `name`, `description`, `license`, and `metadata.openclaw`.
- Version uses semver value `1.2.0` in `_meta.json`, `metadata.openclaw.version`, and the publish command for ClawHub compatibility.
- License is declared as `MIT-0` in metadata and documentation.
- Supporting workflow, schema, and scripts are included.
- No private paper content, generated user reports, or user files are bundled.
