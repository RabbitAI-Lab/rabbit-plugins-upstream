# Markdown → PDF 转换技能实测

这是一段**加粗**、*斜体*、以及 `inline code` 的中文渲染测试，验证「所见即所得」的导出效果。

## 二级标题：功能清单

| 功能 | 状态 | 说明 |
|:---|:---|:---|
| 中文渲染 | ✅ | PingFang SC 优先 |
| 表格 | ✅ | 边框 + 斑马纹 |
| 代码块 | ✅ | 等宽字体 + 高亮底色 |
| 引用 | ✅ | 左侧竖线 |

## 代码块测试

```python
def hello(name: str) -> str:
    """打印问候语"""
    return f"你好，{name}！"

if __name__ == "__main__":
    print(hello("莫迪斯"))
```

## 引用与列表

> 这是一段引用：**所见即所得**，导出的 PDF 就是浏览器里 GitHub 预览的样子。

### 有序列表
1. 第一项：读取 Markdown
2. 第二项：marked.js 渲染
3. 第三项：Chrome 打印 PDF

### 无序列表
- 零外部依赖
- 中文友好样式
- 支持自定义 CSS 覆盖

---

**测试结论**：中文、表格、代码块、引用、列表、加粗斜体、行内代码全部渲染正常。
