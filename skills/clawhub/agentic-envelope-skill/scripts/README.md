# Scripts

This directory is reserved for helper scripts and automation that support the skill.

Generator-style skills may keep their primary `generate_*.py` entry point at the repository root for backwards compatibility.
