# 为什么我们要从“大一统 Dashboard”转向“可插拔 Skill-First 架构”

很多系统不是死于功能不够，而是死于“改不动”。

我们这次对 OpenClaw Dashboard 的改造，本质不是 UI 换皮，而是一次工程哲学切换：
**从单体实现，转向 Skill-First、Agent-Readable 的可插拔架构。**

---

## 一、为什么要转变

过去的大一统模式有三个典型问题：
- 任何小改动都可能牵连全局，回归成本高
- 逻辑耦合严重，问题定位慢
- 人能维护，但 Agent 很难稳定自动修改

而我们现在面对的现实是：
- 系统每天都在迭代
- 任务越来越依赖 Agent 协作
- 速度和可靠性都要，不能二选一

所以我们需要的不是“更大的脚本”，而是“更小、更清晰、可组合的能力块”。

---

## 二、为什么是 Skill-First

Skill-First 不是口号，它是操作层面的设计约束：
- 一个 Skill 对应一类稳定能力边界
- 输入输出可预期（接口清晰）
- 可单独测试、单独替换、单独回滚

这让系统拥有两个关键能力：
1. **人类可维护**：改动范围可控，review 成本低
2. **Agent 可维护**：模型能理解上下文，不需要读完整个巨文件

换句话说，Skill-First 是“面向未来协作对象（人+AI）”的工程接口设计。

---

## 三、为什么强调 Agent-Readable

传统系统往往只考虑“让人看懂”。
我们这次增加了一个维度：**让 Agent 也看得懂、改得准、回归得动**。

这要求我们在代码和结构上做三件事：
- 模块小而直：一个 provider 做一类事
- 合约明确：API 字段稳定，fallback 明确
- 真相统一：Ground Truth 单源，避免“文档说一套、运行另一套”

当系统满足这些条件后，Agent 不再只是“调用者”，而是“可控贡献者”。

---

## 四、这次转变的实际收益

这次重构后，我们拿到了非常实际的结果：
- 顶部决策卡片可快速重组，不再牵一发动全身
- 成本/模型/状态数据可以按 provider 快速修复
- Spark 与系统状态栏可以独立演进
- 兼容层保留，能渐进迁移而非一次性推倒

最重要的是：
**系统开始具备持续演进能力，而不是靠一次“大重构”续命。**

---

## 五、结论

我们不是为了“模块化而模块化”，也不是为了“AI 友好”做表面工作。

我们做这个转变，是因为在 Agent 时代，一个系统的核心竞争力不只是功能数量，
而是：
- 能不能快速、安全地迭代
- 能不能让人和 Agent 共同维护
- 能不能在复杂度上升时保持可解释与可恢复

所以我们选择：
**Skill-First，Agent-Readable，Ground-Truth-Driven。**

这不是妥协，而是下一阶段工程效率的起点。
