interface:
  display_name: "Dataify Amazon Global Product"
  short_description: "Collect Amazon Global Product data and return results"
  default_prompt: "Use $dataify-amazon-global-product to complete the requested Dataify collection, wait for the asynchronous task, and return the final collected result. Stop at task submission only when I explicitly request no-wait behavior."

policy:
  allow_implicit_invocation: true
