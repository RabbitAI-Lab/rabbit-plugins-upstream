# Security — lygo-sanctuary-guardian v1.0.0

## Permissions

| Capability | Default |
|------------|---------|
| Network | **None** |
| Subprocess / shell | **None** |
| Filesystem write | Only `--write` with `--i-consent` |
| Publish | **None** |

## Epistemic limits

- Local sanctuary shield receipt ≠ physical force field or network firewall  
- Mandala petals are cryptographic materials, not ritual magic claims  
- Non-collapsing geodesic barrier is a **policy invariant**, not a physics claim  
- Truth lock is local Merkle integrity across named nodes — not live mesh consensus  

## Operator rules

- Do not store secrets in `--truth` / `--light` / `--seed` / node ids  
- Prefer Continuum capsules for consequential claims  
- Human remains the publisher  

## Proof

```bash
python scripts/self_check.py
```
