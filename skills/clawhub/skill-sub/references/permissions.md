# 权限说明

权限扫描风险等级：**MEDIUM**

## 权限总览

共 2 类权限风险，按类别分组如下：

### 文件操作（1 项）
> **权限作用**：创建/编辑/删除调用链 JSON 文件

| # | 文件 | 行号 | 匹配内容 | 风险等级 | 授权方式 | 说明 |
|---|------|------|----------|----------|----------|------|
| 1 | `scripts/chain_manager.py` | 246, 366 | `open(` 写入 | 🟡 中 | 统一授权 | 创建/编辑/删除调用链 JSON 文件 |

### 网络访问（1 项）
> **权限作用**：启动本地 HTTP 服务器（配置界面）

| # | 文件 | 行号 | 匹配内容 | 风险等级 | 授权方式 | 说明 |
|---|------|------|----------|----------|----------|------|
| 1 | `scripts/settings.py` | 19, 259 | `HTTPServer` | 🟡 中 | 统一授权 | 启动本地 HTTP 服务器（配置界面） |

## 授权方式说明

- **即时授权**：每次执行前需获得用户批准
- **统一授权**：首次执行前获得用户批准，后续不再询问
- **静默授权**：无需用户交互，自动执行并记录

## 详细风险列表

1. **[中] 文件操作（创建/编辑/删除调用链 JSON 文件）**
   - 位置：`scripts/chain_manager.py` 第 246 行（写入链）、第 366 行（写入配置）
   - 原因：中风险操作，首次执行前需用户确认
   - 缓解：`--force` 参数强制删除前需用户确认

2. **[中] 网络访问（启动本地 HTTP 服务器）**
   - 位置：`scripts/settings.py` 第 19 行（导入）、第 259 行（启动）
   - 原因：中风险操作，首次执行前需用户确认
   - 缓解：仅监听 localhost，不对外暴露

---

**注**：本文件由 skill-standardization 自动生成（R-15 修复）。
