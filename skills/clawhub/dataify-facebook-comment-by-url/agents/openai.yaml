interface:
  display_name: "Dataify Facebook Comment By URL"
  short_description: "Collect Facebook Comment By URL data and return results"
  icon_small: "./assets/dataify-facebook-comment-by-url-small.svg"
  icon_large: "./assets/dataify-facebook-comment-by-url-large.svg"
  default_prompt: "Use $dataify-facebook-comment-by-url to complete the requested Dataify collection, wait for the asynchronous task, and return the final collected result. Stop at task submission only when I explicitly request no-wait behavior."

policy:
  allow_implicit_invocation: true
