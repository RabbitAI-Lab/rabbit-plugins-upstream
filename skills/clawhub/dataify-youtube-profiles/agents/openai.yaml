interface:
  display_name: "Dataify YouTube Profiles"
  short_description: "Collect YouTube Profiles data and return results"
  icon_small: "./assets/dataify-youtube-profiles-small.svg"
  icon_large: "./assets/dataify-youtube-profiles-large.svg"
  default_prompt: "Use $dataify-youtube-profiles to complete the requested Dataify collection, wait for the asynchronous task, and return the final collected result. Stop at task submission only when I explicitly request no-wait behavior."

policy:
  allow_implicit_invocation: true
