# Android SDK 接口索引

## 初始化接口

| 接口 | 说明 |
|------|------|
| `GDTAction.init(Context, String userActionSetId, String appSecretKey)` | 基础初始化（3 参数） |
| `GDTAction.init(Context, String userActionSetId, String appSecretKey, ChannelType channel, String channelId)` | 带渠道的初始化（5 参数） |
| `GDTAction.start()` | 启动个人信息采集，**初始化后必须调用** |

## 通用上报接口

| 接口 | 说明 |
|------|------|
| `GDTAction.logAction(String actionType)` | 通用行为上报（无参数） |
| `GDTAction.logAction(String actionType, JSONObject actionParam)` | 通用行为上报（带参数） |

## 预定义行为接口（ActionUtils 类）

| 接口 | 说明 |
|------|------|
| `ActionUtils.onRegister(String method, boolean success)` | 注册 |
| `ActionUtils.onLogin(String method, boolean success)` | 登录 |
| `ActionUtils.onBindAccount(String type, boolean success)` | 绑定账号 |
| `ActionUtils.onQuestFinish(String questID, String questType, String questName, int questNumber, String desc, boolean success)` | 完成任务 |
| `ActionUtils.onCreateRole(String roleName)` | 创建角色 |
| `ActionUtils.onUpdateLevel(int level)` | 升级 |
| `ActionUtils.onShare(String channel, boolean success)` | 分享 |
| `ActionUtils.onRateApp(int rate)` | 应用评分 |
| `ActionUtils.onViewContent(String contentType, String contentName, String contentID)` | 浏览内容 |
| `ActionUtils.onAddToCart(String contentType, String contentName, String contentID, int contentNumber, boolean success)` | 加入购物车 |
| `ActionUtils.onCheckout(String contentType, String contentName, String contentID, int contentNumber, boolean isVirtual, String virtualType, String realType, boolean success)` | 发起结算 |
| `ActionUtils.onPurchase(String contentType, String contentName, String contentID, int contentNumber, String channel, String currency, int amount, boolean success)` | 付费 |
| `ActionUtils.onAddPaymentChannel(String channel, boolean success)` | 添加支付渠道 |

## 其他辅助接口

| 接口 | 说明 |
|------|------|
| `GDTAction.setUserUniqueId(String)` | 设置自定义用户 ID |
| `GDTAction.getClickID(Context)` | 获取点击 ID（仅游戏场景） |
| `GDTAction.getChannelID(Context)` | 获取渠道包 ID |
| `GDTAction.setAnidEnable(boolean)` | 开启/关闭 ANID（默认开启） |
| `GDTAction.setAutoStartEnable(boolean)` | 是否自动上报 START_APP（v1.9.4+，传 `false` 关闭） |

## SDK 自动采集事件

| 事件 | 说明 |
|------|------|
| `TICKET` | 票据事件 |
| `ENTER_FOREGROUND` | 应用进入前台 |
| `ENTER_BACKGROUND` | 应用进入后台 |

> **注意：** `START_APP` 在 Android 端不是自动采集事件。

## actionParam 参数约束

- **Key：** 仅支持字母、数字和下划线，必须以字母开头，最大长度 255 字符，不允许使用 `ams_reserved_` 前缀
- **Value：** 支持 String、Number、Boolean、JSONArray（数组内元素类型需统一）

## 代码示例快速导航

- [初始化示例](../接入指引/接入流程.md#4-初始化-sdk) — Application 中的初始化代码
- [权限配置示例](../接入指引/接入流程.md#3-添加-sdk-权限) — AndroidManifest.xml 权限声明
- [预定义方法示例](../接入指引/接入流程.md#51-预定义方法actionutils-类) — 注册、付费等行为上报
- [通用方法示例](../接入指引/接入流程.md#52-通用方法) — 自定义事件上报
- [actionParam 约束](../接入指引/接入流程.md#actionparam-参数约束) — 参数 Key/Value 规范
