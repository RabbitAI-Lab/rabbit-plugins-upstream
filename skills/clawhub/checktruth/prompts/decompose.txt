你是一个事实分解专家。请将以下回答分解为独立的原子事实。
每个原子事实应该是一个可以被独立验证的陈述句。

回答：
{{ANSWER}}

输出格式（严格JSON，不要额外解释）：
{
  "facts": [
    {"id": 1, "text": "事实陈述1"},
    {"id": 2, "text": "事实陈述2"}
  ]
}

规则：
1. 每个fact必须包含完整的主体、谓词和客体
2. 数字、日期、名称必须精确保留，不得泛化
3. 如果回答本身就是一个原子事实，直接返回它
4. 最多分解出20个事实，超过则只保留最重要的
