# 示例提示词

## 默认 CocoaPods 接入

```text
使用 ios-sharesdk-integreation skill，帮我把 ShareSDK 接到这个 iOS 工程里。默认走 CocoaPods，先扫描工程，再只问我一个阻塞问题。
```

## 只接指定平台

```text
使用 ios-sharesdk-integreation skill，这次只接微信和 QQ。请先确认 Podfile、Info.plist 和入口文件分别要改哪里。
```

## 带隐私合规要求

```text
使用 ios-sharesdk-integreation skill，帮我把 ShareSDK 接进项目，并确保 SDK 初始化放在用户同意隐私政策之后。
```

## 先生成配置表

```text
使用 ios-sharesdk-integreation skill，先帮我生成 ShareSDK 的 iOS 配置 Excel 模板，我填完凭证后再继续。
```

## 清理旧残留

```text
使用 ios-sharesdk-integreation skill，当前工程里可能已经有旧的 ShareSDK 残留。先扫描并区分哪些必须保留，哪些可以删。
```
