interface:
  display_name: "AIOS App Service"
  short_description: "Read ontology first, then invoke AIOS app services"
  default_prompt: "Use $aios-call-app-service to read /var/aios/data/ontology, invoke AIOS application interfaces, continue from live results, and keep any workspace files inside the current senderId folder."

policy:
  allow_implicit_invocation: true
