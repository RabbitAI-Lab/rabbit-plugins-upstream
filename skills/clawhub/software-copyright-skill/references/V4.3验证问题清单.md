# V4.3 验证问题清单（2026-06-26）

手动验证过程中发现的16个问题及修复方案。

## 第一轮验证问题

| # | 问题 | 根因 | 修复方案 |
|---|------|------|----------|
| 1 | 页眉页脚占位符没替换 | generate_manual.py 没有处理页眉页脚 | 添加 `replace_header_footer()` 函数 |
| 2 | 封面标题没有系统名称 | content.json 缺少 cover 字段 | 添加 cover.title/subtitle/company/date |
| 3 | 没有添加页码 | 代码中没有页码逻辑 | 添加 PAGE 域代码 |
| 4 | 正文字间距被拉大 | 两端对齐(justify)导致 | 改为左对齐(left) |
| 5 | 封面标题不居中 | apply_formatting 覆盖了封面格式 | 跳过封面段落（第一个 Heading 1 之前） |
| 6 | 图片列表（文字形式） | 模板中有 `{% for img %}` 循环 | 新模板去掉图片列表循环 |
| 7 | 注意事项 | 模板中有 `{% for note %}` 循环 | content.json 删除 notes 字段 |
| 8 | 架构图没有插入 | 只有文字标记，没有实际图片 | 用 excalidraw 生成并插入 |
| 9 | 图片插入位置不对 | `doc.add_paragraph()` 插入到文档末尾 | 用 `insert_paragraph_before()` 在图名上方插入 |
| 10 | 图名重复两行 | content 和 images 字段都有图名 | 图名只保留在 images 字段 |
| 11 | 标题之间空行太多 | Jinja2 标记渲染后变成空行 | `post_process.py` 删除连续空行 |

## 第二轮验证问题（续）

| # | 问题 | 根因 | 修复方案 |
|---|------|------|----------|
| 12 | 长段落首行缩进丢失 | docxtpl将多个\n放在一个段落中 | `post_process.py` 的 `split_long_paragraphs()` |
| 13 | 图名格式无法单独设置 | 图名混在正文中 | `post_process.py` 的 `split_figure_captions()` 分离图名 |
| 14 | 图片应插入到图名上方 | 使用了addnext | 改用 `addprevious()` |
| 15 | 缺少章节层级标题 | content.json只有（一）（二） | sections必须有层级：一、→（一）→（二） |
| 16 | 注意事项仍出现 | 只删了notes字段 | content字段中也不要写注意事项 |

## 图名格式规范

**图名必须是独立段落，格式为14pt、居中对齐。**

```python
# post_process.py 中的设置
new_para.alignment = WD_ALIGN_PARAGRAPH.CENTER
run = new_para.add_run(fig_caption)
run.font.size = Pt(14)
```

## 关键修复逻辑

### 封面保护
```python
# 找到第一个Heading 1的位置，之前的都是封面
first_heading_idx = 0
for i, para in enumerate(doc.paragraphs):
    if para.style.name == 'Heading 1':
        first_heading_idx = i
        break

# 跳过封面段落
if para_idx < first_heading_idx:
    continue
```

### 图片插入（在图名上方）
```python
def insert_paragraph_before(paragraph):
    new_p = OxmlElement('w:p')
    paragraph._element.addprevious(new_p)
    return Paragraph(new_p, paragraph._parent)

# 在图名段落前插入图片
new_para = insert_paragraph_before(caption_para)
new_para.alignment = WD_ALIGN_PARAGRAPH.CENTER
run = new_para.add_run()
run.add_picture(img_path, width=Inches(5.0))
```

### 分割图名段落
```python
# 将包含多个图名的段落分割成独立段落
fig_pattern = r'(图\d+\s+[^\n]+)'
matches = list(re.finditer(fig_pattern, text))
# 从后往前分割，保持顺序
```

### 删除连续空行
```python
# 检测连续空行
for i in range(len(paragraphs) - 1):
    curr_empty = not paragraphs[i].text.strip()
    next_empty = not paragraphs[i+1].text.strip()
    if curr_empty and next_empty:
        to_remove.append(i+1)
```
