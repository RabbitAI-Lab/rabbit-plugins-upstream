interface:
  display_name: "Dataify Google Shopping Keywords"
  short_description: "Collect Google Shopping Keywords data and return results"
  default_prompt: "Use $dataify-google-shopping-keywords to complete the requested Dataify collection, wait for the asynchronous task, and return the final collected result. Stop at task submission only when I explicitly request no-wait behavior."
