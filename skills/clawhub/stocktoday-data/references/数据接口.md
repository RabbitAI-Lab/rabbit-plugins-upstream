# StockToday 数据接口

> 基于 StockToday 自定义后端 + Tushare 协议, 跟 tushare 官方接口 100% 兼容. 
> Python 调用方式: `import tushare as ts; pro = ts.pro_api(token); pro.<接口名>(...)`
> 接口数: 235 | 分类数: 23

---

## 目录

- [债券 (6)](#债券)
- [其他 (8)](#其他)
- [可转债 (7)](#可转债)
- [基金 (13)](#基金)
- [外汇 (2)](#外汇)
- [宏观经济 (17)](#宏观经济)
- [实时数据 (13)](#实时)
- [打板专题 (16)](#打板专题)
- [指数数据 (22)](#指数)
- [期权 (2)](#期权)
- [期货 (10)](#期货)
- [港股 (9)](#港股)
- [现货 (2)](#现货)
- [用户自查 (1)](#用户自查)
- [美股 (9)](#美股)
- [股票-两融 (7)](#股票两融)
- [股票-参考数据 (9)](#股票参考)
- [股票-基础数据 (18)](#股票基础)
- [股票-特色数据 (16)](#股票特色)
- [股票-行情数据 (21)](#股票行情)
- [股票-财务数据 (17)](#股票财务)
- [资讯 (2)](#资讯)
- [资金流向 (8)](#资金流向)
---

## 债券 (6)

### `bc_bestotcqt`

**银行间最优报价**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `trade_date` | `string` | N | 报价日期(YYYYMMDD格式，下同) 格式: YYYYMMDD, e.g. `20240115` (`^\\d{8}$`) |
| `start_date` | `string` | N | 开始日期 格式: YYYYMMDD, e.g. `20240101` (`^\\d{8}$`) |
| `end_date` | `string` | N | 结束日期 格式: YYYYMMDD, e.g. `20241231` (`^\\d{8}$`) |
| `ts_code` | `string` | N | TS代码 格式: 6位数字+交易所后缀, e.g. `600519.SH` / `000001.SZ` / `830799.BJ` (`^\\d{6}\\.(SZ|SH|BJ)$`) |

### `bc_otcqt`

**银行间报价 (start_date + end_date)**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `trade_date` | `string` | N | 交易日期(YYYYMMDD格式，下同) 格式: YYYYMMDD, e.g. `20240115` (`^\\d{8}$`) |
| `start_date` | `string` | N | 开始日期 格式: YYYYMMDD, e.g. `20240101` (`^\\d{8}$`) |
| `end_date` | `string` | N | 结束日期 格式: YYYYMMDD, e.g. `20241231` (`^\\d{8}$`) |
| `ts_code` | `string` | N | TS代码 格式: 6位数字+交易所后缀, e.g. `600519.SH` / `000001.SZ` / `830799.BJ` (`^\\d{6}\\.(SZ|SH|BJ)$`) |
| `bank` | `string` | N | 报价机构 |

### `bond_blk`

**债券大宗交易 (start_date + end_date)**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `ts_code` | `string` | N | 债券代码 格式: 6位数字+交易所后缀, e.g. `600519.SH` / `000001.SZ` / `830799.BJ` (`^\\d{6}\\.(SZ|SH|BJ)$`) |
| `trade_date` | `string` | N | 交易日期（YYYYMMDD格式，下同） 格式: YYYYMMDD, e.g. `20240115` (`^\\d{8}$`) |
| `start_date` | `string` | N | 开始日期 格式: YYYYMMDD, e.g. `20240101` (`^\\d{8}$`) |
| `end_date` | `string` | N | 结束日期 格式: YYYYMMDD, e.g. `20241231` (`^\\d{8}$`) |

### `bond_blk_detail`

**债券大宗交易明细**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `ts_code` | `string` | N | 债券代码 格式: 6位数字+交易所后缀, e.g. `600519.SH` / `000001.SZ` / `830799.BJ` (`^\\d{6}\\.(SZ|SH|BJ)$`) |
| `trade_date` | `string` | N | 交易日期（YYYYMMDD格式，下同） 格式: YYYYMMDD, e.g. `20240115` (`^\\d{8}$`) |
| `start_date` | `string` | N | 开始日期 格式: YYYYMMDD, e.g. `20240101` (`^\\d{8}$`) |
| `end_date` | `string` | N | 结束日期 格式: YYYYMMDD, e.g. `20241231` (`^\\d{8}$`) |

### `repo_daily`

**回购每日行情 (ts_code + trade_date)**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `ts_code` | `string` | N | TS代码 格式: 6位数字+交易所后缀, e.g. `600519.SH` / `000001.SZ` / `830799.BJ` (`^\\d{6}\\.(SZ|SH|BJ)$`) |
| `trade_date` | `string` | N | 交易日期(YYYYMMDD格式，下同) 格式: YYYYMMDD, e.g. `20240115` (`^\\d{8}$`) |
| `start_date` | `string` | N | 开始日期 格式: YYYYMMDD, e.g. `20240101` (`^\\d{8}$`) |
| `end_date` | `string` | N | 结束日期 格式: YYYYMMDD, e.g. `20241231` (`^\\d{8}$`) |

### `yc_cb`

**国债收益率曲线 (ts_code='1001.CB' 国债, curve_type='0'到期/'1'即期, 可选 curve_term 年限)**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `ts_code` | `string` | N | 收益率曲线编码：1001.CB-国债收益率曲线 格式: 6位数字+交易所后缀, e.g. `600519.SH` / `000001.SZ` / `830799.BJ` (`^\\d{6}\\.(SZ|SH|BJ)$`) |
| `curve_type` | `string` | N | 曲线类型：0-到期，1-即期 |
| `trade_date` | `string` | N | 交易日期 格式: YYYYMMDD, e.g. `20240115` (`^\\d{8}$`) |
| `start_date` | `string` | N | 查询起始日期 格式: YYYYMMDD, e.g. `20240101` (`^\\d{8}$`) |
| `end_date` | `string` | N | 查询结束日期 格式: YYYYMMDD, e.g. `20241231` (`^\\d{8}$`) |
| `curve_term` | `number` | N | 期限 |

---

## 其他 (8)

### `bo_cinema`

**影院日度票房**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `date` | `string` | Y | 日期(格式:YYYYMMDD) (`^\\d{8}$`) |

### `bo_daily`

**电影日度票房 (date 必填)**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `date` | `string` | Y | 日期 （格式YYYYMMDD） (`^\\d{8}$`) |

### `bo_monthly`

**电影月度票房**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `date` | `string` | Y | 日期（每月1号，格式YYYYMMDD） (`^\\d{8}$`) |

### `bo_weekly`

**电影周度票房**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `date` | `string` | Y | 日期（每周一日期，格式YYYYMMDD） (`^\\d{8}$`) |

### `film_record`

**电影剧本备案公示 (ann_date 必填)**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `ann_date` | `string` | N | 公布日期 （至少输入一个参数，格式：YYYYMMDD，日期不连续，定期公布） 格式: YYYYMMDD (`^\\d{8}$`) |
| `start_date` | `string` | N | 开始日期 格式: YYYYMMDD, e.g. `20240101` (`^\\d{8}$`) |
| `end_date` | `string` | N | 结束日期 格式: YYYYMMDD, e.g. `20241231` (`^\\d{8}$`) |

### `teleplay_record`

**电视剧备案公示 (按月份)**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `report_date` | `string` | N | 备案月份（YYYYMM） |
| `start_date` | `string` | N | 备案开始月份（YYYYMM） 格式: YYYYMMDD, e.g. `20240101` (`^\\d{8}$`) |
| `end_date` | `string` | N | 备案结束月份（YYYYMM） 格式: YYYYMMDD, e.g. `20241231` (`^\\d{8}$`) |
| `org` | `string` | N | 备案机构 |

### `tmt_twincome`

**台湾电子产业月营收**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `date` | `string` | N | 报告期 (`^\\d{8}$`) |
| `item` | `string` | Y | 产品代码 |
| `start_date` | `string` | N | 报告期开始日期 格式: YYYYMMDD, e.g. `20240101` (`^\\d{8}$`) |
| `end_date` | `string` | N | 报告期结束日期 格式: YYYYMMDD, e.g. `20241231` (`^\\d{8}$`) |

### `tmt_twincomedetail`

**TMT 月营收明细**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `date` | `string` | N | 报告期 (`^\\d{8}$`) |
| `item` | `string` | N | 产品代码 |
| `symbol` | `string` | N | 公司代码 |
| `start_date` | `string` | N | 报告期开始日期 格式: YYYYMMDD, e.g. `20240101` (`^\\d{8}$`) |
| `end_date` | `string` | N | 报告期结束日期 格式: YYYYMMDD, e.g. `20241231` (`^\\d{8}$`) |
| `source` | `string` | N | None |

---

## 可转债 (7)

### `cb_basic`

**可转债基本信息 (注意: ts_code 6 位,无后缀, e.g. '110044.SH')**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `ts_code` | `string` | N | 转债代码 格式: 6位数字+交易所后缀, e.g. `600519.SH` / `000001.SZ` / `830799.BJ` (`^\\d{6}\\.(SZ|SH|BJ)$`) |
| `list_date` | `string` | N | 上市日期 格式: YYYYMMDD (`^\\d{8}$`) |
| `exchange` | `string` | N | 上市交易所 枚举: '', 'SSE', 'SZSE', 'BSE', 'HK' — SSE=上交所, SZSE=深交所, BSE=北交所, HK=港交所 (枚举: `"SSE", "SZSE", "BSE", "HK"`) |

### `cb_call`

**可转债回售 (回售触发价+回售期)**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `ts_code` | `string` | N | 转债代码，支持多值输入 格式: 6位数字+交易所后缀, e.g. `600519.SH` / `000001.SZ` / `830799.BJ` (`^\\d{6}\\.(SZ|SH|BJ)$`) |
| `ann_date` | `string` | N | 公告日期(YYYYMMDD格式，下同) 格式: YYYYMMDD (`^\\d{8}$`) |
| `start_date` | `string` | N | 公告开始日期 格式: YYYYMMDD, e.g. `20240101` (`^\\d{8}$`) |
| `end_date` | `string` | N | 公告结束日期 格式: YYYYMMDD, e.g. `20241231` (`^\\d{8}$`) |

### `cb_daily`

**可转债日线**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `ts_code` | `string` | N | TS代码 格式: 6位数字+交易所后缀, e.g. `600519.SH` / `000001.SZ` / `830799.BJ` (`^\\d{6}\\.(SZ|SH|BJ)$`) |
| `trade_date` | `string` | N | 交易日期(YYYYMMDD格式，下同) 格式: YYYYMMDD, e.g. `20240115` (`^\\d{8}$`) |
| `start_date` | `string` | N | 开始日期 格式: YYYYMMDD, e.g. `20240101` (`^\\d{8}$`) |
| `end_date` | `string` | N | 结束日期 格式: YYYYMMDD, e.g. `20241231` (`^\\d{8}$`) |

### `cb_issue`

**可转债发行信息**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `ts_code` | `string` | N | TS代码 格式: 6位数字+交易所后缀, e.g. `600519.SH` / `000001.SZ` / `830799.BJ` (`^\\d{6}\\.(SZ|SH|BJ)$`) |
| `ann_date` | `string` | N | 发行公告日 格式: YYYYMMDD (`^\\d{8}$`) |
| `start_date` | `string` | N | 公告开始日期 格式: YYYYMMDD, e.g. `20240101` (`^\\d{8}$`) |
| `end_date` | `string` | N | 公告结束日期 格式: YYYYMMDD, e.g. `20241231` (`^\\d{8}$`) |

### `cb_price_chg`

**可转债价格变化 (强赎/下修等事件)**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `ts_code` | `string` | Y | 转债代码，支持多值输入 格式: 6位数字+交易所后缀, e.g. `600519.SH` / `000001.SZ` / `830799.BJ` (`^\\d{6}\\.(SZ|SH|BJ)$`) |

### `cb_rate`

**可转债转股溢价率 (转股价值+溢价率, ts_code 可多个)**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `ts_code` | `string` | Y | 转债代码，支持多值输入 格式: 6位数字+交易所后缀, e.g. `600519.SH` / `000001.SZ` / `830799.BJ` (`^\\d{6}\\.(SZ|SH|BJ)$`) |

### `cb_share`

**可转债转股 (转股数量+转股价格变动)**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `ts_code` | `string` | Y | 转债代码，支持多值输入 格式: 6位数字+交易所后缀, e.g. `600519.SH` / `000001.SZ` / `830799.BJ` (`^\\d{6}\\.(SZ|SH|BJ)$`) |
| `ann_date` | `string` | Y | 公告日期（YYYYMMDD格式，下同） 格式: YYYYMMDD (`^\\d{8}$`) |
| `start_date` | `string` | N | 公告开始日期 格式: YYYYMMDD, e.g. `20240101` (`^\\d{8}$`) |
| `end_date` | `string` | N | 公告结束日期 格式: YYYYMMDD, e.g. `20241231` (`^\\d{8}$`) |

---

## 基金 (13)

### `etf_basic`

**etf_basic**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `ts_code` | `string` | N | ETF代码（带.SZ/.SH后缀的6位数字，如：159526.SZ） 格式: 6位数字+交易所后缀, e.g. `600519.SH` / `000001.SZ` / `830799.BJ` (`^\\d{6}\\.(SZ|SH|BJ)$`) |
| `index_code` | `string` | N | 跟踪指数代码 |
| `list_date` | `string` | N | 上市日期（格式：YYYYMMDD） 格式: YYYYMMDD (`^\\d{8}$`) |
| `list_status` | `string` | N | 上市状态（L上市 D退市 P待上市） 枚举: 'L', 'D', 'P' — L=上市, D=退市, P=暂停 (枚举: `"L", "D", "P"`) |
| `exchange` | `string` | N | 交易所（SH上交所 SZ深交所） 枚举: '', 'SSE', 'SZSE', 'BSE', 'HK' — SSE=上交所, SZSE=深交所, BSE=北交所, HK=港交所 (枚举: `"SSE", "SZSE", "BSE", "HK"`) |
| `mgr` | `string` | N | 管理人（简称，e.g.华夏基金) |

### `etf_index`

**etf_index**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `ts_code` | `string` | N | 指数代码 格式: 6位数字+交易所后缀, e.g. `600519.SH` / `000001.SZ` / `830799.BJ` (`^\\d{6}\\.(SZ|SH|BJ)$`) |
| `pub_date` | `string` | N | 发布日期（格式：YYYYMMDD） |
| `base_date` | `string` | N | 指数基期（格式：YYYYMMDD） |

### `fund_adj`

**基金复权净值**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `ts_code` | `string` | N | TS基金代码（支持多只基金输入） 格式: 6位数字+交易所后缀, e.g. `600519.SH` / `000001.SZ` / `830799.BJ` (`^\\d{6}\\.(SZ|SH|BJ)$`) |
| `trade_date` | `string` | N | 交易日期（格式：yyyymmdd，下同） 格式: YYYYMMDD, e.g. `20240115` (`^\\d{8}$`) |
| `start_date` | `string` | N | 开始日期 格式: YYYYMMDD, e.g. `20240101` (`^\\d{8}$`) |
| `end_date` | `string` | N | 结束日期 格式: YYYYMMDD, e.g. `20241231` (`^\\d{8}$`) |
| `offset` | `string` | N | 开始行数 |
| `limit` | `string` | N | 最大行数 |

### `fund_basic`

**基金基本信息 (market='O' 场外 / 'E' 场内)**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `ts_code` | `string` | N | 基金代码 格式: 6位数字+交易所后缀, e.g. `600519.SH` / `000001.SZ` / `830799.BJ` (`^\\d{6}\\.(SZ|SH|BJ)$`) |
| `market` | `string` | N | 交易市场: E场内 O场外（默认E） 枚举: 'SSE', 'SZSE', 'BSE' — SSE=上交所, SZSE=深交所, BSE=北交所 (枚举: `"SSE", "SZSE", "BSE"`) |
| `status` | `string` | N | 存续状态 D摘牌 I发行 L上市中 (枚举: `"D", "I", "P", "S"`) |

### `fund_daily`

**基金日线行情**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `ts_code` | `string` | N | 基金代码 格式: 6位数字+交易所后缀, e.g. `600519.SH` / `000001.SZ` / `830799.BJ` (`^\\d{6}\\.(SZ|SH|BJ)$`) |
| `trade_date` | `string` | N | 交易日期(YYYYMMDD格式，下同) 格式: YYYYMMDD, e.g. `20240115` (`^\\d{8}$`) |
| `start_date` | `string` | N | 开始日期 格式: YYYYMMDD, e.g. `20240101` (`^\\d{8}$`) |
| `end_date` | `string` | N | 结束日期 格式: YYYYMMDD, e.g. `20241231` (`^\\d{8}$`) |

### `fund_div`

**基金分红**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `ann_date` | `string` | N | 公告日（以下参数四选一） 格式: YYYYMMDD (`^\\d{8}$`) |
| `ex_date` | `string` | N | 除息日 格式: YYYYMMDD (`^\\d{8}$`) |
| `pay_date` | `string` | N | 派息日 |
| `ts_code` | `string` | N | 基金代码 格式: 6位数字+交易所后缀, e.g. `600519.SH` / `000001.SZ` / `830799.BJ` (`^\\d{6}\\.(SZ|SH|BJ)$`) |

### `fund_factor_pro`

**基金因子**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `ts_code` | `string` | N | 基金代码 格式: 6位数字+交易所后缀, e.g. `600519.SH` / `000001.SZ` / `830799.BJ` (`^\\d{6}\\.(SZ|SH|BJ)$`) |
| `start_date` | `string` | N | 开始日期 格式: YYYYMMDD, e.g. `20240101` (`^\\d{8}$`) |
| `end_date` | `string` | N | 结束日期 格式: YYYYMMDD, e.g. `20241231` (`^\\d{8}$`) |
| `trade_date` | `string` | N | 交易日期 格式: YYYYMMDD, e.g. `20240115` (`^\\d{8}$`) |

### `fund_manager`

**基金经理 (任职基金+历史业绩)**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `ts_code` | `string` | N | 基金代码，支持多只基金，逗号分隔 格式: 6位数字+交易所后缀, e.g. `600519.SH` / `000001.SZ` / `830799.BJ` (`^\\d{6}\\.(SZ|SH|BJ)$`) |
| `ann_date` | `string` | N | 公告日期，格式：YYYYMMDD 格式: YYYYMMDD (`^\\d{8}$`) |
| `name` | `string` | N | 基金经理姓名 |
| `offset` | `string` | N | 开始行数 |
| `limit` | `integer` | N | 每页行数 |

### `fund_nav`

**基金净值 (必传 ts_code + nav_date)**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `ts_code` | `string` | N | TS基金代码 （二选一） 格式: 6位数字+交易所后缀, e.g. `600519.SH` / `000001.SZ` / `830799.BJ` (`^\\d{6}\\.(SZ|SH|BJ)$`) |
| `nav_date` | `string` | N | 净值日期 （二选一） |
| `market` | `string` | N | E场内 O场外 枚举: 'SSE', 'SZSE', 'BSE' — SSE=上交所, SZSE=深交所, BSE=北交所 (枚举: `"SSE", "SZSE", "BSE"`) |
| `start_date` | `string` | N | 净值开始日期 格式: YYYYMMDD, e.g. `20240101` (`^\\d{8}$`) |
| `end_date` | `string` | N | 净值结束日期 格式: YYYYMMDD, e.g. `20241231` (`^\\d{8}$`) |

### `fund_portfolio`

**基金持仓 (前 10 大重仓股, 必传 ts_code + period)**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `ts_code` | `string` | N | 基金代码 (ts_code,ann_date,period至少输入一个参数) 格式: 6位数字+交易所后缀, e.g. `600519.SH` / `000001.SZ` / `830799.BJ` (`^\\d{6}\\.(SZ|SH|BJ)$`) |
| `symbol` | `string` | N | 股票代码 |
| `ann_date` | `string` | N | 公告日期（YYYYMMDD格式） 格式: YYYYMMDD (`^\\d{8}$`) |
| `period` | `string` | N | 季度（每个季度最后一天的日期，比如20131231表示2013年年报） |
| `start_date` | `string` | N | 报告期开始日期（YYYYMMDD格式） 格式: YYYYMMDD, e.g. `20240101` (`^\\d{8}$`) |
| `end_date` | `string` | N | 报告期结束日期（YYYYMMDD格式） 格式: YYYYMMDD, e.g. `20241231` (`^\\d{8}$`) |

### `fund_sales_ratio`

**fund_sales_ratio**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `年份` | `string` | N | 年度 |

### `fund_sales_vol`

**fund_sales_vol**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `year` | `string` | N | 年度 |
| `quarter` | `string` | N | 季度 |

### `fund_share`

**基金份额 (规模变动)**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `ts_code` | `string` | Y | TS基金代码 必传 (不传会返 '参数不能为空')。格式: 6位数字+交易所后缀, 例: `510300.SH`(沪深300ETF)/`510500.SH`(中证500ETF)/`159919.SZ`(嘉实沪深300) (`^\\d{6}\\.(SZ|SH|BJ)$`) |
| `trade_date` | `string` | N | 交易日期 格式: YYYYMMDD, e.g. `20240115` (`^\\d{8}$`) |
| `start_date` | `string` | N | 开始日期 格式: YYYYMMDD, e.g. `20240101` (`^\\d{8}$`) |
| `end_date` | `string` | N | 结束日期 格式: YYYYMMDD, e.g. `20241231` (`^\\d{8}$`) |
| `market` | `string` | N | 市场代码（SH上交所, SZ深交所） 枚举: 'SSE', 'SZSE', 'BSE' — SSE=上交所, SZSE=深交所, BSE=北交所 (枚举: `"SSE", "SZSE", "BSE"`) |

---

## 外汇 (2)

### `fx_daily`

**外汇日线 (GMT 时间, 比北京晚一天)**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `ts_code` | `string` | Y | TS代码 必传 (从 fx_obasic 获取)。格式: `XXXXX.FXCM`, 例如 `AUDCAD.FXCM`/`EURUSD.FXCM`/`USDCNH.FXCM`/`XAUUSD.FXCM` (`^[A-Z0-9]+\\.FXCM$`) |
| `trade_date` | `string` | N | 交易日期（GMT，日期是格林尼治时间，比北京时间晚一天） 格式: YYYYMMDD, e.g. `20240115` (`^\\d{8}$`) |
| `start_date` | `string` | N | 开始日期（GMT） 格式: YYYYMMDD, e.g. `20240101` (`^\\d{8}$`) |
| `end_date` | `string` | N | 结束日期（GMT） 格式: YYYYMMDD, e.g. `20241231` (`^\\d{8}$`) |
| `exchange` | `string` | N | 交易商, 固定 'FXCM' 或不传 (`^FXCM$`) |

### `fx_obasic`

**外汇基本信息 (ts_code='EURUSD.FXCM')**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `exchange` | `string` | N | 交易商, 固定 'FXCM' 或不传 (`^FXCM$`) |
| `classify` | `string` | N | 分类, 例如 'FX'/'METAL'/'INDEX'/'COMMODITY' 等 |
| `ts_code` | `string` | N | TS代码 (查单个合约), 格式: `XXXXX.FXCM` (`^[A-Z0-9]+\\.FXCM$`) |

---

## 宏观经济 (17)

### `cn_cpi`

**中国 CPI 同比/环比 (m='202401')**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `m` | `string` | N | 月份（YYYYMM，下同），支持多个月份同时输入，逗号分隔 |
| `start_m` | `string` | N | 开始月份 |
| `end_m` | `string` | N | 结束月份 |

### `cn_gdp`

**中国 GDP (q='2024Q1')**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `q` | `string` | N | 季度（2019Q1表示，2019年第一季度） |
| `start_q` | `string` | N | 开始季度 |
| `end_q` | `string` | N | 结束季度 |
| `fields` | `string` | N | 指定输出字段（e.g. fields='quarter,gdp,gdp_yoy'） |

### `cn_m`

**中国货币供应 M0/M1/M2**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `m` | `string` | N | 月度（202001表示，2020年1月） |
| `start_m` | `string` | N | 开始月度 |
| `end_m` | `string` | N | 结束月度 |
| `fields` | `string` | N | 指定输出字段（e.g. fields='month,m0,m1,m2'） |

### `cn_pmi`

**中国 PMI 制造业/非制造业**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `m` | `string` | N | 月度（202401表示，2024年1月） |
| `start_m` | `string` | N | 开始月度 |
| `end_m` | `string` | N | 结束月度（e.g. fields='month,pmi010000,pmi010400'） |

### `cn_ppi`

**中国 PPI 工业品价格**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `m` | `string` | N | 月份（YYYYMM，下同），支持多个月份同时输入，逗号分隔 |
| `start_m` | `string` | N | 开始月份 |
| `end_m` | `string` | N | 结束月份 |

### `eco_cal`

**经济日历 (重要宏观事件, date 或 start_date+end_date)**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `date` | `string` | N | 日期（YYYYMMDD格式） (`^\\d{8}$`) |
| `start_date` | `string` | N | 开始日期 格式: YYYYMMDD, e.g. `20240101` (`^\\d{8}$`) |
| `end_date` | `string` | N | 结束日期 格式: YYYYMMDD, e.g. `20241231` (`^\\d{8}$`) |
| `currency` | `string` | N | 货币代码 |
| `country` | `string` | N | 国家（比如：中国、美国） |
| `event` | `string` | N | 事件 （支持模糊匹配： \\*非农\\*） |

### `hibor`

**Hibor 利率 (港币)**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `date` | `string` | N | 日期  (日期输入格式：YYYYMMDD，下同) (`^\\d{8}$`) |
| `start_date` | `string` | N | 开始日期 格式: YYYYMMDD, e.g. `20240101` (`^\\d{8}$`) |
| `end_date` | `string` | N | 结束日期 格式: YYYYMMDD, e.g. `20241231` (`^\\d{8}$`) |

### `libor`

**Libor 利率 (美元/欧元/日元/英镑/瑞郎)**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `date` | `string` | N | 日期 (日期输入格式：YYYYMMDD，下同) (`^\\d{8}$`) |
| `start_date` | `string` | N | 开始日期 格式: YYYYMMDD, e.g. `20240101` (`^\\d{8}$`) |
| `end_date` | `string` | N | 结束日期 格式: YYYYMMDD, e.g. `20241231` (`^\\d{8}$`) |
| `curr_type` | `string` | N | 货币代码  (USD美元  EUR欧元  JPY日元  GBP英镑  CHF瑞郎，默认是USD) |

### `sf_month`

**上海黄金现货月报**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `m` | `string` | N | 月份（YYYYMM，下同），支持多个月份同时输入，逗号分隔 |
| `start_m` | `string` | N | 开始月份 |
| `end_m` | `string` | N | 结束月份 |

### `shibor`

**Shibor 利率 (上海银行间同业拆放, date 必填)**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `date` | `string` | N | 日期 (日期输入格式：YYYYMMDD，下同) (`^\\d{8}$`) |
| `start_date` | `string` | N | 开始日期 格式: YYYYMMDD, e.g. `20240101` (`^\\d{8}$`) |
| `end_date` | `string` | N | 结束日期 格式: YYYYMMDD, e.g. `20241231` (`^\\d{8}$`) |

### `shibor_lpr`

**LPR 贷款基础利率**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `date` | `string` | N | 日期  (日期输入格式：YYYYMMDD，下同) (`^\\d{8}$`) |
| `start_date` | `string` | N | 开始日期 格式: YYYYMMDD, e.g. `20240101` (`^\\d{8}$`) |
| `end_date` | `string` | N | 结束日期 格式: YYYYMMDD, e.g. `20241231` (`^\\d{8}$`) |

### `shibor_quote`

**Shibor 报价明细**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `date` | `string` | N | 日期 (日期输入格式：YYYYMMDD，下同) (`^\\d{8}$`) |
| `start_date` | `string` | N | 开始日期 格式: YYYYMMDD, e.g. `20240101` (`^\\d{8}$`) |
| `end_date` | `string` | N | 结束日期 格式: YYYYMMDD, e.g. `20241231` (`^\\d{8}$`) |

### `us_tbr`

**美国短期国债利率 (13/17/26/52 周)**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `date` | `string` | N | 日期 (`^\\d{8}$`) |
| `start_date` | `string` | N | 开始日期(YYYYMMDD格式) 格式: YYYYMMDD, e.g. `20240101` (`^\\d{8}$`) |
| `end_date` | `string` | N | 结束日期 格式: YYYYMMDD, e.g. `20241231` (`^\\d{8}$`) |
| `fields` | `string` | N | 指定输出字段(e.g. fields='w4_bd,w52_ce') |

### `us_tltr`

**美国国债长期利率平均值**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `date` | `string` | N | 日期 (`^\\d{8}$`) |
| `start_date` | `string` | N | 开始日期 格式: YYYYMMDD, e.g. `20240101` (`^\\d{8}$`) |
| `end_date` | `string` | N | 结束日期 格式: YYYYMMDD, e.g. `20241231` (`^\\d{8}$`) |
| `fields` | `string` | N | 指定字段 |

### `us_trltr`

**美国国债实际长期利率**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `date` | `string` | N | 日期 (`^\\d{8}$`) |
| `start_date` | `string` | N | 开始日期 格式: YYYYMMDD, e.g. `20240101` (`^\\d{8}$`) |
| `end_date` | `string` | N | 结束日期 格式: YYYYMMDD, e.g. `20241231` (`^\\d{8}$`) |
| `fields` | `string` | N | 指定字段 |

### `us_trycr`

**美国国债实际收益率曲线**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `date` | `string` | N | 日期 （YYYYMMDD格式，下同） (`^\\d{8}$`) |
| `start_date` | `string` | N | 开始日期 格式: YYYYMMDD, e.g. `20240101` (`^\\d{8}$`) |
| `end_date` | `string` | N | 结束日期 格式: YYYYMMDD, e.g. `20241231` (`^\\d{8}$`) |
| `fields` | `string` | N | 指定输出字段 |

### `us_tycr`

**美国国债收益率曲线 (1月/2月/3月/6月/1年/.../30年)**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `date` | `string` | N | 日期 （YYYYMMDD格式，下同） (`^\\d{8}$`) |
| `start_date` | `string` | N | 开始日期 格式: YYYYMMDD, e.g. `20240101` (`^\\d{8}$`) |
| `end_date` | `string` | N | 结束日期 格式: YYYYMMDD, e.g. `20241231` (`^\\d{8}$`) |
| `fields` | `string` | N | 指定输出字段（e.g. fields='m1,y1'） |

---

## 实时数据 (13)

### `rt_idx_k`

**指数实时 K 线**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `ts_code` | `string` | Y | 指数代码 必传, 支持通配符 (如 `00000*.SH`/`399*.SZ`)。A 股指数格式: `000001.SH`/`000300.SH`/`000905.SH`/`399001.SZ`/`399006.SZ` 等 (`^(\\d{6}|\\d{5}\\*|\\d{4}\\*)\\.(SZ|SH)$`) |

### `rt_etf_k`

**ETF 实时 K 线**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `ts_code` | `string` | Y | 支持通配符方式，e.g. 5\\*.SH、15\\*.SZ、159101.SZ 格式: 6位数字+交易所后缀, e.g. `600519.SH` / `000001.SZ` / `830799.BJ` (`^\\d{6}\\.(SZ|SH|BJ)$`) |
| `topic` | `string` | Y | 分类参数，取上海ETF时，需要输入'HQ_FND_TICK'，参考下面例子 |

### `rt_etf_min`

**ETF 实时分钟**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `ts_code` | `string` | Y | ETF 代码 必传, 支持多 ts_code 逗号分隔, 例: `510300.SH` 或 `510300.SH,510500.SH` (`^\d{6}(\.(SH|SZ|BJ))(,\d{6}(\.(SH|SZ|BJ)))*$`) |
| `freq` | `string` | Y | 必传大写 1MIN/5MIN/15MIN/30MIN/60MIN, 单次最大 1000 行 (枚举: `"1MIN", "5MIN", "15MIN", "30MIN", "60MIN"`) |

### `rt_etf_tick`

**ETF 实时 Tick**

### `rt_hk_k`

**港股实时 K 线**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `ts_code` | `string` | Y | 支持通配符方式，e.g. 00001.HK、02*.HK 格式: 5位数字+HK, e.g. `00700.HK` (`^\\d{4,5}\\.HK$`) |

### `rt_hk_tick`

**港股实时 Tick**

### `rt_idx_min`

**指数实时分钟**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `ts_code` | `string` | Y | 指数代码 必传, 支持多 ts_code 逗号分隔, 例: `000001.SH` 或 `000001.SH,399300.SZ` (`^\\d{6}(\\.(SH|SZ|BJ))(,\\d{6}(\\.(SH|SZ|BJ)))*$`) |
| `freq` | `string` | Y | 必传大写 1MIN/5MIN/15MIN/30MIN/60MIN, 单次最大 1000 行, ⚠️ 大小写敏感, 必须大写 (枚举: `"1MIN", "5MIN", "15MIN", "30MIN", "60MIN"`) |

### `rt_idx_tick`

**指数实时 Tick**

### `rt_k`

**股票实时日线 K 线 (ts_code 必填)**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `ts_code` | `string` | Y | 支持通配符方式，e.g. 所有上交所股票：6\\*.SH、所有创业板股票3\\*.SZ、所有科创板股票688*.SH，或单个股票600000.SH 格式: 6位数字+交易所后缀, e.g. `600519.SH` / `000001.SZ` / `830799.BJ` (`^\\d{6}\\.(SZ|SH|BJ)$`) |

### `rt_min`

**股票实时分钟 (ts_code + freq='1MIN'/'5MIN'/'15MIN'/'30MIN'/'60MIN' **大写**)**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `ts_code` | `string` | Y | A 股代码 必传, 支持多 ts_code 逗号分隔, 例: `600000.SH` 或 `600000.SH,000001.SZ` (`^\\d{6}(\\.(SH|SZ|BJ))(,\\d{6}(\\.(SH|SZ|BJ)))*$`) |
| `freq` | `string` | Y | 必传大写 1MIN/5MIN/15MIN/30MIN/60MIN, 单次最大 1000 行, ⚠️ 大小写敏感, 必须大写 (枚举: `"1MIN", "5MIN", "15MIN", "30MIN", "60MIN"`) |

### `rt_fut_min`

**期货实时分钟 (注意 freq **大写**: '1MIN'/'5MIN'/'15MIN'/'30MIN'/'60MIN')**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `ts_code` | `string` | Y | 期货合约代码 必传, 格式: `品种代码+交割年月+.交易所`, 例: `CU2507.SHF`/`AU2506.SHFE`/`AG2506.SHFE`/`I2507.DCE` (`^[A-Z]{1,2}\\d{4}\\.[A-Z]+$`) |
| `freq` | `string` | Y | 必传大写 1MIN/5MIN/15MIN/30MIN/60MIN, 单次最大 1000 行, ⚠️ 大小写敏感, 必须大写 (枚举: `"1MIN", "5MIN", "15MIN", "30MIN", "60MIN"`) |

### `rt_sw_tick`

**申万实时 Tick**

### `rt_tick`

**股票实时 Tick (只返回最后一条, ts_code 必填)**

---

## 打板专题 (16)

### `dc_hot`

**东财热榜 (trade_date 必填)**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `trade_date` | `string` | Y | 交易日期 格式: YYYYMMDD, e.g. `20240115` (`^\\d{8}$`) |
| `ts_code` | `string` | N | TS代码 格式: 6位数字+交易所后缀, e.g. `600519.SH` / `000001.SZ` / `830799.BJ` (`^\\d{6}\\.(SZ|SH|BJ)$`) |
| `market` | `string` | N | 类型(A股市场、ETF基金、港股市场、美股市场) 枚举: 'SSE', 'SZSE', 'BSE' — SSE=上交所, SZSE=深交所, BSE=北交所 (枚举: `"SSE", "SZSE", "BSE"`) |
| `hot_type` | `string` | N | 热点类型(人气榜、飙升榜) |
| `is_new` | `string` | N | 是否最新（默认Y，如果为N则为盘中和盘后阶段采集，具体时间可参考rank_time字段，状态N每小时更新一次，状态Y更新时间为22：30） |

### `dc_index`

**东财概念/行业/地域指数 (idx_type 必填)**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `ts_code` | `string` | N | 指数代码（支持多个代码同时输入，用逗号分隔） 格式: 6位数字+交易所后缀, e.g. `600519.SH` / `000001.SZ` / `830799.BJ` (`^\\d{6}\\.(SZ|SH|BJ)$`) |
| `trade_date` | `string` | Y | 交易日期（YYYYMMDD格式，下同） 格式: YYYYMMDD, e.g. `20240115` (`^\\d{8}$`) |
| `start_date` | `string` | N | 开始日期 格式: YYYYMMDD, e.g. `20240101` (`^\\d{8}$`) |
| `end_date` | `string` | N | 结束日期 格式: YYYYMMDD, e.g. `20241231` (`^\\d{8}$`) |
| `idx_type` | `string` | Y | 板块类型(行业板块、概念板块、地域板块) |

### `dc_member`

**东财成分股 (trade_date 必填)**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `ts_code` | `string` | N | 板块指数代码 格式: 6位数字+交易所后缀, e.g. `600519.SH` / `000001.SZ` / `830799.BJ` (`^\\d{6}\\.(SZ|SH|BJ)$`) |
| `con_code` | `string` | N | 成分股票代码 |
| `trade_date` | `string` | Y | 交易日期（YYYYMMDD格式） 格式: YYYYMMDD, e.g. `20240115` (`^\\d{8}$`) |
| `start_date` | `string` | N | 开始日期（YYYYMMDD格式） 格式: YYYYMMDD, e.g. `20240101` (`^\\d{8}$`) |
| `end_date` | `string` | N | 结束日期（YYYYMMDD格式） 格式: YYYYMMDD, e.g. `20241231` (`^\\d{8}$`) |

### `hm_detail`

**游资详情 (历史操作风格)**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `trade_date` | `string` | N | 交易日期(YYYYMMDD) 格式: YYYYMMDD, e.g. `20240115` (`^\\d{8}$`) |
| `ts_code` | `string` | N | 股票代码 格式: 6位数字+交易所后缀, e.g. `600519.SH` / `000001.SZ` / `830799.BJ` (`^\\d{6}\\.(SZ|SH|BJ)$`) |
| `start_date` | `string` | N | 开始日期(YYYYMMDD) 格式: YYYYMMDD, e.g. `20240101` (`^\\d{8}$`) |
| `end_date` | `string` | N | 结束日期(YYYYMMDD) 格式: YYYYMMDD, e.g. `20241231` (`^\\d{8}$`) |

### `kpl_concept`

**开盘啦概念列表 (trade_date 必填)**

### `kpl_concept_cons`

**开盘啦概念成分 (trade_date 必填)**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `trade_date` | `string` | N | 交易日期（YYYYMMDD格式） 格式: YYYYMMDD, e.g. `20240115` (`^\\d{8}$`) |
| `ts_code` | `string` | N | 题材代码（xxxxxx.KP格式） 格式: 6位数字+交易所后缀, e.g. `600519.SH` / `000001.SZ` / `830799.BJ` (`^\\d{6}\\.(SZ|SH|BJ)$`) |
| `con_code` | `string` | N | 成分代码（xxxxxx.SH格式） |

### `kpl_list`

**开盘啦榜单 (涨停/炸板/跌停/竞价, trade_date 必填)**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `ts_code` | `string` | N | 股票代码 格式: 6位数字+交易所后缀, e.g. `600519.SH` / `000001.SZ` / `830799.BJ` (`^\\d{6}\\.(SZ|SH|BJ)$`) |
| `trade_date` | `string` | N | 交易日期 格式: YYYYMMDD, e.g. `20240115` (`^\\d{8}$`) |
| `tag` | `string` | N | 板单类型（涨停/炸板/跌停/自然涨停/竞价，默认为涨停) |
| `start_date` | `string` | N | 开始日期 格式: YYYYMMDD, e.g. `20240101` (`^\\d{8}$`) |
| `end_date` | `string` | N | 结束日期 格式: YYYYMMDD, e.g. `20241231` (`^\\d{8}$`) |

### `limit_cpt_list`

**涨停最强板块统计 (trade_date 必填)**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `trade_date` | `string` | Y | 交易日期（格式：YYYYMMDD，下同） 格式: YYYYMMDD, e.g. `20240115` (`^\\d{8}$`) |
| `ts_code` | `string` | N | 板块代码 格式: 6位数字+交易所后缀, e.g. `600519.SH` / `000001.SZ` / `830799.BJ` (`^\\d{6}\\.(SZ|SH|BJ)$`) |
| `start_date` | `string` | N | 开始日期 格式: YYYYMMDD, e.g. `20240101` (`^\\d{8}$`) |
| `end_date` | `string` | N | 结束日期 格式: YYYYMMDD, e.g. `20241231` (`^\\d{8}$`) |

### `limit_list_d`

**涨跌停和炸板数据 (trade_date 必填)**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `trade_date` | `string` | Y | 交易日期 格式: YYYYMMDD, e.g. `20240115` (`^\\d{8}$`) |
| `ts_code` | `string` | N | 股票代码 格式: 6位数字+交易所后缀, e.g. `600519.SH` / `000001.SZ` / `830799.BJ` (`^\\d{6}\\.(SZ|SH|BJ)$`) |
| `limit_type` | `string` | N | 涨跌停类型（U涨停D跌停Z炸板） 枚举: 'U', 'D', 'Z' — U=涨停, D=跌停, Z=炸板 (枚举: `"U", "D", "Z"`) |
| `exchange` | `string` | N | 交易所（SH上交所SZ深交所BJ北交所） 枚举: '', 'SSE', 'SZSE', 'BSE', 'HK' — SSE=上交所, SZSE=深交所, BSE=北交所, HK=港交所 (枚举: `"SSE", "SZSE", "BSE", "HK"`) |
| `start_date` | `string` | N | 开始日期 格式: YYYYMMDD, e.g. `20240101` (`^\\d{8}$`) |
| `end_date` | `string` | N | 结束日期 格式: YYYYMMDD, e.g. `20241231` (`^\\d{8}$`) |

### `limit_list_ths`

**同花顺涨跌停榜单 (trade_date 必填)**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `trade_date` | `string` | Y | 交易日期 格式: YYYYMMDD, e.g. `20240115` (`^\\d{8}$`) |
| `ts_code` | `string` | N | 股票代码 格式: 6位数字+交易所后缀, e.g. `600519.SH` / `000001.SZ` / `830799.BJ` (`^\\d{6}\\.(SZ|SH|BJ)$`) |
| `market` | `string` | N | HS-沪深主板 GEM-创业板 STAR-科创板 枚举: 'SSE', 'SZSE', 'BSE' — SSE=上交所, SZSE=深交所, BSE=北交所 (枚举: `"SSE", "SZSE", "BSE"`) |
| `start_date` | `string` | N | 开始日期 格式: YYYYMMDD, e.g. `20240101` (`^\\d{8}$`) |
| `end_date` | `string` | N | 结束日期 格式: YYYYMMDD, e.g. `20241231` (`^\\d{8}$`) |

### `limit_step`

**涨停连板天梯 (nums='2,3' 查 2-3 连板, trade_date 必填)**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `trade_date` | `string` | Y | 交易日期（格式：YYYYMMDD，下同） 格式: YYYYMMDD, e.g. `20240115` (`^\\d{8}$`) |
| `ts_code` | `string` | N | 股票代码 格式: 6位数字+交易所后缀, e.g. `600519.SH` / `000001.SZ` / `830799.BJ` (`^\\d{6}\\.(SZ|SH|BJ)$`) |
| `start_date` | `string` | N | 开始日期 格式: YYYYMMDD, e.g. `20240101` (`^\\d{8}$`) |
| `end_date` | `string` | N | 结束日期 格式: YYYYMMDD, e.g. `20241231` (`^\\d{8}$`) |
| `nums` | `string` | N | 连板次数，支持多个输入，例如nums='2,3' |

### `ths_hot`

**同花顺热榜 (trade_date 必填)**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `trade_date` | `string` | Y | 交易日期 格式: YYYYMMDD, e.g. `20240115` (`^\\d{8}$`) |
| `ts_code` | `string` | N | TS代码 格式: 6位数字+交易所后缀, e.g. `600519.SH` / `000001.SZ` / `830799.BJ` (`^\\d{6}\\.(SZ|SH|BJ)$`) |
| `market` | `string` | N | 热榜类型(热股、ETF、可转债、行业板块、概念板块、期货、港股、热基、美股) 枚举: 'SSE', 'SZSE', 'BSE' — SSE=上交所, SZSE=深交所, BSE=北交所 (枚举: `"SSE", "SZSE", "BSE"`) |
| `is_new` | `string` | N | 是否最新（默认Y，如果为N则为盘中和盘后阶段采集，具体时间可参考rank_time字段，状态N每小时更新一次，状态Y更新时间为22：30） |

### `ths_index`

**同花顺概念/行业/地域指数列表 (type='N'概念/'I'行业/'R'地域)**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `ts_code` | `string` | N | 指数代码 格式: 6位数字+交易所后缀, e.g. `600519.SH` / `000001.SZ` / `830799.BJ` (`^\\d{6}\\.(SZ|SH|BJ)$`) |
| `exchange` | `string` | N | 市场类型A-a股 HK-港股 US-美股 枚举: '', 'SSE', 'SZSE', 'BSE', 'HK' — SSE=上交所, SZSE=深交所, BSE=北交所, HK=港交所 (枚举: `"SSE", "SZSE", "BSE", "HK"`) |
| `type` | `string` | N | 指数类型 N-概念指数 I-行业指数 R-地域指数 S-同花顺特色指数 ST-同花顺风格指数 TH-同花顺主题指数 BB-同花顺宽基指数 (枚举: `"N", "I", "R", "S", "ST", "TH", "BB"`) |

### `ths_member`

**同花顺成分股 (ts_code)**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `ts_code` | `string` | Y | 板块指数代码 格式: 6位数字+交易所后缀, e.g. `600519.SH` / `000001.SZ` / `830799.BJ` (`^\\d{6}\\.(SZ|SH|BJ)$`) |
| `con_code` | `string` | N | 股票代码 |

### `top_inst`

**龙虎榜机构席位 (机构专用席位的买卖, trade_date 必填)**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `trade_date` | `string` | Y | 交易日期 格式: YYYYMMDD, e.g. `20240115` (`^\\d{8}$`) |
| `ts_code` | `string` | N | TS代码 格式: 6位数字+交易所后缀, e.g. `600519.SH` / `000001.SZ` / `830799.BJ` (`^\\d{6}\\.(SZ|SH|BJ)$`) |

### `top_list`

**龙虎榜上榜明细 (涨幅/跌幅/换手率/振幅触发, trade_date 必填)**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `trade_date` | `string` | Y | 交易日期 格式: YYYYMMDD, e.g. `20240115` (`^\\d{8}$`) |
| `ts_code` | `string` | N | 股票代码 格式: 6位数字+交易所后缀, e.g. `600519.SH` / `000001.SZ` / `830799.BJ` (`^\\d{6}\\.(SZ|SH|BJ)$`) |

---

## 指数数据 (22)

### `ci_daily`

**中证指数日线**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `ts_code` | `string` | N | 行业代码 格式: 6位数字+交易所后缀, e.g. `600519.SH` / `000001.SZ` / `830799.BJ` (`^\\d{6}\\.(SZ|SH|BJ)$`) |
| `trade_date` | `string` | N | 交易日期（YYYYMMDD格式，下同） 格式: YYYYMMDD, e.g. `20240115` (`^\\d{8}$`) |
| `start_date` | `string` | N | 开始日期 格式: YYYYMMDD, e.g. `20240101` (`^\\d{8}$`) |
| `end_date` | `string` | N | 结束日期 格式: YYYYMMDD, e.g. `20241231` (`^\\d{8}$`) |

### `ci_index_member`

**ci_index_member**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `l1_code` | `string` | N | 一级行业代码 |
| `l2_code` | `string` | N | 二级行业代码 |
| `l3_code` | `string` | N | 三级行业代码 |
| `ts_code` | `string` | N | 股票代码 格式: 6位数字+交易所后缀, e.g. `600519.SH` / `000001.SZ` / `830799.BJ` (`^\\d{6}\\.(SZ|SH|BJ)$`) |
| `is_new` | `string` | N | 是否最新（默认为“Y是”） |

### `daily_info`

**daily_info**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `trade_date` | `string` | N | 交易日期（YYYYMMDD格式，下同） 格式: YYYYMMDD, e.g. `20240115` (`^\\d{8}$`) |
| `ts_code` | `string` | N | 板块代码（请参阅下方列表） 格式: 6位数字+交易所后缀, e.g. `600519.SH` / `000001.SZ` / `830799.BJ` (`^\\d{6}\\.(SZ|SH|BJ)$`) |
| `exchange` | `string` | N | 股票市场（SH上交所 SZ深交所） 枚举: '', 'SSE', 'SZSE', 'BSE', 'HK' — SSE=上交所, SZSE=深交所, BSE=北交所, HK=港交所 (枚举: `"SSE", "SZSE", "BSE", "HK"`) |
| `start_date` | `string` | N | 开始日期 格式: YYYYMMDD, e.g. `20240101` (`^\\d{8}$`) |
| `end_date` | `string` | N | 结束日期 格式: YYYYMMDD, e.g. `20241231` (`^\\d{8}$`) |
| `fields` | `string` | N | 指定提取字段 |

### `dc_daily`

**东财指数日线**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `ts_code` | `string` | N | 板块代码（格式：xxxxx.DC) 格式: 6位数字+交易所后缀, e.g. `600519.SH` / `000001.SZ` / `830799.BJ` (`^\\d{6}\\.(SZ|SH|BJ)$`) |
| `trade_date` | `string` | N | 交易日期(格式：YYYYMMDD下同） 格式: YYYYMMDD, e.g. `20240115` (`^\\d{8}$`) |
| `start_date` | `string` | N | 开始日期 格式: YYYYMMDD, e.g. `20240101` (`^\\d{8}$`) |
| `end_date` | `string` | N | 结束日期 格式: YYYYMMDD, e.g. `20241231` (`^\\d{8}$`) |
| `idx_type` | `string` | N | 板块类型： 概念板块、行业板块、地域板块 |

### `gz_index`

**国证指数**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `date` | `string` | N | 日期 (`^\\d{8}$`) |
| `start_date` | `string` | N | 开始日期 格式: YYYYMMDD, e.g. `20240101` (`^\\d{8}$`) |
| `end_date` | `string` | N | 结束日期 格式: YYYYMMDD, e.g. `20241231` (`^\\d{8}$`) |

### `idx_factor_pro`

**指数因子 (PE/PB/动量/波动率)**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `ts_code` | `string` | N | 指数代码(大盘指数 申万指数 中信指数) 格式: 6位数字+交易所后缀, e.g. `600519.SH` / `000001.SZ` / `830799.BJ` (`^\\d{6}\\.(SZ|SH|BJ)$`) |
| `start_date` | `string` | N | 开始日期 格式: YYYYMMDD, e.g. `20240101` (`^\\d{8}$`) |
| `end_date` | `string` | N | 结束日期 格式: YYYYMMDD, e.g. `20241231` (`^\\d{8}$`) |
| `trade_date` | `string` | N | 交易日期 格式: YYYYMMDD, e.g. `20240115` (`^\\d{8}$`) |

### `index_basic`

**指数基本信息 (market='SSE' 上证 / 'SZSE' 深证 / 'BSE' 北证)**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `ts_code` | `string` | N | TS指数代码 格式: 6位数字+交易所后缀, e.g. `600519.SH` / `000001.SZ` / `830799.BJ` (`^\\d{6}\\.(SZ|SH|BJ)$`) |
| `symbol` | `string` | N | 指数代码，支持多值输入，如000300,000001 |
| `name` | `string` | N | 指数简称 |
| `market` | `string` | N | 交易所或服务商(默认SSE) 枚举: 'SSE', 'SZSE', 'BSE' — SSE=上交所, SZSE=深交所, BSE=北交所 (枚举: `"SSE", "SZSE", "BSE"`) |
| `publisher` | `string` | N | 发布商 |
| `category` | `string` | N | 指数类别 |

### `index_classify`

**指数分类**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `index_code` | `string` | N | 指数代码 |
| `level` | `string` | N | 行业分级（L1/L2/L3） |
| `parent_code` | `string` | N | 父级代码（一级为0） |
| `src` | `string` | N | 指数来源（SW2014：申万2014年版本，SW2021：申万2021年版本） (枚举: `"SINA", "Wallstreetcn", "10jqka", "THS", "Eastmoney"`) |

### `index_daily`

**指数日线 (必传 ts_code, e.g. '000300.SH' 沪深300 / '000001.SH' 上证)**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `ts_code` | `string` | Y | 指数代码，来源指数基础信息接口 格式: 6位数字+交易所后缀, e.g. `600519.SH` / `000001.SZ` / `830799.BJ` (`^\\d{6}\\.(SZ|SH|BJ)$`) |
| `trade_date` | `string` | N | 交易日期 （日期格式：YYYYMMDD，下同） 格式: YYYYMMDD, e.g. `20240115` (`^\\d{8}$`) |
| `start_date` | `string` | N | 开始日期 格式: YYYYMMDD, e.g. `20240101` (`^\\d{8}$`) |
| `end_date` | `string` | N | 结束日期 格式: YYYYMMDD, e.g. `20241231` (`^\\d{8}$`) |

### `index_dailybasic`

**指数每日指标 (PE/PB)**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `trade_date` | `string` | N | 交易日期 （格式：YYYYMMDD，比如20181018，下同） 格式: YYYYMMDD, e.g. `20240115` (`^\\d{8}$`) |
| `ts_code` | `string` | N | TS代码 格式: 6位数字+交易所后缀, e.g. `600519.SH` / `000001.SZ` / `830799.BJ` (`^\\d{6}\\.(SZ|SH|BJ)$`) |
| `start_date` | `string` | N | 开始日期 格式: YYYYMMDD, e.g. `20240101` (`^\\d{8}$`) |
| `end_date` | `string` | N | 结束日期 格式: YYYYMMDD, e.g. `20241231` (`^\\d{8}$`) |

### `index_global`

**index_global**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `ts_code` | `string` | N | TS指数代码，见下表 格式: 6位数字+交易所后缀, e.g. `600519.SH` / `000001.SZ` / `830799.BJ` (`^\\d{6}\\.(SZ|SH|BJ)$`) |
| `trade_date` | `string` | N | 交易日期，YYYYMMDD格式，下同 格式: YYYYMMDD, e.g. `20240115` (`^\\d{8}$`) |
| `start_date` | `string` | N | 开始日期 格式: YYYYMMDD, e.g. `20240101` (`^\\d{8}$`) |
| `end_date` | `string` | N | 结束日期 格式: YYYYMMDD, e.g. `20241231` (`^\\d{8}$`) |

### `index_member_all`

**指数成分股 (index_code)**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `l1_code` | `string` | N | 一级行业代码 |
| `l2_code` | `string` | N | 二级行业代码 |
| `l3_code` | `string` | N | 三级行业代码 |
| `ts_code` | `string` | N | 股票代码 格式: 6位数字+交易所后缀, e.g. `600519.SH` / `000001.SZ` / `830799.BJ` (`^\\d{6}\\.(SZ|SH|BJ)$`) |
| `is_new` | `string` | N | 是否最新（默认为“Y是”） |

### `index_monthly`

**指数月线**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `ts_code` | `string` | N | TS代码 格式: 6位数字+交易所后缀, e.g. `600519.SH` / `000001.SZ` / `830799.BJ` (`^\\d{6}\\.(SZ|SH|BJ)$`) |
| `trade_date` | `string` | N | 交易日期 格式: YYYYMMDD, e.g. `20240115` (`^\\d{8}$`) |
| `start_date` | `string` | N | 开始日期 格式: YYYYMMDD, e.g. `20240101` (`^\\d{8}$`) |
| `end_date` | `string` | N | 结束日期 格式: YYYYMMDD, e.g. `20241231` (`^\\d{8}$`) |

### `index_weekly`

**指数周线**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `ts_code` | `string` | N | TS代码 格式: 6位数字+交易所后缀, e.g. `600519.SH` / `000001.SZ` / `830799.BJ` (`^\\d{6}\\.(SZ|SH|BJ)$`) |
| `trade_date` | `string` | N | 交易日期 格式: YYYYMMDD, e.g. `20240115` (`^\\d{8}$`) |
| `start_date` | `string` | N | 开始日期 格式: YYYYMMDD, e.g. `20240101` (`^\\d{8}$`) |
| `end_date` | `string` | N | 结束日期 格式: YYYYMMDD, e.g. `20241231` (`^\\d{8}$`) |

### `index_weight`

**指数成分股权重 (index_code + trade_date)**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `index_code` | `string` | Y | 指数代码，来源指数基础信息接口 |
| `trade_date` | `string` | N | 交易日期（格式YYYYMMDD，下同） 格式: YYYYMMDD, e.g. `20240115` (`^\\d{8}$`) |
| `start_date` | `string` | N | 开始日期 格式: YYYYMMDD, e.g. `20240101` (`^\\d{8}$`) |
| `end_date` | `string` | N | 结束日期 格式: YYYYMMDD, e.g. `20241231` (`^\\d{8}$`) |

### `sw_daily`

**申万指数日线**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `ts_code` | `string` | N | 行业代码 格式: 6位数字+交易所后缀, e.g. `600519.SH` / `000001.SZ` / `830799.BJ` (`^\\d{6}\\.(SZ|SH|BJ)$`) |
| `trade_date` | `string` | N | 交易日期 格式: YYYYMMDD, e.g. `20240115` (`^\\d{8}$`) |
| `start_date` | `string` | N | 开始日期 格式: YYYYMMDD, e.g. `20240101` (`^\\d{8}$`) |
| `end_date` | `string` | N | 结束日期 格式: YYYYMMDD, e.g. `20241231` (`^\\d{8}$`) |

### `sz_daily_info`

**sz_daily_info**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `trade_date` | `string` | N | 交易日期（YYYYMMDD格式，下同） 格式: YYYYMMDD, e.g. `20240115` (`^\\d{8}$`) |
| `ts_code` | `string` | N | 板块代码 格式: 6位数字+交易所后缀, e.g. `600519.SH` / `000001.SZ` / `830799.BJ` (`^\\d{6}\\.(SZ|SH|BJ)$`) |
| `start_date` | `string` | N | 开始日期 格式: YYYYMMDD, e.g. `20240101` (`^\\d{8}$`) |
| `end_date` | `string` | N | 结束日期 格式: YYYYMMDD, e.g. `20241231` (`^\\d{8}$`) |

### `tdx_daily`

**通达信日线**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `ts_code` | `string` | N | 板块代码：xxxxxx.TDX 格式: 6位数字+交易所后缀, e.g. `600519.SH` / `000001.SZ` / `830799.BJ` (`^\\d{6}\\.(SZ|SH|BJ)$`) |
| `trade_date` | `string` | N | 交易日期，格式YYYYMMDD,下同 格式: YYYYMMDD, e.g. `20240115` (`^\\d{8}$`) |
| `start_date` | `string` | N | 开始日期 格式: YYYYMMDD, e.g. `20240101` (`^\\d{8}$`) |
| `end_date` | `string` | N | 结束日期 格式: YYYYMMDD, e.g. `20241231` (`^\\d{8}$`) |

### `tdx_index`

**通达信指数 (idx_type 必填: 概念/行业/风格/地区)**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `ts_code` | `string` | N | 板块代码：xxxxxx.TDX 格式: 6位数字+交易所后缀, e.g. `600519.SH` / `000001.SZ` / `830799.BJ` (`^\\d{6}\\.(SZ|SH|BJ)$`) |
| `trade_date` | `string` | N | 交易日期(格式：YYYYMMDD） 格式: YYYYMMDD, e.g. `20240115` (`^\\d{8}$`) |
| `idx_type` | `string` | N | 板块类型：概念板块、行业板块、风格板块、地区板块 |

### `tdx_member`

**通达信成分股**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `ts_code` | `string` | N | 板块代码：xxxxxx.TDX 格式: 6位数字+交易所后缀, e.g. `600519.SH` / `000001.SZ` / `830799.BJ` (`^\\d{6}\\.(SZ|SH|BJ)$`) |
| `con_code` | `string` | N | 成分股票代码 |
| `trade_date` | `string` | N | 交易日期：（YYYYMMDD格式） 格式: YYYYMMDD, e.g. `20240115` (`^\\d{8}$`) |
| `start_date` | `string` | N | 开始日期：（YYYYMMDD格式） 格式: YYYYMMDD, e.g. `20240101` (`^\\d{8}$`) |
| `end_date` | `string` | N | 结束日期：（YYYYMMDD格式） 格式: YYYYMMDD, e.g. `20241231` (`^\\d{8}$`) |

### `ths_daily`

**同花顺指数日线 (必传 ts_code + trade_date)**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `ts_code` | `string` | N | 指数代码 格式: 6位数字+交易所后缀, e.g. `600519.SH` / `000001.SZ` / `830799.BJ` (`^\\d{6}\\.(SZ|SH|BJ)$`) |
| `trade_date` | `string` | N | 交易日期（YYYYMMDD格式，下同） 格式: YYYYMMDD, e.g. `20240115` (`^\\d{8}$`) |
| `start_date` | `string` | N | 开始日期 格式: YYYYMMDD, e.g. `20240101` (`^\\d{8}$`) |
| `end_date` | `string` | N | 结束日期 格式: YYYYMMDD, e.g. `20241231` (`^\\d{8}$`) |

### `wz_index`

**万德指数**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `date` | `string` | N | 日期 (`^\\d{8}$`) |
| `start_date` | `string` | N | 开始日期 格式: YYYYMMDD, e.g. `20240101` (`^\\d{8}$`) |
| `end_date` | `string` | N | 结束日期 格式: YYYYMMDD, e.g. `20241231` (`^\\d{8}$`) |

---

## 期权 (2)

### `opt_basic`

**期权基本信息 (call_put='C'/'P')**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `ts_code` | `string` | N | TS期权代码 格式: 6位数字+交易所后缀, e.g. `600519.SH` / `000001.SZ` / `830799.BJ` (`^\\d{6}\\.(SZ|SH|BJ)$`) |
| `exchange` | `string` | N | 交易所代码  枚举: '', 'SSE', 'SZSE', 'BSE', 'HK' — SSE=上交所, SZSE=深交所, BSE=北交所, HK=港交所 (枚举: `"SSE", "SZSE", "BSE", "HK"`) |
| `list_date` | `string` | N | 上市交易日 格式: YYYYMMDD (`^\\d{8}$`) |
| `opt_code` | `string` | N | 标准合约代码，OP+期货合约TS_CODE，如棕榈油2207合约，输入OPP2207.DCE |
| `call_put` | `string` | N | 期权类型 |

### `opt_daily`

**期权日线**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `ts_code` | `string` | N | TS合约代码（输入代码或时间至少任意一个参数） 格式: 6位数字+交易所后缀, e.g. `600519.SH` / `000001.SZ` / `830799.BJ` (`^\\d{6}\\.(SZ|SH|BJ)$`) |
| `trade_date` | `string` | N | 交易日期 格式: YYYYMMDD, e.g. `20240115` (`^\\d{8}$`) |
| `start_date` | `string` | N | 开始日期 格式: YYYYMMDD, e.g. `20240101` (`^\\d{8}$`) |
| `end_date` | `string` | N | 结束日期 格式: YYYYMMDD, e.g. `20241231` (`^\\d{8}$`) |
| `exchange` | `string` | N | 交易所(SSE/SZSE/CFFEX/DCE/SHFE/CZCE） 枚举: '', 'SSE', 'SZSE', 'BSE', 'HK' — SSE=上交所, SZSE=深交所, BSE=北交所, HK=港交所 (枚举: `"SSE", "SZSE", "BSE", "HK"`) |

---

## 期货 (10)

### `cb_factor_pro`

**可转债因子**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `ts_code` | `string` | N | 可转债代码 格式: 6位数字+交易所后缀, e.g. `600519.SH` / `000001.SZ` / `830799.BJ` (`^\\d{6}\\.(SZ|SH|BJ)$`) |
| `start_date` | `string` | N | 开始日期 格式: YYYYMMDD, e.g. `20240101` (`^\\d{8}$`) |
| `end_date` | `string` | N | 结束日期 格式: YYYYMMDD, e.g. `20241231` (`^\\d{8}$`) |
| `trade_date` | `string` | N | 交易日期 格式: YYYYMMDD, e.g. `20240115` (`^\\d{8}$`) |

### `ft_limit`

**ft_limit**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `ts_code` | `string` | N | 合约代码 格式: 6位数字+交易所后缀, e.g. `600519.SH` / `000001.SZ` / `830799.BJ` (`^\\d{6}\\.(SZ|SH|BJ)$`) |
| `trade_date` | `string` | N | 交易日期（格式：YYYYMMDD） 格式: YYYYMMDD, e.g. `20240115` (`^\\d{8}$`) |
| `start_date` | `string` | N | 开始日期 格式: YYYYMMDD, e.g. `20240101` (`^\\d{8}$`) |
| `end_date` | `string` | N | 结束日期 格式: YYYYMMDD, e.g. `20241231` (`^\\d{8}$`) |
| `cont` | `string` | N | 合约代码（例如：cont='CU') |
| `exchange` | `string` | N | 交易所代码 （例如：exchange='DCE') 枚举: '', 'SSE', 'SZSE', 'BSE', 'HK' — SSE=上交所, SZSE=深交所, BSE=北交所, HK=港交所 (枚举: `"SSE", "SZSE", "BSE", "HK"`) |

### `fut_basic`

**期货基本信息 (exchange='DCE'大商所/'CZCE'郑商所/'CFFEX'中金所/'SHFE'上期所/'INE'上能源/'GFEX'广期所)**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `exchange` | `string` | Y | 交易所代码 CFFEX-中金所 DCE-大商所 CZCE-郑商所 SHFE-上期所 INE-上海国际能源交易中心 GFEX-广州期货交易所 枚举: '', 'SSE', 'SZSE', 'BSE', 'HK' — SSE=上交所, SZSE=深交所, BSE=北交所, HK=港交所 (枚举: `"SSE", "SZSE", "BSE", "HK"`) |
| `fut_type` | `string` | N | 合约类型 (1 普通合约 2主力与连续合约 默认取全部) |
| `fut_code` | `string` | N | 标准合约代码，如白银AG、AP鲜苹果等 |
| `list_date` | `string` | N | 上市开始日期(格式YYYYMMDD，从某日开始以来所有合约） 格式: YYYYMMDD (`^\\d{8}$`) |

### `fut_daily`

**期货日线 (ts_code='CU2507.SHF' 铜 2507 合约)**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `trade_date` | `string` | N | 交易日期(YYYYMMDD格式，下同) 格式: YYYYMMDD, e.g. `20240115` (`^\\d{8}$`) |
| `ts_code` | `string` | N | 合约代码 格式: 6位数字+交易所后缀, e.g. `600519.SH` / `000001.SZ` / `830799.BJ` (`^\\d{6}\\.(SZ|SH|BJ)$`) |
| `exchange` | `string` | N | 交易所代码 枚举: '', 'SSE', 'SZSE', 'BSE', 'HK' — SSE=上交所, SZSE=深交所, BSE=北交所, HK=港交所 (枚举: `"SSE", "SZSE", "BSE", "HK"`) |
| `start_date` | `string` | N | 开始日期 格式: YYYYMMDD, e.g. `20240101` (`^\\d{8}$`) |
| `end_date` | `string` | N | 结束日期 格式: YYYYMMDD, e.g. `20241231` (`^\\d{8}$`) |

### `fut_holding`

**期货持仓量 (symbol + start_date + end_date)**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `trade_date` | `string` | N | 单日交易日期 (⚠️ 后端不支持单日, 仅用 start+end 范围) |
| `symbol` | `string` | N | 合约或产品代码 (如 'CU'/'AU'/'AG'), 与 start+end 一起用过滤 |
| `start_date` | `string` | Y | 开始日期 必传 (YYYYMMDD, e.g. `20240101`) (`^\\d{8}$`) |
| `end_date` | `string` | Y | 结束日期 必传 (YYYYMMDD, e.g. `20241231`) (`^\\d{8}$`) |
| `exchange` | `string` | N | 交易所代码 (SHFE/DCE/CZCE/CFFEX/INE/GFEX, 大写) (枚举: `"SHFE", "DCE", "CZCE", "CFFEX", "INE", "GFEX"`) |

### `fut_mapping`

**期货主力合约映射**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `ts_code` | `string` | N | 合约代码 格式: 6位数字+交易所后缀, e.g. `600519.SH` / `000001.SZ` / `830799.BJ` (`^\\d{6}\\.(SZ|SH|BJ)$`) |
| `trade_date` | `string` | N | 交易日期(YYYYMMDD格式，下同) 格式: YYYYMMDD, e.g. `20240115` (`^\\d{8}$`) |
| `start_date` | `string` | N | 开始日期 格式: YYYYMMDD, e.g. `20240101` (`^\\d{8}$`) |
| `end_date` | `string` | N | 结束日期 格式: YYYYMMDD, e.g. `20241231` (`^\\d{8}$`) |

### `fut_settle`

**期货结算价**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `trade_date` | `string` | N | 交易日期 （trade_date/ts_code至少需要输入一个参数） 格式: YYYYMMDD, e.g. `20240115` (`^\\d{8}$`) |
| `ts_code` | `string` | N | 合约代码 格式: 6位数字+交易所后缀, e.g. `600519.SH` / `000001.SZ` / `830799.BJ` (`^\\d{6}\\.(SZ|SH|BJ)$`) |
| `start_date` | `string` | N | 开始日期(YYYYMMDD格式，下同) 格式: YYYYMMDD, e.g. `20240101` (`^\\d{8}$`) |
| `end_date` | `string` | N | 结束日期 格式: YYYYMMDD, e.g. `20241231` (`^\\d{8}$`) |
| `exchange` | `string` | N | 交易所代码 枚举: '', 'SSE', 'SZSE', 'BSE', 'HK' — SSE=上交所, SZSE=深交所, BSE=北交所, HK=港交所 (枚举: `"SSE", "SZSE", "BSE", "HK"`) |

### `fut_weekly_detail`

**期货每周详情 (按品种聚合)**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `week` | `string` | N | 周期（每年第几周，e.g. 202001 表示2020第1周） |
| `prd` | `string` | N | 期货品种（支持多品种输入，逗号分隔） |
| `start_week` | `string` | N | 开始周期 |
| `end_week` | `string` | N | 结束周期 |
| `exchange` | `string` | N | 交易所  枚举: '', 'SSE', 'SZSE', 'BSE', 'HK' — SSE=上交所, SZSE=深交所, BSE=北交所, HK=港交所 (枚举: `"SSE", "SZSE", "BSE", "HK"`) |
| `fields` | `string` | N | 提取的字段，e.g. fields='prd,name,vol' |

### `fut_weekly_monthly`

**期货周月线**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `ts_code` | `string` | N | TS代码 格式: 6位数字+交易所后缀, e.g. `600519.SH` / `000001.SZ` / `830799.BJ` (`^\\d{6}\\.(SZ|SH|BJ)$`) |
| `trade_date` | `string` | N | 交易日期 格式: YYYYMMDD, e.g. `20240115` (`^\\d{8}$`) |
| `start_date` | `string` | N | 开始交易日期 格式: YYYYMMDD, e.g. `20240101` (`^\\d{8}$`) |
| `end_date` | `string` | N | 结束交易日期 格式: YYYYMMDD, e.g. `20241231` (`^\\d{8}$`) |
| `freq` | `string` | Y | 频率week周，month月 枚举: 'D', 'W', 'M', '1min', '5min', '15min', '30min', '60min' — D=日, W=周, M=月, 1/5/15/30/60min=分钟 (枚举: `"D", "W", "M", "1min", "5min", "15min", "30min", "60min"`) |
| `exchange` | `string` | N | 交易所 枚举: '', 'SSE', 'SZSE', 'BSE', 'HK' — SSE=上交所, SZSE=深交所, BSE=北交所, HK=港交所 (枚举: `"SSE", "SZSE", "BSE", "HK"`) |

### `fut_wsr`

**期货持仓排名 (symbol='CU' 铜, 多空持仓)**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `trade_date` | `string` | N | 交易日期 格式: YYYYMMDD, e.g. `20240115` (`^\\d{8}$`) |
| `symbol` | `string` | N | 产品代码 |
| `start_date` | `string` | N | 开始日期(YYYYMMDD格式，下同) 格式: YYYYMMDD, e.g. `20240101` (`^\\d{8}$`) |
| `end_date` | `string` | N | 结束日期 格式: YYYYMMDD, e.g. `20241231` (`^\\d{8}$`) |
| `exchange` | `string` | N | 交易所代码 枚举: '', 'SSE', 'SZSE', 'BSE', 'HK' — SSE=上交所, SZSE=深交所, BSE=北交所, HK=港交所 (枚举: `"SSE", "SZSE", "BSE", "HK"`) |

---

## 港股 (9)

### `hk_adjfactor`

**港股复权因子**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `ts_code` | `string` | N | 股票代码 格式: 5位数字+HK, e.g. `00700.HK` (`^\\d{4,5}\\.HK$`) |
| `trade_date` | `string` | N | 交易日期（格式：YYYYMMDD，下同） 格式: YYYYMMDD, e.g. `20240115` (`^\\d{8}$`) |
| `start_date` | `string` | N | 开始日期 格式: YYYYMMDD, e.g. `20240101` (`^\\d{8}$`) |
| `end_date` | `string` | N | 结束日期 格式: YYYYMMDD, e.g. `20241231` (`^\\d{8}$`) |

### `hk_balancesheet`

**港股资产负债表**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `ts_code` | `string` | Y | 股票代码 格式: 5位数字+HK, e.g. `00700.HK` (`^\\d{4,5}\\.HK$`) |
| `period` | `string` | N | 报告期(格式：YYYYMMDD） |
| `ind_name` | `string` | N | 指标名（如：应收帐款） |
| `start_date` | `string` | N | 报告期开始日期（格式：YYYYMMDD） 格式: YYYYMMDD, e.g. `20240101` (`^\\d{8}$`) |
| `end_date` | `string` | N | 报告结束始日期（格式：YYYYMMDD） 格式: YYYYMMDD, e.g. `20241231` (`^\\d{8}$`) |

### `hk_basic`

**港股基本信息 (注意代码 5 位,无 .SH/.SZ 后缀, e.g. '00700.HK')**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `ts_code` | `string` | N | TS代码 格式: 5位数字+HK, e.g. `00700.HK` (`^\\d{4,5}\\.HK$`) |
| `list_status` | `string` | N | 上市状态 L上市 D退市 P暂停上市 ，默认L 枚举: 'L', 'D', 'P' — L=上市, D=退市, P=暂停 (枚举: `"L", "D", "P"`) |

### `hk_cashflow`

**港股现金流量表**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `ts_code` | `string` | Y | 股票代码 格式: 5位数字+HK, e.g. `00700.HK` (`^\\d{4,5}\\.HK$`) |
| `period` | `string` | N | 报告期(格式：YYYYMMDD） |
| `ind_name` | `string` | N | 指标名（如：新增贷款） |
| `start_date` | `string` | N | 报告期开始日期（格式：YYYYMMDD） 格式: YYYYMMDD, e.g. `20240101` (`^\\d{8}$`) |
| `end_date` | `string` | N | 报告结束始日期（格式：YYYYMMDD） 格式: YYYYMMDD, e.g. `20241231` (`^\\d{8}$`) |

### `hk_daily`

**港股日线 (ts_code='00700.HK')**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `ts_code` | `string` | N | 股票代码 格式: 5位数字+HK, e.g. `00700.HK` (`^\\d{4,5}\\.HK$`) |
| `trade_date` | `string` | N | 交易日期 格式: YYYYMMDD, e.g. `20240115` (`^\\d{8}$`) |
| `start_date` | `string` | N | 开始日期 格式: YYYYMMDD, e.g. `20240101` (`^\\d{8}$`) |
| `end_date` | `string` | N | 结束日期 格式: YYYYMMDD, e.g. `20241231` (`^\\d{8}$`) |

### `hk_daily_adj`

**港股复权日线**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `ts_code` | `string` | N | 股票代码（e.g. 00001.HK） 格式: 5位数字+HK, e.g. `00700.HK` (`^\\d{4,5}\\.HK$`) |
| `trade_date` | `string` | N | 交易日期（YYYYMMDD） 格式: YYYYMMDD, e.g. `20240115` (`^\\d{8}$`) |
| `start_date` | `string` | N | 开始日期（YYYYMMDD） 格式: YYYYMMDD, e.g. `20240101` (`^\\d{8}$`) |
| `end_date` | `string` | N | 结束日期（YYYYMMDD） 格式: YYYYMMDD, e.g. `20241231` (`^\\d{8}$`) |

### `hk_fina_indicator`

**港股财务指标**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `ts_code` | `string` | Y | 股票代码 格式: 5位数字+HK, e.g. `00700.HK` (`^\\d{4,5}\\.HK$`) |
| `period` | `string` | N | 报告期(格式：YYYYMMDD） |
| `report_type` | `string` | N | 报告期类型（Q1一季报Q2半年报Q3三季报Q4年报） |
| `start_date` | `string` | N | 报告期开始日期(格式：YYYYMMDD） 格式: YYYYMMDD, e.g. `20240101` (`^\\d{8}$`) |
| `end_date` | `string` | N | 报告结束日期(格式：YYYYMMDD） 格式: YYYYMMDD, e.g. `20241231` (`^\\d{8}$`) |

### `hk_income`

**港股利润表 (period='YYYYMMDD')**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `ts_code` | `string` | Y | 股票代码 格式: 5位数字+HK, e.g. `00700.HK` (`^\\d{4,5}\\.HK$`) |
| `period` | `string` | N | 报告期(格式：YYYYMMDD） |
| `ind_name` | `string` | N | 指标名（如：营业额） |
| `start_date` | `string` | N | 报告期开始日期（格式：YYYYMMDD） 格式: YYYYMMDD, e.g. `20240101` (`^\\d{8}$`) |
| `end_date` | `string` | N | 报告结束始日期（格式：YYYYMMDD） 格式: YYYYMMDD, e.g. `20241231` (`^\\d{8}$`) |

### `hk_tradecal`

**港股交易日历**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `start_date` | `string` | Y | 开始日期 格式: YYYYMMDD, e.g. `20240101` (`^\\d{8}$`) |
| `end_date` | `string` | N | 结束日期 格式: YYYYMMDD, e.g. `20241231` (`^\\d{8}$`) |
| `is_open` | `string` | N | 是否交易 &#39;0&#39;休市 &#39;1&#39;交易 枚举: '1', '0' — 1=交易日, 0=非交易日 (枚举: `"1", "0"`) |

---

## 现货 (2)

### `sge_basic`

**上海黄金交易所合约基本信息 (ts_code='Au99.99.SGE')**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `ts_code` | `string` | Y | 合约代码 必传 (不传会返 '参数不能为空')。格式: `Au99.99.SGE`/`Au99.95.SGE`/`Au(T+D).SGE` 等, 多个用逗号分隔。 (`^[A-Za-z0-9.()+-]+\\.SGE$`) |

### `sge_daily`

**上海黄金日线**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `ts_code` | `string` | N | 合约代码  格式: 6位数字+交易所后缀, e.g. `600519.SH` / `000001.SZ` / `830799.BJ` (`^\\d{6}\\.(SZ|SH|BJ)$`) |
| `trade_date` | `string` | N | 交易日期 格式: YYYYMMDD, e.g. `20240115` (`^\\d{8}$`) |
| `start_date` | `string` | N | 开始日期 格式: YYYYMMDD, e.g. `20240101` (`^\\d{8}$`) |
| `end_date` | `string` | N | 结束日期 格式: YYYYMMDD, e.g. `20241231` (`^\\d{8}$`) |

---

## 用户自查 (1)

### `token_info`

**⚠️ 自查 token 状态 (必调用! 返 token 有效期/权限/已开通插件)**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `token` | `string` | Y | 要查询的 TOKEN (通常等于调用时使用的 token, 只能查自己) |

---

## 美股 (9)

### `us_adjfactor`

**美股复权因子**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `ts_code` | `string` | N | 美股代码 必传纯 ticker (如 `AAPL`), ⚠️ 不要带 .US 后缀(后端不支持, 返 0 行), 例: `AAPL`/`TSLA`/`MSFT` (`^[A-Z0-9.\-]+$`) |
| `trade_date` | `string` | N | 交易日期（格式：YYYYMMDD，下同） 格式: YYYYMMDD, e.g. `20240115` (`^\\d{8}$`) |
| `start_date` | `string` | N | 开始日期 格式: YYYYMMDD, e.g. `20240101` (`^\\d{8}$`) |
| `end_date` | `string` | N | 结束日期 格式: YYYYMMDD, e.g. `20241231` (`^\\d{8}$`) |

### `us_balancesheet`

**美股资产负债表**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `ts_code` | `string` | Y | 美股代码 必传纯 ticker (如 `AAPL`), ⚠️ 不要带 .US 后缀(后端不支持, 返 0 行), 例: `AAPL`/`TSLA`/`MSFT` (`^[A-Z0-9.\-]+$`) |
| `period` | `string` | N | 报告期（格式：YYYYMMDD，每个季度最后一天的日期，如20241231) |
| `ind_name` | `string` | N | 指标名(如：新增借款） |
| `report_type` | `string` | N | 报告期类型(Q1一季报Q2半年报Q3三季报Q4年报) |
| `start_date` | `string` | N | 报告期开始时间（格式：YYYYMMDD） 格式: YYYYMMDD, e.g. `20240101` (`^\\d{8}$`) |
| `end_date` | `string` | N | 报告结束始时间（格式：YYYYMMDD） 格式: YYYYMMDD, e.g. `20241231` (`^\\d{8}$`) |

### `us_basic`

**美股基本信息 (注意代码是纯 ticker, e.g. 'AAPL' 无 .US 后缀)**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `ts_code` | `string` | N | 美股代码 必传纯 ticker (如 `AAPL`), ⚠️ 不要带 .US 后缀(后端不支持, 返 0 行), 例: `AAPL`/`TSLA`/`MSFT` (`^[A-Z0-9.\-]+$`) |
| `classify` | `string` | N | 股票分类 |
| `offset` | `string` | N | 开始行数 |
| `limit` | `string` | N | 每页最大行数 |

### `us_cashflow`

**美股现金流量表**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `ts_code` | `string` | Y | 美股代码 必传纯 ticker (如 `AAPL`), ⚠️ 不要带 .US 后缀(后端不支持, 返 0 行), 例: `AAPL`/`TSLA`/`MSFT` (`^[A-Z0-9.\-]+$`) |
| `period` | `string` | N | 报告期（格式：YYYYMMDD，每个季度最后一天的日期，如20241231) |
| `ind_name` | `string` | N | 指标名(如：新增借款） |
| `report_type` | `string` | N | 报告期类型(Q1一季报Q2半年报Q3三季报Q4年报) |
| `start_date` | `string` | N | 报告期开始时间（格式：YYYYMMDD） 格式: YYYYMMDD, e.g. `20240101` (`^\\d{8}$`) |
| `end_date` | `string` | N | 报告结束始时间（格式：YYYYMMDD） 格式: YYYYMMDD, e.g. `20241231` (`^\\d{8}$`) |

### `us_daily`

**美股日线 (ts_code='AAPL', 注意无 .US)**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `ts_code` | `string` | N | 美股代码 必传纯 ticker (如 `AAPL`), ⚠️ 不要带 .US 后缀(后端不支持, 返 0 行), 例: `AAPL`/`TSLA`/`MSFT` (`^[A-Z0-9.\-]+$`) |
| `trade_date` | `string` | N | 交易日期（YYYYMMDD） 格式: YYYYMMDD, e.g. `20240115` (`^\\d{8}$`) |
| `start_date` | `string` | N | 开始日期（YYYYMMDD） 格式: YYYYMMDD, e.g. `20240101` (`^\\d{8}$`) |
| `end_date` | `string` | N | 结束日期（YYYYMMDD） 格式: YYYYMMDD, e.g. `20241231` (`^\\d{8}$`) |

### `us_daily_adj`

**美股复权日线**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `ts_code` | `string` | N | 美股代码 必传纯 ticker (如 `AAPL`), ⚠️ 不要带 .US 后缀(后端不支持, 返 0 行), 例: `AAPL`/`TSLA`/`MSFT` (`^[A-Z0-9.\-]+$`) |
| `trade_date` | `string` | N | 交易日期（YYYYMMDD） 格式: YYYYMMDD, e.g. `20240115` (`^\\d{8}$`) |
| `start_date` | `string` | N | 开始日期（YYYYMMDD） 格式: YYYYMMDD, e.g. `20240101` (`^\\d{8}$`) |
| `end_date` | `string` | N | 结束日期（YYYYMMDD） 格式: YYYYMMDD, e.g. `20241231` (`^\\d{8}$`) |
| `exchange` | `string` | N | 交易所（NAS/NYS/OTC) 枚举: '', 'SSE', 'SZSE', 'BSE', 'HK' — SSE=上交所, SZSE=深交所, BSE=北交所, HK=港交所 (枚举: `"SSE", "SZSE", "BSE", "HK"`) |
| `offset` | `integer` | N | 开始行数 |
| `limit` | `integer` | N | 每页行数行数 |

### `us_fina_indicator`

**美股财务指标**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `ts_code` | `string` | Y | 美股代码 必传纯 ticker (如 `AAPL`), ⚠️ 不要带 .US 后缀(后端不支持, 返 0 行), 例: `AAPL`/`TSLA`/`MSFT` (`^[A-Z0-9.\-]+$`) |
| `period` | `string` | N | 报告期（格式：YYYYMMDD，每个季度最后一天的日期，如20241231) |
| `report_type` | `string` | N | 报告期类型(Q1一季报Q2半年报Q3三季报Q4年报) |
| `start_date` | `string` | N | 报告期开始时间（格式：YYYYMMDD） 格式: YYYYMMDD, e.g. `20240101` (`^\\d{8}$`) |
| `end_date` | `string` | N | 报告结束始时间（格式：YYYYMMDD） 格式: YYYYMMDD, e.g. `20241231` (`^\\d{8}$`) |

### `us_income`

**美股利润表 (period='YYYYMMDD')**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `ts_code` | `string` | Y | 美股代码 必传纯 ticker (如 `AAPL`), ⚠️ 不要带 .US 后缀(后端不支持, 返 0 行), 例: `AAPL`/`TSLA`/`MSFT` (`^[A-Z0-9.\-]+$`) |
| `period` | `string` | N | 报告期（格式：YYYYMMDD，每个季度最后一天的日期，如20241231) |
| `ind_name` | `string` | N | 指标名(如：新增借款） |
| `report_type` | `string` | N | 报告期类型(Q1一季报Q2半年报Q3三季报Q4年报) |
| `start_date` | `string` | N | 报告期开始时间（格式：YYYYMMDD） 格式: YYYYMMDD, e.g. `20240101` (`^\\d{8}$`) |
| `end_date` | `string` | N | 报告结束始时间（格式：YYYYMMDD） 格式: YYYYMMDD, e.g. `20241231` (`^\\d{8}$`) |

### `us_tradecal`

**美股交易日历**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `start_date` | `string` | N | 开始日期 格式: YYYYMMDD, e.g. `20240101` (`^\\d{8}$`) |
| `end_date` | `string` | N | 结束日期 格式: YYYYMMDD, e.g. `20241231` (`^\\d{8}$`) |
| `is_open` | `string` | N | 是否交易 枚举: '1', '0' — 1=交易日, 0=非交易日 (枚举: `"1", "0"`) |

---

## 股票-两融 (7)

### `margin`

**融资融券汇总 (trade_date 必填)**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `trade_date` | `string` | N | 交易日期（格式：YYYYMMDD，下同） 格式: YYYYMMDD, e.g. `20240115` (`^\\d{8}$`) |
| `start_date` | `string` | N | 开始日期 格式: YYYYMMDD, e.g. `20240101` (`^\\d{8}$`) |
| `end_date` | `string` | N | 结束日期 格式: YYYYMMDD, e.g. `20241231` (`^\\d{8}$`) |
| `exchange_id` | `string` | N | 交易所代码（SSE上交所SZSE深交所BSE北交所） |

### `margin_detail`

**融资融券明细 (ts_code + trade_date)**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `trade_date` | `string` | N | 交易日期（格式：YYYYMMDD，下同） 格式: YYYYMMDD, e.g. `20240115` (`^\\d{8}$`) |
| `ts_code` | `string` | N | TS代码 格式: 6位数字+交易所后缀, e.g. `600519.SH` / `000001.SZ` / `830799.BJ` (`^\\d{6}\\.(SZ|SH|BJ)$`) |
| `start_date` | `string` | N | 开始日期 格式: YYYYMMDD, e.g. `20240101` (`^\\d{8}$`) |
| `end_date` | `string` | N | 结束日期 格式: YYYYMMDD, e.g. `20241231` (`^\\d{8}$`) |

### `margin_secs`

**融资融券标的证券**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `ts_code` | `string` | N | 标的代码 格式: 6位数字+交易所后缀, e.g. `600519.SH` / `000001.SZ` / `830799.BJ` (`^\\d{6}\\.(SZ|SH|BJ)$`) |
| `trade_date` | `string` | N | 交易日 格式: YYYYMMDD, e.g. `20240115` (`^\\d{8}$`) |
| `exchange` | `string` | N | 交易所（SSE上交所 SZSE深交所 BSE北交所） 枚举: '', 'SSE', 'SZSE', 'BSE', 'HK' — SSE=上交所, SZSE=深交所, BSE=北交所, HK=港交所 (枚举: `"SSE", "SZSE", "BSE", "HK"`) |
| `start_date` | `string` | N | 开始日期 格式: YYYYMMDD, e.g. `20240101` (`^\\d{8}$`) |
| `end_date` | `string` | N | 结束日期 格式: YYYYMMDD, e.g. `20241231` (`^\\d{8}$`) |

### `slb_len`

**融资期限 (转融通)**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `trade_date` | `string` | N | 交易日期（YYYYMMDD格式，下同） 格式: YYYYMMDD, e.g. `20240115` (`^\\d{8}$`) |
| `start_date` | `string` | N | 开始日期 格式: YYYYMMDD, e.g. `20240101` (`^\\d{8}$`) |
| `end_date` | `string` | N | 结束日期 格式: YYYYMMDD, e.g. `20241231` (`^\\d{8}$`) |

### `slb_len_mm`

**融资期限明细**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `trade_date` | `string` | N | 交易日期（YYYYMMDD格式，下同） 格式: YYYYMMDD, e.g. `20240115` (`^\\d{8}$`) |
| `ts_code` | `string` | N | 股票代码 格式: 6位数字+交易所后缀, e.g. `600519.SH` / `000001.SZ` / `830799.BJ` (`^\\d{6}\\.(SZ|SH|BJ)$`) |
| `start_date` | `string` | N | 开始日期 格式: YYYYMMDD, e.g. `20240101` (`^\\d{8}$`) |
| `end_date` | `string` | N | 结束日期 格式: YYYYMMDD, e.g. `20241231` (`^\\d{8}$`) |

### `slb_sec`

**融券余量 (trade_date + 可选 ts_code)**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `trade_date` | `string` | N | 交易日期（YYYYMMDD格式，下同） 格式: YYYYMMDD, e.g. `20240115` (`^\\d{8}$`) |
| `ts_code` | `string` | N | 股票代码 格式: 6位数字+交易所后缀, e.g. `600519.SH` / `000001.SZ` / `830799.BJ` (`^\\d{6}\\.(SZ|SH|BJ)$`) |
| `start_date` | `string` | N | 开始日期 格式: YYYYMMDD, e.g. `20240101` (`^\\d{8}$`) |
| `end_date` | `string` | N | 结束日期 格式: YYYYMMDD, e.g. `20241231` (`^\\d{8}$`) |

### `slb_sec_detail`

**融券余量明细**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `trade_date` | `string` | N | 交易日期（YYYYMMDD格式，下同） 格式: YYYYMMDD, e.g. `20240115` (`^\\d{8}$`) |
| `ts_code` | `string` | N | 股票代码 格式: 6位数字+交易所后缀, e.g. `600519.SH` / `000001.SZ` / `830799.BJ` (`^\\d{6}\\.(SZ|SH|BJ)$`) |
| `start_date` | `string` | N | 开始日期 格式: YYYYMMDD, e.g. `20240101` (`^\\d{8}$`) |
| `end_date` | `string` | N | 结束日期 格式: YYYYMMDD, e.g. `20241231` (`^\\d{8}$`) |

---

## 股票-参考数据 (9)

### `block_trade`

**大宗交易 (折溢价+买卖方营业部)**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `ts_code` | `string` | N | TS代码（股票代码和日期至少输入一个参数） 格式: 6位数字+交易所后缀, e.g. `600519.SH` / `000001.SZ` / `830799.BJ` (`^\\d{6}\\.(SZ|SH|BJ)$`) |
| `trade_date` | `string` | N | 交易日期（格式：YYYYMMDD，下同） 格式: YYYYMMDD, e.g. `20240115` (`^\\d{8}$`) |
| `start_date` | `string` | N | 开始日期 格式: YYYYMMDD, e.g. `20240101` (`^\\d{8}$`) |
| `end_date` | `string` | N | 结束日期 格式: YYYYMMDD, e.g. `20241231` (`^\\d{8}$`) |

### `pledge_detail`

**股权质押明细 (单笔质押记录)**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `ts_code` | `string` | Y | 股票代码 格式: 6位数字+交易所后缀, e.g. `600519.SH` / `000001.SZ` / `830799.BJ` (`^\\d{6}\\.(SZ|SH|BJ)$`) |

### `pledge_stat`

**股权质押统计 (质押比例+质押笔数+总股本占比)**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `ts_code` | `string` | N | 股票代码 格式: 6位数字+交易所后缀, e.g. `600519.SH` / `000001.SZ` / `830799.BJ` (`^\\d{6}\\.(SZ|SH|BJ)$`) |
| `end_date` | `string` | N | 截止日期 格式: YYYYMMDD, e.g. `20241231` (`^\\d{8}$`) |

### `repurchase`

**股份回购 (公司回购自己股票, 进度+金额)**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `ann_date` | `string` | N | 公告日期（任意填参数，如果都不填，单次默认返回2000条） 格式: YYYYMMDD (`^\\d{8}$`) |
| `start_date` | `string` | N | 公告开始日期 格式: YYYYMMDD, e.g. `20240101` (`^\\d{8}$`) |
| `end_date` | `string` | N | 公告结束日期 格式: YYYYMMDD, e.g. `20241231` (`^\\d{8}$`) |

### `share_float`

**流通股本变动 (限售解禁时间表)**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `ts_code` | `string` | N | TS股票代码 格式: 6位数字+交易所后缀, e.g. `600519.SH` / `000001.SZ` / `830799.BJ` (`^\\d{6}\\.(SZ|SH|BJ)$`) |
| `ann_date` | `string` | N | 公告日期（日期格式：YYYYMMDD，下同） 格式: YYYYMMDD (`^\\d{8}$`) |
| `float_date` | `string` | N | 解禁日期 |
| `start_date` | `string` | N | 解禁开始日期 格式: YYYYMMDD, e.g. `20240101` (`^\\d{8}$`) |
| `end_date` | `string` | N | 解禁结束日期 格式: YYYYMMDD, e.g. `20241231` (`^\\d{8}$`) |

### `stk_holdernumber`

**股东户数 (越少筹码越集中)**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `ts_code` | `string` | N | TS股票代码 格式: 6位数字+交易所后缀, e.g. `600519.SH` / `000001.SZ` / `830799.BJ` (`^\\d{6}\\.(SZ|SH|BJ)$`) |
| `ann_date` | `string` | N | 公告日期 格式: YYYYMMDD (`^\\d{8}$`) |
| `enddate` | `string` | N | 截止日期 |
| `start_date` | `string` | N | 公告开始日期 格式: YYYYMMDD, e.g. `20240101` (`^\\d{8}$`) |
| `end_date` | `string` | N | 公告结束日期 格式: YYYYMMDD, e.g. `20241231` (`^\\d{8}$`) |

### `stk_holdertrade`

**股东增减持 (trade_type='1' 增持/'2' 减持)**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `ts_code` | `string` | N | TS股票代码 格式: 6位数字+交易所后缀, e.g. `600519.SH` / `000001.SZ` / `830799.BJ` (`^\\d{6}\\.(SZ|SH|BJ)$`) |
| `ann_date` | `string` | N | 公告日期 格式: YYYYMMDD (`^\\d{8}$`) |
| `start_date` | `string` | N | 公告开始日期 格式: YYYYMMDD, e.g. `20240101` (`^\\d{8}$`) |
| `end_date` | `string` | N | 公告结束日期 格式: YYYYMMDD, e.g. `20241231` (`^\\d{8}$`) |
| `trade_type` | `string` | N | 交易类型IN增持DE减持 枚举: '1', '2' — 1=增持, 2=减持 (枚举: `"1", "2"`) |
| `holder_type` | `string` | N | 股东类型C公司P个人G高管 |

### `top10_floatholders`

**前十大流通股东 (流通股持股+变动)**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `ts_code` | `string` | Y | TS代码 格式: 6位数字+交易所后缀, e.g. `600519.SH` / `000001.SZ` / `830799.BJ` (`^\\d{6}\\.(SZ|SH|BJ)$`) |
| `period` | `string` | N | 报告期（YYYYMMDD格式，一般为每个季度最后一天） |
| `ann_date` | `string` | N | 公告日期 格式: YYYYMMDD (`^\\d{8}$`) |
| `start_date` | `string` | N | 报告期开始日期 格式: YYYYMMDD, e.g. `20240101` (`^\\d{8}$`) |
| `end_date` | `string` | N | 报告期结束日期 格式: YYYYMMDD, e.g. `20241231` (`^\\d{8}$`) |

### `top10_holders`

**前十大股东 (持股数+持股比例+股东类型)**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `ts_code` | `string` | Y | TS代码 格式: 6位数字+交易所后缀, e.g. `600519.SH` / `000001.SZ` / `830799.BJ` (`^\\d{6}\\.(SZ|SH|BJ)$`) |
| `period` | `string` | N | 报告期（YYYYMMDD格式，一般为每个季度最后一天） |
| `ann_date` | `string` | N | 公告日期 格式: YYYYMMDD (`^\\d{8}$`) |
| `start_date` | `string` | N | 报告期开始日期 格式: YYYYMMDD, e.g. `20240101` (`^\\d{8}$`) |
| `end_date` | `string` | N | 报告期结束日期 格式: YYYYMMDD, e.g. `20241231` (`^\\d{8}$`) |

---

## 股票-基础数据 (18)

### `bak_basic`

**备用基础数据 (同 stock_basic 备份, 用于离线)**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `trade_date` | `string` | N | 交易日期 格式: YYYYMMDD, e.g. `20240115` (`^\\d{8}$`) |
| `ts_code` | `string` | N | 股票代码 格式: 6位数字+交易所后缀, e.g. `600519.SH` / `000001.SZ` / `830799.BJ` (`^\\d{6}\\.(SZ|SH|BJ)$`) |

### `bse_mapping`

**北交所 8 开头旧代码 -> 92 开头新代码 对照表**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `o_code` | `string` | N | 旧代码 |
| `n_code` | `string` | N | 新代码 |

### `irm_qa_sh`

**上证 e 互动问答 (ts_code + trade_date)**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `ts_code` | `string` | N | 股票代码 格式: 6位数字+交易所后缀, e.g. `600519.SH` / `000001.SZ` / `830799.BJ` (`^\\d{6}\\.(SZ|SH|BJ)$`) |
| `trade_date` | `string` | N | 交易日期（格式YYYYMMDD，下同） 格式: YYYYMMDD, e.g. `20240115` (`^\\d{8}$`) |
| `start_date` | `string` | N | 开始日期 格式: YYYYMMDD, e.g. `20240101` (`^\\d{8}$`) |
| `end_date` | `string` | N | 结束日期 格式: YYYYMMDD, e.g. `20241231` (`^\\d{8}$`) |
| `pub_date` | `string` | N | 发布开始日期(格式：2025-06-03 16:43:03) |
| `pub_date` | `string` | N | 发布结束日期(格式：2025-06-03 18:43:23) |

### `irm_qa_sz`

**深证易互动问答**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `ts_code` | `string` | N | 股票代码 格式: 6位数字+交易所后缀, e.g. `600519.SH` / `000001.SZ` / `830799.BJ` (`^\\d{6}\\.(SZ|SH|BJ)$`) |
| `trade_date` | `string` | N | 交易日期（格式YYYYMMDD，下同） 格式: YYYYMMDD, e.g. `20240115` (`^\\d{8}$`) |
| `start_date` | `string` | N | 开始日期 格式: YYYYMMDD, e.g. `20240101` (`^\\d{8}$`) |
| `end_date` | `string` | N | 结束日期 格式: YYYYMMDD, e.g. `20241231` (`^\\d{8}$`) |
| `pub_date` | `string` | N | 发布开始日期(格式：2025-06-03 16:43:03) |
| `pub_date` | `string` | N | 发布结束日期(格式：2025-06-03 18:43:23) |

### `namechange`

**股票名称变更记录 (起止日期过滤)**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `ts_code` | `string` | N | TS代码 格式: 6位数字+交易所后缀, e.g. `600519.SH` / `000001.SZ` / `830799.BJ` (`^\\d{6}\\.(SZ|SH|BJ)$`) |
| `start_date` | `string` | N | 公告开始日期 格式: YYYYMMDD, e.g. `20240101` (`^\\d{8}$`) |
| `end_date` | `string` | N | 公告结束日期 格式: YYYYMMDD, e.g. `20241231` (`^\\d{8}$`) |

### `new_share`

**新股发行 (需 start_date + end_date)**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `start_date` | `string` | N | 上网发行开始日期 格式: YYYYMMDD, e.g. `20240101` (`^\\d{8}$`) |
| `end_date` | `string` | N | 上网发行结束日期 格式: YYYYMMDD, e.g. `20241231` (`^\\d{8}$`) |

### `stk_account`

**股票账户统计 (周/月,需 start_date+end_date)**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `date` | `string` | N | 日期 (`^\\d{8}$`) |
| `start_date` | `string` | N | 开始日期 格式: YYYYMMDD, e.g. `20240101` (`^\\d{8}$`) |
| `end_date` | `string` | N | 结束日期 格式: YYYYMMDD, e.g. `20241231` (`^\\d{8}$`) |

### `stk_account_old`

**股票账户统计(旧表, 字段不同)**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `start_date` | `string` | N | 开始日期 格式: YYYYMMDD, e.g. `20240101` (`^\\d{8}$`) |
| `end_date` | `string` | N | 结束日期 格式: YYYYMMDD, e.g. `20241231` (`^\\d{8}$`) |

### `stk_ah_comparison`

**AH 股价差对比 (同公司 A股 vs 港股)**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `hk_code` | `string` | N | 港股股票代码（xxxxx.HK) |
| `ts_code` | `string` | N | A股票代码(xxxxxx.SH/SZ/BJ) 格式: 6位数字+交易所后缀, e.g. `600519.SH` / `000001.SZ` / `830799.BJ` (`^\\d{6}\\.(SZ|SH|BJ)$`) |
| `trade_date` | `string` | N | 交易日期（格式：YYYYMMDD下同） 格式: YYYYMMDD, e.g. `20240115` (`^\\d{8}$`) |
| `start_date` | `string` | N | 开始日期 格式: YYYYMMDD, e.g. `20240101` (`^\\d{8}$`) |
| `end_date` | `string` | N | 结束日期 格式: YYYYMMDD, e.g. `20241231` (`^\\d{8}$`) |

### `stk_managers`

**公司管理层 (董监高, 学历, 任期)**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `ts_code` | `string` | N | 股票代码，支持单个或多个股票输入 格式: 6位数字+交易所后缀, e.g. `600519.SH` / `000001.SZ` / `830799.BJ` (`^\\d{6}\\.(SZ|SH|BJ)$`) |
| `ann_date` | `string` | N | 公告日期（YYYYMMDD格式，下同） 格式: YYYYMMDD (`^\\d{8}$`) |
| `start_date` | `string` | N | 公告开始日期 格式: YYYYMMDD, e.g. `20240101` (`^\\d{8}$`) |
| `end_date` | `string` | N | 公告结束日期 格式: YYYYMMDD, e.g. `20241231` (`^\\d{8}$`) |

### `stk_premarket`

**新股上市信息 (需 start_date + end_date)**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `ts_code` | `string` | N | 股票代码 格式: 6位数字+交易所后缀, e.g. `600519.SH` / `000001.SZ` / `830799.BJ` (`^\\d{6}\\.(SZ|SH|BJ)$`) |
| `trade_date` | `string` | N | 交易日期(YYYYMMDD格式，下同) 格式: YYYYMMDD, e.g. `20240115` (`^\\d{8}$`) |
| `start_date` | `string` | N | 开始日期 格式: YYYYMMDD, e.g. `20240101` (`^\\d{8}$`) |
| `end_date` | `string` | N | 结束日期 格式: YYYYMMDD, e.g. `20241231` (`^\\d{8}$`) |

### `stk_rewards`

**高管薪酬 (按年披露)**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `ts_code` | `string` | Y | TS股票代码，支持单个或多个代码输入 格式: 6位数字+交易所后缀, e.g. `600519.SH` / `000001.SZ` / `830799.BJ` (`^\\d{6}\\.(SZ|SH|BJ)$`) |
| `end_date` | `string` | N | 报告期 格式: YYYYMMDD, e.g. `20241231` (`^\\d{8}$`) |

### `stk_surv`

**调研记录 (机构调研频次+关注点)**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `ts_code` | `string` | N | 股票代码 格式: 6位数字+交易所后缀, e.g. `600519.SH` / `000001.SZ` / `830799.BJ` (`^\\d{6}\\.(SZ|SH|BJ)$`) |
| `trade_date` | `string` | N | 调研日期 格式: YYYYMMDD, e.g. `20240115` (`^\\d{8}$`) |
| `start_date` | `string` | N | 调研开始日期 格式: YYYYMMDD, e.g. `20240101` (`^\\d{8}$`) |
| `end_date` | `string` | N | 调研结束日期 格式: YYYYMMDD, e.g. `20241231` (`^\\d{8}$`) |

### `stock_basic`

**股票基本信息列表 (默认上市,可用 market/列表状态过滤)**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `ts_code` | `string` | N | TS股票代码(格式说明) 格式: 6位数字+交易所后缀, e.g. `600519.SH` / `000001.SZ` / `830799.BJ` (`^\\d{6}\\.(SZ|SH|BJ)$`) |
| `market` | `string` | N | 市场类别 （主板/创业板/科创板/CDR/北交所） 枚举: 'SSE', 'SZSE', 'BSE' — SSE=上交所, SZSE=深交所, BSE=北交所 (枚举: `"SSE", "SZSE", "BSE"`) |
| `list_status` | `string` | N | 上市状态 L上市 D退市 P暂停上市 G 未交易，默认是L 枚举: 'L', 'D', 'P' — L=上市, D=退市, P=暂停 (枚举: `"L", "D", "P"`) |
| `exchange` | `string` | N | 交易所 SSE上交所 SZSE深交所 BSE北交所 枚举: '', 'SSE', 'SZSE', 'BSE', 'HK' — SSE=上交所, SZSE=深交所, BSE=北交所, HK=港交所 (枚举: `"SSE", "SZSE", "BSE", "HK"`) |
| `is_hs` | `string` | N | 是否沪深港通标的，N否 H沪股通 S深股通 (枚举: `"N", "H", "S"`) |

### `stock_company`

**上市公司基本信息 (含注册资本/法人/经营范围)**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `ts_code` | `string` | N | 股票代码 格式: 6位数字+交易所后缀, e.g. `600519.SH` / `000001.SZ` / `830799.BJ` (`^\\d{6}\\.(SZ|SH|BJ)$`) |
| `exchange` | `string` | N | 交易所代码 ，SSE上交所 SZSE深交所 BSE北交所 枚举: '', 'SSE', 'SZSE', 'BSE', 'HK' — SSE=上交所, SZSE=深交所, BSE=北交所, HK=港交所 (枚举: `"SSE", "SZSE", "BSE", "HK"`) |

### `stock_hsgt`

**沪深港通标的股票列表 (is_hs='H' 沪股通/'S' 深股通/'N' 否)**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `ts_code` | `string` | N | 股票代码 格式: 6位数字+交易所后缀, e.g. `600519.SH` / `000001.SZ` / `830799.BJ` (`^\\d{6}\\.(SZ|SH|BJ)$`) |
| `trade_date` | `string` | N | 交易日期（格式：YYYYMMDD） 格式: YYYYMMDD, e.g. `20240115` (`^\\d{8}$`) |
| `type` | `string` | Y | 类型（参考下表） |
| `start_date` | `string` | N | 开始时间 格式: YYYYMMDD, e.g. `20240101` (`^\\d{8}$`) |
| `end_date` | `string` | N | 结束时间 格式: YYYYMMDD, e.g. `20241231` (`^\\d{8}$`) |

### `stock_st`

**ST/*ST 股票列表 (需 trade_date)**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `ts_code` | `string` | N | 股票代码 格式: 6位数字+交易所后缀, e.g. `600519.SH` / `000001.SZ` / `830799.BJ` (`^\\d{6}\\.(SZ|SH|BJ)$`) |
| `trade_date` | `string` | N | 交易日期（格式：YYYYMMDD下同） 格式: YYYYMMDD, e.g. `20240115` (`^\\d{8}$`) |
| `start_date` | `string` | N | 开始时间 格式: YYYYMMDD, e.g. `20240101` (`^\\d{8}$`) |
| `end_date` | `string` | N | 结束时间 格式: YYYYMMDD, e.g. `20241231` (`^\\d{8}$`) |

### `trade_cal`

**交易日历 (is_open='1' 查交易日 / '0' 查休市日, 调接口前先查这个)**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `exchange` | `string` | N | 交易所 SSE上交所,SZSE深交所,CFFEX 中金所,SHFE 上期所,CZCE 郑商所,DCE 大商所,INE 上能源 枚举: '', 'SSE', 'SZSE', 'BSE', 'HK' — SSE=上交所, SZSE=深交所, BSE=北交所, HK=港交所 (枚举: `"SSE", "SZSE", "BSE", "HK"`) |
| `start_date` | `string` | N | 开始日期 （格式：YYYYMMDD 下同） 格式: YYYYMMDD, e.g. `20240101` (`^\\d{8}$`) |
| `end_date` | `string` | N | 结束日期 格式: YYYYMMDD, e.g. `20241231` (`^\\d{8}$`) |
| `is_open` | `string` | N | 是否交易 '0'休市 '1'交易 枚举: '1', '0' — 1=交易日, 0=非交易日 (枚举: `"1", "0"`) |

---

## 股票-特色数据 (16)

### `anns_d`

**上市公司公告 (ts_code + start_date + end_date)**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `ts_code` | `string` | N | 股票代码 格式: 6位数字+交易所后缀, e.g. `600519.SH` / `000001.SZ` / `830799.BJ` (`^\\d{6}\\.(SZ|SH|BJ)$`) |
| `ann_date` | `string` | N | 公告日期（yyyymmdd格式，下同） 格式: YYYYMMDD (`^\\d{8}$`) |
| `start_date` | `string` | N | 公告开始日期 格式: YYYYMMDD, e.g. `20240101` (`^\\d{8}$`) |
| `end_date` | `string` | N | 公告结束日期 格式: YYYYMMDD, e.g. `20241231` (`^\\d{8}$`) |

### `broker_recommend`

**broker_recommend**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `month` | `string` | Y | 月度（YYYYMM） |

### `ccass_hold`

**中央结算系统持股 (港股, 港交所CCASS)**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `ts_code` | `string` | N | 股票代码 (e.g. 605009.SH) 格式: 5位数字+HK, e.g. `00700.HK` (`^\\d{4,5}\\.HK$`) |
| `hk_code` | `string` | N | 港交所代码 （e.g. 95009） |
| `trade_date` | `string` | N | 交易日期(YYYYMMDD格式，下同) 格式: YYYYMMDD, e.g. `20240115` (`^\\d{8}$`) |
| `start_date` | `string` | N | 开始日期 格式: YYYYMMDD, e.g. `20240101` (`^\\d{8}$`) |
| `end_date` | `string` | N | 结束日期 格式: YYYYMMDD, e.g. `20241231` (`^\\d{8}$`) |

### `ccass_hold_detail`

**中央结算持股明细**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `ts_code` | `string` | N | 股票代码 (e.g. 605009.SH) 格式: 5位数字+HK, e.g. `00700.HK` (`^\\d{4,5}\\.HK$`) |
| `hk_code` | `string` | N | 港交所代码 （e.g. 95009） |
| `trade_date` | `string` | N | 交易日期(YYYYMMDD格式，下同) 格式: YYYYMMDD, e.g. `20240115` (`^\\d{8}$`) |
| `start_date` | `string` | N | 开始日期 格式: YYYYMMDD, e.g. `20240101` (`^\\d{8}$`) |
| `end_date` | `string` | N | 结束日期 格式: YYYYMMDD, e.g. `20241231` (`^\\d{8}$`) |

### `cctv_news`

**新闻联播文字稿 (date 必填)**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `date` | `string` | Y | 日期（输入格式：YYYYMMDD 比如：20181211） (`^\\d{8}$`) |

### `cyq_chips`

**筹码分布 (各价格区间持仓占比)**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `ts_code` | `string` | Y | 股票代码 格式: 6位数字+交易所后缀, e.g. `600519.SH` / `000001.SZ` / `830799.BJ` (`^\\d{6}\\.(SZ|SH|BJ)$`) |
| `trade_date` | `string` | N | 交易日期（YYYYMMDD） 格式: YYYYMMDD, e.g. `20240115` (`^\\d{8}$`) |
| `start_date` | `string` | N | 开始日期 格式: YYYYMMDD, e.g. `20240101` (`^\\d{8}$`) |
| `end_date` | `string` | N | 结束日期 格式: YYYYMMDD, e.g. `20241231` (`^\\d{8}$`) |

### `cyq_perf`

**筹码活跃度 (90%成本价+获利比例)**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `ts_code` | `string` | Y | 股票代码 格式: 6位数字+交易所后缀, e.g. `600519.SH` / `000001.SZ` / `830799.BJ` (`^\\d{6}\\.(SZ|SH|BJ)$`) |
| `trade_date` | `string` | N | 交易日期（YYYYMMDD） 格式: YYYYMMDD, e.g. `20240115` (`^\\d{8}$`) |
| `start_date` | `string` | N | 开始日期 格式: YYYYMMDD, e.g. `20240101` (`^\\d{8}$`) |
| `end_date` | `string` | N | 结束日期 格式: YYYYMMDD, e.g. `20241231` (`^\\d{8}$`) |

### `hk_hold`

**沪深港通北向持股 (ts_code + trade_date)**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `code` | `string` | N | 交易所代码 |
| `ts_code` | `string` | N | TS股票代码 格式: 5位数字+HK, e.g. `00700.HK` (`^\\d{4,5}\\.HK$`) |
| `trade_date` | `string` | N | 交易日期 格式: YYYYMMDD, e.g. `20240115` (`^\\d{8}$`) |
| `start_date` | `string` | N | 开始日期 格式: YYYYMMDD, e.g. `20240101` (`^\\d{8}$`) |
| `end_date` | `string` | N | 结束日期 格式: YYYYMMDD, e.g. `20241231` (`^\\d{8}$`) |
| `exchange` | `string` | N | 类型：SH沪股通（北向）SZ深股通（北向）HK港股通（南向持股） 枚举: '', 'SSE', 'SZSE', 'BSE', 'HK' — SSE=上交所, SZSE=深交所, BSE=北交所, HK=港交所 (枚举: `"SSE", "SZSE", "BSE", "HK"`) |

### `npr`

**国家政策库 (按关键词搜索, e.g. '新能源')**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `start_date` | `string` | N | 开始日期 格式: YYYYMMDD, e.g. `20240101` (`^\\d{8}$`) |
| `end_date` | `string` | N | 结束日期 格式: YYYYMMDD, e.g. `20241231` (`^\\d{8}$`) |

### `report_rc`

**研报 (券商分析师报告+评级+目标价)**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `ts_code` | `string` | N | 股票代码 格式: 6位数字+交易所后缀, e.g. `600519.SH` / `000001.SZ` / `830799.BJ` (`^\\d{6}\\.(SZ|SH|BJ)$`) |
| `report_date` | `string` | N | 报告日期 |
| `start_date` | `string` | N | 报告开始日期 格式: YYYYMMDD, e.g. `20240101` (`^\\d{8}$`) |
| `end_date` | `string` | N | 报告结束日期 格式: YYYYMMDD, e.g. `20241231` (`^\\d{8}$`) |

### `research_report`

**券商研究报告**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `trade_date` | `string` | N | 研报日期（格式：YYYYMMDD，下同） 格式: YYYYMMDD, e.g. `20240115` (`^\\d{8}$`) |
| `start_date` | `string` | N | 研报开始日期 格式: YYYYMMDD, e.g. `20240101` (`^\\d{8}$`) |
| `end_date` | `string` | N | 研报结束日期 格式: YYYYMMDD, e.g. `20241231` (`^\\d{8}$`) |
| `report_type` | `string` | N | 研报类别：个股研报/行业研报 |
| `ts_code` | `string` | N | 股票代码 格式: 6位数字+交易所后缀, e.g. `600519.SH` / `000001.SZ` / `830799.BJ` (`^\\d{6}\\.(SZ|SH|BJ)$`) |

### `stk_auction`

**集合竞价数据 (trade_date 必填)**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `ts_code` | `string` | N | 股票代码 格式: 6位数字+交易所后缀, e.g. `600519.SH` / `000001.SZ` / `830799.BJ` (`^\\d{6}\\.(SZ|SH|BJ)$`) |
| `trade_date` | `string` | N | 交易日期（YYYYMMDD格式，下同) 格式: YYYYMMDD, e.g. `20240115` (`^\\d{8}$`) |
| `start_date` | `string` | N | 开始日期 格式: YYYYMMDD, e.g. `20240101` (`^\\d{8}$`) |
| `end_date` | `string` | N | 结束日期 格式: YYYYMMDD, e.g. `20241231` (`^\\d{8}$`) |

### `stk_auction_c`

**收盘竞价**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `ts_code` | `string` | N | 股票代码 格式: 6位数字+交易所后缀, e.g. `600519.SH` / `000001.SZ` / `830799.BJ` (`^\\d{6}\\.(SZ|SH|BJ)$`) |
| `trade_date` | `string` | N | 交易日期(YYYYMMDD) 格式: YYYYMMDD, e.g. `20240115` (`^\\d{8}$`) |
| `start_date` | `string` | N | 开始日期(YYYYMMDD) 格式: YYYYMMDD, e.g. `20240101` (`^\\d{8}$`) |
| `end_date` | `string` | N | 结束日期(YYYYMMDD) 格式: YYYYMMDD, e.g. `20241231` (`^\\d{8}$`) |

### `stk_auction_o`

**开盘竞价成交明细**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `ts_code` | `string` | N | 股票代码 格式: 6位数字+交易所后缀, e.g. `600519.SH` / `000001.SZ` / `830799.BJ` (`^\\d{6}\\.(SZ|SH|BJ)$`) |
| `trade_date` | `string` | N | 交易日期(YYYYMMDD) 格式: YYYYMMDD, e.g. `20240115` (`^\\d{8}$`) |
| `start_date` | `string` | N | 开始日期(YYYYMMDD) 格式: YYYYMMDD, e.g. `20240101` (`^\\d{8}$`) |
| `end_date` | `string` | N | 结束日期(YYYYMMDD) 格式: YYYYMMDD, e.g. `20241231` (`^\\d{8}$`) |

### `stk_factor_pro`

**200+ 因子库 (动量/价值/质量/技术, 必传 ts_code + trade_date)**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `ts_code` | `string` | N | 股票代码 格式: 6位数字+交易所后缀, e.g. `600519.SH` / `000001.SZ` / `830799.BJ` (`^\\d{6}\\.(SZ|SH|BJ)$`) |
| `trade_date` | `string` | N | 交易日期(格式：yyyymmdd，下同) 格式: YYYYMMDD, e.g. `20240115` (`^\\d{8}$`) |
| `start_date` | `string` | N | 开始日期 格式: YYYYMMDD, e.g. `20240101` (`^\\d{8}$`) |
| `end_date` | `string` | N | 结束日期 格式: YYYYMMDD, e.g. `20241231` (`^\\d{8}$`) |

### `stk_nineturn`

**九转序列 (技术指标, 9 天高低点)**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `ts_code` | `string` | N | 股票代码 格式: 6位数字+交易所后缀, e.g. `600519.SH` / `000001.SZ` / `830799.BJ` (`^\\d{6}\\.(SZ|SH|BJ)$`) |
| `trade_date` | `string` | N | 交易日期 （格式：YYYY-MM-DD HH:MM:SS) 格式: YYYYMMDD, e.g. `20240115` (`^\\d{8}$`) |
| `freq` | `string` | N | 频率(日daily) 枚举: 'D', 'W', 'M', '1min', '5min', '15min', '30min', '60min' — D=日, W=周, M=月, 1/5/15/30/60min=分钟 (枚举: `"D", "W", "M", "1min", "5min", "15min", "30min", "60min"`) |
| `start_date` | `string` | N | 开始时间 格式: YYYYMMDD, e.g. `20240101` (`^\\d{8}$`) |
| `end_date` | `string` | N | 结束时间 格式: YYYYMMDD, e.g. `20241231` (`^\\d{8}$`) |

---

## 股票-行情数据 (21)

### `adj_factor`

**复权因子 (除权除息调整系数)**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `ts_code` | `string` | N | 股票代码 格式: 6位数字+交易所后缀, e.g. `600519.SH` / `000001.SZ` / `830799.BJ` (`^\\d{6}\\.(SZ|SH|BJ)$`) |
| `trade_date` | `string` | N | 交易日期(YYYYMMDD，下同) 格式: YYYYMMDD, e.g. `20240115` (`^\\d{8}$`) |
| `start_date` | `string` | N | 开始日期 格式: YYYYMMDD, e.g. `20240101` (`^\\d{8}$`) |
| `end_date` | `string` | N | 结束日期 格式: YYYYMMDD, e.g. `20241231` (`^\\d{8}$`) |

### `bak_daily`

**备用日线行情 (同 daily 备份, 数据可能有 1 天延迟)**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `ts_code` | `string` | N | 股票代码 格式: 6位数字+交易所后缀, e.g. `600519.SH` / `000001.SZ` / `830799.BJ` (`^\\d{6}\\.(SZ|SH|BJ)$`) |
| `trade_date` | `string` | N | 交易日期 格式: YYYYMMDD, e.g. `20240115` (`^\\d{8}$`) |
| `start_date` | `string` | N | 开始日期 格式: YYYYMMDD, e.g. `20240101` (`^\\d{8}$`) |
| `end_date` | `string` | N | 结束日期 格式: YYYYMMDD, e.g. `20241231` (`^\\d{8}$`) |
| `offset` | `string` | N | 开始行数 |
| `limit` | `string` | N | 最大行数 |

### `daily`

**日线行情 OHLCV (必传 ts_code + trade_date 或 start_date+end_date)**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `ts_code` | `string` | N | 股票代码（支持多个股票同时提取，逗号分隔） 格式: 6位数字+交易所后缀, e.g. `600519.SH` / `000001.SZ` / `830799.BJ` (`^\\d{6}\\.(SZ|SH|BJ)$`) |
| `trade_date` | `string` | N | 交易日期（YYYYMMDD） 格式: YYYYMMDD, e.g. `20240115` (`^\\d{8}$`) |
| `start_date` | `string` | N | 开始日期(YYYYMMDD) 格式: YYYYMMDD, e.g. `20240101` (`^\\d{8}$`) |
| `end_date` | `string` | N | 结束日期(YYYYMMDD) 格式: YYYYMMDD, e.g. `20241231` (`^\\d{8}$`) |

### `daily_basic`

**每日技术指标 PE/PB/换手率/总市值/流通市值 (必传 ts_code + trade_date)**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `ts_code` | `string` | Y | 股票代码（二选一） 格式: 6位数字+交易所后缀, e.g. `600519.SH` / `000001.SZ` / `830799.BJ` (`^\\d{6}\\.(SZ|SH|BJ)$`) |
| `trade_date` | `string` | N | 交易日期 （二选一） 格式: YYYYMMDD, e.g. `20240115` (`^\\d{8}$`) |
| `start_date` | `string` | N | 开始日期(YYYYMMDD) 格式: YYYYMMDD, e.g. `20240101` (`^\\d{8}$`) |
| `end_date` | `string` | N | 结束日期(YYYYMMDD) 格式: YYYYMMDD, e.g. `20241231` (`^\\d{8}$`) |

### `etf_mins`

**ETF 历史分钟**

### `ft_mins`

**期货历史分钟**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `ts_code` | `string` | Y | 股票代码，e.g.CU2310.SHF 格式: 6位数字+交易所后缀, e.g. `600519.SH` / `000001.SZ` / `830799.BJ` (`^\\d{6}\\.(SZ|SH|BJ)$`) |
| `freq` | `string` | Y | 分钟频度（1min/5min/15min/30min/60min） 枚举: 'D', 'W', 'M', '1min', '5min', '15min', '30min', '60min' — D=日, W=周, M=月, 1/5/15/30/60min=分钟 (枚举: `"1min", "5min", "15min", "30min", "60min"`) |
| `start_date` | `string` | N | 开始日期 格式：2023-08-25 09:00:00 格式: `YYYY-MM-DD HH:MM:SS`, e.g. `2024-08-25 09:00:00` (`^\\d{4}-\\d{2}-\\d{2} \\d{2}:\\d{2}:\\d{2}$`) |
| `end_date` | `string` | N | 结束时间 格式：2023-08-25 19:00:00 格式: `YYYY-MM-DD HH:MM:SS`, e.g. `2024-08-25 09:00:00` (`^\\d{4}-\\d{2}-\\d{2} \\d{2}:\\d{2}:\\d{2}$`) |

### `ggt_daily`

**港股通每日净买入 (trade_date 必填)**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `trade_date` | `string` | N | 交易日期 （格式YYYYMMDD，下同。支持单日和多日输入） 格式: YYYYMMDD, e.g. `20240115` (`^\\d{8}$`) |
| `start_date` | `string` | N | 开始日期 格式: YYYYMMDD, e.g. `20240101` (`^\\d{8}$`) |
| `end_date` | `string` | N | 结束日期 格式: YYYYMMDD, e.g. `20241231` (`^\\d{8}$`) |

### `ggt_monthly`

**港股通每月净买入 (trade_date 必填)**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `month` | `string` | N | 月度（格式YYYYMM，下同，支持多个输入） |
| `start_month` | `string` | N | 开始月度 |
| `end_month` | `string` | N | 结束月度 |

### `ggt_top10`

**港股通成交前 10 (trade_date 必填 + market_type 2=沪港通/4=深港通)**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `ts_code` | `string` | N | 股票代码（二选一） 格式: 6位数字+交易所后缀, e.g. `600519.SH` / `000001.SZ` / `830799.BJ` (`^\\d{6}\\.(SZ|SH|BJ)$`) |
| `trade_date` | `string` | N | 交易日期（二选一） 格式: YYYYMMDD, e.g. `20240115` (`^\\d{8}$`) |
| `start_date` | `string` | N | 开始日期 格式: YYYYMMDD, e.g. `20240101` (`^\\d{8}$`) |
| `end_date` | `string` | N | 结束日期 格式: YYYYMMDD, e.g. `20241231` (`^\\d{8}$`) |
| `market_type` | `string` | N | 市场类型 2：港股通（沪） 4：港股通（深） |

### `hk_mins`

**港股历史分钟**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `ts_code` | `string` | Y | 股票代码，e.g.00001.HK 格式: 5位数字+HK, e.g. `00700.HK` (`^\\d{4,5}\\.HK$`) |
| `freq` | `string` | Y | 分钟频度（1min/5min/15min/30min/60min） 枚举: 'D', 'W', 'M', '1min', '5min', '15min', '30min', '60min' — D=日, W=周, M=月, 1/5/15/30/60min=分钟 (枚举: `"1min", "5min", "15min", "30min", "60min"`) |
| `start_date` | `string` | N | 开始日期 格式：2023-03-13 09:00:00 格式: `YYYY-MM-DD HH:MM:SS`, e.g. `2024-08-25 09:00:00` (`^\\d{4}-\\d{2}-\\d{2} \\d{2}:\\d{2}:\\d{2}$`) |
| `end_date` | `string` | N | 结束时间 格式：2023-03-13 19:00:00 格式: `YYYY-MM-DD HH:MM:SS`, e.g. `2024-08-25 09:00:00` (`^\\d{4}-\\d{2}-\\d{2} \\d{2}:\\d{2}:\\d{2}$`) |

### `hsgt_top10`

**沪深股通成交前 10 (trade_date 必填 + market_type 1=沪/3=深)**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `ts_code` | `string` | N | 股票代码（二选一） 格式: 6位数字+交易所后缀, e.g. `600519.SH` / `000001.SZ` / `830799.BJ` (`^\\d{6}\\.(SZ|SH|BJ)$`) |
| `trade_date` | `string` | N | 交易日期（二选一） 格式: YYYYMMDD, e.g. `20240115` (`^\\d{8}$`) |
| `start_date` | `string` | N | 开始日期 格式: YYYYMMDD, e.g. `20240101` (`^\\d{8}$`) |
| `end_date` | `string` | N | 结束日期 格式: YYYYMMDD, e.g. `20241231` (`^\\d{8}$`) |
| `market_type` | `string` | N | 市场类型（1：沪市 3：深市） |

### `idx_mins`

**指数历史分钟**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `ts_code` | `string` | Y | 指数代码，e.g. 000001.SH 格式: 6位数字+交易所后缀, e.g. `600519.SH` / `000001.SZ` / `830799.BJ` (`^\\d{6}\\.(SZ|SH|BJ)$`) |
| `freq` | `string` | Y | 分钟频度（1min/5min/15min/30min/60min） 枚举: 'D', 'W', 'M', '1min', '5min', '15min', '30min', '60min' — D=日, W=周, M=月, 1/5/15/30/60min=分钟 (枚举: `"1min", "5min", "15min", "30min", "60min"`) |
| `start_date` | `string` | N | 开始日期 格式：2023-08-25 09:00:00 格式: `YYYY-MM-DD HH:MM:SS`, e.g. `2024-08-25 09:00:00` (`^\\d{4}-\\d{2}-\\d{2} \\d{2}:\\d{2}:\\d{2}$`) |
| `end_date` | `string` | N | 结束时间 格式：2023-08-25 19:00:00 格式: `YYYY-MM-DD HH:MM:SS`, e.g. `2024-08-25 09:00:00` (`^\\d{4}-\\d{2}-\\d{2} \\d{2}:\\d{2}:\\d{2}$`) |

### `monthly`

**月线行情 OHLCV (必传 ts_code + trade_date 或 start_date+end_date)**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `ts_code` | `string` | N | TS代码 （ts_code,trade_date两个参数任选一） 格式: 6位数字+交易所后缀, e.g. `600519.SH` / `000001.SZ` / `830799.BJ` (`^\\d{6}\\.(SZ|SH|BJ)$`) |
| `trade_date` | `string` | N | 交易日期 （每月最后一个交易日日期，YYYYMMDD格式） 格式: YYYYMMDD, e.g. `20240115` (`^\\d{8}$`) |
| `start_date` | `string` | N | 开始日期 格式: YYYYMMDD, e.g. `20240101` (`^\\d{8}$`) |
| `end_date` | `string` | N | 结束日期 格式: YYYYMMDD, e.g. `20241231` (`^\\d{8}$`) |

### `opt_mins`

**期权分钟线**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `ts_code` | `string` | Y | 股票代码，e.g：10007976.SH 格式: 6位数字+交易所后缀, e.g. `600519.SH` / `000001.SZ` / `830799.BJ` (`^\\d{6}\\.(SZ|SH|BJ)$`) |
| `freq` | `string` | Y | 分钟频度（1min/5min/15min/30min/60min） 枚举: 'D', 'W', 'M', '1min', '5min', '15min', '30min', '60min' — D=日, W=周, M=月, 1/5/15/30/60min=分钟 (枚举: `"1min", "5min", "15min", "30min", "60min"`) |
| `start_date` | `string` | N | 开始日期 格式：2024-08-25 09:00:00 格式: `YYYY-MM-DD HH:MM:SS`, e.g. `2024-08-25 09:00:00` (`^\\d{4}-\\d{2}-\\d{2} \\d{2}:\\d{2}:\\d{2}$`) |
| `end_date` | `string` | N | 结束时间 格式：2024-08-25 19:00:00 格式: `YYYY-MM-DD HH:MM:SS`, e.g. `2024-08-25 09:00:00` (`^\\d{4}-\\d{2}-\\d{2} \\d{2}:\\d{2}:\\d{2}$`) |

### `pro_bar`

**复权行情+技术指标 (必传 ts_code + start_date + end_date + adj='qfq'/'hfq'/'none' + freq='D'/'W'/'M')**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `ts_code` | `string` | Y | 格式: 6位数字+交易所后缀, e.g. `600519.SH` / `000001.SZ` / `830799.BJ` (`^\\d{6}\\.(SZ|SH|BJ)$`) |

### `stk_limit`

**每日涨跌停价格 (必传 ts_code + trade_date)**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `ts_code` | `string` | N | 股票代码 格式: 6位数字+交易所后缀, e.g. `600519.SH` / `000001.SZ` / `830799.BJ` (`^\\d{6}\\.(SZ|SH|BJ)$`) |
| `trade_date` | `string` | N | 交易日期 格式: YYYYMMDD, e.g. `20240115` (`^\\d{8}$`) |
| `start_date` | `string` | N | 开始日期 格式: YYYYMMDD, e.g. `20240101` (`^\\d{8}$`) |
| `end_date` | `string` | N | 结束日期 格式: YYYYMMDD, e.g. `20241231` (`^\\d{8}$`) |

### `stk_mins`

**股票历史分钟线 (ts_code + freq='1min'~'60min' **小写**)**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `ts_code` | `string` | Y | ETF代码，e.g. 159001.SZ 格式: 6位数字+交易所后缀, e.g. `600519.SH` / `000001.SZ` / `830799.BJ` (`^\\d{6}\\.(SZ|SH|BJ)$`) |
| `freq` | `string` | Y | 分钟频度（1min/5min/15min/30min/60min） 枚举: 'D', 'W', 'M', '1min', '5min', '15min', '30min', '60min' — D=日, W=周, M=月, 1/5/15/30/60min=分钟 (枚举: `"1min", "5min", "15min", "30min", "60min"`) |
| `start_date` | `string` | N | 开始日期 格式：2025-06-01 09:00:00 格式: `YYYY-MM-DD HH:MM:SS`, e.g. `2024-08-25 09:00:00` (`^\\d{4}-\\d{2}-\\d{2} \\d{2}:\\d{2}:\\d{2}$`) |
| `end_date` | `string` | N | 结束时间 格式：2025-06-20 19:00:00 格式: `YYYY-MM-DD HH:MM:SS`, e.g. `2024-08-25 09:00:00` (`^\\d{4}-\\d{2}-\\d{2} \\d{2}:\\d{2}:\\d{2}$`) |

### `stk_week_month_adj`

**周月线复权行情 (同 pro_bar 但返回更长周期)**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `ts_code` | `string` | N | TS代码 格式: 6位数字+交易所后缀, e.g. `600519.SH` / `000001.SZ` / `830799.BJ` (`^\\d{6}\\.(SZ|SH|BJ)$`) |
| `trade_date` | `string` | N | 交易日期（格式：YYYYMMDD，每周或每月最后一天的日期） 格式: YYYYMMDD, e.g. `20240115` (`^\\d{8}$`) |
| `start_date` | `string` | N | 开始交易日期 格式: YYYYMMDD, e.g. `20240101` (`^\\d{8}$`) |
| `end_date` | `string` | N | 结束交易日期 格式: YYYYMMDD, e.g. `20241231` (`^\\d{8}$`) |
| `freq` | `string` | Y | 频率week周，month月 枚举: 'D', 'W', 'M', '1min', '5min', '15min', '30min', '60min' — D=日, W=周, M=月, 1/5/15/30/60min=分钟 (枚举: `"D", "W", "M", "1min", "5min", "15min", "30min", "60min"`) |

### `stk_weekly_monthly`

**周月线行情 (高频首选,必传 ts_code + freq='W' 或 'M' + start_date+end_date)**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `ts_code` | `string` | N | TS代码 格式: 6位数字+交易所后缀, e.g. `600519.SH` / `000001.SZ` / `830799.BJ` (`^\\d{6}\\.(SZ|SH|BJ)$`) |
| `trade_date` | `string` | N | 交易日期(格式：YYYYMMDD，每周或每月最后一天的日期） 格式: YYYYMMDD, e.g. `20240115` (`^\\d{8}$`) |
| `start_date` | `string` | N | 开始交易日期 格式: YYYYMMDD, e.g. `20240101` (`^\\d{8}$`) |
| `end_date` | `string` | N | 结束交易日期 格式: YYYYMMDD, e.g. `20241231` (`^\\d{8}$`) |
| `freq` | `string` | Y | 频率week周，month月 枚举: 'D', 'W', 'M', '1min', '5min', '15min', '30min', '60min' — D=日, W=周, M=月, 1/5/15/30/60min=分钟 (枚举: `"D", "W", "M", "1min", "5min", "15min", "30min", "60min"`) |

### `suspend_d`

**停复牌记录 (必传 ts_code + start_date + end_date)**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `ts_code` | `string` | N | 股票代码(可输入多值) 格式: 6位数字+交易所后缀, e.g. `600519.SH` / `000001.SZ` / `830799.BJ` (`^\\d{6}\\.(SZ|SH|BJ)$`) |
| `trade_date` | `string` | N | 交易日日期 格式: YYYYMMDD, e.g. `20240115` (`^\\d{8}$`) |
| `start_date` | `string` | N | 停复牌查询开始日期 格式: YYYYMMDD, e.g. `20240101` (`^\\d{8}$`) |
| `end_date` | `string` | N | 停复牌查询结束日期 格式: YYYYMMDD, e.g. `20241231` (`^\\d{8}$`) |
| `suspend_type` | `string` | N | 停复牌类型：S-停牌,R-复牌 (枚举: `"S", "R"`) |

### `weekly`

**周线行情 OHLCV (必传 ts_code + trade_date 或 start_date+end_date)**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `ts_code` | `string` | N | TS代码 （ts_code,trade_date两个参数任选一） 格式: 6位数字+交易所后缀, e.g. `600519.SH` / `000001.SZ` / `830799.BJ` (`^\\d{6}\\.(SZ|SH|BJ)$`) |
| `trade_date` | `string` | N | 交易日期 （每周最后一个交易日期，YYYYMMDD格式） 格式: YYYYMMDD, e.g. `20240115` (`^\\d{8}$`) |
| `start_date` | `string` | N | 开始日期 格式: YYYYMMDD, e.g. `20240101` (`^\\d{8}$`) |
| `end_date` | `string` | N | 结束日期 格式: YYYYMMDD, e.g. `20241231` (`^\\d{8}$`) |

---

## 股票-财务数据 (17)

### `balancesheet`

**资产负债表 (总资产/负债/股东权益, 必传 ts_code + 可选 period)**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `ts_code` | `string` | Y | 股票代码 格式: 6位数字+交易所后缀, e.g. `600519.SH` / `000001.SZ` / `830799.BJ` (`^\\d{6}\\.(SZ|SH|BJ)$`) |
| `ann_date` | `string` | N | 公告日期(YYYYMMDD格式，下同) 格式: YYYYMMDD (`^\\d{8}$`) |
| `start_date` | `string` | N | 公告日开始日期 格式: YYYYMMDD, e.g. `20240101` (`^\\d{8}$`) |
| `end_date` | `string` | N | 公告日结束日期 格式: YYYYMMDD, e.g. `20241231` (`^\\d{8}$`) |
| `period` | `string` | N | 报告期(每个季度最后一天的日期，比如20171231表示年报，20170630半年报，20170930三季报) |
| `report_type` | `string` | N | 报告类型：见下方详细说明 |
| `comp_type` | `string` | N | 公司类型：1一般工商业 2银行 3保险 4证券 |

### `balancesheet_vip`

**VIP 资产负债表 (字段更细, 需 V2 高级 token)**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `ts_code` | `string` | Y | 格式: 6位数字+交易所后缀, e.g. `600519.SH` / `000001.SZ` / `830799.BJ` (`^\\d{6}\\.(SZ|SH|BJ)$`) |

### `cashflow`

**现金流量表 (经营/投资/筹资三大现金流, 必传 ts_code + 可选 period)**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `ts_code` | `string` | Y | 股票代码 格式: 6位数字+交易所后缀, e.g. `600519.SH` / `000001.SZ` / `830799.BJ` (`^\\d{6}\\.(SZ|SH|BJ)$`) |
| `ann_date` | `string` | N | 公告日期（YYYYMMDD格式，下同） 格式: YYYYMMDD (`^\\d{8}$`) |
| `f_ann_date` | `string` | N | 实际公告日期 |
| `start_date` | `string` | N | 公告日开始日期 格式: YYYYMMDD, e.g. `20240101` (`^\\d{8}$`) |
| `end_date` | `string` | N | 公告日结束日期 格式: YYYYMMDD, e.g. `20241231` (`^\\d{8}$`) |
| `period` | `string` | N | 报告期(每个季度最后一天的日期，比如20171231表示年报，20170630半年报，20170930三季报) |
| `report_type` | `string` | N | 报告类型：见下方详细说明 |
| `comp_type` | `string` | N | 公司类型：1一般工商业 2银行 3保险 4证券 |
| `is_calc` | `integer` | N | 是否计算报表 |

### `cashflow_vip`

**VIP 现金流量表 (字段更细, 需 V2 高级 token)**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `ts_code` | `string` | Y | 格式: 6位数字+交易所后缀, e.g. `600519.SH` / `000001.SZ` / `830799.BJ` (`^\\d{6}\\.(SZ|SH|BJ)$`) |

### `disclosure_date`

**财报披露日期 (计划披露日 vs 实际披露日, 必传 ts_code + end_date)**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `ts_code` | `string` | N | TS股票代码 格式: 6位数字+交易所后缀, e.g. `600519.SH` / `000001.SZ` / `830799.BJ` (`^\\d{6}\\.(SZ|SH|BJ)$`) |
| `end_date` | `string` | N | 财报周期（每个季度最后一天的日期，比如20181231表示2018年年报，20180630表示中报) 格式: YYYYMMDD, e.g. `20241231` (`^\\d{8}$`) |
| `pre_date` | `string` | N | 计划披露日期 |
| `ann_date` | `string` | N | 最新披露公告日 格式: YYYYMMDD (`^\\d{8}$`) |
| `actual_date` | `string` | N | 实际披露日期 |

### `dividend`

**分红送股记录 (每股分红+送转股+股权登记日)**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `ts_code` | `string` | N | TS代码 格式: 6位数字+交易所后缀, e.g. `600519.SH` / `000001.SZ` / `830799.BJ` (`^\\d{6}\\.(SZ|SH|BJ)$`) |
| `ann_date` | `string` | N | 公告日 格式: YYYYMMDD (`^\\d{8}$`) |
| `record_date` | `string` | N | 股权登记日期 格式: YYYYMMDD (`^\\d{8}$`) |
| `ex_date` | `string` | N | 除权除息日 格式: YYYYMMDD (`^\\d{8}$`) |
| `imp_ann_date` | `string` | N | 实施公告日 格式: YYYYMMDD (`^\\d{8}$`) |

### `express`

**业绩快报 (财报正式发布前的简版, 必传 ts_code 或 ann_date 范围)**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `ts_code` | `string` | Y | 股票代码 格式: 6位数字+交易所后缀, e.g. `600519.SH` / `000001.SZ` / `830799.BJ` (`^\\d{6}\\.(SZ|SH|BJ)$`) |
| `ann_date` | `string` | N | 公告日期 格式: YYYYMMDD (`^\\d{8}$`) |
| `start_date` | `string` | N | 公告开始日期 格式: YYYYMMDD, e.g. `20240101` (`^\\d{8}$`) |
| `end_date` | `string` | N | 公告结束日期 格式: YYYYMMDD, e.g. `20241231` (`^\\d{8}$`) |
| `period` | `string` | N | 报告期(每个季度最后一天的日期,比如20171231表示年报，20170630半年报，20170930三季报) |

### `express_vip`

**VIP 业绩快报**

### `fina_audit`

**财务审计意见 (审计机构+费用+审计结果)**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `ts_code` | `string` | Y | 股票代码 格式: 6位数字+交易所后缀, e.g. `600519.SH` / `000001.SZ` / `830799.BJ` (`^\\d{6}\\.(SZ|SH|BJ)$`) |
| `ann_date` | `string` | N | 公告日期 格式: YYYYMMDD (`^\\d{8}$`) |
| `start_date` | `string` | N | 公告开始日期 格式: YYYYMMDD, e.g. `20240101` (`^\\d{8}$`) |
| `end_date` | `string` | N | 公告结束日期 格式: YYYYMMDD, e.g. `20241231` (`^\\d{8}$`) |
| `period` | `string` | N | 报告期(每个季度最后一天的日期,比如20171231表示年报) |

### `fina_indicator`

**财务指标 (ROE/EPS/PE/PB/资产负债率, 必传 ts_code + 可选 period)**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `ts_code` | `string` | Y | TS股票代码,e.g. 600001.SH/000001.SZ 格式: 6位数字+交易所后缀, e.g. `600519.SH` / `000001.SZ` / `830799.BJ` (`^\\d{6}\\.(SZ|SH|BJ)$`) |
| `ann_date` | `string` | N | 公告日期 格式: YYYYMMDD (`^\\d{8}$`) |
| `start_date` | `string` | N | 报告期开始日期 格式: YYYYMMDD, e.g. `20240101` (`^\\d{8}$`) |
| `end_date` | `string` | N | 报告期结束日期 格式: YYYYMMDD, e.g. `20241231` (`^\\d{8}$`) |
| `period` | `string` | N | 报告期(每个季度最后一天的日期,比如20171231表示年报) |

### `fina_indicator_vip`

**VIP 财务指标 (字段更全)**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `ts_code` | `string` | Y | 格式: 6位数字+交易所后缀, e.g. `600519.SH` / `000001.SZ` / `830799.BJ` (`^\\d{6}\\.(SZ|SH|BJ)$`) |

### `fina_mainbz`

**主营业务构成 (按产品/地区/行业拆分营收, 必传 ts_code + period + type='P'产品/'D'地区/'I'行业)**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `ts_code` | `string` | Y | 股票代码 格式: 6位数字+交易所后缀, e.g. `600519.SH` / `000001.SZ` / `830799.BJ` (`^\\d{6}\\.(SZ|SH|BJ)$`) |
| `period` | `string` | N | 报告期(每个季度最后一天的日期,比如20171231表示年报) |
| `type` | `string` | N | 类型：P按产品 D按地区 I按行业（请输入大写字母P或者D） |
| `start_date` | `string` | N | 报告期开始日期 格式: YYYYMMDD, e.g. `20240101` (`^\\d{8}$`) |
| `end_date` | `string` | N | 报告期结束日期 格式: YYYYMMDD, e.g. `20241231` (`^\\d{8}$`) |

### `fina_mainbz_vip`

**VIP 主营业务**

### `forecast`

**业绩预告 (预计净利润变动方向+幅度, 必传 ts_code 或 ann_date 范围)**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `ts_code` | `string` | N | 股票代码(二选一) 格式: 6位数字+交易所后缀, e.g. `600519.SH` / `000001.SZ` / `830799.BJ` (`^\\d{6}\\.(SZ|SH|BJ)$`) |
| `ann_date` | `string` | N | 公告日期 (二选一) 格式: YYYYMMDD (`^\\d{8}$`) |
| `start_date` | `string` | N | 公告开始日期 格式: YYYYMMDD, e.g. `20240101` (`^\\d{8}$`) |
| `end_date` | `string` | N | 公告结束日期 格式: YYYYMMDD, e.g. `20241231` (`^\\d{8}$`) |
| `period` | `string` | N | 报告期(每个季度最后一天的日期，比如20171231表示年报，20170630半年报，20170930三季报) |
| `type` | `string` | N | 预告类型(预增/预减/扭亏/首亏/续亏/续盈/略增/略减) |

### `forecast_vip`

**VIP 业绩预告**

### `income`

**利润表 (营收/成本/各项费用/净利润, 必传 ts_code, 可选 period='YYYYMMDD' 报告期末)**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `ts_code` | `string` | Y | 股票代码 格式: 6位数字+交易所后缀, e.g. `600519.SH` / `000001.SZ` / `830799.BJ` (`^\\d{6}\\.(SZ|SH|BJ)$`) |
| `ann_date` | `string` | N | 公告日期（YYYYMMDD格式，下同） 格式: YYYYMMDD (`^\\d{8}$`) |
| `f_ann_date` | `string` | N | 实际公告日期 |
| `start_date` | `string` | N | 公告日开始日期 格式: YYYYMMDD, e.g. `20240101` (`^\\d{8}$`) |
| `end_date` | `string` | N | 公告日结束日期 格式: YYYYMMDD, e.g. `20241231` (`^\\d{8}$`) |
| `period` | `string` | N | 报告期(每个季度最后一天的日期，比如20171231表示年报，20170630半年报，20170930三季报) |
| `report_type` | `string` | N | 报告类型，参考文档最下方说明 |
| `comp_type` | `string` | N | 公司类型（1一般工商业2银行3保险4证券） |

### `income_vip`

**VIP 利润表 (字段比标准 income 多, 需 V2 高级 token)**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `ts_code` | `string` | Y | 格式: 6位数字+交易所后缀, e.g. `600519.SH` / `000001.SZ` / `830799.BJ` (`^\\d{6}\\.(SZ|SH|BJ)$`) |

---

## 资讯 (2)

### `major_news`

**重要资讯**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `src` | `string` | N | 新闻来源（新华网、凤凰财经、同花顺、新浪财经、华尔街见闻、中证网、财新网、第一财经、财联社） (枚举: `"SINA", "Wallstreetcn", "10jqka", "THS", "Eastmoney"`) |
| `start_date` | `string` | N | 新闻发布开始时间，e.g. 2018-11-21 00:00:00 格式: YYYYMMDD, e.g. `20240101` (`^\\d{8}$`) |
| `end_date` | `string` | N | 新闻发布结束时间，e.g. 2018-11-22 00:00:00 格式: YYYYMMDD, e.g. `20241231` (`^\\d{8}$`) |

### `news`

**新闻资讯 (start_date+end_date, src='cls'财联社/'sina'/'ths'同花顺/'eastmoney'东方财富)**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `start_date` | `string` | Y | 开始日期(格式：2018-11-20 09:00:00） 格式: YYYYMMDD, e.g. `20240101` (`^\\d{8}$`) |
| `end_date` | `string` | Y | 结束日期 格式: YYYYMMDD, e.g. `20241231` (`^\\d{8}$`) |
| `src` | `string` | Y | 新闻来源 见下表 (枚举: `"SINA", "Wallstreetcn", "10jqka", "THS", "Eastmoney"`) |

---

## 资金流向 (8)

### `moneyflow`

**个股资金流向 (大单/特大单净流, 必传 ts_code + trade_date)**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `ts_code` | `string` | N | 股票代码 （股票和时间参数至少输入一个） 格式: 6位数字+交易所后缀, e.g. `600519.SH` / `000001.SZ` / `830799.BJ` (`^\\d{6}\\.(SZ|SH|BJ)$`) |
| `trade_date` | `string` | N | 交易日期 格式: YYYYMMDD, e.g. `20240115` (`^\\d{8}$`) |
| `start_date` | `string` | N | 开始日期 格式: YYYYMMDD, e.g. `20240101` (`^\\d{8}$`) |
| `end_date` | `string` | N | 结束日期 格式: YYYYMMDD, e.g. `20241231` (`^\\d{8}$`) |

### `moneyflow_cnt_ths`

**同花顺概念资金流**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `ts_code` | `string` | N | 代码 格式: 6位数字+交易所后缀, e.g. `600519.SH` / `000001.SZ` / `830799.BJ` (`^\\d{6}\\.(SZ|SH|BJ)$`) |
| `trade_date` | `string` | N | 交易日期(格式：YYYYMMDD，下同) 格式: YYYYMMDD, e.g. `20240115` (`^\\d{8}$`) |
| `start_date` | `string` | N | 开始日期 格式: YYYYMMDD, e.g. `20240101` (`^\\d{8}$`) |
| `end_date` | `string` | N | 结束日期 格式: YYYYMMDD, e.g. `20241231` (`^\\d{8}$`) |

### `moneyflow_dc`

**东方财富个股资金流**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `ts_code` | `string` | N | 股票代码 格式: 6位数字+交易所后缀, e.g. `600519.SH` / `000001.SZ` / `830799.BJ` (`^\\d{6}\\.(SZ|SH|BJ)$`) |
| `trade_date` | `string` | N | 交易日期（YYYYMMDD格式，下同） 格式: YYYYMMDD, e.g. `20240115` (`^\\d{8}$`) |
| `start_date` | `string` | N | 开始日期 格式: YYYYMMDD, e.g. `20240101` (`^\\d{8}$`) |
| `end_date` | `string` | N | 结束日期 格式: YYYYMMDD, e.g. `20241231` (`^\\d{8}$`) |

### `moneyflow_hsgt`

**沪深港通北向资金 (hgt 沪股通/sgt 深股通/north_money 合计, trade_date 必填)**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `trade_date` | `string` | N | 交易日期 (二选一) 格式: YYYYMMDD, e.g. `20240115` (`^\\d{8}$`) |
| `start_date` | `string` | N | 开始日期 (二选一) 格式: YYYYMMDD, e.g. `20240101` (`^\\d{8}$`) |
| `end_date` | `string` | N | 结束日期 格式: YYYYMMDD, e.g. `20241231` (`^\\d{8}$`) |

### `moneyflow_ind_dc`

**东方财富行业资金流**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `ts_code` | `string` | N | 代码 格式: 6位数字+交易所后缀, e.g. `600519.SH` / `000001.SZ` / `830799.BJ` (`^\\d{6}\\.(SZ|SH|BJ)$`) |
| `trade_date` | `string` | N | 交易日期（YYYYMMDD格式，下同） 格式: YYYYMMDD, e.g. `20240115` (`^\\d{8}$`) |
| `start_date` | `string` | N | 开始日期 格式: YYYYMMDD, e.g. `20240101` (`^\\d{8}$`) |
| `end_date` | `string` | N | 结束日期 格式: YYYYMMDD, e.g. `20241231` (`^\\d{8}$`) |
| `content_type` | `string` | N | 资金类型(行业、概念、地域) |

### `moneyflow_ind_ths`

**同花顺行业资金流**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `ts_code` | `string` | N | 代码 格式: 6位数字+交易所后缀, e.g. `600519.SH` / `000001.SZ` / `830799.BJ` (`^\\d{6}\\.(SZ|SH|BJ)$`) |
| `trade_date` | `string` | N | 交易日期(YYYYMMDD格式，下同) 格式: YYYYMMDD, e.g. `20240115` (`^\\d{8}$`) |
| `start_date` | `string` | N | 开始日期 格式: YYYYMMDD, e.g. `20240101` (`^\\d{8}$`) |
| `end_date` | `string` | N | 结束日期 格式: YYYYMMDD, e.g. `20241231` (`^\\d{8}$`) |

### `moneyflow_mkt_dc`

**东方财富市场总资金流**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `trade_date` | `string` | N | 交易日期(YYYYMMDD格式，下同） 格式: YYYYMMDD, e.g. `20240115` (`^\\d{8}$`) |
| `start_date` | `string` | N | 开始日期 格式: YYYYMMDD, e.g. `20240101` (`^\\d{8}$`) |
| `end_date` | `string` | N | 结束日期 格式: YYYYMMDD, e.g. `20241231` (`^\\d{8}$`) |

### `moneyflow_ths`

**同花顺个股资金流**

**输入参数**

| 名称 | 类型 | 必选 | 描述 |
|------|------|:----:|------|
| `ts_code` | `string` | N | 股票代码 格式: 6位数字+交易所后缀, e.g. `600519.SH` / `000001.SZ` / `830799.BJ` (`^\\d{6}\\.(SZ|SH|BJ)$`) |
| `trade_date` | `string` | N | 交易日期（YYYYMMDD格式，下同） 格式: YYYYMMDD, e.g. `20240115` (`^\\d{8}$`) |
| `start_date` | `string` | N | 开始日期 格式: YYYYMMDD, e.g. `20240101` (`^\\d{8}$`) |
| `end_date` | `string` | N | 结束日期 格式: YYYYMMDD, e.g. `20241231` (`^\\d{8}$`) |

---
