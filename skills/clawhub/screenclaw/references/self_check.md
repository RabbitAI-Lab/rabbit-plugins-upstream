---
name: screenclaw-self-check
description: screenclaw自检程序。当服务端要求 self_check 时读取本文档，并按照文档要求执行。
---

# ScreenClaw 自检清单

当 API 返回 `SELF_CHECK_REQUIRED` 时，阅读理解本文档，并按照文档要求执行。

本文档目的是：为了让你重新装载关键上下文，以便在任务中正确使用 ScreenClaw 相关指令，避免因上下文衰减导致任务失败。

## 步骤1：总结任务和经验
1. 总结当前任务进展
2. 总结自上次自检后，你从成功和失败案例里学习了什么内容，这些内容对本次任务有什么帮助
3. 如果当前任务命中了场景模板，必须说明 `active_template_path`、`current_node`、`completed_nodes`、`next_node`，以及当前是否仍按模板执行

## 步骤2：更新待办
1. 更新、动态调整任务待办
2. 如果当前待办没有包含“截图理解、裁剪放大、marker 反验、操作后截图验证”，按任务风险补齐
3. 如果当前任务命中了场景模板，待办必须按模板节点组织；不要因为自检而切回无模板的通用流程
4. 如果模板节点多次失败、界面与模板明显不符、产品更新或坐标/窗口信息失效，将失败节点标记为“模板节点降级”，从该节点开始使用通用截图推理和坐标校正继续完成目标

## 步骤3：装载关键上下文

### 模板执行上下文
如果当前任务命中了场景模板，自检时必须重新打开当前模板文件，恢复以下内容：

1. 模板路径：`active_template_path`
2. 当前节点：`current_node`
3. 已完成节点：`completed_nodes`
4. 下一节点：`next_node`
5. 当前节点的操作层级、窗口组、坐标复用策略、验证标准和异常处理

模板是当前任务的主控文档。通用工作循环只用于模板节点里的截图、定位、marker、执行和验证；不得因为上下文衰减而忘记模板、跳过模板节点或自行改写业务流。

模板也可能过期或沉淀不足。若同一模板节点多次执行失败，或截图事实证明界面与模板不一致，应保留模板的业务目标和已完成节点，将失败节点降级为通用视觉操作：重新截图理解界面，按当前 UI 读取坐标和推理动作，操作后截图验证，再决定是否回到模板后续节点。

### 坐标概念：
ScreenClaw 对软件截图时，会在截图上叠加一层坐标网格。在网格交叉点上，有坐标数字。即，图片上自带了坐标，不需要你转换你自己内部的归一化坐标，你只需要读出来。坐标格式为 XxY（如 50x35），表示截图中距左边界X%、距上边界Y%的位置。

目标元素可能被多个交叉点覆盖。任意一个交叉点的坐标就是目标元素的坐标。
坐标的准确性取决于读坐标方法论。

注意：坐标数字通过"x"字符来分隔，X坐标 x Y坐标。坐标数字范围是0-100


### 读坐标程序
高精度场景（误差低于10px）、高风险场景（只有一次机会）必须执行完整程序。
1. 全面了解+大概定位
    - 彩色截图了解当前页面有哪些区域、每个区域的重点信息是什么。理解每个区域的作用
    - 找到目标元素的大概位置（如在窗口的哪个区域，靠近哪个明显元素等），学习目标元素的特征（直到目标元素应该长什么样），获取大概坐标。若不存在，调整截图参数
2. 裁剪放大+精细定位
    - 基于目标的大概坐标，调用 crop_zoom_screenshot ,裁剪并放大局部，基于全局理解的目标元素特征，在局部中寻找目标元素。
    - 读出数字：为了避免数字太多而遮挡内容，网格不是每个交叉点都有数字，你需要基于网格密度、目标附近的其它交叉点上的坐标数字，计算出目标元素的坐标数字。例如，网格密度为3，数字密度为2，那 A-B-C三个交叉点，B没有坐标数字展示，你需要将X(A)+3=X(B)。
    - 当你在裁剪的图片里找不到或者找不全目标，就说明裁剪参数错误，重新调整
3. 标记点反验
    - 证伪论：认定标记点上没有目标元素，基于标记点坐标，你到完整截图或裁剪后的截图里，找到该坐标上有什么对象。再评估该对象是否与目标元素匹配。
    - 当你要猜标记点，就说明标记点参数错误，重新调整


### 截图参数调整指南
#### 请求参数

| 参数 | 类型 | 默认值 | 说明 | 调整方案 |
|------|------|--------|------|----------|
| `window_id` | int | 是 | - | 目标窗口句柄 |截图没有目标元素，就更换窗口 |
| `main_window_id` | int | 是 | - | 主窗口ID（用于激活最小化窗口） | |
| `coordinate_type` | string | 否 | "grid" | 坐标类型：grid（带网格）/no（无网格） | 定位坐标场景，带网格。内容汇报、分析内容场景，不带网格 |
| `color_mode` | string | 否 | "grayscale" | 颜色模式：grayscale（灰度）/color（原色） | 原色用于整体理解内容。灰度用于排除颜色干扰，增大网格与内容对比度，也排除彩色对关键内容的影响 |

#### 网格参数（coordinate_type=grid时）

| 参数 | 类型 | 默认值 | 说明 | 调整方案 |
|------|------|--------|------|----------|
| `grid.density_x` | float | 5.0 | 水平网格密度百分比（0-100），支持1位小数。5.5就代表每5.5%就画一条线 | 网格横线之间间距大，没有覆盖到目标元素，就减小数值。若间距太小，看不到目标元素，或目标被遮挡，就增大 |
| `grid.density_y` | float | 5.0 | 垂直网格密度百分比（0-100），支持1位小数。5.5就代表每5.5%就画一条线 | 网格竖线之间间距大，没有覆盖到目标元素，减小数值。反之增大 |
| `grid.opacity` | int | 50 | 网格透明度(0-100) | 若网格密度不能增加，再增加就导致目标元素没有交叉点。那只能让网格更透明。数字越小越透明 |
| `grid.color` | string | "#ff0000" | 网格颜色(HEX) | 如果网格与截图内容的对比度不够大，就变更颜色，增大差异，让网格更明显 |

**density_x / density_y 说明**：
- 水平和垂直网格密度可独立设置，适用于不同宽高比的窗口（如竖屏手机模拟器 vs 横屏显示器）
- 示例：`density_x=5.0, density_y=10.0` 表示水平每5%一条竖线，垂直每10%一条横线
- 支持小数（如 `3.3`），适用于4K高分辨率截图的精细定位

**注意**：目标元素必须有网格的交叉点，才能让坐标精准。若无交叉点覆盖，建议调整网格参数，不能推测坐标。

#### 数字参数（coordinate_type=grid时）

| 参数 | 类型 | 默认值 | 说明 | 调整方案 |
|------|------|--------|------|----------|
| `coordinate.number_density` | int | 2 | 数字显示密度 | 数字是多少，就每隔多少个网格展示坐标数字。例如2就是，每隔2格才有坐标。数字越小越密，有坐标数字的交叉点就越多。但可能会遮挡目标元素。数字越大，有数字的交叉点越小，可能会导致你不知道目标元素的具体坐标数字，毕竟只有网格你会读不出来。配合网格密度，你就能从周围的坐标数字计算出目标坐标。网格密度越大，建议数字越稀疏 |
| `coordinate.number_decimal` | int | 1 | 小数位数(0-4) | 4位小数实际在坐标数字上会展示成2位，因为坐标是百分比。当网格密度大时，就需要更精细的数字 |
| `coordinate.number_size` | int | 14 | 字体大小(4-64) | 数字字体很小，你看不清楚时就增大。增大到不要遮挡太多元素就行。优先推荐调整。中度幅度增大，不要仅仅步进1 |
| `coordinate.number_color` | string | "#ff0000" | 数字颜色(HEX) | 数字颜色与截图内容的对比度不高，导致看不清时，就更换颜色 |
| `coordinate.number_opacity` | int | 100 | 数字透明度(0-100) | 数字遮挡了目标元素，但数字密度和大小都调整无效，就调整透明度。数字越小越透明 |

**注意**：如果裁剪放大后，目标元素周围的交叉点都看不到数字，就调整数字密度、数字大小。


### 裁剪参数调整指南

1. 裁剪后仍看不清 → 缩小crop_width/crop_height，增大zoom_scale
2. 没看到目标元素 → 裁剪位置错了，也就是中心点坐标读错了，导致裁剪错误。可以增大crop的宽高，读放大图去找。也可以重新从全局里寻找大概坐标，重新裁剪放大找。
3. 有目标元素，但不全 → 增大crop_width/crop_height
4. 渐进式放大：先用大crop区域（如30%）粗看，再缩小（如10%）精看
5. 可重复使用：对同一张source_image_path可以多次裁剪，调整参数即可

### 标记点调整指南
`marker` 支持传入单个对象或数组，用于在截图上标记坐标位置（外圈空心圆 + 中心实心点）。你可通过标记预览或确认操作坐标是否准确，调整后再执行操作。

每个标记点参数：

| 参数 | 类型 | 默认值 | 说明 | 调整方案 |
|------|------|--------|------|----------|
| `x` | float | - | 标记点横坐标百分比（0-100） | 待确认的坐标 |
| `y` | float | - | 标记点纵坐标百分比（0-100） | 待确认的坐标 |
| `ring_radius` | int | 12 | 外圈空心圆半径（像素） | 标记点外圈的半径，数字越大，圆越大，但可能会遮挡其它元素 |
| `ring_line_width` | int | 2 | 外圈线宽（像素） | 数字越大越粗，但可能会遮挡其它元素 |
| `ring_color` | string | "#FF0000" | 外圈颜色（HEX） | 如果外圈与截图内容对比度不够大，就更换颜色。标记点的颜色不建议与网格颜色一致 |
| `dot_radius` | int | 3 | 中心实心圆半径（像素） | 数字越大越粗，但可能会遮挡其它元素 |
| `dot_color` | string | "#FF0000" | 中心实心圆颜色（HEX） | 如果内圈与截图内容对比度不够大，就更换颜色 |


### 其它内容
有疑惑、有问题，重新阅读`skill.md`


## 步骤4：总结自检内容
**核心关键**：不要为了通过，敷衍了事，不能直接从会话记忆里构造自检内容。每次自检，必须重新阅读 `self_check.md`文档，这样你才能重载关键上下文偷懒只会让你逐渐偏离正轨，无法完成目标，浪费用户token。

将你的总结自检内容传输给API。同时，向用户输出，表达你正在一边学习一边完成任务，没在偷懒。

以下内容，建议包含在自检内容里，加强你的上下文记忆：
1. 你本次总结，是否重新阅读了自检文档，是否重载了关键上下文
2. 本次会话关键成功经验、失败反思
3. 验证结果不符合期望，99%问题，是坐标问题。坐标问题就去调节截图参数、裁剪参数。
4. 当前下一步要执行的具体动作，以及为什么这样做
5. 已确认的关键坐标、窗口、页面状态、截图参数
6. 如果正在执行模板，必须包含模板路径、当前节点、下一节点、已完成节点；若已降级，说明是哪个节点降级以及降级原因

```text
我已执行自检程序：当前任务是{任务目标}，已完成{已完成步骤}，下一步是{下一步动作}。{如果使用模板：active_template_path={模板路径}，current_node={当前节点}，completed_nodes={已完成节点}，next_node={下一节点}，当前仍按模板执行/节点{节点名}已因{原因}降级到通用视觉流程。}自上次自检后，我确认了{关键窗口/页面状态/坐标/截图参数}；成功经验是{哪些截图参数、裁剪区域、marker 或操作方式有效}；失败反思是{哪些坐标、窗口、参数或推测导致偏差}。后续读坐标必须直接依据网格交叉点上的 XxY 数字，不能使用内部归一化坐标或凭感觉推测；高风险或首次动态坐标要先裁剪放大，再用 marker 反验，先找标记点实际落在哪里，再判断是否等于目标；操作后必须截图验证结果。如果模板节点连续失败，我会保留模板业务目标和已完成节点，从失败节点开始重新截图推理；如果非模板坐标连续微调失败，我会停止试探，重新阅读 skill.md 和对应 API 文档。
```
