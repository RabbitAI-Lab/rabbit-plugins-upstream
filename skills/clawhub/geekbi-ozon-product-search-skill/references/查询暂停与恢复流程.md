# 查询暂停与恢复流程

当脚本返回退出码 `2` 或响应包含 `actionRequired` 时：

1. 立即停止当前查询和分析。
2. 只向用户展示一级中文提示与有效操作链接；不要展示 token、设备码、请求头、认证文件、原始错误 JSON 或异常栈。
3. 等待用户完成登录、授权、额度、开通或升级操作。
4. 用户回复“继续”或确认完成后，使用原站点和原参数原样重跑。
5. 只有真实接口成功返回后才能继续形成结论。

退出码 `1` 是普通查询失败，应给出简短错误原因；退出码 `0` 才表示查询成功。
