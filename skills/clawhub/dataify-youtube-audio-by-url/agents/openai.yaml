interface:
  display_name: "Dataify YouTube Audio By URL"
  short_description: "Collect YouTube Audio By URL data and return results"
  icon_small: "./assets/dataify-youtube-audio-by-url-small.svg"
  icon_large: "./assets/dataify-youtube-audio-by-url-large.svg"
  default_prompt: "Use $dataify-youtube-audio-by-url to complete the requested Dataify collection, wait for the asynchronous task, and return the final collected result. Stop at task submission only when I explicitly request no-wait behavior."

policy:
  allow_implicit_invocation: true
