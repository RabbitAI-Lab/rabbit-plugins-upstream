interface:
  display_name: "Dataify YouTube Transcript By ID"
  short_description: "Collect YouTube Transcript By ID data and return results"
  icon_small: "./assets/dataify-youtube-transcript-by-id-small.svg"
  icon_large: "./assets/dataify-youtube-transcript-by-id-large.svg"
  default_prompt: "Use $dataify-youtube-transcript-by-id to complete the requested Dataify collection, wait for the asynchronous task, and return the final collected result. Stop at task submission only when I explicitly request no-wait behavior."

policy:
  allow_implicit_invocation: true
