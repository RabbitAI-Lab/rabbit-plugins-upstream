interface:
  display_name: "Dataify YouTube Video Post"
  short_description: "Collect YouTube Video Post data and return results"
  icon_small: "./assets/dataify-youtube-video-post-small.svg"
  icon_large: "./assets/dataify-youtube-video-post-large.svg"
  default_prompt: "Use $dataify-youtube-video-post to complete the requested Dataify collection, wait for the asynchronous task, and return the final collected result. Stop at task submission only when I explicitly request no-wait behavior."

policy:
  allow_implicit_invocation: true
