# Release Guard

Local, non-destructive pre-publish checks for OpenClaw skill folders.

Version 1.0.1 removes the previous dynamic shell-expression pattern and narrows the documentation to what the script actually checks.
