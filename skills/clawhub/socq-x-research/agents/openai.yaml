interface:
  display_name: "SocQ X Research"
  short_description: "Collect and analyze public X data"
  default_prompt: "Use $socq-x-research to collect and analyze this public X research request."
dependencies:
  tools:
    - type: "mcp"
      value: "socq"
      description: "Hosted SocQ X data tools"
      transport: "streamable_http"
      url: "https://api.socq.ai/mcp?platforms=x"
