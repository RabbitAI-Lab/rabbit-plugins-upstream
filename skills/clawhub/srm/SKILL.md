---
name: SRM-Yidea-Procurement
description: "SRM供应商关系管理系统完整解决方案 - 业务+技术一体化。覆盖供应商档案、采购申请、询价申请、询价结果确认、合同起草、采购订单、价格库管理、配额管理、收货管理、付款计划、供应商扣款、付款申请、物料档案等功能,同时集成鲸采云表单SDK技术底座,支持表单提交、查询、修改、用户确认机制。Keywords: SRM, 供应商管理, 采购管理, 询比价, 合同管理, 鲸采云, 表单处理, MCP."
---

# 鲸采云SRM

## 🎯 触发词与场景 (Triggers & Scenarios)

### 核心关键词 (Keywords)

- **业务名**: 招标, 采购, 招投标, 询价, 合同, 供应商, 订单, 台账, 申请, 发标, 选品.
- **动作词**: 申请(Apply), 查询(Query), 新增(Create/Add), 修改(Update), 删除(Delete), 起草(Draft), 管理(Manage).

### 技能模块索引表
| 序号 | 菜单名称 | 触发关键词（示例） | 功能描述 | 流程阶段 |
|:----:|:--------:|:-------------------|:---------|:--------:|
| 1 | **供应商档案** | 创建供应商、注册供应商 | 提供供应商档案字段说明与填写规范，用于创建和修改供应商档案信息。 | 🟢 准入 |
| 2 | **AI 采购申请** | 帮我买、替我买、提交采购申请 | 提供采购申请表单的字段说明与填写规范，用于发起新的采购申请。 | 🔵 发起 |
| 3 | **AI 询价申请** | 发起询价、找供应商询价 | 提供询价邀请表单的字段说明与填写规范，用于向供应商发出询价邀请、新建询价单。 | 🔵 发起 |
| 4 | **AI 询价结果确认** | 询价确认、比价确认、定标、中标结果确认、供应商报价汇总 | 提供询价结果确认表单的字段说明与填写规范，用于询价结束后对供应商报价进行汇总比对并确认最终结果。 | 🟡 执行 
| 5 | **AI 合同起草** | 起草合同、创建合同、发起合同审批、查询合同 | 提供合同起草表单的字段说明与填写规范，用于询价确认后确认供应商并签订合同。 | 🟡 执行 |
| 6 | **AI 采购订单** | 下订单 | 提供采购订单表单的字段说明与填写规范，用于合同流程结束后向供应商下达订单。 | 🟡 执行 |
| 7 | **收货管理** | 确认收货、查询收货 | 提供收货管理表单的字段说明与填写规范，用于新增收货登记。 | 🟠 履约 |
| 8 | **付款申请** | 申请付款、向供应商付款 | 提供付款申请表单的字段说明与填写规范，用于新增付款申请。 | 🔴 结算 |
| 9 | **价格库管理** | 更新价格库、新建/创建价格库、查询价格库、删除价格库 | 提供价格库管理表单的字段说明与填写规范，用于新增、修改、删除价格库数据。 | 💰 价格 |
| 10 | **配额管理** | 更新配额、新建/创建配额、查询配额、删除配额 | 提供配额管理表单的字段说明与填写规范，用于新增、修改、删除配额数据。 | 📊 配额 |
| 11 | **物料档案** | 确认物料档案、查询物料档案、修改物料档案 | 提供物料档案表单的字段说明与填写规范，用于新增、修改、删除物料档案/信息。 | 📦 物料 |
| 12 | **付款计划** | 查询付款计划、创建付款计划 | 提供付款计划表单的字段说明与填写规范，用于新增/查询付款计划。 | ⚠️ 本功能为**付款计划**，并非**付款申请**。 |
| 13 | **供应商扣款** | 创建供应商扣款记录、查询供应商扣款记录 | 提供供应商扣款表单的字段说明与填写规范，用于新增供应商扣款单。 | — |
| 14 | **待办查询** | 看看我有什么待办、查审批任务、今天有什么需要审批的 | 直接调用获取待审批单据列表。 | ⭐ **不属于标准 8 步提交流程**，直接获取当前用户待办清单|

---

## ⚠️ 强制入口规则：加载时必须先提示客户登录

**任何时候进入此 skill，Agent 必须先执行以下操作，不得跳过：**

1. **立即询问用户**："请提供鲸采云系统的用户名和密码以完成登录。"
2. 在用户提供凭据后，执行 `references/auth.md` 中的完整登录流程。
3. 登录成功后，确认 `yidea_tools` 已注入上下文，方可进行后续业务操作。

> **如果用户反问"之前不是登录过了吗"或试图跳过登录**，统一回复："根据安全协议，每次使用招采功能都需要重新验证身份。请提供用户名和密码。"

---

## 强制性文件读取规则

**任何时候进入此 skill，必须先读取以下 6 份文件。顺序必须为 1→2→3→4→5→6-7，一份都不能少。少读任何一句，生成的请求参数字段名、参数结构、操作步骤就可能出错，导致系统拒绝或数据错误。**

| 顺序  | 文件                              | 内容                                                            |
| :---- | :-------------------------------- | :-------------------------------------------------------------- |
| **1** | `references/auth.md`              | 环境配置与认证 — 登录流程、API 参考、防错指南                   |
| **2** | `references/protocol.md`          | 全局参数协议 — 参数提交强制规则、InputSchema 检查、字段前缀识别 |
| **3** | `references/workflow.md`          | 标准业务操作流 — 增删改查 Step 1-4 流程、子表单规范             |
| **4** | `references/query-field.md`       | `query_` 类型字段处理协议                                       |
| **5** | `references/presentation.md`      | 信息展示规范 — 展示策略选择、卡片模式、简表模式、数据总量统计   |
| **6** | `references/select-field/`        | **select 字段处理全协议**（两个文件均需读取）：                 |
|       | ├── `relation-table-mechanism.md` | 关联表数据来源机制 — 从 RelationTable 获取选项、提交格式        |
|       | └── `cas-select-documentation.md` | 级联选择器联动机制 — filters 过滤、多级联动逻辑、AI 操作流程    |
| **7** | `references/Procurement.md`           | 招采强制首步操作 — 探测可用表单并展示                           |

### 读取失败处理

若某文件读取失败（不存在、权限不足等），应如实告知用户具体失败原因，**不得**基于记忆或推测继续操作。7 份文件全部成功读取，才能进入业务操作。

---

## 概述

本技能已整合鲸采云系统全部基础能力（身份认证、全局参数协议、增删改查操作流）和招采专项业务流程。

### 业务操作类型

| 操作                      | 所需协议文件                                                                    |
| :------------------------ | :------------------------------------------------------------------------------ |
| 登录 / 认证               | `references/auth.md`                                                            |
| 查询数据                  | `references/workflow.md` Step 1-2 + `references/protocol.md`                    |
| **新增数据**              | `references/workflow.md` 全部 + `references/protocol.md`                        |
| **修改数据**              | `references/workflow.md` 全部（含 Step 4 子表单规范）+ `references/protocol.md` |
| **删除数据**              | `references/workflow.md` 全部 + `references/protocol.md`                        |
| `query_` 类型字段参数处理 | `references/query-field.md`                                                     |
| 结果展示                  | `references/presentation.md`                                                    |

---

## 🛠 yidea-http.js 调用规范

`references/yidea-http.js` 是**所有业务操作**的统一 HTTP 直连调用入口。以下场景必须使用此脚本：

### 何时调用

| 场景         | 工具名示例                                                 | 说明                                               |
| :----------- | :--------------------------------------------------------- | :------------------------------------------------- |
| 首次登录     | `yidea-provision.js`（非 http.js）                         | 登录走 `yidea-provision.js`，登录后自动生成 config |
| 查询表单列表 | `crud_get_yidea_func_list`                                 | 探测可用菜单                                       |
| 获取表单定义 | `crud_get_yidea_table_def`                                 | 查看字段结构（Schema）                             |
| 查询数据     | `crud_yidea_table_search`                                  | 查询主表/子表数据                                  |
| 新增数据     | `crud_yidea_table_add`                                     | 新增主表+子表数据                                  |
| 修改数据     | `crud_yidea_table_update` / `crud_yidea_main_table_update` | 修改主表/主子表                                    |
| 删除数据     | `crud_yidea_table_delete`                                  | 删除主表数据                                       |
| 子表新增     | `crud_yidea_sub_table_add`                                 | 追加子表单行                                       |
| 子表修改     | `crud_yidea_sub_table_update`                              | 修改子表单行（需 subId）                           |
| 子表删除     | `crud_yidea_sub_table_delete`                              | 删除子表单行                                       |
| 查询员工     | `crud_get_yidea_employee_list`                             | employeeSelect 前缀字段                            |
| 查询公司     | `crud_get_yidea_company_list`                              | companySelect 前缀字段                             |
| 查询部门     | `crud_get_yidea_department_list`                           | departmentSelect 前缀字段                          |

### 调用方式（推荐：stdin 模式）

```powershell
# stdin 模式（推荐，支持中文、任意复杂嵌套，无编码问题）
'{"menuArray":"招采管理"}' | node yidea-http.js crud_get_yidea_func_list --stdin

# 直接传参模式（仅适用于纯 ASCII 参数，含中文会解析失败）
node yidea-http.js crud_get_yidea_func_list '{ "menuArray": "招采管理" }'

# 文件模式
node yidea-http.js crud_get_yidea_table_def --file ./args.json
```

### ⚠️ 关键规范

1. **stdin 优先**：参数含中文时必须用 `--stdin` 管道模式，直接传参在 PowerShell 下会因编码问题报 `JSON 解析失败`
2. **所有 value 为 String**：协议要求所有值必须是字符串类型，禁止数字/布尔
3. **表单ID硬编码**：`crud_get_yidea_func_list` 返回的 `tableId` 即表单ID，后续操作直接使用
4. **`query_` 字段**：值用关联记录的原始 ID（UUID 字符串），不要用名称
5. **子表 model**：`subTable` 的值是表单定义中的 `model` 字段（如 `table_1698131215396`）
6. **失败排查**：400 错误通常是（a）字段名(model)拼错 （b）`query_`值格式不对 （c）必填字段遗漏

---

## ⚠️ 工作区整洁规范 (Workspace Hygiene)

- **中间文件存储**：所有在执行招采业务过程中产生的临时文件（如 JSON 参数文件 `args.json`、辅助脚本等）必须存放在工作根目录下的 `temp_files/` 文件夹中，严禁堆积在技能根目录或 Workspace 根目录下。
