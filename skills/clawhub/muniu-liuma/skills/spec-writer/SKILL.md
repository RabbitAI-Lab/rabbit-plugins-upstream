---
name: spec-writer
description: 将需求文档 / PRD / 功能想法转换为结构化 Spec（六要素框架），执行三遍校验法提取功能点、验收标准与约束，用 8 维模糊检测 + 矛盾检测生成追问清单；也可用于检查已有 Spec 或 proposal 的质量（遗漏、模糊、矛盾）。输入：需求文本 / 文件内容 / 网页内容 / 已有 Spec。输出：结构化 Spec + 待确认问题清单。典型触发词：解析 PRD、帮我写 Spec、整理需求、检查这个 Spec、审查 proposal。
---

# spec-writer：需求 → 结构化 Spec

## 何时使用

- 用户提供需求文档 / PRD / 功能描述，需要产出结构化 Spec
- 用户要求检查已有 Spec / proposal / 需求文档的质量（遗漏、模糊、矛盾）
- 用户回答追问后需要回填 Spec，或提供更新版 PRD 需要增量更新

**不适用**：已有完成的 Spec 且用户要求架构设计（使用 arch-designer）；仅讨论需求不产出文档（直接对话即可）。

## 输入

- 纯文本（粘贴或消息中的文字）
- 本地文件内容（读取后传入，如 `.md` / `.txt`）
- 网页内容（通过 WebFetch 抓取后传入）
- 已有 Spec / proposal（质量检查场景）
- 无输入时主动追问获取信息
- 多份文档：逐份处理，每份产出对应的独立 Spec，不合并；用户明确要求合并视图时，额外输出合并视图（见"多文档合并视图"），不替代独立 Spec

> **只接受文本输入**。文件读取、网页抓取、内容提取均在调用前完成。

## 前置规划（可选，Brownfield 场景）

以下场景在正式解析前先探索项目情况，将结果作为解析上下文：

1. 探索代码库 — 扫描项目目录结构、已有模块、技术栈、现有 API 风格
2. 识别相关模块 — 判断新需求涉及哪些现有模块（新增/修改）
3. 发现隐式约束 — 从现有代码中提取命名规范、目录结构、依赖版本等
4. 输出规划摘要 — 作为上下文传入三遍校验法

仅当：项目目录已有代码、需求涉及多个模块或跨系统交互、用户主动要求"先看看项目情况"。

## 执行流程：三遍校验法

### 第一遍：粗提取（广度覆盖）

通读全文，识别所有功能点和约束条件。

- 宁可多列不漏列；提取所有显式和隐式的功能需求
- 标记约束条件（技术约束、业务约束、安全约束）
- 输出：功能点清单 + 约束条件草稿

### 第二遍：细校验（精度保证）

逐段对照原文，检查第一遍的提取结果。

- 逐段扫描原始文档，对比已提取的功能点
- 补充遗漏的功能点、约束条件、业务规则
- 修正第一遍中理解偏差的条目
- **第二遍未完成前，不得输出最终结果**

### 为每个功能点生成验收标准

- 格式：Given / When / Then
- 每个功能点至少一个验收标准
- 验收标准必须是可客观验证的描述

### 第三遍：模糊检测（质量达标）

逐条检查每个功能点的表述是否明确，按以下 8 个维度检测：

| 模糊类型 | 检测方式 | 追问示例 |
|---------|---------|---------|
| **量化缺失** | 无具体数字/指标 | "快速"的具体指标是？<1s？<500ms？ |
| **主语缺失** | 谁来做？ | 前端校验还是后端校验？ |
| **条件缺失** | 什么条件？什么结果？ | 具体异常类型？提示文案/弹窗？ |
| **程度模糊** | "主流""常见"等无定义词 | "主流车型"具体指哪些？ |
| **范围模糊** | "部分""某些"无划分规则 | 划分规则是什么？按角色/标签/权限？ |
| **时态模糊** | "后续""将来"无明确版本 | 当前版本做不做？P0 还是未来规划？ |
| **参照模糊** | "类似 XX 那样"无具体指向 | 微信的哪个具体功能？交互/视觉/全部？ |
| **冲突未处理** | 多处说法不一致 | A 处说"必须登录"，B 处说"游客可用"，以哪个为准？ |

#### 矛盾检测

除单条模糊表述外，还需检测需求间的交叉矛盾：

| 矛盾类型 | 检测方式 | 追问示例 |
|---------|---------|----------|
| **功能矛盾** | 交叉对比功能点要求，检查是否有冲突 | A 要求"实时同步"，B 要求"批量处理"，以哪个为准？ |
| **约束矛盾** | 检查约束条件间是否冲突 | 技术约束要求"轻量级"，业务约束要求"支持并发1000"，是否兼容？ |
| **优先级矛盾** | 同一功能在不同段落的优先级是否一致 | 第3章说 P0，第5章说 P2，以哪个为准？ |
| **时序矛盾** | 检查依赖关系是否可行（循环依赖、不可能序列） | A 依赖 B，B 依赖 C，C 依赖 A，依赖循环如何打破？ |
| **范围矛盾** | 功能描述与范围排除/已有决策交叉检查——功能是否触碰到排除项边界，需确认归属与实现边界 | "支持整单折扣" vs "不做优惠券与营销功能"，折扣是否属于被排除的营销范围？ |

矛盾检测结果纳入模糊项表格，标注矛盾类型和影响范围（涉及哪些功能点）。

- **第三遍检测出模糊项但未生成追问，不得输出最终结果**

### 分析依赖关系

- 识别功能间的依赖关系（A 依赖 B 才能运行）
- 识别功能与已有系统的依赖

## 输出格式：六要素 Spec 框架

Spec 输出包含六个核心要素，确保下游（arch-designer）拿到完整上下文：

1. **Outcomes（预期成果）** — 需求实现后的业务目标和成功指标
2. **Scope Boundaries（范围边界）** — 明确包含什么和不包含什么
3. **功能点 + 验收标准** — 结构化提取的核心内容
4. **Constraints（约束条件）** — 技术、业务、安全约束
5. **Prior Decisions（已有决策）** — 已确定的设计方向、技术选型、架构约定
6. **Open Questions（待确认项）** — 模糊项和追问清单

```markdown
# 结构化 Spec：[文档标题]

## 预期成果
- 业务目标：...
- 成功指标：...

## 范围边界
- 包含：...
- 不包含（明确排除）：...

## 功能点清单
| ID | 功能点 | 优先级 | 关联需求 |
|----|-------|--------|----------|
| F-01 | ... | P0 | ... |

## 验收标准
| ID | 功能点 | Given | When | Then |
|----|-------|-------|------|------|
| AC-01 | F-01 | ... | ... | ... |

## 约束条件
- 技术约束：...
- 业务约束：...
- 安全约束：...

## 已有决策
- 技术选型：...
- 架构约定：...
- 设计方向：...

## 模糊项
| 问题 | 建议 | 优先级 |
|------|------|--------|
| "..."未明确 | 建议确认... | 高/中/低 |

## 依赖关系
- 本功能依赖已有功能：...
- 本功能被其他功能依赖：...
```

输出末尾附加验证标记，供下游 SKILL 检查上游产物完整性：

```markdown
<!-- CONTEXT-CHECK: F-{功能点数量}, AC-{验收标准数量}, C-{约束条件数量}, Q-{模糊项数量} -->
```

### 格式切换（可选）

默认输出 Markdown（以上模板）。用户明确要求 JSON / CSV 时：

- **JSON**：按产物章节结构输出 JSON 对象，字段映射如下，内容与执行方法完全一致，仅改变呈现格式
- **CSV**：功能点清单、验收标准、模糊项等表格类产物以 CSV 输出，每行一条记录，列名取表头
- **CONTEXT-CHECK 标记在任何格式下保留**（JSON 置于顶层字段 `context_check`，CSV 置于末尾注释行），供下游检查
- 格式切换不改变执行流程、判定口径与自检清单

```json
{
  "title": "结构化 Spec：...",
  "outcomes": {"goals": "...", "success_metrics": "..."},
  "scope": {"included": "...", "excluded": "..."},
  "features": [{"id": "F-01", "name": "...", "priority": "P0", "requirement": "..."}],
  "acceptance_criteria": [{"id": "AC-01", "feature": "F-01", "given": "...", "when": "...", "then": "..."}],
  "constraints": {"technical": "...", "business": "...", "security": "..."},
  "decisions": ["..."],
  "open_questions": [{"question": "...", "suggestion": "...", "priority": "高/中/低"}],
  "dependencies": ["..."],
  "context_check": "F-{n}, AC-{n}, C-{n}, Q-{n}"
}
```

## 追问回填机制

当用户回答追问清单中的问题后，将答案回填到 Spec：

1. 将答案回填到 Spec 对应的功能点描述中
2. 更新受影响的验收标准
3. 将已回填的模糊项状态从"待确认"更新为"已确认"
4. 如追问答案引发新的模糊项 → 补充到追问清单

回填完成后输出更新摘要："已回填 N 项，剩余 M 项待确认，新增 K 项追问。"

> 无需重新执行完整解析流程，直接在已有 Spec 上更新。

## 增量更新场景

当用户提供已解析过的 PRD 的更新版本时：

1. 对比新旧版本 PRD 的差异
2. 仅重新解析变更部分，更新 Spec 中对应的功能点、验收标准和约束条件
3. 保留未变更部分的原有 Spec 内容
4. 标注变更影响范围："以下 N 个功能点因 PRD 更新而变更，下游产物可能已过期"

### Delta 分类

增量更新时，每个功能点变更必须标注 Delta 类型：

- **ADDED**：新增功能点（包含完整功能点描述、验收标准）
- **MODIFIED**：修改已有功能点（包含变更说明 + 修改前文本 + 修改后文本）
- **REMOVED**：删除已有功能点（标注删除原因）

### 增量 Spec 输出格式

```markdown
# 增量 Spec 更新

## Delta 统计
- ADDED: N 个功能点
- MODIFIED: N 个功能点
- REMOVED: N 个功能点

## 变更详情

### ADDED
| ID | 功能点 | 优先级 | 关联需求 |
|----|-------|--------|----------|
| F-XX | ... | P1 | ... |

### MODIFIED
| ID | 功能点 | 变更说明 | 修改前 | 修改后 |
|----|-------|---------|-------|-------|
| F-XX | ... | 新增了XX条件 | 原文 | 新文 |

### REMOVED
| ID | 功能点 | 删除原因 |
|----|-------|----------|
```

> 增量更新仅在用户明确提供"更新版 PRD"时触发，普通新 PRD 仍走全量解析。

## 多文档合并视图（可选）

用户提供多份 PRD 且明确要求合并视图时，在逐份独立 Spec 之外，**额外**输出合并视图。合并视图是附加产物，**不替代也不合并独立 Spec**（与"常见错误：多份文档错误合并"划清边界——独立 Spec 始终逐份保留，合并视图仅提供跨文档视角）。

**输出格式**：

```markdown
# 多文档合并视图：[范围说明]

## 功能点总表
| 文档 | 功能点 ID | 功能点 | 优先级 |
|------|----------|--------|--------|
| PRD-01 | F1-01 | ... | P0 |
| PRD-02 | F2-01 | ... | P0 |

## 跨文档依赖
- F1-03（PRD-01）依赖 F2-01（PRD-02）：...
- 共享资源：...

## 冲突检测
- 重复功能点：F1-01 与 F2-02 描述重叠，待确认归属
- 优先级矛盾：...
- 范围矛盾：...
- 时序矛盾：...

## 待确认清单
- ...（冲突与重复项交用户裁决，不擅自合并）
```

**规则**：

- 功能点 ID 加文档前缀（F1-xx / F2-xx）防跨文档冲突
- 跨文档依赖检测维度：功能引用、数据流、共享资源
- 重复/冲突功能点显式列出并交用户裁决，不擅自合并
- 合并视图不附加 CONTEXT-CHECK（该标记仅用于单份 Spec 完整性校验），但每项标注来源文档与编号，供下游追溯
- 合并视图的依赖/冲突结论可在后续增量更新中同步修订
- 合并视图的 JSON 结构（用户要求格式切换时）：`{"title", "documents": [...], "feature_total": [{"document", "feature_id", "name", "priority"}], "cross_document_dependencies": [...], "conflicts": [{"type", "document", "detail", "status"}], "open_questions": [...]}`——与 Markdown 章节一一对应，独立 Spec 仍按单 Spec JSON 映射输出

## Spec 摘要（长文档场景）

当 Spec 超过 500 行时，在输出开头生成摘要：

```markdown
## Spec 摘要
| 章节 | 核心内容 |
|------|----------|
| 预期成果 | ... 1-2 句话 |
| 范围边界 | ... 1-2 句话 |
| 功能点 | N 个功能点，核心：F-01, F-02, F-03 |
| 验收标准 | N 条，覆盖全部功能点 |
| 约束条件 | N 条技术约束，N 条业务约束 |
| 已有决策 | ... 1-2 句话 |
| 模糊项 | N 项待确认 |
```

## 自检清单

输出最终结果前逐项检查，未全部通过不得输出：

- [ ] 所有功能点（含隐式的）均已提取，无遗漏
- [ ] 每个功能点至少有一条 Given/When/Then 验收标准
- [ ] 模糊项全部标记并生成追问，无"默认理解"项
- [ ] 交叉矛盾已检测并纳入模糊项表格（含范围矛盾：功能描述 vs 范围排除/已有决策）
- [ ] 优先级（P0/P1/P2）已区分
- [ ] 六要素齐全，CONTEXT-CHECK 标记已附加
- [ ] 未修改用户原始意图，仅做结构化提取

## 边界与常见错误

- **不做什么**：不进行架构设计（用 arch-designer）、不拆解任务（用 task-planner）、不输出代码
- **常见错误**：第二遍未完成就输出；检测到模糊项但不追问；把"我认为"当作需求写入 Spec；未经用户要求合并多份文档（合并视图须显式请求，默认逐份独立 Spec）；功能描述与范围排除项交叉矛盾漏检（如"支持折扣" vs "不做营销功能"）
- **输入缺失**：无需求文本时主动追问，不凭空生成 Spec
