---
name: audit-trace
description: 汇聚开发全流程产物与实现代码，执行八维完工审计（功能覆盖、架构落地、无主代码、任务闭环、验收达标、实现指导对照、测试结果核对、证据核对），支持对抗性审查（Builder/Critic 角色分离、差异对照）与三层审计门控（Quality/Review/Acceptance），输出审计报告与交付状态判定。输入：Spec / 架构方案 / 任务清单 / 实现指导（至少提供 Spec+代码）+ 实现代码 +（可选）实现方自评。输出：完工审计报告。典型触发词：审计完成情况、检查需求做完了吗、完工审计、交付检查、对抗性审查、过审计门。
---

# audit-trace：产物+代码 → 完工审计报告

## 何时使用

- 用户提供（或对话中已有）开发产物与实现代码，要求审计需求完成情况
- 用户要求判定交付状态（可交付/有条件交付/不可交付）
- 用户要求核对任务证据、无主代码、验收标准达标情况

**不适用**：没有实现代码或产物时（至少需要 Spec + 代码才能审计）；要求生成实现指导（用 impl-guide）。

## 输入

- **[必需] 结构化 Spec** — 功能点清单、验收条件（spec-writer 输出）
- **[必需] 实现代码** — 项目代码（用于代码扫描和审计）
- **[可选] 架构设计方案** — 模块划分（arch-designer 输出）
- **[可选] 任务清单** — 任务 ID 和依赖关系、证据要求（task-planner 输出）
- **[可选] 实现指导** — 三层边界约束、一致性测试要求（impl-guide 输出）
- **[可选] 实现方自评** — 实现方对各功能点/任务的完成声明（✅/⚠️/❌ + 依据），用于对抗性审查差异对照（P1-04 A-02）；缺失时跳过对照维度并标注

**输入缺失处理**（按顺序）：

1. **检查对话上下文** — 优先从当前对话中查找已有产物
2. **用户提供** — 缺失产物询问用户补充（不自动回溯调用上游 SKILL）
3. **部分审计** — 如用户明确要求"只看能查的部分"，跳过缺失维度，标注"XX 维度因缺少 XX 输入未审计"

## 执行流程

### 1. 收集输入

从对话上下文或用户提供方式获取产物，按输入缺失处理规则补齐。

### 2. 上下文完整性检查

检查上游产物是否包含验证标记 `<!-- CONTEXT-CHECK: ... -->`：

- 如验证标记存在且数值合理 → 继续审计
- 如验证标记缺失或数值异常（如 F-0 但任务清单有内容） → 提示用户"上游产物可能不完整，建议确认后继续"

### 3. 执行审计：八个维度

| 审计维度 | 检查内容 | 数据来源 |
|---------|---------|---------|
| **功能覆盖** | Spec 中每个功能点是否有对应的任务记录或代码实现 | Spec × 任务清单 × 代码 |
| **架构落地** | 架构设计中定义的每个模块是否在代码中有对应实现 | 架构设计 × 代码 |
| **无主代码** | 是否有实现了但不对应任何 Spec 功能点的代码 | 代码 × Spec（反向扫描） |
| **任务闭环** | 全部任务是否在代码中有对应实现 | 任务清单 × 代码 |
| **验收标准达标** | Spec 中 Given/When/Then 场景是否在代码逻辑中被满足 | Spec 验收标准 × 代码逻辑 |
| **实现指导对照** | 代码实现是否遵循三层边界约束、一致性测试要求是否达标 | 实现指导 × 代码 |
| **测试结果核对** | 实际测试执行结果（通过/失败、覆盖率）是否与验收标准一致（V-02，详见 TEST-SKELETON-SPEC.md） | 测试结果文件 × Spec 验收标准 |
| **证据核对** | 任务清单中的证据要求是否已提供，证据是否与实现匹配 | 任务清单 × 用户提供的证据 |

**测试结果核对规则（V-02/V-03/V-04）**：

1. 按任务清单证据要求字段定位测试结果文件（JSON 或 Markdown 报告）
2. 解析结果，按 `source.ac_ids` 关联到 Spec 功能点
3. 逐 AC 判定：

| 测试结果 | 判定 | 说明 |
|---------|------|------|
| 该 AC 全部用例 passed | ✅ 测试验证通过 | 替代纯代码阅读判定 |
| 存在 failed 用例 | ❌ 存在失败用例 | 失败清单列入报告 |
| 无结果文件/未执行 | ⚠️ 未验证（V-04 降级） | 不默认判定达标 |

**降级规则（V-04）**：任一 P0 功能点"未验证"→ 交付状态不得为"可交付"（最高 ⚠️ 有条件交付）；覆盖率 < 80% 时提示"覆盖率不足，建议补充测试"。

### 3.1 对抗性审查对照（P1-04）

**启用场景**：实现与审计由两次独立调用/独立 Agent 完成（Builder/Critic 角色分离，A-01）；或用户明确要求"对抗性审查"。

**执行规则**：
- 审计判定不采信实现方自评，仅将自评作为对照项（A-03 审计基线固定：逐条核对 Spec/任务清单，禁止"整体印象式"结论）
- 实现方自评缺失时：跳过对照维度，标注"无自评，仅输出审计方判定"

**差异对照表**（A-02）：

| 功能点/任务 | 实现方自评 | 审计方判定 | 双方依据 | 差异 |
|------------|-----------|-----------|---------|------|
| F-01 ... | ✅ 已完成 | ✅ 确认 | 自评：... / 审计：代码路径 | 无 |
| F-07 ... | ✅ 已完成 | ❌ 未实现 | 自评：... / 审计：无对应代码 | ⚠️ 差异 |

- 差异项在报告中高亮，列出双方依据，由用户裁决，审计方不强行推翻自评
- 差异项数量计入报告摘要

### 3.2 判定口径补充

**占位文件 vs 无主代码**：占位文件（仅 TODO/桩体）对应 Spec 内需求 → 判定"未实现缺口"（计入功能覆盖 ❌）；对应 Spec 外 → 判定"无主代码"。两者显式区分，不得将 Spec 内占位误报为无主代码。

**替代鉴权识别**：以签名令牌/验签（HMAC + timingSafeEqual）替代内部鉴权属合法模式（外部信源验签 / 令牌即凭证），不误报为鉴权缺失；核对安全强度（密钥管理、有效期、时序安全）并显式标注替代依据。

**静态快照证据核对**：curl/截图/日志等运行时证据在静态快照中不可见时，以"代码内验证"标注（代码路径验证替代运行时证据）；运行时任务（定时扫描/压测）以"复用路径/基线项"闭环判定，不要求快照内出现运行时痕迹。

**快照边界声明**：审计报告头部声明快照范围；快照不包含的内容（如前端代码缺失）按"范围外"标注，不纳入缺口判定——"范围外"与"未实现"必须区分。

**指导对照核查深度**：逐条核对全部三层边界条目，不依赖已知缺口清单——可发现预埋缺口之外的独立违规项（如"金额未加密落库"）。

**任务闭环计数口径**：任务闭环判定绑定任务验收条件（任务清单 AC 字段），而非仅看是否存在对应代码。全部 AC 在代码中满足 → ✅ 闭环（计入闭环数）；有代码但部分 AC 未满足（如函数级实现但路由未注册/测试缺失）→ ⚠️ 部分闭环（不计入闭环数，单列并说明缺项）；无对应代码 → ❌ 未闭环。闭环数 = ✅ 任务数 / 任务总数，部分闭环明细单列不并入闭环数。

**完成度计数口径**：功能覆盖完成度默认按"完整实现"计数——功能点全部 AC 路径在代码中完整闭环（入口→处理→落库/响应，含路由注册与关键结果校验）才计 ✅ 完整实现；有实现但部分路径缺失（如仅状态机引擎无流转接口、核心链路缺一环）→ ⚠️ 部分实现（单列明细，不计入完成度分子）；无对应代码 → ❌ 未实现。分级呈现："X/Y 完整实现 + Z 部分实现"，避免"有实现"与"完整实现"口径混用（同一样本两种口径可分别报告，但不得并列比较）。

**代码可运行性核查（默认动作）**：对所有声称已实现的功能点执行可运行性静态核查——引用符号已定义/导入（如模块引用未定义的 db）、调用的函数存在、关键结果被校验（扣减/回调结果未检查）。该核查为无自评时的默认动作，不依赖对抗性审查触发；对抗性审查下对自评声明"已完成"的功能点加强核查粒度。

### 4. 代码扫描判定标准

| 判定方式 | 适用场景 | 说明 |
|---------|---------|------|
| 文件/目录存在性检查 | 模块级审计 | 检查架构定义的模块是否有对应目录/文件 |
| 函数/类名匹配 | 任务级审计 | 精确匹配 + 模糊匹配（语义相近） |
| 调用链追踪 | 功能覆盖审计 | 从入口点追踪完整功能路径 |

**语言适配**：
- 动态语言（Python/JS/TS）：侧重运行时调用分析和 import/require 分析
- 静态语言（Java/Go/Rust）：利用类型系统和编译时检查

**置信度标注**：每个判定结果标注置信度：
- ✅ **确认** — 高置信度匹配
- ⚠️ **疑似** — 模糊匹配，需用户确认
- ❌ **未实现** — 未找到对应代码

### 5. 三层审计门控（P1-05）

审计结果按三段式门控逐层把关，任一 Gate 未通过则输出缺口清单并阻止进入下一 Gate：

| 门控 | 检查内容（对应审计维度） | 通过条件 |
|------|-------------------------|---------|
| Quality Gate | 无主代码、实现指导对照（违规项）、代码质量基线 | 无 ❌ 项 |
| Review Gate | 功能覆盖、架构落地、任务闭环 | P0 全覆盖（无 P0 缺口；P0 未验证归入 Acceptance Gate 按 V-04 降级） |
| Acceptance Gate | 验收标准达标、测试结果核对、证据核对 | 全部 ✅ 或已标记例外 |

**门控流程**：

1. Quality Gate 未通过（存在 ❌ 项：无主代码/违规实现）→ 输出缺口清单，**阻止进入 Review Gate**，交付状态 ❌ 不可交付
2. Review Gate 未通过（P0 功能点未实现）→ 输出缺口清单，**阻止进入 Acceptance Gate**，交付状态 ❌ 不可交付
3. Acceptance Gate 未通过（存在未标记例外的缺口）→ 输出缺口清单，交付状态 ❌ 不可交付
4. Acceptance Gate 通过但存在已标记例外（P1/P2 缺口、P0 未验证按 V-04 强制降级）→ ⚠️ **有条件交付**
5. 三 Gate 全部通过 → ✅ **可交付**

**例外标记规则**：P0 功能点"未验证"不允许标记例外（V-04：最高 ⚠️ 有条件交付）；P1/P2 缺口可标记例外并列入交付建议。

**门控阻断语义**：被阻断的后续 Gate 输出"⛔ 未执行"，并给出预判说明（"若执行：XX 维度 → 未通过/通过"），供用户了解阻断影响。

**判定梯度**：交付状态按缺口性质区分——多 P0 缺口 ❌ 不可交付 / 单 P1 缺口 ⚠️ 有条件交付（例外）/ 全通过 ✅ 可交付；判定须逐点敏感（每个差异点独立影响结论），不得"整体印象式"一刀切。

**缺口同源标注**：同一根因影响多个审计维度时，以"同源缺口（根因）"贯穿标注，各维度引用同一根因而非重复计错。

## 输出格式

```markdown
# 完工审计报告

## 总体判断
- 需求完成度：X/Y 个功能点已完成（Z%）
- 状态：✅/⚠️/❌
- 阻塞项：...

## 分项审计

### 1. 功能覆盖
| 功能点 | 优先级 | 对应任务 | 代码位置 | 状态 |
|--------|-------|---------|---------|------|
| F-01 ... | P0 | T-01 | src/... | ✅/❌ |

### 2. 架构落地
| 模块 | 架构定义 | 代码位置 | 状态 |
|------|---------|---------|------|
| ... | ... | ... | ✅/❌ |

### 3. 无主代码检查
| 代码位置 | 内容说明 | 建议 |
|---------|---------|------|
| ... | ... | 确认是否为遗漏需求或冗余代码 |

### 4. 任务闭环
| 任务 ID | 任务描述 | 代码位置 | 状态 |
|---------|---------|---------|------|
| T-01 | ... | ... | ✅/❌ |

### 5. 验收标准达标
| 功能点 | 验收标准 | 代码验证 | 置信度 | 状态 |
|--------|---------|---------|--------|------|
| F-01 ... | Given...When...Then... | src/...:L... | ✅确认 | ✅达标 |

### 6. 实现指导对照
| 检查项 | 指导规范 | 代码实现 | 状态 |
|---------|---------|---------|------|
| Always: ... | ... | ... | ✅达标 / ❌不达标 |
| Never: ... | ... | ... | ✅达标 / ❌违规 |
| Conformance: ... | ... | ... | ✅达标 / ❌不达标 |

### 7. 证据核对
| 任务 ID | 任务描述 | 证据要求 | 提供状态 | 证据匹配 |
|---------|---------|---------|---------|----------|
| T-01 | ... | 测试通过截图 | ✅已提供 / ❌未提供 | ✅匹配 / ⚠️疑似 / ❌不匹配 |

### 8. 测试结果核对
| 功能点 | 验收标准 | 关联测试用例 | 测试结果 | 覆盖率 | 判定 |
|--------|---------|-------------|---------|--------|------|
| F-01 | AC-01 ... | test_ac01_login_success | ✅ 5 通过 / 0 失败 | 87.5% | ✅ 测试验证通过 / ⚠️ 未验证 |

### 9. 差异对照（对抗性审查，自评缺失时省略并标注）
| 功能点/任务 | 实现方自评 | 审计方判定 | 双方依据 | 差异 | 裁决 |
|------------|-----------|-----------|---------|------|------|
| F-07 | ✅ 已完成 | ❌ 未实现 | 自评：... / 审计：无对应代码 | ⚠️ 差异 | ⬜ 待用户裁决 |

## 审计门控结果
| 门控 | 检查项 | 结果 | 缺口/例外 |
|------|--------|------|----------|
| Quality Gate | 无主代码 / 违规项 | ✅ 通过 / ❌ 未通过 | ... |
| Review Gate | P0 全覆盖 | ✅ 通过 / ❌ 未通过 | ... |
| Acceptance Gate | 验收 / 测试 / 证据 | ✅ 通过 / ⚠️ 例外 | ... |

## 交付建议
1. **阻塞项**：...
2. **低优先级**：...
3. **建议**：解决以上问题后重新运行完工审计
```

### 格式切换（可选）

默认输出 Markdown（以上模板）。用户明确要求 JSON / CSV 时：

- **JSON**：按产物章节结构输出 JSON 对象，字段映射如下，内容与执行方法完全一致，仅改变呈现格式
- **CSV**：分项审计表、证据核对表、差异对照表等表格类产物以 CSV 输出，每行一条记录，列名取表头
- **CONTEXT-CHECK 标记在任何格式下保留**（JSON 置于顶层字段 `context_check`，CSV 置于末尾注释行），供下游检查
- 格式切换不改变执行流程、判定口径与自检清单

```json
{
  "title": "完工审计报告",
  "snapshot_scope": "审计范围声明（含'范围外'与'未实现'区分）",
  "verdict": {"completeness": "X/Y 个功能点已完成（Z%）", "status": "✅/⚠️/❌", "blockers": ["..."]},
  "dimensions": {
    "feature_coverage": [{"feature": "F-01", "priority": "P0", "task": "T-01", "location": "src/...", "status": "✅/❌"}],
    "architecture": [{"module": "...", "defined": "...", "location": "...", "status": "✅/❌"}],
    "orphan_code": [{"location": "...", "description": "...", "suggestion": "..."}],
    "task_closure": [{"task": "T-01", "description": "...", "location": "...", "status": "✅/❌"}],
    "acceptance": [{"feature": "F-01", "ac": "...", "verification": "src/...:L...", "confidence": "✅确认", "status": "✅达标"}],
    "impl_guide": [{"item": "...", "rule": "...", "implementation": "...", "status": "✅达标/❌不达标"}],
    "evidence": [{"task": "T-01", "description": "...", "required": "...", "provided": "✅/❌", "match": "✅匹配/⚠️疑似/❌不匹配"}],
    "test_results": [{"feature": "F-01", "ac": "AC-01", "cases": "test_ac01_login_success", "result": "✅ 5 通过 / 0 失败", "coverage": "87.5%", "verdict": "✅/⚠️"}],
    "adversarial": [{"item": "F-07", "claimed": "✅ 已完成", "audited": "❌ 未实现", "evidence": "...", "difference": "⚠️ 差异", "decision": "⬜ 待用户裁决"}]
  },
  "degraded_dimensions": [{"dimension": "...", "reason": "跳过维度及原因标注"}],
  "runnability_check": [{"feature": "F-01", "finding": "可运行性核查发现（引用未定义/函数缺失/结果未校验）", "status": "✅ 通过/⚠️ 疑点"}],
  "gates": {"quality": "✅ 通过/❌ 未通过", "review": "✅ 通过/❌ 未通过", "acceptance": "✅ 通过/⚠️ 例外"},
  "suggestions": ["..."],
  "context_check": "S-{审计范围快照}"
}
```

> 字段与 Markdown 章节一一对应（含快照范围声明、降级审计标注、可运行性核查结果），JSON 模式不得省略章节字段。

## 自检清单

输出最终结果前逐项检查，未全部通过不得输出：

- [ ] 八个维度全部覆盖（除非输入缺失并明确标注）
- [ ] 实现指导已提供时，三层边界约束和一致性测试要求已逐条对照
- [ ] 测试结果文件存在时，已按 AC 关联核对通过/失败/未验证（V-02），P0 未验证未判"可交付"（V-04）
- [ ] 任务清单含证据要求时，已逐任务检查证据提供状态
- [ ] 证据未提供的任务已在交付建议中列出
- [ ] 每个判定结果标注了置信度（✅ 确认 / ⚠️ 疑似 / ❌ 未实现）
- [ ] P0 未实现已判定"❌ 不可交付"
- [ ] 无主代码已标注
- [ ] 验收标准逐条对照 Spec 的 Given/When/Then
- [ ] 降级审计已标注跳过维度及原因
- [ ] 对抗性审查启用时，差异对照表已输出且差异项含双方依据（A-02）
- [ ] 占位文件已按"Spec 内=缺口 / Spec 外=无主代码"区分
- [ ] 替代鉴权已核对安全强度并显式标注
- [ ] 快照范围已声明，"范围外"与"未实现"已区分
- [ ] 同源缺口已贯穿标注而非重复计错
- [ ] 任务闭环已按 AC 判定（部分闭环 ⚠️ 不计入闭环数，单列缺项）
- [ ] 功能覆盖完成度已按"完整实现"口径分级呈现（完整 X + 部分 Y，口径未混用）
- [ ] 已对声称已实现的功能点执行可运行性静态核查（引用未定义/函数缺失/结果未校验）
- [ ] 三 Gate 已按 Quality → Review → Acceptance 顺序执行，未通过的 Gate 已输出缺口清单并阻止进入下一 Gate（P1-05）
- [ ] P0 未验证未标记例外（V-04），例外项均已标注依据

## 边界与常见错误

- **不做什么**：不修复代码（只报告缺口）、不重新规划任务（用 task-planner）、不生成实现指导（用 impl-guide）、不代替用户裁决差异项
- **常见错误**：跳过维度且未标注原因；判定不带置信度；无主代码忽略；P0 缺口仍判定"可交付"；证据核对凭印象不对照任务清单；门控顺序颠倒或跳过失败 Gate 继续判定；P0 未验证标记例外仍判"可交付"；对抗性审查时采信自评结论；Spec 内占位误报无主代码；范围外内容误判缺口；同源缺口重复计错；部分闭环（AC 未全满足）计入闭环数；完成度"有实现"与"完整实现"口径混用；声称已实现的功能点未做可运行性核查
- **输入缺失**：提示用户补充缺失产物或执行部分审计，不自动回溯调用上游 SKILL
