interface:
  display_name: "Automate Mermail Triage"
  short_description: "Configure Mermail task triage automation"
  default_prompt: "Use $mermail-automate-triage to configure this mailbox triage workflow."
dependencies:
  tools:
    - type: "mcp"
      value: "mermail"
      description: "Mermail workspace and mailbox MCP server"
      transport: "streamable_http"
      url: "https://console.mermail.app/mcp"
