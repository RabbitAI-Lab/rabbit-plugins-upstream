# Ozon 店铺研究

- 用规模、销量、销售额、价格、粉丝、评分、评论、品牌、类目和生命周期形成店铺画像，不用单一总销量下结论。
- `mallOpenTime` 可能来自已收录商品最早上架时间，必须结合 `mallOpenTimeSource` 解释，不称为官方注册时间。
- `chinaFlag`、`country` 和 `sellerType` 仅按来源字段解释；缺字段不能反向判定卖家国别。
- 趋势只使用非合成观察点；短序列不能声称稳定增长。
- 只有响应提供 `mallUrl` 时才把店铺名做成链接，不拼接地址。
