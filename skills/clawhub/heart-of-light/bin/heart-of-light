#!/bin/sh
# Portable entry point. Invoke through sh because package installers may not preserve mode bits.
set -eu
ROOT=$(CDPATH= cd -- "$(dirname -- "$0")/.." && pwd)
exec python3 "$ROOT/scripts/heart_tool.py" "$@"
