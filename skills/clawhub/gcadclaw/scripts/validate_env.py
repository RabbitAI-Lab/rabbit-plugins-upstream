# AUTO-GENERATED by scripts/pack_skills/pack_skill_python_lib.py
# Safe to regenerate.
from __future__ import annotations

import importlib
import sys


REQUIRED_IMPORTS = 'pygcadwin',


def main() -> int:
    for name in REQUIRED_IMPORTS:
        try:
            module = importlib.import_module(name)
        except Exception as exc:
            print(f"FAILED: cannot import {name}: {exc}", file=sys.stderr)
            return 1
        version = getattr(module, "__version__", "unknown")
        print(f"OK: imported {name}, version={version}")
    return 0


if __name__ == "__main__":
    raise SystemExit(main())
