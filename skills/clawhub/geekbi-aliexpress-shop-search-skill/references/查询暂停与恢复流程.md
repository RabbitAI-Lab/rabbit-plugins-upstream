# 查询暂停与恢复流程

业务脚本先正常查询。服务端在 `data.jumpUrl` 中返回操作地址，表示因未登录或其他可恢复条件要求暂停时：

1. 原样使用服务端中文 `msg`。
2. 将 `jumpUrl` 展示为可点击 Markdown 链接，不自行生成或改写。
3. 停止数据查询；用户完成操作或回复“继续”后，原样重跑原请求。
4. 条件仍未满足时使用服务端最新提示；链接失效时重跑取得新链接。

非零响应没有 `jumpUrl` 时属于普通错误。不得展示访问令牌、设备码、权限标识、请求头或内部认证对象。
