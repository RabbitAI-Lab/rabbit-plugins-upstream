/**
 * thu-epiphany 客户端配置
 *
 * ⚠️ 重要：本文件包含你的访问令牌。
 * 不要公开分享此文件或包含此文件的文件夹。
 *
 * 如需更换令牌，修改下面的 TOKEN 值即可。
 *
 * 使用步骤:
 *   1. 复制本文件为 config.js:  cp config.example.js config.js
 *   2. 编辑 config.js, 填入管理员给你的令牌和服务端地址
 */

module.exports = {
  // 🔗 API 服务端地址（由管理员提供）
  // 示例: "https://api.thu-epiphany.com"
  api_base: "",

  // 🎫 你的访问令牌（由管理员提供，这是唯一一次显示）
  token: "",

  // 🏷️ 你的默认显示名称（选填，提交时标记来源）
  default_from: "@你",
};
