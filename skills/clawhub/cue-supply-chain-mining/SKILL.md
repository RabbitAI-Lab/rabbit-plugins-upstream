---
slug: cue-supply-chain-mining
displayName: 产业链潜力股挖掘
description: 用 Cue 沿产业链挖掘具备业绩爆发潜力的"隐形冠军"——梳理产业链传导路径，基于基本面与弹性逻辑，发现未被充分定价的优质标的。
description_zh: Cue 产业链潜力股挖掘：沿产业链传导路径，挖掘基本面扎实的隐形冠军。
version: 1.1.0
author: sensedeal
tags: [cue, supply-chain, stock-mining, 产业链, 潜力股, 隐形冠军, 传导逻辑]
---

# 产业链潜力股挖掘

> 寻找下一个领涨龙头。梳理产业链传导路径，基于基本面与弹性逻辑，挖掘具备业绩爆发潜力的"隐形冠军"。

## 适用范围与场景（先看这个，别用错了）

**可以查：**

| 维度 | 覆盖 |
|------|------|
| 市场 | 全市场 A 股（沪深北） |
| 产业链类型 | 全行业——制造业（新能源/半导体/机器人/汽车/光伏/储能）、TMT（AI/算力/消费电子）、消费、医药、周期等 |
| 分析深度 | 产业链图谱、传导逻辑、隐形冠军筛选、弹性评估 |

**适用场景：**

| 场景 | 解决的问题 |
|------|-----------|
| 产业链深挖 | 某条产业链上还有哪些没被发现的标的 |
| 隐形冠军筛选 | 找到细分领域市占率高但估值未充分反映的公司 |
| 景气度传导 | 上游景气如何传导到中下游，谁最受益 |
| 替代标的寻找 | 龙头太贵，找产业链上弹性更大的替代品种 |

**不能查：**

- 未上市 / 一级市场公司的产业链信息（非公开数据）
- 非产业链维度的个股技术分析（K 线、MACD 等）
- 实盘买卖建议或投资时点建议（Cue 只做基本面梳理，不做交易推荐）
- 产业链上企业的实时订单数据或产能利用率（非公开经营数据）

> 简单判断：**你能在公开信息里看到这条产业链吗？** 能 → 用它。只是概念炒作、没有实质产业基础 → 用不了。

### 快速判断：你是否需要这个 Skill？

```
你的需求是？
├─ 我想知道某条产业链上有哪些上市公司，各自做什么      → ✅ 用这个
├─ 我想找细分领域的隐形冠军，避开人尽皆知的大白马      → ✅ 用这个
├─ 我想理解产业链景气传导——上游涨价谁受益             → ✅ 用这个
├─ 龙头太贵，我想找同产业链上弹性更大的替代标的        → ✅ 用这个
├─ 我想做产业链全景尽调，为投资决策提供基本面支撑      → ✅ 用这个
├─ 我想看某只股票的技术面、资金流向、K线形态            → ❌ 用不了，去券商 App
├─ 我想要具体的买入/卖出价格建议                       → ❌ 用不了，AI 不荐股
└─ 我想看某家非上市公司的供应链信息                    → ❌ 用不了，非公开信息
```

## 试试这样问

**推荐问法（直接对 Agent 说即可触发本 Skill）：**

- "帮我用产业链潜力股挖掘技能，研究一下人形机器人产业链有哪些潜力股"
- "用 Cue 产业链挖掘，看看 AI 算力链上还有哪些被低估的公司"
- "帮我挖一下固态电池产业链，哪些标的弹性最大"
- "分析一下光伏产业链，现在哪个环节最值得看"
- "用产业链潜力股挖掘，帮我看半导体设备国产替代有哪些标的"

**对应到命令行 query：**

```bash
# 人形机器人产业链
--query "人形机器人 产业链图谱与隐形冠军挖掘：上游→中游→下游传导"

# AI 算力链
--query "AI算力 产业链图谱与隐形冠军挖掘：芯片→服务器→数据中心→应用"

# 固态电池
--query "固态电池 产业链图谱与隐形冠军挖掘：材料→电芯→系统集成，弹性评估"

# 光伏
--query "光伏 产业链传导与隐形冠军：硅料→硅片→电池→组件→电站，景气度对比"

# 半导体设备国产替代
--query "半导体设备 国产替代 产业链图谱与隐形冠军挖掘：光刻→刻蚀→薄膜→检测"
```

> **模板**：`"<产业链名称> 产业链图谱与隐形冠军挖掘：<你想重点分析的方向>"`

---

## 适用人群

| 角色 | 典型问题 |
|------|---------|
| 买方研究员 / 基金经理 | 新能源链上还有哪些没被覆盖的标的？上下游景气度怎么传导？ |
| 行业研究员 | 某条产业链的完整结构是什么？哪些环节集中度高、议价能力强？ |
| 个人投资者 | 龙头太贵，同产业链上有没有基本面扎实但估值更低的小公司？ |
| 产业从业者 | 我们所在环节的竞争格局如何？上下游有哪些关键玩家？ |

---

## Agent 执行摘要

| 顺序 | 做什么 | 禁止 |
|------|--------|------|
| 1 | 确认 Cue runner 就绪 | 禁止跳过 |
| 2 | 告知用户耗时 2-15 分钟 | 禁止中途取消 |
| 3 | 一条命令，`--template-id template_Rezekd`，传入产业链名称 | 禁止连发多条 |
| 4 | `[cue-research] RESULT ok` = 完成 | 禁止编造 |
| 5 | 原样交付 | 禁止概括 |

## 核心能力

1. **产业链图谱梳理** — 上游→中游→下游完整结构
2. **传导逻辑分析** — 景气从何而起、向何处传导
3. **隐形冠军筛选** — 细分龙头、高市占率、低关注度
4. **弹性逻辑评估** — 业绩弹性、估值弹性、事件弹性

## 输出形式

结构化报告：产业链图谱 → 传导逻辑 → 隐形冠军清单 → 弹性评估 → 风险提示 → 来源链接。

---

## 性能预期

查询耗时取决于 Cue 服务端负载和外部数据源响应速度：

| 时段 | 预期耗时 | 说明 |
|------|---------|------|
| 工作日 10:00-16:00 | 3-8 分钟 | 正常负载 |
| 工作日 9:00-10:00 / 16:00-18:00 | 5-15 分钟 | 高峰期，队列可能排队 |
| 夜间 / 周末 | 2-5 分钟 | 低负载，Cue 后端可能有维护 |
| 财报季（1-4月 / 7-8月） | 5-15 分钟 | 数据源更新密集期 |

> 如果超过 15 分钟没返回，说明可能超时或队列拥堵——**不要取消重试**，跑一下健康检查的三合一诊断（见下文），确认服务状态后再决定是等还是换时段。中途中断时用**相同命令**续接，Cue 会从 session 断点继续，不重复计费。Session 状态存储在 `~/.cue/session/task`，**不要手动删除**。

---

## 环境要求

Runner 来源：[GitHub - sensedeal/cue-skills](https://github.com/sensedeal/cue-skills)（[Gitee 镜像](https://gitee.com/sensedeal/cue-skills)）。

依赖：`git` + `python3` + `curl`（macOS 自带，Linux `apt install git python3 curl`）。Python 仅用标准库，无额外 pip 依赖。

Cue API Key：[cuecue.cn](https://cuecue.cn/hub/api-key) 注册获取。新账号送免费积分，每天再免费送 10 分。

---

## 调用说明

```bash
python3 ~/.cue/cue-skills/cue-research/scripts/research_run.py \
  --query "人形机器人 产业链图谱与隐形冠军挖掘：上游→中游→下游传导" \
  --template-id template_Rezekd \
  --output ~/cue-reports/$(date +%Y-%m-%d-%H%M)-supply-chain.md
```

| 参数 | 说明 |
|------|------|
| `--query` | 产业链/赛道名称，**必填** |
| `--template-id` | 固定为 `template_Rezekd` |
| `--output` | 落盘路径 |

---

## 格式转换

Cue 输出 Markdown。安装 pandoc 后可转换为 Word 或 PDF：

```bash
# .md → .docx（Word）
pandoc report.md -o report.docx

# .md → .pdf
pandoc report.md -o report.pdf --pdf-engine=xelatex
```

输出文件与输入同目录、同名、不同后缀。

### 依赖安装

| 目标格式 | 依赖 | macOS | Ubuntu |
|----------|------|-------|--------|
| Word (.docx) | pandoc | `brew install pandoc` | `sudo apt install pandoc` |
| PDF (.pdf) | pandoc + LaTeX | `brew install --cask basictex` | `sudo apt install texlive-xetex` |

---

## 架构说明

本 Skill **不在本地执行检索**。流程是 Agent → Cue API（cuecue.cn）→ 外部数据源。解析结果的质量和时效取决于 Cue 服务端和外部数据源的状态。

| 环节 | 谁控制 | 出问题时 |
|------|--------|---------|
| API Key 鉴权 | 你 | 重新生成 Key，更新 ~/.cue/config.json |
| Cue 服务端 | Cue 运维 | 等恢复，或走降级方案 |
| 外部数据源 | 公开网站 | Cue 用缓存兜底，标注"来源暂不可达" |

---

## 健康检查

跑研究前先验证三件事。一键诊断：

```bash
CUE_KEY=$(python3 -c "import json;print(json.load(open('$HOME/.cue/config.json'))['api_key'])" 2>/dev/null || echo "$CUE_API_KEY")
echo "=== 1/3 API Key ===" && [ -n "$CUE_KEY" ] && echo "已配置" || echo "未配置！"
echo "=== 2/3 Cue 服务 ===" && curl -sS --max-time 10 "https://cuecue.cn/api/health" -H "Authorization: Bearer $CUE_KEY"
echo "=== 3/3 搭子 ===" && curl -sS --max-time 10 "https://cuecue.cn/api/playbook" -H "Authorization: Bearer $CUE_KEY" | python3 -c "import sys,json;scenes=json.load(sys.stdin).get('data',{}).get('scenes',[]);buddy=[b for s in scenes for b in s.get('buddies',[]) if b.get('title')=='产业链潜力股挖掘'];print(f'可用:{len(buddy)}个') if buddy else print('暂不可用')"
```

| 检查 | 预期 | 异常处理 |
|------|------|---------|
| API Key | `已配置` | [cuecue.cn/api-key](https://cuecue.cn/api-key) 重新生成 |
| 服务 | `{"status":"healthy"}` | 等 5 分钟重试 |
| 搭子 | `可用:>0个` | 等 1h 或网页端手动跑 |

---

## 自救指引

### 错误速查

Cue 返回结构化错误，包含 `code`（错误码）、`retryable`（是否可重试）、`user_action`（操作建议）。

| 错误码 | 现象 | 可重试？ | 处理 |
|--------|------|---------|------|
| `AUTH_FAILED` | 401 / Key 无效 | ❌ | 重新生成 Key，更新 `~/.cue/config.json` |
| `TIMEOUT` | 超时 >30s 无响应 | ✅ | 等 5 分钟，跑诊断确认服务在线后重试，最多 3 次 |
| `BUDDY_UNAVAILABLE` | 搭子临时下线 | ✅（延迟） | 等 1h 或网页端直接跑 |
| `INSUFFICIENT_CREDITS` | 积分不足 | ❌ | [cuecue.cn](https://cuecue.cn) 充值或等次日免费额度 |
| `SESSION_INTERRUPTED` | 中途中断 | ✅ | **不换 prompt**，相同命令续接——Cue 从 session 断点继续，不重复计费 |
| `RESULT_EMPTY` | 无匹配结果 | ❌（需改条件） | 缩小产业链范围、换关键词、确认该赛道有公开数据覆盖 |
| `CONFIG_PARSE_ERROR` | config.json 报错 | ❌ | 修复 JSON 格式：`{"api_key": "sk..."}` 无多余逗号 |

### 中断恢复机制

Cue 的调研任务是**有状态**的。如果中途超时或网络波动导致中断：

```
中断 ≠ 失败，cancel 才 = 失败
├─ 超时 → 等一会，相同命令续接（session 存在 ~/.cue/session/task）
├─ 网络波动 → 同上，相同命令续接
├─ 手动 cancel → 无法恢复，需要重新发起
└─ 恢复后 → Cue 从断点继续，已查过的阶段不会重复计费
```

> **关键规则**：中断后**不要换 prompt、不要删 session 文件、不要 cancel**。三个"不要"记住了就不会白跑。

### 重试决策

```
收到错误
├─ retryable = true → 按 user_action 建议操作后重试
│   ├─ 超时/网络类 → 等 5min，跑诊断，最多 3 次
│   └─ 搭子下线 → 等 1h 或换网页端
├─ retryable = false → 不要重试，改了条件再说
│   ├─ Key 问题 → 重新生成 Key
│   ├─ 积分不足 → 充值或等次日
│   └─ 结果空 → 换关键词 / 缩窄范围
└─ 重试 3 次仍失败 → 记下 request_id，走降级方案
```

### 调度建议

| 时段 | 建议 |
|------|------|
| 工作日 10:00-16:00 | 最佳时段，3-8 分钟完成 |
| 工作日 9:00-10:00 / 16:00-18:00 | 高峰期，大查询避开 |
| 夜间/周末 | 可能有维护，跑前先诊断 |
| 新 Key | 必须先诊断确认生效 |
| 连续失败 ≥2 次 | 停 15 分钟，记下 request_id 后重试 |

---

## 降级方案

Cue 长时间不可达时的手动替代渠道：

| 渠道 | 覆盖 | 费用 |
|------|------|------|
| [东方财富行业](https://data.eastmoney.com) | 行业板块及成分股 | 免费 |
| [同花顺产业链](https://www.10jqka.com.cn) | 产业链图谱 | 免费 |
| [企查查](https://www.qcc.com) | 企业供应链信息 | 部分免费 |
| [行业协会官网] | 产业链结构、会员名录 | 免费 |

## 输出示例

[查看完整报告](https://cuecue.cn/share/F8PzjKYbVBgYX2oN9clWB)

## FAQ

**Q: 和券商产业链研报有什么区别？**
A: Cue 多源交叉（工商/招投标/专利/新闻/财报），不只依赖券商已有覆盖，容易发现低关注度标的。

**Q: 怎么知道该用这个还是 cue-deep-research？**
A: 如果你明确要做"产业链维度"的分析——画图谱、找隐形冠军、分析景气传导——就用这个，它有专门的产业链模板。如果问题更宽（"某行业值不值得配置""某公司全面尽调"），去 cue-deep-research。

**Q: 数据来源是什么？**
A: 上市公司财报与公告（巨潮资讯网、港交所披露易、SEC EDGAR）、工商注册信息、招投标公开数据、专利数据库、行业新闻与研报。所有数据均为公开信息，可溯源。

**Q: 查一次要多久？为什么有时候很慢？**
A: 正常 3-8 分钟，高峰期（开盘前后、财报季）可能 10-15 分钟。产业链分析涉及多源数据交叉，步骤比单一公司查询更多。着急用建议避开周一上午和财报密集期（1-4 月、7-8 月）。

**Q: 报告里会有什么内容？**
A: 产业链图谱（上中下游完整结构）→ 传导逻辑（景气从哪来、往哪去）→ 隐形冠军清单（细分龙头、高市占率、低关注度）→ 弹性评估（业绩弹性、估值弹性、事件弹性）→ 风险提示 → 每条结论附带来源链接。具体结构见[示例报告](https://cuecue.cn/share/F8PzjKYbVBgYX2oN9clWB)。

**Q: 中途中断了怎么办？**
A: **不要 cancel、不要换 prompt、不要删 `~/.cue/session/task`**。用完全相同的命令重新执行，Cue 会从断点自动续接，已完成的阶段不重复计费。详见上方「中断恢复机制」。

**Q: 能用英文搜海外产业链吗？**
A: 可以，query 用英文描述即可，Cue 会调用英文公开数据源。美股中概股和港股的产业链信息同样覆盖。

**Q: 产业链分析有没有覆盖非制造业？**
A: 覆盖。TMT（AI/算力/消费电子）、消费/医药、新能源/汽车/光伏/储能等制造业产业链均可分析。关键是该产业链有足够数量的上市公司公开数据可供交叉验证。
