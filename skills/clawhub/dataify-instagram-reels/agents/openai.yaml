interface:
  display_name: "Dataify Instagram Reels"
  short_description: "Collect Instagram Reels data and return results"
  icon_small: "./assets/dataify-instagram-reels-small.svg"
  icon_large: "./assets/dataify-instagram-reels-large.svg"
  default_prompt: "Use $dataify-instagram-reels to complete the requested Dataify collection, wait for the asynchronous task, and return the final collected result. Stop at task submission only when I explicitly request no-wait behavior."

policy:
  allow_implicit_invocation: true
