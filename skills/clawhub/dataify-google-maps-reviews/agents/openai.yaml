interface:
  display_name: "Dataify Google Maps Reviews"
  short_description: "Collect Google Maps Reviews data and return results"
  default_prompt: "Use $dataify-google-maps-reviews to complete the requested Dataify collection, wait for the asynchronous task, and return the final collected result. Stop at task submission only when I explicitly request no-wait behavior."
