---
name: {skill-name}            # ≤64字符，小写/数字/连字符，见名知意
description: {功能一句话}；当用户{description写清触发时机}时使用。  # ≤1024字符，必须含"何时调用"
agent_created: true
---

# {技能标题}

{一句话说明这个技能解决什么、服务谁。}

<!--
厚技能+薄harness 提示：
- 把确定性步骤写成 scripts/ 脚本，跑脚本返回结果，而非让模型每次重写代码
- 关键输出用模板/schema 约束；给足 应触发/不应触发/边界 示例
- 把校验、决策树、自检逻辑固化进技能；harness 只需"调用"
-->

## 指令层（语气 / 格式 / 决策优先级）

- 输出格式：{模板}
- 禁止：{红线}
- 决策优先级：{冲突时如何取舍}

## 知识层（领域知识 / 规则 / 案例）

- {产品文档要点}
- {品牌/合规要求}
- {历史案例参考}

## 工具层（接口声明，仅声明不写实现）

- {API / 数据库 / SaaS 名称 + 用途}

## 示例层（输入输出标准示范——质量上限所在）

### 应触发示例
- 输入：{...} → 输出：{...}

### 不应触发示例（边界外）
- 输入：{...} → 交给其他技能/拒答

### 模糊边界示例
- 输入：{...} → 处理规则：{...}

---
<!--
系统/工作流扩展段（可选，生成 ERP/CRM/HRM/OA/Agentic Workflow 类技能时取消注释并填写；
纯问答/无外部系统/无写动作技能可整段删除。详细方法论见 references/process-systems.md）
-->
## 系统 / 工作流扩展段（流程技能专用）

### 领域本体（Domain Ontology）
- 核心实体与字段：{员工(工号,部门,职级) / 单据(类型,状态,审批人) ...}
- 权威系统（single source of truth）：{字段 → 来源系统}
- 状态枚举：{草稿 / 待审 / 已批 / 驳回 / 撤销}（显式，不用"处理中"等模糊词）

### 连接器声明（Connector Abstraction）
- {能力意图 create_invoice / approve_leave ...} ← 由 MCP/网关/SDK 实现，技能只传结构化参数
- 具体地址/凭证：见治理元数据「作用域隔离」，不写在技能正文

### 状态机（State Machine）
| 状态 | 触发事件 | 动作 | 下一状态 | 超时处理 |
|------|----------|------|----------|----------|
| {草稿} | {提交} | {校验} | {待审} | {转人工} |
| {待审} | {审批通过} | {写系统} | {已批} | {升级催办} |

### 事务安全四件套（写动作必填）
- 幂等：{幂等键 / 业务单号去重；重试安全}
- 回滚：{补偿步骤 / 调用方回滚；不归点(PoNR)={...}}
- 审计：{落审计日志：用户+时间+数据+前后值+链路ID}
- 最小权限：{所需 RBAC 角色/权限集，禁止继承宿主全部权限}

### HITL 门（Human-in-the-loop）
- 门名 / 触发条件 / 确认人 / 超时与驳回去向：{不归点前必确认}

### 降级与可观测
- 超时/重试：{每步超时值、重试上限}
- 降级路径：{LLM/连接器不可用时转人工或规则兜底；部分成功处理}
- 可观测：{trace 字段 = 链路ID/各步状态/耗时/输入哈希}

### LLM 接缝（概率↔确定性）
- 概率侧：{意图识别/抽取/生成}；确定性侧：{状态推进/校验/写入}
- 强 schema：{LLM 输出先过 schema 校验（scripts/），失败拒收}
- 置信闸门：{关键字段低于阈值 → 转 HITL/兜底}
- 模板固化：{固定产出用模板+插槽填充，不自由生成}

---
<!-- 以下为企业模式治理件，个人模式可删除 -->
## 治理元数据（企业模式）

- 命名规范：`{领域}-{动作}`
- 角色捆绑：{销售/工程/客服...}
- RBAC 角色集（流程技能必填）：{技能所需最小角色/权限集，如 hr-leave-approver}
- 作用域隔离：{多租户/项目级/角色级隔离说明}
- 技能审计：{日志+链路+回滚机制}
- 安全审查：8 项清单 + CISO 5 风险（AST10）已逐条确认
- 版本/回滚：生产 pin 版本，Git 为唯一可信源，保留上一版兜底
- 分发渠道：{组织级上传/插件+分组/内部目录/代码库/API}
- 跨平台：遵循 agentskills.io 标准，可在 WorkBuddy/Codex/Claude Code/Cursor/龙虾/Hermes 移植

## 注册表条目

| 技能名 | 领域 | 所有者 | 版本 | 依赖 | 评估状态 | 复审周期 |
|--------|------|--------|------|------|----------|----------|
| {skill-name} | {领域} | {owner} | v0.1 | {依赖} | 待审 | 季度 |

## Evolution Log（持续进化，企业模式推荐）

- v0.1 ({日期}) 初始版本，由 {来源/负责人} 创建
- （后续每次迭代追加：版本/日期/变更内容/触发原因，增量扩展而非覆盖重写）

<!--
个人→企业升级提示：
1. 若本技能由个人版升级而来，逐项核对 references/personal-to-enterprise.md 清单
2. 技术层(能力)交工程、规则层(口径)业务可写，AI 产品经理负责拆分与编排
-->
