interface:
  display_name: "Dataify Indeed Job Listings"
  short_description: "Collect Indeed Job Listings data and return results"
  default_prompt: "Use $dataify-indeed-job-listings to complete the requested Dataify collection, wait for the asynchronous task, and return the final collected result. Stop at task submission only when I explicitly request no-wait behavior."
