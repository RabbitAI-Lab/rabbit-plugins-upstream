# Step 2 — 比对

```bash
python "${SKILL_DIR}/scripts/step2_compare.py" gxpcode_data
```

输入: `gxpcode_data/s1/` + `history.json`（skill 目录）
输出: `gxpcode_data/s2/s2_{源名}.json` + `s2/.done`

history path: `${SKILL_DIR}/gxpcode_data/history.json`
