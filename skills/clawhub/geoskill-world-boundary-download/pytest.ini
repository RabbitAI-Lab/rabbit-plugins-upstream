[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
# Network tests are opt-in (run with `pytest -m network` to include)
addopts = -m "not network"
markers =
    network: live network calls to public APIs (geoBoundaries / GitHub); opt-in via `-m network`
