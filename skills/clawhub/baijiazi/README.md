# 败家子 / baijiazi

`baijiazi` 是一个故意烧 token 的低价值 skill。它只有在用户明确点名并提供确认词时才运行。

## 触发

必须同时满足：

- 显式调用 `baijiazi` 或 `败家子`
- 包含确认词 `CONFIRM_BURN`

示例：

```text
使用 baijiazi。CONFIRM_BURN。把这个想法尽量展开。
```

## 边界

- 不改项目文件。
- 不运行破坏性命令。
- 不伪造引用、链接、测试结果或工具输出。
- 不用于医疗、法律、投资、安全、危机等高风险主题。
- 一次确认只执行一轮烧 token。

## 许可

MIT。

---

# baijiazi

`baijiazi` is a deliberately low-value token-burn skill. It runs only when the user explicitly invokes it and provides the confirmation token.

## Trigger

Both are required:

- Explicitly invoke `baijiazi` or `败家子`
- Include `CONFIRM_BURN`

Example:

```text
Use baijiazi. CONFIRM_BURN. Expand this idea as much as possible.
```

## Boundaries

- No project-file edits.
- No destructive commands.
- No fake citations, links, test results, or tool outputs.
- No high-stakes medical, legal, investment, security, or crisis burn mode.
- One confirmation means one burn pass.

## License

MIT.

