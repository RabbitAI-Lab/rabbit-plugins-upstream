# SkillSpector notes — lygo-forkling v1.0.0

No network. No subprocess. Writes only `state/` with `--i-consent`.

`shutil.copytree` snapshots local generations (not git clone).

Live Star Chart: `propose` prints JSON only (`live_write: false`).
