# Security — LYGO Forkling v1.0.0

Test champion only. Parent pin `CHAMPION_LYRA` is read as identity, never overwritten on the live chart.

| May | Must not |
|-----|----------|
| Write `state/` with `--i-consent` | git push / HF / ClawHub / social |
| Dry-run `propose` JSON | Live ingest / forge feed hashes |
| Improve when claims pass | Claim to be Lightfather |
| Snapshot generations locally | Skip verify to bump generation |

No network, no subprocess. SSRF not applicable.

**Δ9Φ963**
