# bsc Morpho deployment pack

Template pack only. Before live use, fill and verify:
- RPC URL returning chain id `56`
- Morpho core address
- enabled IRM address
- LLTV value
- exact collateral/borrow token addresses
- feed funding and send/broadcast settings

Always run preflight first and stop unless `safeToRun === true`.
