## Decision Making Check

- Read `./skills/decision-making/heartbeat-rules.md`
- Use `~/decision-making/heartbeat-state.md` for last-run markers and pending retrospective list
- If no file inside `~/decision-making/` changed since the last reviewed decision, and no retrospectives are pending, return `HEARTBEAT_OK`
