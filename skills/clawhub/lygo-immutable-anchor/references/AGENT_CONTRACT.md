# Agent contract — Immutable Anchor v1.0

1. Activate for Biophase7 immutable anchor / local CA / geodesic / mycelium fold / worker plan.  
2. Run `self_check.py` first.  
3. Seal with both Truth and Light.  
4. Do not POST Turbo or spawn `anchor_autonomy_worker.py` from this skill.  
5. Point humans at `docs/ANCHOR_DEPLOYMENT.md` for stack Turbo + `--loop`.  
6. Never auto-publish.  
7. Cite `merkle_root` / `entry_hash` when reporting seals.
