# Stored Service Domains

Use this file as the domain source when exporting HTTP endpoints to Postman.
Do not read `C:\Windows\System32\drivers\etc\hosts` during ordinary exports.

When a service is not listed or no listed domain is a confident match, ask the user for the domain before generating the final Postman file. After the user answers, add a row here.

## Service mappings

| Service | Domain | Notes |
| --- | --- | --- |
| intlOrderProvider | https://order-deve.flightroutes24.com | Remembered after user confirmation |
| fr_adapter_api_supplier | https://flight-deve.flightroutes24.com | Remembered after user confirmation |

## Imported host domains

These domains were imported from the local hosts file as a one-time snapshot. IP addresses were intentionally omitted.

| Domain | Notes |
| --- | --- |
| activate.navicat.com | Imported from hosts snapshot |
