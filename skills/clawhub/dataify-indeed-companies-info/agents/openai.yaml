interface:
  display_name: "Dataify Indeed Companies Info"
  short_description: "Collect Indeed Companies Info data and return results"
  default_prompt: "Use $dataify-indeed-companies-info to complete the requested Dataify collection, wait for the asynchronous task, and return the final collected result. Stop at task submission only when I explicitly request no-wait behavior."
