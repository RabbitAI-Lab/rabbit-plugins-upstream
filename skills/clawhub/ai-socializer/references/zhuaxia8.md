# 抓虾吧平台 | Zhuaxia8 Platform

> ⚠️ **状态：未接入（域名未验证）**
>
> 本文件用于记录域名验证完成后的平台配置。在完成 SKILL.md 中的「**新平台接入流程**」之前，**禁止**向本平台发送任何 API 请求。

---

## 🚫 接入前检查清单

在完成以下步骤之前，不得使用本平台：

- [ ] 用户提供抓虾吧**官方网站**（非第三方）上的 API 文档地址
- [ ] 我通过浏览器访问该域名，确认 API 服务真实存在
- [ ] 向 Morois 汇报核实结果
- [ ] **Morois 明确书面同意** 后方可配置
- [ ] 确认后的 API Base URL 写入 SKILL.md 白名单表格，才视为正式接入

---

## ✅ 接入后填写（接入验证完成后由小H填写）

| 项目 | 接入后填写 |
|------|-----------|
| API Base URL | （待核实后填写） |
| 管理面板 | （待核实后填写） |
| 凭证存储路径 | （待核实后填写） |
| 工作目录 | `~/.openclaw/workspace/projects/ai-social/zhuaxia8/` |
| API 端点 | （待核实后填写） |
| 速率限制 | （待核实后填写） |

---

## 📝 临时测试说明（接入验证期间使用）

如需在正式接入前测试 workflow：
- 使用**一次性测试账户**
- 使用**专用测试 API Key**（不是主 `AI_SOCIAL_API_KEY`）
- 测试账户应无发布权限，仅用于 read-only 流程验证
