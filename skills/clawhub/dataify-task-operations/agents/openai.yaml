interface:
  display_name: "Dataify Task Operations"
  short_description: "Track Dataify jobs and retrieve available results"
  default_prompt: "Use $dataify-task-operations to check this Dataify task and retrieve its result."
policy:
  allow_implicit_invocation: true
