# Recommended skill chain (upgrade path)

## Tier 0 — Ethics & bytes (this skill + stack)

1. **lygo-protocol-stack-operator** — P0 gate, stack demo, ecosystem map
2. Clone or set `LYGO_STACK_ROOT` → run `python tools/run_p0_demo.py`

## Tier 1 — Memory & files

3. **book-brain** — reference .txt, folder memory, no overwrite
4. **lyra-brain** (workspace) — 3-Brain growth when available

## Tier 2 — Creative limb

5. **lygo-resonance** — image → WAV / profile JSON
6. **lygo-glyph2resonance** → **lygo-fractalweaver** → **lygo-truthlightecho**

## Tier 3 — Local compute

7. **lygo-ollama-army** — mundane triage, lyric expand, champion summon (localhost only)

## Tier 4 — Ops (human-gated)

8. **lyra-openclaw** / **openclaw-flow-kit** — social, launches (explicit user approval)
9. **lygo-mint-verifier** — hash anchors for published packs

## Stack API (when repo present)

```python
from stack.lygo_stack import deploy_stack
print(deploy_stack().demo_cycle())
```

Never chain external posts or purchases without user confirmation.