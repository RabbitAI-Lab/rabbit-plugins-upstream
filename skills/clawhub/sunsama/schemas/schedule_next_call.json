{
  "name": "schedule_next_call",
  "description": "Schedule the next Sunsama accountability call at a specific date and time. This creates a scheduled call on the user's Sunsama calendar. Confirm the date, time, and intent with the user before executing.",
  "inputSchema": {
    "type": "object",
    "properties": {
      "nextCallDate": {
        "type": "string",
        "description": "The date of the next call (YYYY-MM-DD format)"
      },
      "hour": {
        "type": "number",
        "description": "The hour (0-23)"
      },
      "minute": {
        "type": "string",
        "description": "The minute",
        "enum": ["15", "30", "45", "00"]
      }
    },
    "required": ["nextCallDate", "hour", "minute"]
  }
}
