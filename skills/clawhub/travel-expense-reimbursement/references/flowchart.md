# 差旅费用报销流程图

```mermaid
flowchart TD
    A([用户触发报销请求]) --> B[步骤1: 初始化工作目录]
    B --> C[步骤2: 从邮箱搜索并下载发票]

    C --> C0{用户是否指定发票类型?}
    C0 -->|指定类型 如12306/携程/华住/美团| C0A[按指定发件人关键词搜索]
    C0 -->|未指定| C0B[获取近期所有邮件，智能筛选发票邮件]
    C0A --> C1{选择邮箱访问方式}
    C0B --> C1
    C1 -->|优先| C2[himalaya CLI 搜索邮件]
    C1 -->|超时/报错| C3[Python IMAP fallback]
    C2 --> C4{搜索到发票邮件?}
    C3 --> C4
    C4 -->|否| C5[告知用户，询问是否手动上传PDF]
    C4 -->|是| C6{邮件是否有PDF附件?}

    C5 --> Z1([等待用户操作])
    Z1 -->|用户手动上传| C6B[接收用户上传的PDF到 inbox/]

    C6 -->|有附件| C6A[第二步：下载附件到 inbox/]
    C6 -->|无附件| L0[第三步：提取下载地址]
    
    L0 --> L0A{能否提取到URL?}
    L0A -->|可提取| L2[第四步：url_downloader下载]
    L0A -->|提取失败（按钮文字非链接）| L1[告知用户手动下载，询问是否上传PDF]
    L1 --> Z1
    
    L2 --> L3{发件人类型?}
    L3 -->|juneyaoair 吉祥航空| L4[浏览器导航到链接，等待自动下载PDF]
    L3 -->|baiwang 百望| L5[浏览器导航到链接，点击 div.btn-web 下载PDF]
    L3 -->|其他| L6[浏览器导航，自主判断下载方式]
    L4 --> L7[确认下载完成，移动PDF到 inbox/]
    L5 --> L7
    L6 --> L7
    L7 --> D

    C6A --> D
    C6B --> D

    D[步骤4: OCR解析发票]
    D --> D1[调用 pdf-ocr 技能]
    D1 --> D2{解析成功?}
    D2 -->|失败| D3[保留文件，跳过继续]
    D3 --> E
    D2 -->|成功| E[步骤5: 发票信息汇总]

    E --> E1[按时间排序生成 invoice-list.json]
    E1 --> E2[展示发票汇总表给用户]
    E2 --> F[步骤6: 行程分析]

    F --> F1[读取行程分析指南]
    F1 --> F2[将车站名转换为城市名]
    F2 --> F3{识别行程类型}
    F3 -->|短行程| F4[出发→目的地→返回]
    F3 -->|长行程| F5[多目的地分段]
    F4 --> F6[生成 travel-schedule.md]
    F5 --> F6
    F6 --> F7[生成 travel-detail.md]
    F7 --> G[步骤7: 确认报销参数]

    G --> G1[读取记忆搜索历史报销]
    G1 --> G2[向用户确认报销事由和项目名称]
    G2 --> G3{用户确认?}
    G3 --> G4[写入记忆到 memory/]
    G4 --> H
    G3 -->|用户修改| G2

    H[步骤8: 批量上传发票到FOL]
    H --> H1[调用 fol-invoice-upload 技能]
    H1 --> H2[一次性批量上传所有PDF]
    H2 --> H3{上传成功?}
    H3 -->|失败| H4[记录失败发票，提示手动上传]
    H4 --> I
    H3 -->|成功| I[步骤9: 提交报销]

    I --> I1[调用 fol-travel-expense 技能]
    I1 --> I2[按行程分段提交报销单]
    I2 --> I3{提交成功?}
    I3 -->|失败| I4[保存数据，提示手动提交]
    I3 -->|成功| J[步骤10: 输出结果]
    I4 --> J

    J --> J1[展示: 处理发票数、总金额、行程汇总、提交状态]
    J1 --> K([报销完成])
```

## 步骤索引

| 步骤 | 名称 | 说明 |
|------|------|------|
| 1 | 初始化工作目录 | 创建 `run-{YYYYMMDD}-{序号}` 目录结构: inbox/, processed/, output/ |
| 2 | 搜索下载发票（四步流程） | **第一步**判断附件 → **第二步**下载附件 → **第三步**提取下载地址 → **第四步** url_downloader 下载 |
| 3 | 链接下载发票（废弃，合并至步骤2第四步） | 现已整合到步骤2的第四步 |
| 4 | OCR解析 | 调用 pdf-ocr 技能提取发票文字信息 |
| 5 | 发票汇总 | 生成 invoice-list.json，展示汇总表 |
| 6 | 行程分析 | 按行程分析指南识别行程，生成 travel-schedule.md 和 travel-detail.md |
| 7 | 确认参数 | 从记忆读取历史，确认报销事由和项目名称 |
| 8 | 上传发票 | 批量上传所有PDF到FOL财务系统 |
| 9 | 提交报销 | 按行程分段创建并提交差旅报销单 |
| 10 | 输出结果 | 展示报销处理结果 |

## 关键判断节点

- **第一步** 邮件无附件但也无法提取URL（按钮文字非链接）→ 告知用户手动从发件方官网/App下载
- **第四步** 链接下载失败（超时/需登录/验证码）→ 截图备用，告知用户，询问手动上传PDF
- **OCR解析失败** → 跳过该发票，继续处理其他
- **发票上传失败** → 记录失败，提示手动上传
- **报销提交失败** → 保存数据，提示手动提交
