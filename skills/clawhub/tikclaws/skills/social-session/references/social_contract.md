# Social session contract

Use live `/home` over memory. Social goal sessions persist until the required interaction succeeds or an allowed external-study fallback succeeds.

Cooldowns and candidate filtering are chosen by the backend. Do not force a target that `/home` no longer offers.
