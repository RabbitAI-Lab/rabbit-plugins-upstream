# Dev Update: 2026-02-23 13:50 PST

## What Shipped Today

### Universal Interface (Phase 3)
- **MCP server** (`mcp-server.mjs`) ... `agent_pay` tool via stdio transport
- **OpenClaw plugin** (`openclaw.mjs` + `openclaw.plugin.json`) ... registers `agent_pay` in agent context
- **Skills directory** (`skills/agent-pay/SKILL.md`) ... full frontmatter for OpenClaw discovery
- Both registered: `.mcp.json` (Claude Code), `openclaw.json` (Lēsa), `extensions/wip-agent-pay/` deployed

### Live Worker
- Cloudflare Worker deployed at `pay-wip-computer.wipcomputer.workers.dev`
- KV namespace `PAY_TOKENS` bound
- Worker secret in 1Password + Cloudflare env
- `providers/coinbase.js` wired to pull secret from 1Password and mint real tokens
- End-to-end tested: mint, consume, 410 on second use
- Dynamic URL via `url.origin` (works on workers.dev now, pay.wip.computer later)

### Provider Abstraction
- Interface layer (CLI, MCP, OpenClaw) no longer mentions Coinbase
- Only the provider knows which wallet is behind it
- Swappable providers, nothing else changes

### Security Model
- Full invariant in SPEC.md: app controls wallet, agent only spends
- State machine: 6 states, clear ownership per state
- Single-use enforcement called out as non-negotiable

### README
- Rewritten with "picture this" opener
- Scoble report links as use cases
- Morning Stew linked
- All builders credited: Parker, Lēsa, Claude Code, Grok
- "Works on any agent" lists real tools
- "You control the wallet, not the agent" with link to security model
- 1Password plan requirement noted

### SETUP.md
- Interactive agent walkthrough (two paths: shell access vs chat-only)
- Prerequisites checklist with [x]/[ ] format
- Agent checks what it can, only asks about what it can't

### Dogfooding
- Tested install prompt in Claude Code CLI and Grok
- Fixed: pay.wip.computer refs, internal plumbing leaks, checklist prereqs
- 6 PRs merged to main, branch protection enabled

### Demo Mode Removed
- Provider now mints real one-time URLs (Worker is live)
- CLI, MCP, OpenClaw all output `[DEMO]` prefix if provider returns demo flag
- TTL set to 3 minutes

## What's Next: x402 Protocol Wiring

Plan saved at `ai/plans/x402-protocol-wiring--2026-02-23.md`.

The big move: Agent Pay speaks x402 natively. Agent hits a paywalled URL, gets 402, sends USDC from Coinbase isolated portfolio via API, retries with proof. No hot wallet. No local keys. Coinbase holds the keys.

New CLI command: `wip-agent-pay pay <url>`

Open questions:
- Coinbase API v2 HMAC auth vs CDP SDK JWT
- x402 payment proof format compatibility
- Which network does Coinbase send USDC on (Solana? Base? Ethereum?)

## Who Did What

- **Parker** ... direction, architecture calls, README voice, dogfood testing, Cloudflare Worker deploy, security model invariant
- **Claude Code (Opus 4.6)** ... MCP server, OpenClaw plugin, provider abstraction, SPEC/SETUP rewrites, all PRs
- **Grok (4.20 Beta)** ... original CLI + provider build, README narrative, x402 framing
- **Lēsa (OpenClaw, Opus 4.6)** ... dogfood testing from agent perspective
- **GPT** ... security model feedback, state machine suggestion, demo mode clarity push

## Stats

- 6 PRs merged to main
- 14 files changed, 1600+ lines added
- Worker live, token minting live, self-destruct verified
- Branch protection enabled on main
- MCP + OpenClaw registered locally
