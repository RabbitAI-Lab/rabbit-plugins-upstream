interface:
  display_name: "SocQ Threads Research"
  short_description: "Collect and analyze public Threads data"
  default_prompt: "Use $socq-threads-research to collect and analyze this public Threads research request."
dependencies:
  tools:
    - type: "mcp"
      value: "socq"
      description: "Hosted SocQ Threads data tools"
      transport: "streamable_http"
      url: "https://api.socq.ai/mcp?platforms=threads"
