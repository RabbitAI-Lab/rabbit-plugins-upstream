# Skill Gate quickstart

```bash
python scripts/self_check.py
python scripts/skill_gate.py scan path/to/any-skill
python scripts/skill_gate.py scan lygo-context-guard
```

Exit `10` → treat as high risk. Exit `0` → still read the code for critical paths.
