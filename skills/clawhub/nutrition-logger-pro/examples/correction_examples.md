# 修正和撤销示例

## 撤销上一条

用户：

撤销上一条

期望：

- 调用 `undo-last`。
- 软删除最新有效记录。
- 告诉用户撤销了哪一餐和哪些食物。

## 修正包装热量

用户：

刚刚那个面包不是180 kcal，是220 kcal，蛋白质7g

期望：

- 找最近唯一匹配的“面包” item。
- 把该 item 的 `kcal` 改为 220，`protein_g` 改为 7。
- `source=user_provided`，`confidence=high`。
- 重新计算本餐和当天合计。
- 保留原始 raw_message，并追加修正说明。

## 修正份量

用户：

把午餐鸡胸肉改成120g

期望：

- 优先匹配今天午餐里的鸡胸肉。
- 如果只有一个候选，按 120g 重新估算。
- 如果有多个候选，列出 2-3 个候选让用户选择。

## 不明确修正

用户：

把鸡胸肉改成120g

如果今天有多条鸡胸肉记录，回复：

我无法唯一确定要修正哪一条。请回复对应序号：
1. 12:30 午餐：鸡胸肉 150g
2. 19:10 晚餐：鸡胸肉 一份
