# vecli (Volcano Engine)

Vecli is distinct from Trae CLI and the cloud-resource `ve` CLI. MCP/auth and model routing are service configuration, not a documented portable migration contract; keep them manual.
