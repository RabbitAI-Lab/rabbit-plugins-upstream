# UI-managed MCP clients

Use for iFlyCode (`iflycode`) and Raccoon AI (`raccoon-ai`). Add reviewed `mcpServers` JSON in the product UI. No file-backed MCP, context, or runtime path is documented; do not invent or copy one.
