# baidu-comate-ide (standalone IDE)

MCP is UI-managed (`mcpServers`); rules use `.mdr`; Zulu/custom agents and slash commands are product features. This standalone IDE is distinct from the Comate plugin; global state is manual.
