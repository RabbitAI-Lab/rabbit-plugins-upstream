# sourcegraph-amp

Amp configures MCP through `amp mcp` commands; Skills, plugins, and agents have product-specific paths/schema. Preserve its native HTTP/OAuth flow and review hooks/manual state before migration.
