interface:
  display_name: "Dataify Instagram Profiles"
  short_description: "Collect Instagram Profiles data and return results"
  icon_small: "./assets/dataify-instagram-profiles-small.svg"
  icon_large: "./assets/dataify-instagram-profiles-large.svg"
  default_prompt: "Use $dataify-instagram-profiles to complete the requested Dataify collection, wait for the asynchronous task, and return the final collected result. Stop at task submission only when I explicitly request no-wait behavior."

policy:
  allow_implicit_invocation: true
