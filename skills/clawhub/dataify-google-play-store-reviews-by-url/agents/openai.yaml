interface:
  display_name: "Dataify Play.google Builder"
  short_description: "Collect Play.google Builder data and return results"
  default_prompt: "Use $dataify-google-play-store-reviews-by-url to complete the requested Dataify collection, wait for the asynchronous task, and return the final collected result. Stop at task submission only when I explicitly request no-wait behavior."
