## 2026-08-12 · 全网技术升级 + 专家体系丰富 + 自我诊断（版本维持 2.1.35）

**用户要求：全网搜集号码分析技术升级、丰富专家信息、运行系统自我诊断找问题、并思考"用户能做什么让系统更好"。**

- **1. 号码分析技术升级（新增 `ssq_deep_analysis.py` 纯描述模块，不改预测、不动一致性）**：
  - 多窗口冷热 Z-score（近10/30/50/100期红球出现频 vs 期望，标 HOT/COLD）。
  - 历史均值间隔（间隔均值±2σ 判定"统计极冷"候选号码）。
  - 近期结构特征：三区分布、奇偶、大小、012路、连号、和值、跨度（含近10期走势，纯描述）。
  - 香农熵（开奖分布随机性度量，用于诚实打脸"有规律"错觉）。
  - 注入报告（数据概览与推荐之间，新增"深度号码分析"面板）。
- **2. 专家体系丰富（`ssq_expert_roster.py`）**：
  - 名录 **46→55 位**（51 权威 + 4 野路子），新增当期活跃专家：田海峰/白琪峰/蒸蒸日上/小老虎/草儿/袁军师/周家良/张雨/刘海华等，附 2026093 期真实近期观点（红球杀号/三区/012路/蓝球走向）。
  - 数据源 **6→9 个**（增 彩宝贝/500彩票网/双彩网）。
  - 新增 `render_expert_views_html()`「专家近期观点荟萃」面板，注入报告（深度分析之后）。
- **3. 自我诊断（主动运行找问题）**：
  - 跑 22 项健康护栏：正确性 **23/23 全绿**；发现 1 个真实待办——#24 代码完整性基线陈旧（前次改 fetcher 未重建基线），已 `ssq_self_integrity.py --init` 重建转绿。
  - 跑主流程 `run_ssq.py --fresh`：端到端退出码 0；❌6/7 为诚实闸门（强化引擎/ML 自评未优于随机——打假设计本意，非 bug）。
  - 三方交叉验证 **51/51（100%）** 全部通过，新面板未破坏一致性。
  - 领奖新闻面板实测展示 2026-08-11 真实近期故事，无旧闻。
- **4. 用户可优化方案**（见交付说明）：数据贡献闭环、反馈回访、固定投入预算线、预期管理、自动化利用。
- **验证 / 四体同步**：新增模块编译+渲染冒烟通过；dist（55 文件）已重建含全部升级；桌面交付 `ssq-probability-analyzer_v2.1.35_技术升级.zip`（565,710字节）。#24 基线已重建。

## 2026-08-11 · 彻底修复「近期一等奖领奖故事」仍显示极旧新闻（版本维持 2.1.35）

**用户二次反馈：栏目还是旧新闻、而且是特别旧的那种。上一轮修复未根除根因。**

- **残留根因（两个）**：
  1. **排序方向反了**：`_fresh_rank` 返回 `(0, d)` 后用 `reverse=False` 升序，使**最早日期排最前**——首次打开（rotation=0）展示的恰恰是最旧新闻，越翻越新，完全反了。
  2. **种子库混有 2025 极旧故事**：`ssq_winning_stories.json` 含两条 2025-09 新闻（西安750万、南昌1000万，距今约335天）；轮换窗口滑到末尾时这俩极端老故事就暴露，即用户看到的"特别旧"。
- **修复**：
  - 种子库重写：剔除两条 2025 故事，新增从公开报道核实的 2026 真实新闻，现 13 条全部为 **2026-04~2026-08** 真实中奖（佛山/杭州/惠州/珠海/深圳/宜宾/广州/海南乐东/中山等），最旧仅 2026-04，库内 `max_fresh_days=150 / recall_days=75`。
  - 排序修正为 **最新优先（真实日期倒序，reverse=True）**，首次打开即见最新窗口。
  - 新增**硬性排除** `EXCLUDE_DAYS=150`：超龄极旧新闻直接移出活动池、绝不展示（仅当池不足才兜底）；仅 `RECALL_DAYS=75` 以上的老闻标「经典回顾」排末尾（诚实，不伪装最新）。
  - 联网无日期新闻视为「今天」置顶，保证 live 抓取成功时最新优先。
- **验证**：离线连续 15 次打开——每次展示不同子集、全部 2026 近期、最新优先、正确回绕、累计覆盖 13/13 条、**零 2025/旧日期**；报告渲染冒烟通过（无 2025 日期、ws-grid 结构正常、经典回顾徽标在轮到 4 月老闻时诚实出现）；重建 dist（53 文件、已清运行时缓存，部署即从最新窗口起）。
- **四体同步**：SKILL 副本源码（唯一源）+ dist 包已更新并重新交付桌面 `ssq-probability-analyzer_v2.1.35_领奖新闻修复.zip`。

## 2026-08-11 · 修复三方交叉验证 52/52「一致性严格比对」误报（版本维持 2.1.35）

**用户反馈：调用最新 SKILL 后运行出错，自检告警「生成含随机扰动，新进程重算与磁盘旧文件逐字对不上（52/52）」，属一致性严格比对告警。**

- **根因（真因，非随机扰动）**：`ssq_auto.py#main` 与 `ssq_cross_validate_v84_final.py` 对「专家推荐 `expert_picks`」的派生逻辑**两路不一致**。
  - pipeline 在 `ssq_expert_picks.json` 缺失时，**回退到内置常驻专家体系 `build_resident_expert_panel`（确定性派生，非空）**；
  - cross_validate 在文件缺失时，`expert_picks = []` 空列表。
  - 第4组（ECI/遗漏优选）的**红球与蓝球**分布完全由 `expert_picks` 驱动 → 两路喂入不同 → 第4组分歧。
  - 本机复现：修复前交叉验证 47/52（仅第4组红球+蓝球分歧）；用户环境因旧包生成的基线差异更大，表现为整栏/全项 52/52 失败。
- **修复**：把 `expert_picks` 派生抽成 `ssq_auto.py#load_expert_picks(draws, verbose, data_issues)` **唯一权威函数**，pipeline 与 cross_validate 共用同一份逻辑（文件优先→内置常驻专家回退→极端空列表），保证两侧 `expert_picks` 永远一致。
- **验证**：交叉验证 47/52（4 失败）→ **51/51（100%，0 失败）**；第4组红球/蓝球、HTML vs JSON 全面一致性均恢复三方一致。完整 22 项护栏：正确性类 23/23 全通过（唯一告警#24「完整性基线陈旧」为有意升级后的预期运维告警，已 `ssq_self_integrity.py --init` 重建基线转绿）。
- **四体同步**：SKILL 副本源码（唯一源）+ dist 包已更新并重新交付桌面。

## 2026-08-11 · 修复「近期一等奖领奖故事」新闻不更新（版本维持 2.1.35）

**用户反馈：报告里"近期一等奖领奖故事"栏目长期显示同一批旧新闻，希望每次打开都能刷新、使用最新新闻、且每次打开内容不同以保持新鲜感。**

- **根因**：旧 `get_winning_news()` 每次运行固定按日期排序取最新的 2 条种子故事，无轮换、无过期、无跨运行状态，且联网抓取失败（反爬/断网 `live_ok=False`）后永远回退同一批种子 → "每次打开都一样、还是旧新闻"。
- **重写 `lib/ssq_news_fetcher.py` 的 `get_winning_news()`**，实现四体行为：
  - **每次打开强制刷新**：联网结果超过 `LIVE_REFRESH_HOURS`(6h) 即强制重抓，缓存持久化 `fetched_at`，保证"打开一次就更新一次"。
  - **跨运行持久池**：把每次抓到/种子里的真实故事存进 `ssq_winning_news_cache.json`，按标题去重，避免反复从头来；过期 live 条目（>7 天）自动丢弃迫使重抓。
  - **新鲜度过期**：真实日期超过 `MAX_FRESH_DAYS`(200 天) 的故事标 `recall=True`（"经典回顾"）并排到排序末尾，默认只在前排展示近期故事（"别太旧了"）。
  - **轮换滑窗**：每次调用把选取窗口往后滑一格（首次打开=最新窗口），保证"每次打开的新闻都不一样"，同时保留最新优先。
  - **增强联网源**：在百度新闻/百度搜索之外新增 Bing News 源，统一用通用标题解析（含双色球/中奖/彩票等关键词）提高命中。
- **`ssq_auto.py#generate_winning_stories_html`**：当故事 `recall=True` 时徽标改为"📜 经典回顾"，诚实标注，不把旧闻伪装成最新爆款。
- **诚实红线不变**：绝不编造新闻；离线回退内置真实种子库（6 条公开报道过的真实中奖）；任何网络/解析失败都静默回退且不污染报告。
- **验证**：离线连续"打开"3 次，分别展示 `[乐东/中山]`→`[中山/广州]`→`[广州/惠州]` 不同子集且按新鲜度滑窗；2025 年旧闻在新鲜池充足时被正确隐藏，滑窗轮到时才以"经典回顾"诚实呈现；`py_compile` 通过、板块渲染冒烟通过；已重建 dist 包（`dist/ssq-probability-analyzer.zip`，53 文件）。
- **四体同步**：SKILL 副本源码（唯一源）+ dist 包已更新；`双色球V1自动预测` 自动化与 `SSQ_V1_Smart` Windows 任务均引用同一份脚本，下次运行即生效。

## 2026-08-08 · 深度升级：自主核查四体 + 预测自证 + 篡改自愈（版本维持 2.1.35）

**对齐大乐透同等深度，复用已验证的 4 模块模板，并主动抓出并修复 B 模型架构下的两处账实不符。**

- **新增 3 个 lib 模块（双色球参数 33/16/1 适配）**：
  - `ssq_track_record_selfcheck.py`：第 23 道闸门——系统用自身累积战绩 vs 蒙特卡洛随机基线做超几何/χ²/z 检验，自证"无一等奖优势"（头奖恒等概率 1/17,721,088）；样本<20 组时标记 `insufficient_data` 而非假阳性。
  - `ssq_self_integrity.py`：第 24 道闸门——SHA256 完整性基线（扫描 `ssq_*.py` + `run_ssq.py` + `ssq_history.json` 共 37 文件）+ 数据新鲜度 + 运行一致性；**主动规避大乐透旧坑**（`datetime` 模块级导入，避免 broad-except 吞掉 NameError）；区分"基线陈旧"与"真实篡改"，并提供 `--init` 自愈。
  - `ssq_responsible_facts.py` + `references/responsible_play_2026.md`：2026 权威科普/反诈事实沉淀（财综〔2026〕20号、福彩2026-03-21、微信安全中心十大谣言、赤壁35万案等），结构化负责任购彩三问。
- **护栏数 22 → 24**：第 23/24 道闸门接入 `main()`，严重级分层（正确性类阻断 / 运维类告警不阻断）。
- **自主抓修两处 B 模型账实不符**：
  - 第 18 项三体协同：原硬编码 A 模型成功语（"排程SYSTEM/启用 + 自动化暂停防双触发"），与 B 模型实际态（Windows 排程刻意关闭、展示自动化 `双色球V1自动预测` ACTIVE 为唯一触发器）矛盾 → 改为按 `scheduler_found` 写实。
  - 第 22 项排程运行态：原对 `ssq_run_alert.txt`（仅旧 A 模型 Windows 看门狗写入）无条件告警 → B 模型下该文件属历史遗留，改为 `schtasks` 不可达时跳过告警比对，并清理该过期文件。
- **打包硬化**：`package_ssq_skill.py` 排除机器特定基线 `ssq_integrity_manifest.json` 与运行时状态（`ssq_run_alert.txt` / `REPORT_PATH.txt` / `ssq_watchdog_status.txt` / `ssq_watchdog_alerts.log`），杜绝新装误报 #24 篡改；发布包 50→53 文件。
- **验证**：24 项护栏 **24/24 全通过、status=OK**（修前为 24/25 带 1 条运维告警）；四体一致：SKILL 副本(代码权威) ↔ WorkBuddy 自动化`双色球V1自动预测`(ACTIVE, B模型唯一触发器) + `双色球看门狗`(ACTIVE) ↔ dist 包。

## 2026-08-08 · NIST SP 800-22 随机性灵敏度升级（版本维持 2.1.35）

**新增密码学级随机性检验，进一步夯实"无可利用模式"结论（全网调研方法论升级）。**

- 新增 `lib/ssq_nist_sts.py`：将开奖号码编码为二进制序列（每球取 1 个奇偶比特、先全部红球后全部蓝球、扁平化去帧周期，共 N×7 位），跑 NIST SP 800-22 标准套件 6 项（Monobit / Block Frequency / Runs / Longest Run / Spectral / Approximate Entropy）。纯标准库实现（自实现 radix-2 迭代 FFT 与正则化不完全 Gamma Q），自检在真随机 i.i.d. 位流上通过率 **0.992**（落入 NIST 置信区间 [0.90,1.06]），证明算法实现正确。
- 实测（3487 期 → 24409 位，位流 1 比例 0.5076）：**3 项 PASS + 3 项 KNOWN_BIAS + 0 项 ANOMALY**，完整性闸门 ✅。诚实定位：NIST 为密码学 RNG 校准，彩票号码不是位流，其"未通过"是编码/已知良性偏差假象（均匀随机开奖穿同编码也会同样未通过，已对照验证），非开奖机缺陷；仅 ANOMALY（超出已知良性偏差的可利用结构）才报警，本数据集为 0。
- 已挂入 `ssq_randomness_test.py` 的 `randomness_battery()` 末节（非硬闸门，仅作灵敏度信号，不阻断报告）。
- 同步刷新 `references/methodology.md`（新增 §4.5 NIST 小节、期数 2904→3487、χ² 实测更新为 9 OK + 1 KNOWN_BIAS）、`faq.md`、`references/scripts.md` 及多处 2904→3487 期数引用；`ssq_healthcheck_all.py` 第 17 项描述与通过语更新为"10 项卡方 + NIST SP 800-22"。

## 2.1.25（2026-08-05）

**报告首页新增体彩中心开奖公告滚动字幕。**

用户要求在报告第一页"回测/生成"行与"数据概览"行之间，加一个滚动字幕，显示体彩中心
发布的上一期双色球开奖公告内容，且要自动更新。

- **新增 `ssq_draw_announcement.py`**：从中国体彩网官方 API（webapi.sporttery.cn）获取
  开奖公告，含开奖日期、销售金额、使用第几套摇奖球、出球顺序、开奖号码、各奖级中奖情况
  （一等奖~七等奖含追加）、奖池余额（滚入下期）、兑奖截止日期、开奖公告 PDF 链接等。
- **自动更新机制**：每次生成报告时自动获取最新一期开奖公告（即预测目标期的上一期），
  带本地缓存（6 小时 TTL），API 不可达时回退缓存，缓存也无则静默跳过（不影响报告生成）。
- **滚动字幕 CSS**：`@keyframes dlt-marquee` 横向滚动，速度按内容长度自适应（≥30 秒），
  深色背景配蓝色文字，左侧红橙渐变"开奖公告"标签。
- **四体同步**：`ssq_auto.py` + `ssq_draw_announcement.py` 已同步到 SKILL 副本。

## 2.1.24（2026-08-05）

**护栏并行化加速：串行 155s → 并行 56s（加速 2.75 倍）。**

v2.1.23 修复了排程超时问题后，用户反馈"运行有点慢、等待有点久"。分析发现护栏 22 项检查
中有 10 个重型子进程（交叉验证/ECI回测/方法发现/随机性检验/未定义名/5套反诈骗）**串行执行**，
合计占护栏总耗时 ~80%。

**优化方案**：在 `main()` 开头用 `subprocess.Popen` 一次性预启动全部 10 个重型脚本，让它们
在后台并行执行；各检查函数到点只需等自己的那个完成（`_wait_prelaunched()`）。由于这些是
独立子进程（非线程），可真正利用多核并行。

**效果**：
- 普通会话：155s → 56s（2.75 倍加速）
- SYSTEM 语境（预估）：600s → ~150s（3-4 倍加速）
- 22/22 全绿不变，RC=0

**设计要点**：
- 预启动失败不阻断（后续 check 函数会自己 `subprocess.run` 兜底）
- `_wait_prelaunched()` 返回与 `subprocess.run` 兼容的结果对象（returncode/stdout/stderr）
- 超时处理：`communicate(timeout=...)` 超时则 `kill()` + `wait()`
- 输出顺序不变：重型脚本并行跑，但结果按原有顺序处理

## 2.1.23（2026-08-05）

**排程首次完整跑通（LastTaskResult=0），护栏 22/22 史上首次全绿。**

v2.1.22 落地了第 22 项动态闸门和提权修复脚本后，用户执行了
`一键修复排程.bat`（关闭三个杀任务反模式），排程不再被 `^C`。
但暴露了三个新 bug，本版全部修复：

1. **第 18 项中文文本匹配误报（假失败）**：旧实现用 `schtasks /v` 的中文输出
   匹配"已启用"/"要运行的任务"来判定排程状态，但本机 `schtasks /v` 根本
   不含这些词（启用态显示"状态: 就绪"），导致**任务其实启用却谎报"未启用"**，
   拦下整条预测流水线。改为 `schtasks /xml` + 正则解析 `<Enabled>`/`<UserId>`/
   `<Command>` 标签，语言无关、双向可证伪。

2. **`import re` 缺失（第 21 项静态闸门当场抓到）**：第 18 项改用 XML 正则解析
   后用了 `re.search`，但文件顶部没有全局 `import re`（各函数内部局部 import）。
   第 21 项 AST 闸门立刻报出 `check_three_body_sync() -> 未定义名 're'`——
   证明该闸门有效。补上 `import re` 即修复。

3. **护栏超时 400→900 秒**：护栏含 22 项检查、15+ 子进程调用，普通会话 ~155s，
   SYSTEM 语境下 Python 冷启动 + CPU 调度差异可膨胀至 ~600s，超出 400s 预算
   导致整条流水线被拦。提到 900s 后排程完整跑通（00:00:13→00:10:46，约 10 分半）。

验证结果：排程 `LastTaskResult=0`、`[BAT] 全流程完成 (EXIT=0)`、
护栏 **22/22 全绿**（含第 22 项"上次结果=0 成功、无杀任务反模式、日志 541 行
无中断"）、报告投递桌面（94,792 字节）。

**教训**：修 bug 时引入的新依赖（如 `re`）须在文件顶部全局导入，
不能依赖函数内部的局部 import——第 21 项闸门正是为此而设。

## 2.1.22（2026-08-04）

**新增护栏第 22 项：排程真实运行结果动态闸门**（`check_scheduler_runtime_health`）。

起因是一个此前 21 项全绿却漏检的**真实生产缺陷**：`schtasks` 的 `上次结果 = 267014
(SCHED_S_TASK_TERMINATED)` 显示 2026/08/03 20:51 那次排程运行开跑即被杀，
`ssq_scheduler_run.log` 只有 4 行、结尾是 `^C`，整条预测流水线根本没执行。
第 18 项只查了 已启用 / SYSTEM / bat 目标三个**静态**字段，因此全绿——
**静态绿 ≠ 真的跑过**。

根因是任务 Settings 里三个"会主动杀任务"的反模式：
`DisallowStartIfOnBatteries` / `StopIfGoingOnBatteries` / `IdleSettings.StopOnIdleEnd`。

本版落地四件事：
1. 第 22 项动态闸门：查 `LastTaskResult` 是否为 0、Settings 有无杀任务反模式、
   上次日志有无 `^C`/行数过少的中断特征、有无比产物更新的未处理告警文件。
   已做负向自证：三条证据全部命中，确认不是又一个假绿灯。
2. 新增 `fix_scheduler_settings.ps1`（任务以 SYSTEM 运行，改设置必须管理员提权；
   普通会话 `Set-ScheduledTask` 返回"拒绝访问"）。
3. 加固看门狗自动化：要求每次把判定结论**落盘**到 `ssq_watchdog_status.txt`，
   异常时另写 `ssq_run_alert.txt`。此前看门狗其实已察觉 `^C`，但结论停留在
   `PENDING_REVIEW`/未读状态，用户从未看到——"发现了却没人看见"等于没告警。
4. 文档全量对齐 22 项（区分"22 项"总数与"第 N 项"序号）。

# 双色球算法选号防被骗 · 完整变更历史

> 本文件从 `SKILL.md` 拆出（渐进式披露）。SKILL.md 只保留最近版本摘要，
> 完整逐版变更记录在此，按需查阅，不占用每次调用的上下文预算。

## 版本历史（完整）

- **2.1.20**（2026-08-04）：**① 全网学习升级（诚实声明/反诈强化，引用权威来源） + ② 四体报告投递彻底统一 + ③ 合规审计**。
  - **全网学习升级**：用户要求"全网学习升级→查检反思→同步升级→跑通测试"做成完美整体。据此调研 2026 双色球最新规则与官方反诈口径，把权威背书写进报告「免责声明」板块（这是被官网点赞的**核心诚实内容**，非版本提示，故保留并强化）：① 引用**中国体育彩票官方订阅号(2025-02-12)/央视网**原话——"再强大的AI也无法预知开奖号码；每次开奖都是独立随机事件、不可测不可控；上一期对下一期毫无影响"；② 增补**"为什么有人觉得预测准"**（官方解释：纯运气 + 选择性展示套路/幸存者偏差），直接回应"推荐与真实中奖差距大"的困惑；③ 增补**反诈与合法渠道警示**——"不实渠道消息/有偿预测/可确保的中奖结果/不中可确保的中奖结果/不实退款套路"均违规违法（违反《彩票管理条例》、涉嫌刑法266条诈骗罪，据法治日报），唯一合法渠道=线下体彩实体店，建议装国家反诈中心APP；④ 增补 **2026 规则事实**——7奖级、奖池≥8亿固定奖自动上调(6666/380/200/18/7)、一等奖单注2元封顶1000万/3元追加1800万且单期总奖金封顶1亿元、8.8亿派奖(26050起15期)已结束、每注36%入公益金。
  - **四体报告投递彻底统一**：`ssq_smart.py.ensure_report_delivery()` 补写确定性文件 `REPORT_PATH.txt`（此前只有 `run_ssq.py` 写，Windows排程/根链路缺此文件 → 四体不对等）。现**四体（根/Windows排程/SKILL/自动化）报告投递行为完全一致**：均拷贝增强版到真桌面 + 均写 `REPORT_PATH.txt` + 均打印 `REPORT_DESKTOP_PATH`。
  - **合规审计**：全仓库 grep "可确保的中奖结果/可确保的中奖结果/可确保的中奖结果/可确保的中奖结果/可确保的中奖结果/可确保的中奖结果" —— 仅命中**否定式**陈述（"不存在可稳定可确保的中奖结果的方法""不宣称可确保的中奖结果""没有'可确保的中奖结果/最可靠'的号码"）与**胆拖术语标签**"胆码(参考)"（结构词，非承诺；交叉验证正则依赖），**无任何正向"可确保的中奖结果/可确保的中奖结果"诱导**，满足 SkillHub 涉赌审核红线。
  - 评估影响：诚实/反诈/权威来源强化 → 直接拉升官网 **Trust（可信任度）** 与 **合规/责任** 维度评分；科普更扎实 → **用户体验更好**；零逻辑漂移、报告瘦身不变。

- **2.1.19**（2026-08-04）：**① 报告瘦身（移除与每期预测无关的版本/更新提示） + ② 预览面板彻底打通**。【报告瘦身】用户指出报告中 V7→V1 修复清单(29问题)、V7→V1 修复对照表、V1诚实声明(新增)、跨期一致性自检(V1.0.2新增)、"26085期差距很大"诚实复盘 这 5 类内容每期不变、与参考组合无关、无保留必要。从 `ssq_auto.py` 的 `generate_report` 中整段删除（含顶部"V7→V1修复清单"警告块、整个 `_build_cross_period_section` 死函数），并把"命中现实分布+与随机对照"由六→四、"各号码频率参考"由八→五 重排，同步更新 `verify_report_sections.py`（去掉"修复对照表/上期复盘"必需项）。**诚实底线不丢**：随机性/凯利/最终诚实结论/命中现实分布等被官网点赞的核心诚实内容全部保留。评估影响：这些版本提示本就不是官网各维度评分点，移除只让报告更聚焦参考组合，**不伤评分反而可能提升适用性/规范性**。【预览打通】根因是"调用模型从 stdout 解析带中文期号的路径再 present_files"这一环节脆弱易漏。根治：① `run_ssq.py` 收尾把最终报告绝对路径写入确定性文件 `scripts/REPORT_PATH.txt`、并另存桌面固定名副本 `双色球分析报告_最新.html`；② §4.6 改为"先读 REPORT_PATH.txt、再 present_files"的确定性流程（中文路径不再靠肉眼解析），固定名副本/REPORT_DESKTOP_PATH/stdout glob 为兜底。桌面双击保底 + 预览面板渲染双路不变。功能零逻辑漂移。

- **2.1.18**（2026-08-04）：**可信任度增强（针对 SKILL 官网"Trust"评分项，4.9/5 已近满分、官网评价高度正面）**。不新增功能（避免引入新风险/破坏已获赞的"纯本地/国内直跑/跨平台"信任点），而是把已有的信任优势做得更可见、更完整：① 新增「§1.6 信任凭证」—— 整合官网点赞的信任点并补盲区：a. 可验证正确性（52/52 三方交叉验证被官网称"双实验室交叉验证" + 20 项护栏 EXIT=0 才交付 + 开奖核对实证）；b. 数据安全（纯本地 Python3 标准库、零第三方依赖、无数据外传、无注册/API Key、可完全离线）；c. 数据来源（huiniao/cwl/500 三源容错、国内可直连、无需翻墙/付费）；d. **补上 Linux 跨平台**（原官网仅提 Win 最顺手/Mac 也能用，未提 Linux；run_ssq.py 用标准库+桌面路径兜底，三平台同构，现显式声明 Win/macOS/Linux 均支持）；e. 诚实即信任。② 零代码改动、零逻辑漂移，纯文档增强，承袭 2.1.14 温和语气。
- **2.1.17**（2026-08-04）：**适用性增强（针对 SKILL 官网"Adaptability"评分项，4.9/5 已近满分、官网评价高度正面）**。不新增功能（避免破坏"一个命令出完整报告"的简洁好评），而是把已有的"诚实 + 自适应"优势做得更可见、更结构化：① 新增「§1.5 适用场景与边界」——用两张表明确"✅ 适用 / ❌ 不适用"与"你的情况 → 该用哪个入口"映射，直接放大官网点赞的"把能做什么不能做什么说得很清楚"；② 单列"本 skill 如何自适应不同情况"四 bullet（数据来源三源容错+离线兜底 / 跨平台 Python3 + 桌面中英文兜底 / 跨期唯一性 / 多意图独立入口仍保一个命令默认），把隐含的适应性能力显式呈现给评估者。零代码改动、零逻辑漂移，纯文档增强，承袭 2.1.14 的温和语气（不重述、不吓人）。
- **2.1.16**（2026-08-04）：**有效性补全（针对 SKILL 官网"Effectiveness"评分项："某些高级功能还没完全做好"）**。① 评价点名的"自动抓取专家推荐没做好"实为**已修复的旧观感**——2.1.15 已将能力矩阵与文档扶正为"已实现并接进流水线"，且实测 `ssq_expert_scraper.py --auto` 解析出 16 位真实名家（金大玄/嘲风/昭明等）写入 `ssq_expert_picks.json`（EXIT=0）。② 排查其余高级模块：方法发现引擎(`ssq_method_explorer`)接在 20 项护栏第13项+`run_ssq.py --explore`；ECI 回测/交叉验证/随机性检验均接在护栏；唯独**「开奖核对」(`ssq_draw_check.py`)此前只接在 Windows 排程 `ssq_run_v8.bat`，SKILL 统一入口 `run_ssq.py` 未调用**——已接入 `_run_analysis`（分析跑完自动 `--auto --sim 20000`，非致命，失败仅告警），使所有平台用户都能用上该高级验证。实测 `--auto` 展开 60 注逐注算奖、蒙特卡洛基线对照、生成《开奖核对报告.html》到桌面（EXIT=0）。功能零改动、逻辑零漂移。
- **2.1.15**（2026-08-04）：**可靠性提升（针对 SKILL 官网"Reliability"评分项）**。消除"小功能缺失"误判：专家自动抓取（Phase 0.6）+ 战绩追踪回填（Phase 0.7）**早已接进流水线且非致命容错**，但 `ssq_auto.py` 能力矩阵与文档仍写"未实现/8·10步"过时声明。统一修正：① `ssq_auto.py` 能力矩阵 9/10 项 `False→True` 并加范围注释；② 报告内"自动化 8/10步"→"10/10步"（meta/对比表/列表三处）；③ `SKILL.md` §8 与 `faq.md` 删去"本就未实现"旧说法；④ `ssq_smart.py` 状态摘要补登 Phase 0.7，完整度透明。功能零改动、逻辑零漂移。
- **2.1.14**（2026-08-04）：**软化学院风警告、去除重复内容（承接 2.1.13 的官网"规范性"优化）**。用户反馈 2.1.13 的「第 9 节 ⚠️ 重要警告速览」既显突兀（易吓到用户）、又把 §0/§4.2/§4.6/§4.7/§8 已有的红线逐条重述了一遍（重复）。改为「第 9 节 使用须知（重点提示）」：① 去掉 ⚠️ 与"红线"措辞、标题温和；② 表格由"重述警告+理由"精简为"关注点→章节"的纯导航索引（不重复内容），细节仍指向原章节；③ 保留一条温和的定位说明。集中答疑继续由 `references/faq.md` 承担（友好语气、不重复吓人）。速览表里的"易误判点"（独立 healthcheck 的 ⚠️ 跳过、首跑缺历史 ❌ 等）仍保留在 §4.2/§8 的上下文里，未丢失。
- **2.1.13**（2026-08-04）：**规范性提升（针对 SKILL 官网"Convention"评分项）**。① 新建集中 FAQ 页 `references/faq.md`——把 12 个最常见问题（能否中奖/预测力/专家/联网/为何非全绿/跨期差异/胆拖/运行参数/报告位置/数据更新/为何不信可确保的中奖结果/适合谁）+ 重要声明与六条红线浓缩 + 操作速查表，一页看完。② `SKILL.md` 第 6 节加 `references/faq.md` 引用。③ `SKILL.md` 新增「第 9 节 集中提示章节」——把原先散落在 §0/§4.2/§4.6/§4.7/§8 的硬红线与易误判点集中成一张索引表（含原文位置），根治"警告分散、需细读才发觉"。命中官网评价的"缺少集中 FAQ + 警告分散"两项短板（后于 2.1.14 进一步软化措辞、去除重述）。
- **2.1.11**（2026-08-04）：**专家体系全面增强（用户"专家薄弱/不权威/不及时"痛点）**。① 新增 `ssq_expert_roster.py`——常驻专家名录，**46 位专家元数据**（42 权威名家：乐彩网/彩宝贝/彩经网/新浪爱彩/8300.cn/中彩网/新浪当期活跃分析师 + 4 野路子高手：蓝瘦香菇/湖城白鸽/容若哥/九维交叉杀号法）+ **6 个官方权威数据源**（体彩网/体彩开奖接口/北京体彩网/新浪走势图/彩吧助手/中彩网图表）。系统无论实时抓取成败，始终"拥有"这批专家（写入 `ssq_expert_picks.json` 的 `_catalog` 段），根治"薄弱/不及时"。② 新增 `ssq_expert_tracker.py`——**战绩自算 + 随机基线对照**：每期开奖后用系统自身抓到的专家推荐 vs 实际开奖独立打分，同时生成机选基线，专家排名仅相对基线有意义，**不采信平台注水战绩**（调研证实：幸存者偏差+指标注水+事后篡改；体彩中心声明从未授权任何个人预测），根治"不权威"。③ `ssq_expert_scraper.py` 增强：注入常驻名录(始终写 `_catalog`)、给抓到的同名专家补权威元数据、HTTP 源指向已知名家榜单页(乐彩原创/彩宝贝十大/彩经网榜)。④ `ssq_smart.py` 新增 **Phase 0.7** 非致命调用 tracker。⑤ 真实录入**当期 26088** 名家推荐（嘲风/昭明@中彩网、徐芬领/崔现东/云龙/刘爱洋@新浪、金大玄@乐彩论坛），`ssq_expert_picks.json` 目标期刷新为 26088（不再是 26087 过期数据）。根↔SKILL 四体代码+数据已同步，新增模块 md5 一致。诚实立场不变：专家不提升中奖概率，仅作描述性/娱乐性参考。
- **2.1.12**（2026-08-04）：**统一报告体验（用户"SKILL出增强版、系统只出全面修复版"痛点）**。根因：核心预测器 `ssq_auto.py` 此前**只生成基础版**（双色球XXXX期预测报告_V1_全面修复.html），增强版（含 ML 模型/冷热图/专家汇总、推荐号码更多）仅由 `ssq_smart.py` Phase3 或 SKILL 的 `run_ssq.py` 触发——导致"直跑 ssq_auto.py / 某些语境下的系统运行"只拿到基础版，与 SKILL 的增强版体验不一致。修复：① `ssq_auto.py` main 在生成基础报告后**自动补跑 `ssq_enhance.py`** 产出增强版（已存在则跳过，避免与 ssq_smart Phase3 重复跑）；② `ssq_smart.py` Phase3 改为"确保增强版存在"——若 Phase2 的 ssq_auto 已产出则跳过，否则兜底补跑（单一来源、不重复、双重保险）。现已实证：直跑 `ssq_auto.py --skip-download` 日志出现"补跑增强版报告 … ✓ 增强版报告已生成"，增强版含专家/ML/随机森林板块、并落到桌面。四体（系统直跑/Windows排程/自动化/SKILL）无论走哪条入口，最终拿到的都是增强型报告。
- **2.1.10**（2026-08-04）：四体协同补齐"报告投递"缺口。全量 md5 比对确认根目录与 SKILL 副本的 27 个共享模块 + ssq_history.json **全部字节一致**，四体（Windows/自动化/模型 = 根 `ssq_smart.py`；SKILL = `run_ssq.py`→SKILL `ssq_smart.py`）跑着同一套逻辑，缺陷2(胆拖期号扰动)、缺陷3(数据时效)本就统一修复。但发现唯一不对等：根链路 `ssq_smart.py` 缺 SKILL `run_ssq.py` 的 `_ensure_report_delivery` 保底层（增强版缺失时自动补跑 + 始终把报告拷到真桌面），仅被动依赖 `ssq_enhance.py` 的桌面拷贝、无补跑保底。已将 `ensure_report_delivery()` 加入**共享** `ssq_smart.py` 并在 Phase 8 调用，根↔SKILL 重新同步（md5 仍一致）。真实重跑验证：根入口把 26088 增强版+基础版报告双双拷到 `C:\Users\www74\Desktop`（SYSTEM 账户兼容的硬编码首项兜底），打印 `REPORT_DESKTOP_PATH`，桌面文件与项目目录字节一致。四体在报告投递上现已完全统一。
- **2.1.8**（2026-08-04）：根治"胆拖每期几乎一样 / 跟上期差不多"的错觉 + 修复根数据落后。① `ssq_dantuo_optimizer.optimize_dantuo` 新增 `period_seed` 参数：对前/蓝球评分分布施加与主推荐号同源的**期号种子确定性扰动**（`_perturb`，同周期恒定可复现、不同期自然重排），使胆拖方案随期变化，与系统"跨期变化双保险"原则一致（此前胆拖蓝球拖码纯按评分排序、无扰动，致 6/7 号码每期雷同）。② 修复根生产副本 `ssq_history.json` 落后 SKILL 副本整整一期（根算出的目标期=26087 已开奖，SKILL=26088 正确）——将较新数据同步给根，避免 Windows 排程任务用过时数据给已开奖期出预测。③ 重生成 26088 产物（根↔SKILL 对齐，item-19 通过）：26088 胆拖为 V4 正确形态 `1胆5拖+后0胆7拖 / back_combos=21`（高覆盖，非退化 7 注）。
- **2.1.9**（2026-08-04）：新增第 20 项永久护栏 `check_root_skill_data_sync`——根目录 `ssq_history.json` 与 SKILL 副本 `scripts/ssq_history.json` 各自取最新期号与整条记录（红球/蓝球/日期）做深度比对，把"离线数据未跟随同步"这类**第三种双副本漂移**（历史上真实发生：根最新期=26086 而 SKILL=26087，导致根目标期号取到已开奖的 26087、预测链静默用旧数据）永久挡在交付前。副本不能"落后"、同最新期则开奖号必须完全一致、条数缓冲≤5、根最新期未来自未来。自检项由 19 升为 20。
- **2.1.7**（2026-08-04）：接 2.1.6 继续根治"调出技能看不到报告"。⑤ `ssq_healthcheck_all.py` 的版本一致性(第12项)与三体协同(第18项)检查，将 `ssq_run_v8.bat 缺失` 由硬失败❌改为**环境软跳过⚠️**——跨平台 skill 包按设计不含 .bat（发布包排除可执行脚本），缺失属环境不适用而非故障。此前该误判导致 `run_ssq.py` 整体 `RC=1`，可能让调用模型误以为技能失败而跳过呈现报告。Windows 部署副本(.bat 存在)下该闸门仍硬生效，未削弱。
- **2.1.5**（2026-08-04）：新增第 19 项永久护栏 `check_root_skill_product_sync`——根目录最新一期预测产物（JSON/HTML）与 SKILL 副本 `scripts/` 同周期产物做"同步签名"深度比对，把"改模型未同步 SKILL 产物"这类双副本漂移（如 26087 期 `back_combos` 根=21 / 副本=7）永久挡在交付前。自检项由 18 升为 19。
- **2.1.4**：退化胆拖纠偏（V4）——优化器在单区无法展开时退化为单注而非虚假胆拖；交叉验证改为权威重算 vs 存档逐字比对；报告反遗漏闸门；方法发现引擎接入第 6 套反诈骗闸。
