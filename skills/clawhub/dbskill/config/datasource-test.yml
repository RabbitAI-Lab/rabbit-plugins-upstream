datasource:
  url: "jdbc:mysql://localhost:3306/orange?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC"
  driver: "com.mysql.cj.jdbc.Driver"
  username: "${DB_USER}"
  password: "${DB_PASS}"
  pool:
    maxSize: 5
    minIdle: 1
    connectionTimeout: 5000
