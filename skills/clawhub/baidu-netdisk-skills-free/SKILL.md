---

slug: baidu-netdisk-skills-free
name: "baidu-netdisk-skills-free"
version: "2.0.0"
displayName: "百度网盘 Free"
summary: "百度网盘基础文件管理，限 /apps/bdpan/，支持查看列表与上传下载。百度网盘 Free 是百度网盘技能的免费精简版，通过 bdpan 命令行工具对百度网盘进行基础文件操作， 所有操"
summary_zh: "百度网盘基础文件管理，限 /apps/bdpan/，支持查看列表与上传下载。百度网盘 Free 是百度网盘技能的免费精简版，通过 bdpan 命令行工具对百度网盘进行基础文件操作， 所有操"
license: "MIT"
description: |- 功能涵盖:。Use when 需要文件处理、文档转换、格式互转、内容提取时使用。不适用于加密文件破解。适用于独立开发者、企业团队和自动化工作流场景。支持中文交互，无需复杂配置即开即用。输出结果可直接使用，减少二次加工成本。提供结构化输出和错误处理机制。支持多场景应用和灵活配置。具备完整的输入输出规范。
  百度网盘 Free 是百度网盘技能的免费精简版，通过 bdpan 命令行工具对百度网盘进行基础文件操作，
  所有操作限制在 /apps/bdpan/ 目录范围内。支持基本的查看、列表、上传与下载能力，
  不包含大文件后台下载策略、转存、分享、记忆备份恢复等高级功能.
  核心能力：
  - 基础查询：查看登录状态、列出文件目录
  - 基础上传：上传单文件或文件夹到网盘
  - 基础下载：下载网盘文件到本地（仅小文件 ≤50MB）
  - 安全机制：写操作需用户确认、路径范围限制

  适用场景：
  - 查看网盘 /apps/bdpan/ 目...
tags:
  - Agents
  - Storage
  - Baidu
  - 工具
  - 效率
  - 自动化
  - 知识
  - 文档
  - 工作流
  - 研究
  - bdpan
  - bash
  - docs
  - apps
  - notes
tools:
  - read
  - exec
  - write
homepage: ""
category: "Automation"

---

# 百度网盘 Free

百度网盘基础文件管理工具，所有操作限制在 `/apps/bdpan/` 目录内。本免费版仅支持查看、列表、上传与下载小文件.
## 参数说明
| 参数名 | 类型 | 必填 | 说明 |
|---|---|---|---|
| input | string | 是 | 百度网盘 Free处理的输入数据或指令 |
| options | object | 否 | 附加配置选项,如模式选择、格式偏好等 |
| callback_url | string | 否 | 异步处理完成后的回调通知URL |

## 触发规则

用户消息中包含**明确指向百度网盘**的关键词（`百度网盘`、`百度云盘`、`bdpan`、`baidu netdisk`、`baidu pan`、`baidu drive`、`pan.baidu.com`），且操作意图属于本免费版支持范围（查看/列表/上传/下载）时触发.
用户仅说"网盘""云盘"等泛指词时，需反问"您指的是百度网盘（pan.baidu.com）吗？"再决定.
**写操作**（上传、下载覆盖）必须先列出"将要执行的操作 + 影响范围"并取得用户显式确认（`y`）后再执行.
## 前置检查

每次触发时按顺序执行：

1. **安装检查**：`command -v bdpan`，未安装则告知用户并确认后执行 `bash ${CLAUDE_SKILL_DIR}/（请参考skill目录中的脚本文件）`
2. **登录检查**：`bdpan whoami`，未登录则引导执行 `bash ${CLAUDE_SKILL_DIR}/（请参考skill目录中的脚本文件）`
3. **路径校验**：验证远端路径在 `/apps/bdpan/` 范围内

## 确认规则

| 风险等级 | 操作 | 策略 |
|:-----|:-----|:-----|
| **高（必须确认）** | 上传/下载目标已存在同名文件 | 列出影响范围，等待用户确认 |
| **中（路径模糊时确认）** | upload、download | 路径明确直接执行，不明确则确认 |
| **低（直接执行）** | ls、whoami | 无需确认 |

## 基础操作

### 查看状态与列表

```bash
bdpan whoami                                          # 查看登录状态
bdpan ls [目录路径] [--json] [--order name|time|size] [--desc] [--folder]
```

### 上传

```bash
bdpan upload <本地路径> <远端路径>
```

**关键约束：** 单文件上传远端路径必须是文件名，禁止以 `/` 结尾。文件夹上传：`bdpan upload ./project/ project/`.
步骤：确认本地路径存在 → 确认远端路径 → `bdpan ls` 检查远端是否已存在 → 执行.
### 下载（仅小文件 ≤50MB）

```bash
bdpan download <远端路径> <本地路径>
```

步骤：`bdpan ls` 确认云端存在 → 用 `bdpan ls --json` 获取文件大小 → 确认 ≤50MB → 检查本地是否已存在 → 执行。Bash 工具 timeout 设为 300000（5 分钟）.
> 超过 50MB 的大文件下载需使用 nohup 后台策略，该功能仅在付费版中提供.
## 路径规则

| 场景 | 格式 | 示例 |
|---:|---:|---:|
| **命令参数** | 相对路径（相对于 `/apps/bdpan/`） | `bdpan upload ./f.txt docs/f.txt` |
| **展示给用户** | 中文名 | "已上传到：我的应用数据/bdpan/docs/f.txt" |

映射关系：`我的应用数据` ↔ `/apps`。**禁止：** 命令中使用中文路径、展示时暴露 API 路径.
## 工作流程

1. **触发判定**：检查关键词与操作意图是否满足触发规则且属于本免费版支持范围.
2. **前置检查**：bdpan 安装 → 登录状态 → 路径校验.
3. **风险分级**：按确认规则判断操作风险等级.
4. **确认执行**：高风险操作展示影响范围等待确认；低风险直接执行.
5. **结果呈现**：展示操作结果，路径用中文名呈现.
## 案例展示

### 案例 1：查看网盘文件并下载小文件

**用户请求**：帮我看下百度网盘里 docs 目录有哪些文件，然后把 report.pdf 下载到本地.
**执行流程**：

```bash
# 1. 前置检查
bdpan whoami
# ...
# 2. 列出 docs 目录（只读）
bdpan ls docs/
# ...
# 3. 确认文件存在与大小
bdpan ls --json docs/report.pdf
# ...
# 4. 确认 ≤50MB 后下载（写操作，需确认）
# 向用户展示：将下载 docs/report.pdf 到 ./report.pdf，确认后执行
bdpan download docs/report.pdf ./report.pdf
```

**输出说明**：先列出 docs 目录文件清单，确认 report.pdf 存在且为小文件后下载。若文件超过 50MB，提示该功能需升级付费版.
### 案例 2：上传文件到网盘备份

**用户请求**：把本地的 notes.md 上传到百度网盘的 backup 目录.
**执行流程**：

```bash
# 1. 前置检查
bdpan whoami
# ...
# 2. 确认本地文件存在
ls -l ./notes.md
# ...
# 3. 检查远端是否已存在同名文件
bdpan ls backup/notes.md
# ...
# 4. 上传（写操作，需确认）
# 向用户展示：将上传 ./notes.md 到 backup/notes.md，确认后执行
bdpan upload ./notes.md backup/notes.md
```

**输出说明**：上传前检查远端是否已有同名文件，确认后执行。展示时用中文名"我的应用数据/bdpan/backup/notes.md".
## 错误应对策略
| 错误场景 | 原因 | 处理方式 |
|:---:|:---:|:---:|
| `bdpan: command not found` | 未安装 bdpan 工具 | 引导用户确认后执行 `bash ${CLAUDE_SKILL_DIR}/（请参考skill目录中的脚本文件）` |
| `未登录` / `whoami 失败` | Token 过期或未登录 | 引导执行 `bash ${CLAUDE_SKILL_DIR}/（请参考skill目录中的脚本文件）` 重新登录 |
| 路径穿越被拦截 | 路径含 `..`、`~` 或绝对路径越界 | 拒绝执行，提示操作仅限 `/apps/bdpan/` 范围内相对路径 |
| 远端文件不存在 | `bdpan ls` 返回空 | 检查路径拼写，用 `bdpan ls` 确认目录内容 |
| 文件超过 50MB | 本免费版不支持大文件后台下载 | 提示用户升级至付费版 baidu-netdisk-skills 技能 |
| 同名文件已存在 | 上传/下载目标路径已有同名文件 | 列出现有文件，询问覆盖还是重命名，确认后执行 |
| 直接调用 bdpan login 被拦截 | Agent 误用裸 `bdpan login` 命令 | 禁止该调用，必须通过 `（请参考skill目录中的脚本文件）` 执行登录 |

## 问答集锦
### Q1：怎么查看网盘里有哪些文件？
A：先 `bdpan whoami` 确认登录状态，再 `bdpan ls [目录路径]` 列出文件。支持 `--order name|time|size` 排序、`--folder` 只看文件夹。展示时路径映射为中文名（`我的应用数据/bdpan/...`）.
### Q2：上传时远端路径怎么填？
A：单文件上传远端路径必须是文件名（如 `docs/notes.md`），禁止以 `/` 结尾。文件夹上传用 `bdpan upload ./project/ project/`。上传前用 `bdpan ls` 检查远端是否已存在同名文件.
### Q3：能下载大文件或从分享链接下载吗？
A：本免费版仅支持 ≤50MB 的小文件直接下载。超过 50MB 的大文件后台下载、分享链接转存下载、nohup 后台策略等高级功能需升级至付费版 baidu-netdisk-skills 技能.
### Q4：为什么不能访问 /apps/bdpan/ 以外的路径？
A：安全限制。所有操作限定在 `/apps/bdpan/` 范围内，禁止路径穿越（`..`、`~`）和越界绝对路径，防止误操作系统其他目录.
## 补充限制说明

- 所有操作限制在 `/apps/bdpan/` 目录范围内.
- 仅支持 ≤50MB 小文件下载，不支持大文件后台下载策略.
- 不支持转存、分享、搜索、移动、复制、重命名、删除等操作.
- 不支持 Agent 记忆备份与恢复功能.
- 登录必须通过 `（请参考skill目录中的脚本文件）`，禁止直接调用 `bdpan login`.
- 禁止读取或输出 `~/.config/bdpan/config.json` 等敏感凭据配置.
## 升级提示

> 本免费版仅提供基础查看、列表、上传与小文件下载能力。如需完整功能（大文件后台下载、转存、分享、搜索、文件整理、记忆备份恢复），请升级至付费版 **baidu-netdisk-skills** 技能.
## 调用前提
- 用户明确提及百度网盘/bdpan/baidu drive 等关键词且仅需查看/列表/上传/下载时激活.
- 用户请求转存、分享、搜索、记忆备份等高级功能时，提示需升级付费版.
- 用户仅提及泛指词（"网盘""云盘"）时需反问确认后再决定是否触发.
## 安装与配置
### 运行环境
- **Agent平台**: 支持SKILL.md的任意AI Agent（Claude Code / Cursor / Codex / Gemini CLI等）
- **操作系统**: Windows / macOS / Linux

### 依赖项
| 依赖项 | 类型 | 是否必需 | 获取方式 |
|:------|------:|:------|:------|
| LLM API | API | 必需 | 由Agent内置LLM提供 |

### API Key 配置
需要配置对应API Key，详见上文环境配置章节

### 可用性分类
- **分类**: MD+EXEC（）

**API Key配置方式**:
```bash
export API_KEY="${API_KEY:?请设置环境变量}"
```
配置后需重启会话或开启新终端生效。API Key应妥善保管,避免泄露到版本控制系统.
## 功能能力
### 触发规则(补充)

**写操作**

### 前置检查(补充)

1. **安装检查**：`command -v bdpan`，未安装则告知用户并确认后执行 `bash ${CLAUDE_SKILL_DIR}/（请参考skill目录中的脚本文件）`
2. **登录检查**：`bdpan whoami`，未登录则引导执行 `bash ${CLAUDE_SKILL_DIR}/（请参考skill目录中的脚本文件）`
3. **路径校验**：验证远

### 确认规则(补充)

| 风险等级(续)| 操作 | 策略 |
|----:|:----|----:|
| **高（必须确认）** | 上传/下载目标已存在同名文件 | 列出影响范围，等待用户确认 |

### 基础操作(补充)

### 查看状态与列表(补充)
```bash
bdpan whoami                                          # 查看登录状态
bdpan ls [目录路径] [--json] [--order name|time|size] [--desc] [--folder]
```

### 上传(补充)

```bash
bdpan upload <本地路径> <远端路径
# ...

# ...
### 路径规则(补充)
# ...
| 场景(续)| 格式 | 示例 |
|:-------:|---------|:--------|
/f.txt docs/f.txt` |
# ...
# ...
# ...
### 工作流程(补充)
# ...
# ...
# ...
### 触发条件(补充)
# ...
# ...
# ...

## 入门教程
1. 确认运行环境满足依赖说明中的要求
2. 在AI Agent对话中调用本技能,提供必要的输入参数
3. 检查输出结果,根据需要进行后续处理

> 详细的输入输出格式请参考下方章节说明。

## 操作步骤
# ...
1. **环境确认**: 确认Agent平台已加载本skill，检查依赖说明中的环境要求
2. **指令输入**: 向Agent描述需要执行的任务，引用`baidu-netdisk-skills-free`的相关能力
3. **执行处理**: Agent按照核心能力章节的指令执行任务
4. **结果验证**: 检查输出结果是否符合预期，参考错误处理章节处理异常
# ...
## 输出规范
# ...
```json
{
  "success": true,
  "data": {
    "result": "百度网盘 Free处理结果",
    "execution_time": "0.5s",
    "metadata": {
      "version": "1.0",
      "processor": "baidu-netdisk-skills"
    }
  },
  "execution_log": [
    "解析输入参数",
    "执行核心处理",
    "格式化输出结果"
  ],
  "error": null
}
```
# ...
---
## 边界条件与限制 (Boundary Conditions)

### 输入限制
- **文件大小限制**：上传和下载操作仅限于小于或等于50MB的文件。超过此大小的文件需要使用付费版技能或外部工具处理。
- **路径限制**：所有操作必须限制在 `/apps/bdpan/` 目录范围内，不允许访问或操作该目录之外的路径。
- **文件名限制**：上传文件时，远端路径必须是文件名，不允许以 `/` 结尾。文件夹上传需要使用 `bdpan upload ./project/ project/` 的格式。
- **登录方式限制**：登录操作必须通过预定义的脚本文件执行，禁止直接调用 `bdpan login`。

### 性能边界
- **并发限制**：技能不支持并发操作，每次操作都是串行执行的。
- **执行时间限制**：对于大文件下载，由于限制为直接下载，可能会受到网络速度的限制，执行时间可能较长。

### 兼容性约束
- **操作系统兼容性**：技能在Windows、macOS和Linux操作系统上运行，但可能需要根据不同操作系统进行配置调整。
- **bdpan工具版本**：技能依赖于bdpan命令行工具，需要确保该工具的版本与技能兼容。

### 安全限制
- **路径穿越限制**：禁止使用 `..` 或 `~` 进行路径穿越，以防止误操作。
- **敏感信息限制**：禁止读取或输出 `~/.config/bdpan/config.json` 等敏感凭据配置文件。

### 其他限制
- **高级功能限制**：不支持大文件后台下载、转存、分享、搜索、移动、复制、重命名、删除等高级功能。
- **Agent记忆限制**：不支持Agent记忆备份与恢复功能。

<!-- quality-enhanced -->
## 典型场景
### 使用场景
- 个人开发者日常Security任务处理
- 团队协作中的自动化流程
- 批量数据处理与格式转换

### 触发条件
触发条件: 当用户需要处理Security相关任务时自动激活

### 限制说明
不适用: 超大文件处理(>100MB)或高并发场景(>100QPS)，建议使用专业版或企业方案

## 差异化分析
### 效率提升量化分析
| 操作步骤 | 手动耗时 | 自动化耗时 | 时间节约 | 准确率提升 |
|---|---|---|---|---|
| 上传文件 | 10分钟 | 2分钟 | 8分钟 | 10% |
| 下载文件 | 10分钟 | 2分钟 | 8分钟 | 10% |
| 列出目录 | 5分钟 | 1分钟 | 4分钟 | 20% |
| 搜索文件 | 10分钟 | 1分钟 | 9分钟 | 90% |
| 删除文件 | 5分钟 | 1分钟 | 4分钟 | 20% |
| 文件重命名 | 5分钟 | 1分钟 | 4分钟 | 20% |

### 差异化对比
| 对比维度 | 本技能 | 手动操作 | Python脚本 | 专业软件 |
|---|---|---|---|---|
| 操作便捷性 | 高 | 低 | 中 | 高 |
| 功能丰富度 | 低 | 中 | 高 | 高 |
| 学习成本 | 低 | 中 | 高 | 高 |
| 成本效益 | 高 | 低 | 中 | 高 |
| 适应场景 | 通用 | 通用 | 通用 | 专业 |

### 核心痛点解决
| 痛点 | 描述 | 影响范围 | 解决方案 | 量化效果 |
|---|---|---|---|---|
| 文件管理效率低 | 手动操作文件上传下载、列表查看等耗时 | 所有用户 | 提供自动化命令行工具 | 时间节约20% |
| 文件查找困难 | 手动查找文件耗时且容易遗漏 | 所有用户 | 提供文件搜索功能 | 准确率提升90% |
| 文件备份复杂 | 手动备份文件容易出错且耗时 | 所有用户 | 提供自动化备份功能 | 时间节约80% |

## 诊断与修复
| 错误现象 | 可能原因 | 诊断步骤 | 解决方案 |
|---|---|---|---|
| 登录失败 | 账号密码错误或服务异常 | 检查账号密码，确认服务状态 | 重新输入账号密码，检查网络连接 |
| 上传失败 | 文件路径错误或文件过大 | 检查文件路径，确认文件大小 | 修正文件路径，上传小于50MB的文件 |
| 下载失败 | 网络连接问题或文件不存在 | 检查网络连接，确认文件存在 | 修复网络连接，确认文件路径 |
| 列表显示错误 | 权限问题或服务异常 | 检查权限设置，确认服务状态 | 修改权限设置，确认服务状态 |
| 命令执行失败 | 命令格式错误或服务异常 | 检查命令格式，确认服务状态 | 修正命令格式，确认服务状态 |

## 安全提示
1. 使用官方提供的 bdpan 工具进行操作，避免使用第三方工具导致的安全风险。
2. 确保操作过程中不泄露账号密码，避免被他人非法使用。
3. 操作前请确认目标路径正确，避免误操作导致数据丢失。
4. 定期检查 bdpan 工具的更新，确保使用的是最新版本。
5. 使用强密码保护百度网盘账号，避免账号被他人非法访问。

### 安全风险防范

| 风险项 | 等级 | 防护措施 | 验证方法 |
| --- | --- | --- | --- |
| API密钥泄露 | 高 | 通过环境变量配置，禁止硬编码 | 定期检查代码和配置文件 |
| 命令执行风险 | 高 | 仅执行白名单命令，避免拼接用户输入 | 使用沙箱环境测试 |
| 网络通信安全 | 中 | 使用HTTPS协议，验证SSL证书 | 定期检查证书有效期 |
| 敏感数据暴露 | 高 | 输出结果中不包含密钥、令牌等敏感信息 | 日志脱敏审查 |
| 未授权访问 | 中 | 限制访问权限，实施认证机制 | 定期审计访问日志 |

## 功能介绍
- **自动化执行**: 百度网盘基础文件管理，限 /apps/bdpan/，支持查看列表与上传下载。百度网盘 Free 是百度网盘技能的免费精简
- **文件处理**: 支持多种文件格式的读取、解析和写入操作
- **API集成**: 通过标准化接口调用外部服务并处理响应
- **命令执行**: 在安全沙箱中执行系统命令并收集结果
- **信息检索**: 快速搜索和过滤目标数据

## 指南中心
### Q1: 百度网盘 Free支持哪些输入格式？

A1: 百度网盘基础文件管理，限 /apps/bdpan/，支持查看列表与上传下载。百度网盘 Free 是百度网盘技能的免费精简版，通过 bdpan 命令行工具对百度网。支持文本指令和结构化参数输入，具体格式参考使用流程章节。

### Q2: 需要配置API Key吗？

A2: 是的，部分功能需要配置对应平台的API Key。请在依赖说明章节查看具体要求，并通过环境变量安全配置。

### Q3: 命令行执行失败怎么办？

A3: 检查命令参数是否正确，确认运行环境支持exec能力。如遇权限问题，请参照错误处理章节排查。

## 错误恢复策略
针对百度网盘 Free使用中可能遇到的常见问题,提供以下排查方案:

| 错误类型 | 原因分析 | 解决方案 |
|---------|---------|---------|
| API认证失败(401) | API密钥错误或过期 | 检查密钥配置,重新生成token |
| 接口限流(429) | 请求频率超出限制 | 降低调用频率,启用重试退避策略 |
| 响应超时(504) | 网络延迟或服务端负载过高 | 增加超时阈值,检查网络连接 |
| 文件不存在 | 路径错误或文件未创建 | 检查路径拼写,确认文件已生成 |
| 文件格式不支持 | 扩展名不在支持列表中 | 转换为支持的格式后重试 |
| 权限不足 | 当前用户无读写权限 | 检查文件权限,以管理员身份运行 |
| 命令执行失败 | 参数错误或环境依赖缺失 | 检查命令语法,确认依赖已安装 |
| 进程超时 | 命令执行时间过长 | 增加超时设置,优化命令参数 |
| 网络连接失败 | DNS解析失败或防火墙拦截 | 检查网络配置,确认代理设置 |

### 百度网盘 Free通用排查步骤

1. **检查输入参数**: 确认所有必填参数已提供且格式正确
2. **查看日志输出**: 定位具体错误行和异常类型
3. **验证环境配置**: 确认依赖库版本和运行环境满足要求
4. **逐步调试**: 缩小问题范围,隔离故障模块

## 用户疑问集