# clipboard-stash

A 50-line scratch clipboard for AI agents and humans alike.

- Backing file: `~/.cache/clipboard-stash/stash.tsv`
- Format: `slug \t ISO-8601 timestamp \t content (with \n-escaped newlines)`
- Zero external dependencies — just `bash`, `awk`, `tail`.

See `SKILL.md` for usage details.
