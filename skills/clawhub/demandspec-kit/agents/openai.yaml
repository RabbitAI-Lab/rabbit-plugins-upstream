interface:
  display_name: "DemandSpec"
  short_description: "需求侧规格、变更、验收与交付工作流"
  default_prompt: "Use $demandspec to inspect the workspace, write concrete and traceable requirement artifacts, validate them, and report unresolved assumptions."
