interface:
  display_name: "MOA Service Client"
  short_description: "Run and verify repository-pinned MOA design workflows"
  default_prompt: "Use $moa-service-client to submit this technical-design task, monitor it to a terminal state, and verify the resulting artifacts."
policy:
  allow_implicit_invocation: true
