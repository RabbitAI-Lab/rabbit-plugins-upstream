---
name: attraction-smart-buy
display_name: "景点门票比价"
description: "美团+飞猪+途牛三平台景点门票实时比价，展示价差、可省金额和预订链接，帮你买到最低价门票。支持全国景点搜索含实景图片，含多票型引导提示。"
version: "2.0.4"
author: "travel-skills"
tags: ["景点", "门票", "景点门票", "门票比价", "旅行", "旅游攻略", "买票"]
tools:
  - name: search_attractions
    description: 搜索景点门票价格
  - name: compare_ticket_prices
    description: 三平台门票比价
  - name: ticket_advisor
    description: 购票决策建议
primaryEnv: PROXY_TOKEN

---
name: attraction-smart-buy

# 景点门票比价 — 3平台找出最低价门票

> 搜一次，比3家。美团/飞猪/途牛并发比价，直接告诉你哪个平台最便宜、能省多少钱。

🔥 **核心亮点：**
- **3源并发比价** — 同时查询美团+飞猪+途牛，标注最低价和可省金额
- **全国景点搜索** — 支持按城市、关键词、景区等级(5A/4A)、类型筛选，含实景图片
- **全票型覆盖** — 门票、套餐、讲解、联票，途牛全部票型展示
- **智能提醒** — 美团仅提供套餐/讲解时自动标注，途牛匹配有误时提醒
- **多票型引导** — 途牛返回多票型时自动提示儿童票、老人票等可选票型及价格

🎯 **试试这样说：**
1. "上海迪士尼门票多少钱"
2. "北京有哪些5A景点"
3. "故宫博物院哪个平台最便宜"

## 触发条件

当用户明确要搜索景点、查询门票价格、比较景点票价、或询问某景点门票是否值得买时触发。不因仅提到景点名称就自动触发。

## 工具

### search_attractions

搜索指定城市的景点，支持关键词、等级和类型筛选，返回景点实景图片。

**输入参数：**
- city（必填）：城市名，如"北京"、"上海"
- keyword（可选）：搜索关键词，如"故宫"、"迪士尼"
- level（可选）：景区等级，如"5A"、"4A"
- category（可选）：景点类型，如"博物馆"、"主题乐园"

**输出：** 景点列表，含名称、等级、类型、门票价格、实景图片、地址和购票链接

**数据流：** 用户输入 → 本技能 → 代理服务 → 飞猪API → 返回结果

### compare_ticket_prices

美团+飞猪+途牛三平台景点门票实时比价，展示各平台报价差异、可省金额、图片和预订链接。

**输入参数：**
- name（必填）：景点名称，如"故宫博物院"、"上海迪士尼度假区"
- city（必填）：城市名

**输出：** 三平台价格对比 + 最低价标注 + 可省金额 + 景点实景图片 + 各平台预订链接 + 途牛全部票型 + 多票型引导提示 + 未匹配平台列表

**特殊标注：**
- 国营5A景区美团仅套餐/讲解时自动标注"仅提供套餐/讲解"
- 途牛匹配有误时标注"⚠️ 匹配可能有误"

**数据流：** 用户输入 → 本技能 → 代理服务(3平台并发) → 美团CPS/飞猪/途牛API → 返回结果

### ticket_advisor

针对指定景点的购票决策建议，5维分析引擎给出是否建议购买的综合判断。

**输入参数：**
- name（必填）：景点名称
- city（必填）：城市名
- ticket_type（可选）：票型偏好，默认"成人票"，可选"儿童票"/"老人票"等

**输出：** 决策信号（🟢建议购买/🟡价格适中/🟠建议观望/🔴暂不建议）+ 5维分析 + 推荐票型

**5维分析：**
1. 价格水平：基于景区等级和类型的参考价格判断（🟢低价/🟡均价/🟠偏高/🔴偏贵）
2. 平台价差：三平台同景点价格对比，计算可省金额
3. 折扣力度：成人票折扣分析，含市场价对比
4. 游客满意度：途牛平台满意度评分 + 评价数量
5. 季节因素：旺季/淡季判断，影响购票紧迫性

**佣金说明：** 同价时按佣金排序（飞猪分佣优先），此排序逻辑已声明。

**数据流：** 用户输入 → 本技能 → 代理服务(3平台并发) → 美团CPS/飞猪/途牛API → 返回结果

## 流程

### A 比价决策（默认）

用户提到某景点想了解门票价格/是否值得买 → compare_ticket_prices 获取三平台报价 → ticket_advisor 给出5维决策分析 → 展示推荐票型和预订链接

### B 搜索发现

用户想找某城市的景点 → search_attractions 搜索 → 展示景点列表含基础门票价格+实景图片 → 用户选定后走A流程

## 多票型说明

途牛接口可返回景点所有票型数据（成人票、儿童票、老人票、亲子家庭票等），脚本会自动过滤非门票类票项（如直通车、游船、演出等），并对有效票型去重排序。当存在非成人票的其他票型时，会生成多票型引导提示，方便用户选择适合的票型。飞猪和美团接口数据限制，暂不返回多票型信息。

## 注意事项

- 网络请求支持自动重试（默认2次），提升稳定性
- 输入景点名称会自动去除城市前缀和噪音词，提升匹配率
- 名称匹配采用多级后缀去除逻辑（风景名胜区/风景区/景区/度假区/公园/旅游区/博物馆/纪念馆等），确保不同命名方式的同一景点能正确匹配
- 途牛数据经过景区名称匹配校验和票项过滤，避免返回错误景点或非门票商品

## 数据流向

用户输入（查询参数）→ 本技能脚本 → 多旅游平台代理（美团CPS/飞猪/途牛）→ 数据源API → 返回结果给用户。查询参数会发送到多旅游平台代理以获取实时数据，代理服务不存储用户数据。

## 🔗 搭配使用

- **全能旅行助手** — 行程规划+机酒火车景点一站式服务
- **酒店比价** — 飞猪+途牛+同程+美团+RG 5源并发比价
- **机票比价** — 飞猪+途牛+同程+美团+RG 5平台直飞航班比价