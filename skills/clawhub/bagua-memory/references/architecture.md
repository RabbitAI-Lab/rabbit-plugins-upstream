# 八卦记忆系统 - 架构参考

> 详细架构统一在 `memory/bagua/README.md`（init 脚本自动创建，权威唯一来源）。
> Skill 被触发且需要深入理解时，读取该文件即可。

## 快速回顾

- **八卦分类**：8 个维度（见 README.md "八卦详解"）
- **归类规则**：8 条判断规则，含坎=reason/震=fact 边界（见 MEMORY.md "归类指南"）
- **卦变关联**：检索时通过预定义卦变关系联想相邻记忆（见 README.md "卦变关联"）
- **生命周期**：新信息 → 卦位 → 7天无引用 → gen/（归档）→ 30天无引用 → 压缩（见 README.md "生命周期"）
