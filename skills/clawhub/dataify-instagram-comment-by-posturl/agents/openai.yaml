interface:
  display_name: "Dataify Instagram Comment By Post URL"
  short_description: "Collect Instagram Comment By Post URL data and return results"
  icon_small: "./assets/dataify-instagram-comment-by-posturl-small.svg"
  icon_large: "./assets/dataify-instagram-comment-by-posturl-large.svg"
  default_prompt: "Use $dataify-instagram-comment-by-posturl to complete the requested Dataify collection, wait for the asynchronous task, and return the final collected result. Stop at task submission only when I explicitly request no-wait behavior."

policy:
  allow_implicit_invocation: true
