# examples/ - 示例文件

本目录用于存放正例和反例输出样本，帮助用户理解正确和错误的行为方式。

## 当前示例

暂无预设示例，建议用户在实际使用中积累自己的正例和反例。

---

**正例积累方向：**
- 任务录入成功的对话示例（包含完整的字段收集和确认）
- 日期转换正确的对话示例（展示三步验证过程）
- 定时提醒输出的正确格式
- 下班前复盘5步闭环的完整示例

**反例积累方向：**
- 日期转换跳步的错误示例（本周五 → 04-25 而不是 04-24）
- 字段放置位置错误的示例（工作要求放到了备注列）
- 状态变更未立即执行的错误示例

---

*如有需要，可上传示例对话或截图到本目录。*