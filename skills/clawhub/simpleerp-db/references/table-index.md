# SimpleERP table index

> **Not generated yet.** Run setup from the skill root:

```bash
npm run setup
```

After setup, this file lists base and journal (`*_JNL`) tables grouped by domain.
