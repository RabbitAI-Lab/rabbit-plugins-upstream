{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://clawhub.ai/orionshaowswmw/skills/free-tier-ai-router/schema/answer.v1.schema.json",
  "title": "ai_router.answer.v1",
  "description": "Answer object emitted by `router.py \"prompt\" --json`. Exit code semantics: 0 success (text present) · 2 all routes quota-dead or failing · 3 no keys configured at all · 4 invalid providers.json.",
  "type": "object",
  "additionalProperties": false,
  "required": ["schema"],
  "properties": {
    "schema": { "const": "ai_router.answer.v1" },
    "text": { "type": "string", "description": "The answer text. Present on success." },
    "provider": { "type": "string", "description": "Provider that answered (e.g. mistral, mock)." },
    "model": { "type": "string", "description": "Model that answered." },
    "cached": { "type": "boolean", "description": "True when served from the local SHA-256 cache (no API call)." },
    "error": { "type": "string", "description": "Machine-readable error kind; present only on failure (e.g. 'invalid providers config')." },
    "detail": { "type": "string", "description": "Human-readable error detail; present only on failure." }
  }
}
