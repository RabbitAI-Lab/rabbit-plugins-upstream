## Step 6 — 转换 PPTX / PDF

根据用户选择，**仅转换对应格式**：

```bash
# A 培训演示 → PPTX
python scripts/convert_pptx_model.py {原始文档stem}_pptx-model.html

# B 学习分享 → PDF
python scripts/convert_pdf_model.py {原始文档stem}_pdf-model.html
```

产物保存到当前工作目录。
