# /qa 示例：统一入口 — 支付接口全量回归

> ⚠️ 本示例展示指令输出效果。实际执行时，该指令会读写 `memory/data/products/` 下的本地文件。请勿在输入中包含未脱敏的敏感信息。
>
> 展示 `/qa` 统一入口的自然语言→多步任务编排→记忆管理全流程。

## 原始输入

```
/qa
对支付接口做全量回归并输出缺陷报告
```

## AI 输出

### 第一步：意图解析

```json
{
  "intent": "multi",
  "steps": [
    {"action": "case", "description": "设计支付接口回归用例", "scope": "支付接口"},
    {"action": "bug", "description": "分析执行中发现的缺陷", "scope": "支付接口"},
    {"action": "report", "description": "生成测试报告", "scope": "支付接口"}
  ],
  "output_goal": "缺陷报告"
}
```

### 第二步：任务编排

```
## 任务执行计划

原始任务：对支付接口做全量回归并输出缺陷报告

执行步骤：
1. 【第一步】/qa-case：设计支付接口全量回归测试用例
   → 写入：用例库（memory/data/test-case/）

2. 【第二步】/qa-bug：基于执行结果分析缺陷
   → 读取：上一步用例数据 → 写入：缺陷库（memory/data/bug/）

3. 【第三步】/qa-report：汇总数据生成缺陷报告
   → 读取：用例库 + 缺陷库 → 写入：报告库（memory/data/report/）

是否按此计划执行？
```

> 用户确认后，逐步骤执行，步骤间数据自动传递。

### 第三步：用例设计（节选）

**PAY-TC001：余额支付正常流程**（核心层/P0/场景法）
- 步骤：1. 选择余额支付 2. 确认支付 3. 网关返回成功
- 预期：订单→支付成功，余额扣减

**PAY-TC002：银行卡支付正常流程**（核心层/P0/场景法）
- 步骤：1. 选择银行卡支付 2. 跳转银行网关 3. 支付成功
- 预期：订单→支付成功

**PAY-TC003：支付网关超时处理**（核心层/P0/场景法）
- 步骤：1. 发起支付 2. 网关无响应超过30s
- 预期：订单→支付超时，触发补偿

**PAY-TC004：并发支付同一订单**（核心层/P1/错误推测）
- 步骤：1. 两客户端同时支付同一订单
- 预期：仅一笔成功，无重复扣款

### 第四步：缺陷分析（节选）

```
## 缺陷分析报告

| BugID | 标题 | 严重程度 |
|-------|------|---------|
| BUG-001 | 余额支付成功但订单状态未更新 | 致命 |
| BUG-002 | 支付网关超时未触发补偿机制 | 严重 |
| BUG-003 | 并发支付同一订单出现两次扣款 | 致命 |

### 根因分类
- 代码缺陷：60%（状态更新、并发锁）
- 设计遗漏：40%（超时补偿、边界校验）
```

### 第五步：报告生成（节选）

```
## 测试阶段报告 — 支付接口回归

### 测试进度
- 计划用例：6 条 → 实际执行：6 条 → 完成率：100%

### 缺陷统计
| 严重级别 | 发现 | 已修复 | 遗留 | 修复率 |
|----------|------|--------|------|--------|
| 致命 | 2 | 0 | 2 | 0% |
| 严重 | 1 | 0 | 1 | 0% |

### 质量评估
- 是否达到发布标准：❌ 否
- 阻塞项：BUG-001 + BUG-003 未修复
```

## 使用技巧

1. **任务描述要包含范围和目标**："/qa 对支付接口做全量回归并输出缺陷报告" 比 "/qa 测试支付" 更精准
2. **多步任务自动传递数据**：前一步的评审问题/用例信息会自动注入下一步，无需手动复制粘贴
3. **中间步骤可打断**：每一步执行前都会输出执行计划，用户可以确认或调整
4. **记忆模块自动沉淀**：所有产出自动写入 `memory/data/`，后续任务可复用
