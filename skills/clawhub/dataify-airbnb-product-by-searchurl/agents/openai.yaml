interface:
  display_name: "Dataify Airbnb Builder"
  short_description: "Collect Airbnb Builder data and return results"
  default_prompt: "Use $dataify-airbnb-product-by-searchurl to complete the requested Dataify collection, wait for the asynchronous task, and return the final collected result. Stop at task submission only when I explicitly request no-wait behavior."
