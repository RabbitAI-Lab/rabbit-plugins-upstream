interface:
  display_name: "Dataify Linkedin Builder"
  short_description: "Collect Linkedin Builder data and return results"
  default_prompt: "Use $dataify-linkedin-company-information-by-url to complete the requested Dataify collection, wait for the asynchronous task, and return the final collected result. Stop at task submission only when I explicitly request no-wait behavior."
