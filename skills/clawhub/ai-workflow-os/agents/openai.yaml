interface:
  display_name: "AI Workflow OS"
  short_description: "Route work across specialized AI workflows"
  default_prompt: "Use $ai-workflow-os to route this multi-surface request to the smallest set of specialist skills, assign one owner per state surface, and return one coherent evidence-backed result."
policy:
  allow_implicit_invocation: true
