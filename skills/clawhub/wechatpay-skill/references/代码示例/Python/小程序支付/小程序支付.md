# 小程序支付 - Python实现

> 微信小程序支付完整实现，补充官方仅有Java/Go的问题

## 与JSAPI的区别

| 对比项 | JSAPI支付 | 小程序支付 |
|--------|-----------|------------|
| 下单API | `/v3/pay/transactions/jsapi` | **完全相同** |
| 获取openid | 公众号网页授权 | 小程序wx.login |
| 调起方式 | WeixinJSBridge.invoke | wx.requestPayment |
| AppID | 公众号AppID | 小程序AppID |

## 完整代码

```python
import json
import time
import random
import string
import base64

import requests
from cryptography.hazmat.primitives import hashes, serialization
from cryptography.hazmat.primitives.asymmetric import padding
from cryptography.hazmat.backends import default_backend


class WechatMiniPay:
    """微信小程序支付"""
    
    def __init__(self, mchid, serial_no, private_key, appid, api_v3_key):
        self.mchid = mchid
        self.serial_no = serial_no
        self.private_key = private_key
        self.appid = appid                    # 小程序AppID
        self.api_v3_key = api_v3_key
        self.base_url = "https://api.mch.weixin.qq.com"
    
    def _generate_nonce(self, length=32):
        chars = string.ascii_letters + string.digits
        return ''.join(random.choice(chars) for _ in range(length))
    
    def _sign(self, sign_str):
        private_key = serialization.load_pem_private_key(
            self.private_key.encode(),
            password=None,
            backend=default_backend()
        )
        signature = private_key.sign(
            sign_str.encode(),
            padding.PKCS1v15(),
            hashes.SHA256()
        )
        return base64.b64encode(signature).decode()
    
    def _get_authorization(self, method, url_path, body=""):
        timestamp = str(int(time.time()))
        nonce = self._generate_nonce()
        sign_str = f"{method}\n{url_path}\n{timestamp}\n{nonce}\n{body}\n"
        signature = self._sign(sign_str)
        
        return (f'WECHATPAY2-SHA256-RSA2048 '
                f'mchid="{self.mchid}",'
                f'nonce_str="{nonce}",'
                f'timestamp="{timestamp}",'
                f'serial_no="{self.serial_no}",'
                f'signature="{signature}"')
    
    def _request(self, method, url_path, body=None):
        url = self.base_url + url_path
        headers = {
            "Authorization": self._get_authorization(
                method, url_path, json.dumps(body) if body else ""
            ),
            "Content-Type": "application/json"
        }
        
        if method == "GET":
            resp = requests.get(url, headers=headers)
        else:
            resp = requests.post(url, headers=headers, json=body)
        
        return resp.json()
    
    def get_openid(self, js_code, appid, secret):
        """
        小程序获取openid
        
        Args:
            js_code: wx.login()获取的code
            appid: 小程序AppID（与支付时相同）
            secret: 小程序AppSecret
        
        Returns:
            dict: 包含openid
        """
        url = "https://api.weixin.qq.com/sns/jscode2session"
        params = {
            "appid": appid,
            "secret": secret,
            "js_code": js_code,
            "grant_type": "authorization_code"
        }
        resp = requests.get(url, params=params)
        return resp.json()
    
    def prepay(self, out_trade_no, description, total_fee, openid, notify_url):
        """
        小程序支付下单
        
        注意：接口与JSAPI完全相同！
        """
        url_path = "/v3/pay/transactions/jsapi"
        body = {
            "appid": self.appid,           # 小程序AppID
            "mchid": self.mchid,
            "description": description,
            "out_trade_no": out_trade_no,
            "notify_url": notify_url,
            "amount": {
                "total": total_fee,
                "currency": "CNY"
            },
            "payer": {
                "openid": openid            # 必须是小程序获取的openid
            }
        }
        
        result = self._request("POST", url_path, body)
        
        if "prepay_id" in result:
            return {"code": 0, "prepay_id": result["prepay_id"]}
        else:
            return {"code": 1, "error": result}
    
    def get_pay_sign(self, prepay_id):
        """
        获取小程序调起支付签名参数
        
        注意：小程序调起签名与JSAPI完全相同！
        """
        timestamp = str(int(time.time()))
        nonce = self._generate_nonce()
        
        # 调起签名串（4行）
        # 第四行必须是 prepay_id=xxx 格式！
        sign_str = f"{self.appid}\n{timestamp}\n{nonce}\nprepay_id={prepay_id}\n"
        signature = self._sign(sign_str)
        
        return {
            "timeStamp": timestamp,
            "nonceStr": nonce,
            "package": f"prepay_id={prepay_id}",
            "signType": "RSA",
            "paySign": signature
        }


# ============ 使用示例 ============

if __name__ == "__main__":
    config = {
        "mchid": "1234567890",
        "serial_no": "XXXXXXXXXXXXXXXXXXXXXXXX",
        "private_key": """-----BEGIN PRIVATE KEY-----
...你的私钥...
-----END PRIVATE KEY-----""",
        "appid": "wxa5f5c1d6e8f9a2b3",    # 小程序AppID
        "api_v3_key": "0123456789abcdef0123456789abcdef"
    }
    
    mini_pay = WechatMiniPay(**config)
    
    # ============ 步骤1: 获取openid（小程序前端调用wx.login获取code后）==========
    code_from_mini = "081xxXxxx..."  # 前端wx.login获取
    secret = "xxxxxxxxxxxxx"          # 小程序AppSecret
    
    openid_result = mini_pay.get_openid(code_from_mini, config["appid"], secret)
    print(f"openid_result: {openid_result}")
    # {'openid': 'oxxxxxxxx', 'session_key': 'xxxxxxxx', 'errcode': 0}
    
    if "openid" in openid_result:
        openid = openid_result["openid"]
        
        # ============ 步骤2: 下单 ===========
        order_result = mini_pay.prepay(
            out_trade_no=f"MINI{time.time()}",
            description="小程序内购商品",
            total_fee=100,              # 1元
            openid=openid,              # 小程序获取的openid
            notify_url="https://yourdomain.com/mini/notify"
        )
        print(f"order_result: {order_result}")
        
        if order_result.get("code") == 0:
            # ============ 步骤3: 获取调起参数 ===========
            prepay_id = order_result["prepay_id"]
            pay_params = mini_pay.get_pay_sign(prepay_id)
            print(f"pay_params: {pay_params}")
            # 返回给小程序前端：timeStamp, nonceStr, package, signType, paySign
```

## 小程序前端调用

```javascript
// 小程序前端代码
wx.login({
  success: (loginRes) => {
    // 1. 获取code
    const code = loginRes.code
    
    // 2. 发送code到后端换取openid
    wx.request({
      url: 'https://yourdomain.com/api/get-openid',
      data: { code: code },
      success: (res) => {
        const openid = res.data.openid
        
        // 3. 后端下单，获取调起参数
        wx.request({
          url: 'https://yourdomain.com/api/create-order',
          method: 'POST',
          data: { openid: openid, goods_id: 'xxx' },
          success: (orderRes) => {
            const payParams = orderRes.data
            
            // 4. 调起微信支付
            wx.requestPayment({
              timeStamp: payParams.timeStamp,
              nonceStr: payParams.nonceStr,
              package: payParams.package,
              signType: payParams.signType,
              paySign: payParams.paySign,
              success: (payRes) => {
                console.log('支付成功')
              },
              fail: (err) => {
                console.log('支付失败', err)
              }
            })
          }
        })
      }
    })
  }
})
```

## 常见坑点

| 坑点 | 表现 | 解决方案 |
|------|------|----------|
| AppID填错 | 调起失败 | 使用**小程序AppID**，不是公众号AppID |
| openid来源错 | INVALID_REQUEST | 必须用wx.login获取code再换openid |
| prepay_id过期 | 订单已失效 | 重新下单 |
| package格式错 | 签名错误 | 必须是`prepay_id=xxx`格式 |

## 检查清单

- [ ] 小程序已认证（个人小程序不支持支付）
- [ ] mchid与小程序AppID已绑定（商户平台手动绑定）
- [ ] openid是通过wx.login获取的
- [ ] 调起签名使用小程序AppID
- [ ] 前端正确调用wx.requestPayment
