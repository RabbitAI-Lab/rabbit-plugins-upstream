# Smart Charts - 依赖声明（精确版本锁定）
# 安装方式: pip install -r requirements.txt
#
# 说明:
#   - 所有依赖使用 == 精确固定版本，避免版本漂移
#   - 如需更新版本，直接修改下方对应包的版本号

# 核心依赖 (必需)
pandas==3.0.1
numpy==2.4.3
openpyxl==3.1.5
xlrd==2.0.1

# 传递依赖
python-dateutil==2.9.0.post0
six==1.17.0
et-xmlfile==2.0.0
