interface:
  display_name: "Mermail Agent Wallet"
  short_description: "Use PayBox balances, sends, swaps, and x402"
  default_prompt: "Use $mermail-agent-wallet for balances, Funding, sends, swaps, or a user-selected x402 paid-service action with exact live-schema terms and PayBox MCP App UI."
dependencies:
  tools:
    - type: "mcp"
      value: "mermail"
      description: "Mermail workspace and mailbox MCP server"
      transport: "streamable_http"
      url: "https://console.mermail.app/mcp"
