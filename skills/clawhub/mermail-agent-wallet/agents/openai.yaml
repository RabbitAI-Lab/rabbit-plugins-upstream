interface:
  display_name: "Mermail Agent Wallet"
  short_description: "Inspect PayBox balances and submit approved USDC, native ETH/SOL, or catalog-token transfers"
  default_prompt: "Use $mermail-agent-wallet to inspect or transfer through Mermail Agent Wallet."
dependencies:
  tools:
    - type: "mcp"
      value: "mermail"
      description: "Mermail workspace and mailbox MCP server"
      transport: "streamable_http"
      url: "https://console.mermail.app/mcp"
