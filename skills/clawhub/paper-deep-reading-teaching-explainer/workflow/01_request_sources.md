# Step 1: Request Sources

If the next stage needs source files, ask the user to upload one zip bundle into Project `sources`.

Typical required files:

- `papers/main/`
- `reviews/openreview/`
- `supplementary/`
- `generated/visuals/`
- `generated/intermediate/`

Wait for confirmation before continuing.
