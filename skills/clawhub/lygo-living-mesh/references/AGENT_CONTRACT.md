# Agent contract — Layer D (Living Mesh)

1. Always run local A+B verify before claiming mesh health.  
2. Never auto-merge remote registries into local eggs.  
3. Gossip **summaries only** — refuse to transmit module bytes / secrets.  
4. Join peers only with user `--i-consent` this turn.  
5. On LOCAL_QUARANTINE / SENTINEL_QUARANTINE — stop mesh growth; fix local first.  
6. On FORK_VISIBLE — report root digests; offer human reconciliation plan.  
7. Wide-area / TLS / public ports — explicit operator sign-off only.  
8. Cite `tests/living_mesh_last_run.json` verdict + `roots_digest` after success.  
9. No auto git push, HF upload, ClawHub publish, or social posts.  
