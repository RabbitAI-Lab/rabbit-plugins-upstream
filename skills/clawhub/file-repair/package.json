{
  "name": "online-file-repair-skills",
  "version": "0.1.0",
  "private": true,
  "description": "OpenClaw skill workspace for online file repair",
  "license": "UNLICENSED",
  "type": "commonjs",
  "scripts": {
    "repair:video": "node scripts/repair-video.js",
    "build": "npm run build:repair",
    "build:repair": "esbuild scripts/repair-video.js --bundle --platform=node --format=cjs --target=node18 --outfile=dist/repair-file.bundle.cjs"
  },
  "devDependencies": {
    "esbuild": "^0.20.2"
  },
  "dependencies": {
    "mime-types": "^2.1.35",
    "ws": "^8.17.0"
  }
}
