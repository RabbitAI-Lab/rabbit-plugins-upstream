interface:
  display_name: "Dataify eBay Products"
  short_description: "Collect eBay Products data and return results"
  icon_small: "./assets/dataify-ebay-products-small.svg"
  icon_large: "./assets/dataify-ebay-products-large.svg"
  default_prompt: "Use $dataify-ebay-products to complete the requested Dataify collection, wait for the asynchronous task, and return the final collected result. Stop at task submission only when I explicitly request no-wait behavior."

policy:
  allow_implicit_invocation: true
