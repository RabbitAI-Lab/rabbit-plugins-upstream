{
  "description": "Secret and credential patterns for long-task handoff validation. This is a superset of agent-memory-cleanup references/default-rules.json secret_patterns plus handoff-specific .env and private-key checks.",
  "patterns": [
    "sk-[A-Za-z0-9_-]{16,}",
    "ghp_[A-Za-z0-9_]{16,}",
    "github_pat_[A-Za-z0-9_]{16,}",
    "xox[baprs]-[A-Za-z0-9-]{16,}",
    "AKIA[0-9A-Z]{16}",
    "(?i)\\b(api[_-]?key|token|password|passwd|secret)\\b\\s*[:=]\\s*['\\\"]?[^'\\\"\\s`]{8,}",
    "(?i)\\bBearer\\s+[A-Za-z0-9._-]{16,}",
    "-----BEGIN [A-Z ]*PRIVATE KEY-----",
    "(?m)^[A-Z][A-Z0-9_]{2,}=.{6,}$",
    "<API_TOKEN_PLACEHOLDER>"
  ]
}
