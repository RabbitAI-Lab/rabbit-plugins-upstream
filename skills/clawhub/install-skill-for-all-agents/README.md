# install-global-skill-from-clawhub

Install or upgrade a ClawHub skill through the official OpenClaw CLI, stage it in the workspace, promote it into the resolved OpenClaw home `skills/` directory, and optionally remove the staging copy.

See `references/scripts.md` for the bundled helper script usage.
