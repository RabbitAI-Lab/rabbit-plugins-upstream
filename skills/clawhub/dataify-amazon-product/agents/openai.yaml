interface:
  display_name: "Dataify Amazon Product"
  short_description: "Collect Amazon Product data and return results"
  default_prompt: "Use $dataify-amazon-product to complete the requested Dataify collection, wait for the asynchronous task, and return the final collected result. Stop at task submission only when I explicitly request no-wait behavior."

policy:
  allow_implicit_invocation: true
