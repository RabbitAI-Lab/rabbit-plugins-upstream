{
  "name": "modellix",
  "version": "3.7.0",
  "description": "Integrate Modellix unified API/CLI for async image and video generation (model run --wait, task download).",
  "primaryCredential": "MODELLIX_API_KEY",
  "primaryEnv": "MODELLIX_API_KEY",
  "requiredEnv": [
    "MODELLIX_API_KEY"
  ],
  "env": [
    {
      "name": "MODELLIX_API_KEY",
      "description": "API key for Modellix CLI and REST API calls.",
      "required": true
    }
  ]
}
