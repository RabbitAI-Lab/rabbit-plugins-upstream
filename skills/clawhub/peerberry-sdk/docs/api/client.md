# API Client Reference (Generated)

This page is generated from source code signatures, constants, and method docstrings.
Do not edit this file manually; edit `docs/gen_client_reference.py` and source docstrings instead.

For low-level symbol browsing, use [Generated Module Reference](reference.md).

## SDK Configuration Schema

These dataclass tables are generated directly from `peerberry_sdk.config`.

### `RetryConfig`

| Field | Type | Default | Description |
| --- | --- | --- | --- |
| `max_retries` | `int` | `2` | Retry attempts after initial failure. |
| `backoff_factor` | `float` | `0.5` | Exponential backoff multiplier. |
| `max_backoff` | `float` | `8.0` | Maximum sleep duration between retries. |
| `retry_status_codes` | `Tuple[int, ...]` | `(429, 500, 502, 503, 504)` | HTTP status codes treated as retryable. |
| `retry_on_network_errors` | `bool` | `True` | Whether network/transport exceptions are retried. |

### `TransportConfig`

| Field | Type | Default | Description |
| --- | --- | --- | --- |
| `timeout` | `Optional[float]` | `30.0` | Default request timeout in seconds. |
| `retry` | `RetryConfig` | `RetryConfig()` | Retry policy configuration object. |
| `header_profile` | `Optional[Dict[str, str]]` | `None` | Inline static headers merged into default request profile. |
| `header_profile_path` | `Optional[str]` | `None` | Path to JSON file containing static header profile. |
| `user_agent` | `Optional[str]` | `None` | Convenience override for x-app-user-agent. |
| `app_system` | `Optional[str]` | `None` | Convenience override for x-app-system. |
| `sec_ch_ua_platform` | `Optional[str]` | `None` | Convenience override for sec-ch-ua-platform. |

### `AuthConfig`

| Field | Type | Default | Description |
| --- | --- | --- | --- |
| `auto_refresh_on_auth_error` | `bool` | `True` | Refresh token automatically after auth failures. |
| `max_refresh_attempts` | `int` | `1` | Refresh attempts for a failing request. |
| `proactive_refresh` | `bool` | `True` | Refresh access token before expiry when possible. |
| `proactive_refresh_skew_seconds` | `int` | `60` | Seconds before expiry to trigger proactive refresh. |
| `token_store` | `TokenStore` | `InMemoryTokenStore()` | Token store implementation used for load/save/clear. |
| `load_tokens_on_init` | `bool` | `True` | Load token pair from token store during client initialization. |

### `ObservabilityConfig`

| Field | Type | Default | Description |
| --- | --- | --- | --- |
| `event_hook` | `Optional[Callable[[Dict[str, Any]], None]]` | `None` | Callback receiving sanitized event dictionaries. |
| `logger` | `Optional[logging.Logger]` | `None` | Logger receiving structured SDK event messages. |
| `verbose_events` | `bool` | `False` | Enable request_start/request_end verbose events. |
| `redactor` | `Optional[Callable[[Dict[str, Any]], Dict[str, Any]]]` | `None` | Optional custom event redaction hook. |

### `LifecycleConfig`

| Field | Type | Default | Description |
| --- | --- | --- | --- |
| `logout_on_close` | `bool` | `False` | Whether PeerberryClient.close() should call logout(). |
| `swallow_logout_errors` | `bool` | `True` | Suppress logout errors during close when true. |

### `SDKConfig`

| Field | Type | Default | Description |
| --- | --- | --- | --- |
| `transport` | `TransportConfig` | `TransportConfig()` | Transport-related runtime configuration. |
| `auth` | `AuthConfig` | `AuthConfig()` | Authentication/token lifecycle configuration. |
| `observability` | `ObservabilityConfig` | `ObservabilityConfig()` | Event and logging configuration. |
| `lifecycle` | `LifecycleConfig` | `LifecycleConfig()` | Client-close and logout behavior configuration. |

## API Methods

## Constructor and Lifecycle

### `PeerberryClient(...)`

Creates a new client instance and optionally authenticates immediately.

```python
PeerberryClient(email: Optional[str] = None, password: Optional[str] = None, tfa_secret: Optional[str] = None, access_token: Optional[str] = None, refresh_token: Optional[str] = None, request_opts: Optional[dict] = None, config: Optional[SDKConfig] = None, auto_login: bool = True)
```

| Parameter | Type | Default | Description |
| --- | --- | --- | --- |
| `email` | `Optional[str]` | `None` | Account's email |
| `password` | `Optional[str]` | `None` | Account's password |
| `tfa_secret` | `Optional[str]` | `None` | Base32 secret used for two-factor authentication |
| `access_token` | `Optional[str]` | `None` | Access token used to authenticate to the API (Optional; Only pass the JWT for it to work!) (Only mandatory if account has two-factor authentication enabled) |
| `refresh_token` | `Optional[str]` | `None` | Existing refresh token used for token rotation. |
| `request_opts` | `Optional[dict]` | `None` | Optional[dict] - Additional options for :any:`requests.sessions.Session.request()`. |
| `config` | `Optional[SDKConfig]` | `None` | Optional[SDKConfig] - Transport/auth behavior overrides. |
| `auto_login` | `bool` | `True` | Automatically authenticate during initialization (True by default). |

- Returns: `Any` - Initialized client instance.
- Potential exceptions: `ValueError`, `PeerberryException`

### `login(...)`

Authenticates session and returns a bearer token string.

```python
api.login() -> str
```

- Returns: `str` - Bearer string in format "Bearer <access_token>".
- Potential exceptions: `InvalidCredentials`, `PeerberryException`

### `token(...)`

Forces access-token refresh and returns a bearer token string.

```python
api.token() -> str
```

- Returns: `str` - Bearer string in format "Bearer <access_token>".
- Potential exceptions: `TokenRefreshError`

### `logout(...)`

Logs out server-side and clears local authentication state.

```python
api.logout() -> str
```

- Returns: `str` - Fixed success message string.
- Potential exceptions: `PeerberryException`

### `close(...)`

Closes underlying transport session, optionally logging out first.

```python
api.close(logout: Optional[bool] = None) -> None
```

| Parameter | Type | Default | Description |
| --- | --- | --- | --- |
| `logout` | `Optional[bool]` | `None` | Optional explicit logout override. If None, lifecycle config is used. |

- Returns: `None` - None
- Potential exceptions: `PeerberryException (optional, when configured not to swallow logout errors)`

## Profile and Portfolio

### `get_profile(...)`

Returns investor profile information as a typed model.

```python
api.get_profile() -> Profile
```

- Returns: `Profile` - Basic information, accounts & balance information
- Potential exceptions: `PeerberryException`

### `get_overview(...)`

Returns portfolio overview data as a typed model wrapper.

```python
api.get_overview() -> Overview
```

- Returns: `Overview` - Available balance, total invested, total profit, current investments, net annual return, etc.
- Potential exceptions: `PeerberryException`

### `get_loyalty_tier(...)`

Returns the highest unlocked loyalty tier as a typed model.

```python
api.get_loyalty_tier() -> LoyaltyTier
```

- Returns: `LoyaltyTier` - Tier, extra return in percentage, the max amount and the minimum amount to be in the tier
- Potential exceptions: `PeerberryException`

### `get_profit_overview(...)`

Returns profit chart points for a date interval.

```python
api.get_profit_overview(start_date: datetime.date, end_date: datetime.date, periodicity: str = "day") -> List[ProfitPoint]
```

| Parameter | Type | Default | Description |
| --- | --- | --- | --- |
| `start_date` | `datetime.date` | `Required` | Start date of profit data |
| `end_date` | `datetime.date` | `Required` | End date of profit data |
| `periodicity` | `str` | `"day"` | Aggregation step for points; must be one of day/month/year. |

- Returns: `List[ProfitPoint]` - Profit overview for portfolio on a daily, monthly or yearly basis
- Accepted values:
  - `periodicity`: `day, month, year`
- Potential exceptions: `InvalidPeriodicity`, `PeerberryException`

### `get_investment_status(...)`

Returns investment status breakdown data.

```python
api.get_investment_status() -> InvestmentStatusOverview
```

- Returns: `InvestmentStatusOverview` - Percentage of funds in current loans and late loans (In 1-15, 16-30, and 31-60 day intervals)
- Potential exceptions: `PeerberryException`

### `get_investment_originators_overview(...)`

Returns originator allocation overview data.

```python
api.get_investment_originators_overview() -> OriginatorsOverview
```

- Returns: `OriginatorsOverview` - Percentage of funds in current loan originators
- Potential exceptions: `PeerberryException`

## Loans

### `get_loans(...)`

Fetches multiple available loans using paginated internal requests.

```python
api.get_loans(quantity: int, start_page: int = 0, max_remaining_term: Optional[int] = None, min_remaining_term: Optional[int] = None, max_interest_rate: Optional[decimal.Decimal] = None, min_interest_rate: Optional[decimal.Decimal] = None, max_available_amount: Optional[decimal.Decimal] = None, min_available_amount: Optional[decimal.Decimal] = None, countries: Optional[List[str]] = None, originators: Optional[List[str]] = None, loan_types: Optional[List[str]] = None, sort: str = "loan_amount", ascending_sort: bool = False, group_guarantee: bool = True, exclude_invested_loans: Optional[bool] = None) -> List[Loan]
```

| Parameter | Type | Default | Description |
| --- | --- | --- | --- |
| `quantity` | `int` | `Required` | Amount of loans to fetch |
| `start_page` | `int` | `0` | Number of start page to start getting loans from |
| `max_remaining_term` | `Optional[int]` | `None` | Maximum remaining term to fetch loan |
| `min_remaining_term` | `Optional[int]` | `None` | Minimum remaining term to fetch loan |
| `max_interest_rate` | `Optional[decimal.Decimal]` | `None` | Maximum interest rate to fetch loan |
| `min_interest_rate` | `Optional[decimal.Decimal]` | `None` | Minimum interest rate to fetch loan |
| `max_available_amount` | `Optional[decimal.Decimal]` | `None` | Maximum available investment amount to fetch loan |
| `min_available_amount` | `Optional[decimal.Decimal]` | `None` | Minimum available investment amount to fetch loan |
| `countries` | `Optional[List[str]]` | `None` | Filter loans by country of origin (Gets loans from all countries by default) |
| `originators` | `Optional[List[str]]` | `None` | Filter loans by originator (Gets loans from all originators by default) |
| `loan_types` | `Optional[List[str]]` | `None` | Filter loans by type (Short-term, long-term, real estate, leasing, and business) |
| `sort` | `str` | `"loan_amount"` | Loan sort key; one of loan_id/term/issued_date/interest_rate/loan_amount. |
| `ascending_sort` | `bool` | `False` | Sort by ascending order when sort is provided (By default sorts in descending order) |
| `group_guarantee` | `bool` | `True` | Restrict loans to only those with a group guarantee |
| `exclude_invested_loans` | `Optional[bool]` | `None` | Exclude loans that have been invested in previously |

- Returns: `List[Loan]` - All available loans for investment according to specified parameters
- Accepted values:
  - `sort`: `interest_rate, issued_date, loan_amount, loan_id, term`
  - `loan_types`: `business, leasing, long_term, real_estate, short_term`
- Potential exceptions: `ValueError`, `TypeError`, `InvalidSort`, `PeerberryException`

### `get_loans_page(...)`

Fetches a single page of available loans with filters.

```python
api.get_loans_page(page_num: int, quantity: int = 40, max_remaining_term: Optional[int] = None, min_remaining_term: Optional[int] = None, max_interest_rate: Optional[decimal.Decimal] = None, min_interest_rate: Optional[decimal.Decimal] = None, max_available_amount: Optional[decimal.Decimal] = None, min_available_amount: Optional[decimal.Decimal] = None, countries: Optional[List[str]] = None, originators: Optional[List[str]] = None, loan_types: Optional[List[str]] = None, sort: str = "loan_id", ascending_sort: bool = False, group_guarantee: Optional[bool] = None, exclude_invested_loans: Optional[bool] = None) -> LoanPage
```

| Parameter | Type | Default | Description |
| --- | --- | --- | --- |
| `page_num` | `int` | `Required` | Number of start page to start getting loans from |
| `quantity` | `int` | `40` | Number of loans to fetch |
| `max_remaining_term` | `Optional[int]` | `None` | Maximum remaining term to fetch loan |
| `min_remaining_term` | `Optional[int]` | `None` | Minimum remaining term to fetch loan |
| `max_interest_rate` | `Optional[decimal.Decimal]` | `None` | Maximum interest rate to fetch loan |
| `min_interest_rate` | `Optional[decimal.Decimal]` | `None` | Minimum interest rate to fetch loan |
| `max_available_amount` | `Optional[decimal.Decimal]` | `None` | Maximum available investment amount to fetch loan |
| `min_available_amount` | `Optional[decimal.Decimal]` | `None` | Minimum available investment amount to fetch loan |
| `countries` | `Optional[List[str]]` | `None` | Filter loans by country of origin (Gets loans from all countries by default) |
| `originators` | `Optional[List[str]]` | `None` | Filter loans by originator (Gets loans from all originators by default) |
| `loan_types` | `Optional[List[str]]` | `None` | Filter loans by type (Short-term, long-term, real estate, leasing, and business) |
| `sort` | `str` | `"loan_id"` | Loan sort key; one of loan_id/term/issued_date/interest_rate/loan_amount. |
| `ascending_sort` | `bool` | `False` | Sort by ascending order (By default sorts in descending order) |
| `group_guarantee` | `Optional[bool]` | `None` | Restrict loans to only those with a group guarantee |
| `exclude_invested_loans` | `Optional[bool]` | `None` | Exclude loans that have been invested in previously |

- Returns: `LoanPage` - A single page of available loans for investment according to specified parameters
- Accepted values:
  - `sort`: `interest_rate, issued_date, loan_amount, loan_id, term`
  - `loan_types`: `business, leasing, long_term, real_estate, short_term`
- Potential exceptions: `ValueError`, `TypeError`, `InvalidSort`, `PeerberryException`

### `get_secondary_loans(...)`

Fetches a single page of secondary-market loan listings with filters.

```python
api.get_secondary_loans(quantity: int, start_page: int = 0, max_selling_price: Optional[decimal.Decimal] = None, min_selling_price: Optional[decimal.Decimal] = None, max_remaining_term: Optional[int] = None, min_remaining_term: Optional[int] = None, max_interest_rate: Optional[decimal.Decimal] = None, min_interest_rate: Optional[decimal.Decimal] = None, countries: Optional[List[str]] = None, originators: Optional[List[str]] = None, loan_types: Optional[List[str]] = None, sort: Optional[str] = None, ascending_sort: bool = False, group_guarantee: Optional[bool] = None, only_my_sales: Optional[bool] = None, reduced_price_only: Optional[bool] = None, only_at_nominal_value: Optional[bool] = None) -> SecondaryLoanPage
```

| Parameter | Type | Default | Description |
| --- | --- | --- | --- |
| `quantity` | `int` | `Required` | Number of secondary-market loan listings to fetch |
| `start_page` | `int` | `0` | Number of start page to start getting listings from |
| `max_selling_price` | `Optional[decimal.Decimal]` | `None` | Maximum selling price to fetch listings |
| `min_selling_price` | `Optional[decimal.Decimal]` | `None` | Minimum selling price to fetch listings |
| `max_remaining_term` | `Optional[int]` | `None` | Maximum remaining term to fetch listings |
| `min_remaining_term` | `Optional[int]` | `None` | Minimum remaining term to fetch listings |
| `max_interest_rate` | `Optional[decimal.Decimal]` | `None` | Maximum interest rate to fetch listings |
| `min_interest_rate` | `Optional[decimal.Decimal]` | `None` | Minimum interest rate to fetch listings |
| `countries` | `Optional[List[str]]` | `None` | Filter listings by country of origin |
| `originators` | `Optional[List[str]]` | `None` | Filter listings by originator |
| `loan_types` | `Optional[List[str]]` | `None` | Filter listings by loan type |
| `sort` | `Optional[str]` | `None` | Secondary-market sort key; one of remaining_term/loan_interest/remaining_principal/sale_amount/final_payment_date/sale_valid_until. |
| `ascending_sort` | `bool` | `False` | Sort by ascending order (By default sorts in descending order) |
| `group_guarantee` | `Optional[bool]` | `None` | Restrict listings to only those with a group guarantee |
| `only_my_sales` | `Optional[bool]` | `None` | Restrict listings to only user-owned sales |
| `reduced_price_only` | `Optional[bool]` | `None` | Restrict listings to reduced price sales only |
| `only_at_nominal_value` | `Optional[bool]` | `None` | Restrict listings to nominal value sales only |

- Returns: `SecondaryLoanPage` - A single page of secondary-market listings according to specified parameters
- Accepted values:
  - `sort`: `final_payment_date, loan_interest, remaining_principal, remaining_term, sale_amount, sale_valid_until`
  - `loan_types`: `business, leasing, long_term, real_estate, short_term`
- Potential exceptions: `ValueError`, `TypeError`, `InvalidSort`, `PeerberryException`

### `get_loan_details(...)`

Returns borrower, loan, schedule, and related details for one loan.

```python
api.get_loan_details(loan_id: int) -> LoanDetails
```

| Parameter | Type | Default | Description |
| --- | --- | --- | --- |
| `loan_id` | `int` | `Required` | ID of loan to get details from |

- Returns: `LoanDetails` - The borrower's data, the loan's data, and the repayment schedule
- Potential exceptions: `PeerberryException`

### `get_agreement(...)`

Returns investment agreement bytes for a loan.

```python
api.get_agreement(loan_id: int, lang: str = "en") -> bytes
```

| Parameter | Type | Default | Description |
| --- | --- | --- | --- |
| `loan_id` | `int` | `Required` | ID of investment to get agreement of |
| `lang` | `str` | `"en"` | Language to return agreement in (ISO code, by default "en" for english) |

- Returns: `bytes` - Raw agreement bytes (file payload).
- Potential exceptions: `PeerberryException`

### `purchase_loan(...)`

Creates a purchase order for a selected loan and amount.

```python
api.purchase_loan(loan_id: int, amount: decimal.Decimal) -> PurchaseOrder
```

| Parameter | Type | Default | Description |
| --- | --- | --- | --- |
| `loan_id` | `int` | `Required` | ID of loan to purchase |
| `amount` | `decimal.Decimal` | `Required` | EUR-denominated investment amount sent to API as string. |

- Returns: `PurchaseOrder` - Object containing an order (not transaction) id
- Potential exceptions: `InsufficientFunds`, `PeerberryException`

## Investments

### `get_investments(...)`

Returns current or finished investments with paging metadata.

```python
api.get_investments(quantity: int, start_page: int = 0, max_date_of_purchase: Optional[datetime.date] = None, min_date_of_purchase: Optional[datetime.date] = None, max_interest_rate: Optional[decimal.Decimal] = None, min_interest_rate: Optional[decimal.Decimal] = None, max_invested_amount: Optional[decimal.Decimal] = None, min_invested_amount: Optional[decimal.Decimal] = None, countries: Optional[List[str]] = None, loan_types: Optional[List[str]] = None, sort: Optional[str] = None, ascending_sort: bool = False, current: bool = True) -> InvestmentPage
```

| Parameter | Type | Default | Description |
| --- | --- | --- | --- |
| `quantity` | `int` | `Required` | Amount of investments to fetch |
| `start_page` | `int` | `0` | Number of start page to start getting loans from |
| `max_date_of_purchase` | `Optional[datetime.date]` | `None` | Set maximum date of purchase to fetch loan |
| `min_date_of_purchase` | `Optional[datetime.date]` | `None` | Set minimum date of purchase to fetch loan |
| `max_interest_rate` | `Optional[decimal.Decimal]` | `None` | Set maximum interest rate to fetch loan |
| `min_interest_rate` | `Optional[decimal.Decimal]` | `None` | Set minimum interest rate to fetch loan |
| `max_invested_amount` | `Optional[decimal.Decimal]` | `None` | Set maximum invested amount to fetch loan |
| `min_invested_amount` | `Optional[decimal.Decimal]` | `None` | Set minimum invested amount to fetch loan |
| `countries` | `Optional[List[str]]` | `None` | Filter investments by country of origin (Gets investments from all countries by default) |
| `loan_types` | `Optional[List[str]]` | `None` | Filter investments by type (Short-term, long-term, real estate, leasing, and business) |
| `sort` | `Optional[str]` | `None` | Investment sort key; accepted values depend on current flag. |
| `ascending_sort` | `bool` | `False` | Sort by ascending order (By default sorts in descending order) |
| `current` | `bool` | `True` | Fetch current or finished investments (Set to False to fetch finished investments) |

- Returns: `InvestmentPage` - All current or finished investments according to specified parameters
- Accepted values:
  - `sort` when `current=True`: `estimated_final_payment_date, interest_rate, loan_amount, purchase_date`
  - `sort` when `current=False`: `estimated_final_payment_date, final_payment_date, interest_rate, loan_amount, purchase_date`
  - `loan_types`: `business, leasing, long_term, real_estate, short_term`
- Potential exceptions: `ValueError`, `TypeError`, `InvalidSort`, `PeerberryException`

### `get_mass_investments(...)`

Returns exported investments file bytes.

```python
api.get_mass_investments(countries: Optional[List[str]] = None, current: bool = True) -> bytes
```

| Parameter | Type | Default | Description |
| --- | --- | --- | --- |
| `countries` | `Optional[List[str]]` | `None` | Filter investments by country of origin (Gets investments from all countries by default) |
| `current` | `bool` | `True` | Fetch current investments or finished investments (Gets current investments by default) |

- Returns: `bytes` - Raw export file bytes.
- Potential exceptions: `TypeError`, `ValueError`, `PeerberryException`

## Transactions and Summary

### `get_account_summary(...)`

Returns normalized account summary for a date interval.

```python
api.get_account_summary(start_date: datetime.date, end_date: datetime.date) -> AccountSummary
```

| Parameter | Type | Default | Description |
| --- | --- | --- | --- |
| `start_date` | `datetime.date` | `Required` | Start date of account summary |
| `end_date` | `datetime.date` | `Required` | End date of account summary |

- Returns: `AccountSummary` - Summary of transactions during the specified time period (Invested funds, interest payments, etc.)
- Potential exceptions: `PeerberryException`

### `get_transactions(...)`

Returns typed transactions list with optional filters.

```python
api.get_transactions(quantity: Optional[int] = None, start_page: int = 0, start_date: Optional[datetime.date] = None, end_date: Optional[datetime.date] = None, period: Optional[str] = None, transaction_types: Optional[List[Union[str, int]]] = None, loan_id: Optional[int] = None) -> List[Transaction]
```

| Parameter | Type | Default | Description |
| --- | --- | --- | --- |
| `quantity` | `Optional[int]` | `None` | Amount of investments to fetch (If quantity is not specified it will fetch all investments) |
| `start_page` | `int` | `0` | Number of start page to start getting loans from |
| `start_date` | `Optional[datetime.date]` | `None` | Start date of transaction data |
| `end_date` | `Optional[datetime.date]` | `None` | End date of transaction data |
| `period` | `Optional[str]` | `None` | Optional named transaction range; today/thisWeek/thisMonth. |
| `transaction_types` | `Optional[List[Union[str, int]]]` | `None` | Types of transactions to fetch (named keys or positive integer type IDs) |
| `loan_id` | `Optional[int]` | `None` | Restrict transactions to a specific loan ID |

- Returns: `List[Transaction]` - All transactions according to specified parameters
- Accepted values:
  - `period`: `thisMonth, thisWeek, today`
  - `transaction_types` named keys: `deposit, fees_and_bonuses, interest_payment, investment, principal_repayment, withdrawal`
  - `transaction_types` numeric IDs: any positive integer (for example `14`)
- Potential exceptions: `InvalidType`, `InvalidPeriodicity`, `PeerberryException`

### `get_mass_transactions(...)`

Returns exported transactions file bytes.

```python
api.get_mass_transactions(start_date: Optional[datetime.date], end_date: Optional[datetime.date], transaction_types: Optional[List[Union[str, int]]] = None, period: Optional[str] = None, loan_id: Optional[int] = None) -> bytes
```

| Parameter | Type | Default | Description |
| --- | --- | --- | --- |
| `start_date` | `Optional[datetime.date]` | `Required` | Start date of transaction data |
| `end_date` | `Optional[datetime.date]` | `Required` | End date of transaction data |
| `transaction_types` | `Optional[List[Union[str, int]]]` | `None` | Types of transactions to fetch (named keys or positive integer type IDs) |
| `period` | `Optional[str]` | `None` | Optional named transaction range; today/thisWeek/thisMonth. |
| `loan_id` | `Optional[int]` | `None` | Restrict transactions to a specific loan ID |

- Returns: `bytes` - Raw export file bytes.
- Accepted values:
  - `period`: `thisMonth, thisWeek, today`
  - `transaction_types` named keys: `deposit, fees_and_bonuses, interest_payment, investment, principal_repayment, withdrawal`
  - `transaction_types` numeric IDs: any positive integer (for example `14`)
- Potential exceptions: `InvalidType`, `InvalidPeriodicity`, `PeerberryException`

## Metadata Helpers

### `get_countries(...)`

Returns supported country definitions keyed by display name.

```python
api.get_countries() -> dict
```

- Returns: `dict` - Dictionary of available countries and corresponding metadata.
- Potential exceptions: `PeerberryException`

### `get_originators(...)`

Returns supported originator definitions keyed by display name.

```python
api.get_originators() -> dict
```

- Returns: `dict` - Dictionary of available originators and corresponding metadata.
- Potential exceptions: `PeerberryException`

## Return Type Matrix

| Method | Return Type |
| --- | --- |
| `PeerberryClient(...)` | `Any` |
| `login()` | `str` |
| `token()` | `str` |
| `logout()` | `str` |
| `close()` | `None` |
| `get_profile()` | `Profile` |
| `get_overview()` | `Overview` |
| `get_loyalty_tier()` | `LoyaltyTier` |
| `get_profit_overview()` | `List[ProfitPoint]` |
| `get_investment_status()` | `InvestmentStatusOverview` |
| `get_investment_originators_overview()` | `OriginatorsOverview` |
| `get_loans()` | `List[Loan]` |
| `get_loans_page()` | `LoanPage` |
| `get_secondary_loans()` | `SecondaryLoanPage` |
| `get_loan_details()` | `LoanDetails` |
| `get_agreement()` | `bytes` |
| `purchase_loan()` | `PurchaseOrder` |
| `get_investments()` | `InvestmentPage` |
| `get_mass_investments()` | `bytes` |
| `get_account_summary()` | `AccountSummary` |
| `get_transactions()` | `List[Transaction]` |
| `get_mass_transactions()` | `bytes` |
| `get_countries()` | `dict` |
| `get_originators()` | `dict` |

## Docstring Coverage Snapshot

- Public callable count: `24`
- With docstrings: `24`
- Without docstrings: `0`

## Source of Accepted Value Sets

- Loan sort keys: `peerberry_sdk.constants.CONSTANTS.LOAN_SORT_TYPES`
- Secondary-market sort keys: `peerberry_sdk.constants.CONSTANTS.SECONDARY_LOAN_SORT_TYPES`
- Investment sort keys: `CONSTANTS.CURRENT_INVESTMENT_SORT_TYPES` / `CONSTANTS.FINISHED_INVESTMENT_SORT_TYPES`
- Loan type keys: `CONSTANTS.LOAN_TYPES_ID`
- Profit periodicities: `CONSTANTS.PERIODICITIES`
- Transaction periodicities: `CONSTANTS.TRANSACTION_PERIODICITIES`
- Transaction type keys: `CONSTANTS.TRANSACTION_TYPES`

## Improving Generated Detail

To enrich this page, improve method docstrings in source code with explicit parameter/return/raise details.
