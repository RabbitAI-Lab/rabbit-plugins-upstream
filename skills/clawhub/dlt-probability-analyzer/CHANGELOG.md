# 变更日志 · dlt-probability-analyzer

## 2.1.48 — 2026-09-08（专家模块重构：时效性 + 核实 + 诚实基线）

- **专家数据时效与核实（用户重点反馈）**。原 `dlt_expert_picks.json` 指向 26100（09-02）、`input.txt` 指向 26096（08-24），严重滞后；`_meta` 标注"未经核实"却又把每个专家标记 `verified:true` 且 `scraped_at` 空——自相矛盾。重构为：仅收录 **26102 期（2026-09-07）**、带来源 URL + `scraped_at` + `verified` 真实抓取时间戳的专家；来源限 新浪体育/一定牛/乐彩网 等真实可核验站点，每条号码均经 `WebFetch` 二次核对原样数字一致。
- **诚实基线对比（no_edge 落地）**。新增 `render_expert_baseline_html()`：用系统自追踪的 9 期真实开奖命中数据，自算"专家单期前区均命中 0.770 vs 随机基线 0.704"，差异 +0.067 但单期 SD≈0.72（SE≈0.065，t≈1.0）→ **未达统计显著**，明确"不采信平台自报命中率、方法无预测优势"。报告新增「专家战绩 vs 随机基线」板块。
- **干掉伪专家/无来源条目**。剔除 Bing 伪专家与无来源号码推荐；专家按"可追溯 + 可复核"准入，否则进"未达追踪标准"隔离区。
- **四体同步**：新 `dlt_expert_views_26102.json` + 刷新 `dlt_expert_picks.json` / `dlt_expert_history.json` / `dlt_expert_input.txt`；护栏 24/24 全绿。包 2.1.47 → 2.1.48。

## 2.1.47 — 2026-09-07（最小成本最大收益升级 + 胆拖对账修复）

- **修复：数据滞后静默漏洞（第 24 项闸门曾告警）**。26100/26101 已开奖但 API 限频未抓取，预测文件存在却无开奖可核对。多源交叉核对真实结果回填 `dlt_history.json`（现 2919 期）并诚实回填 `dlt_performance.json`（新增 26100 记录）。
- **修复：胆拖对账恒为 0/0（欠报中奖）**。`verify_prediction()` 兼容 `dantuo={'standard':{dan,...}}` 结构，重核对后 26088/26091/26092/26095/26100 胆拖实际中 **七等奖**，此前误记"未中奖"。
- **升级：最小成本最大收益覆盖优化器（专攻 3-7 等奖）**。`dlt_coverage_optimizer.py` 纯组合覆盖+蒙特卡洛估计，单式 P(七等)≈6.0% → 最优方案（胆拖前2胆×后区2选2，¥20/10注）P(七等)≈12.1%，约翻倍；已集成进报告板块，诚实标注 no_edge、EV 为负（伤害减损）。
- **版本**：模型 V8.9.9（README/bat/WorkBuddy 自动化三处对齐）；包 2.1.46 → 2.1.47。

## 2.1.38 — 2026-08-29（奖金分档正确性 + 排程休眠根治）

- **修复：固定奖奖金虚高 20%~40%（正确性）**。`PRIZE_PAYOUT` 曾把"奖池≥8 亿上调档"金额写死为常态值，导致奖池<8 亿期次的中奖金额与回测 ROI 被系统性高估。改为双档表 `PRIZE_PAYOUT_BASE` / `PRIZE_PAYOUT_BOOSTED`，新增 `set_pool_mode()` / `prize_table()` / `pool_mode_label()`，默认保底档（保守口径）。
- **修复：四处硬编码奖金副本漂移**。`dlt_draw_check.py` / `dlt_cost_effectiveness.py` / `dlt_exhaustive.py` / `dlt_method_explorer.py` 各自维护过奖金表；固定奖现统一委托单一权威源（浮动奖保留现实估计值并注明，避免封顶值虚高 EV 近 3 倍）。
- **修复：排程被休眠冻结导致预测熔断（#22 根因）**。主机睡眠超时仅 300 秒(AC)，自检需 600 秒+ → 冻结 → 900 秒超时 → EXIT=1。`dlt_smart.py` 新增运行期防休眠（`SetThreadExecutionState`，不改全局电源策略/无需管理员），自检超时 900→1800 兜底。
- **修复：档位标注自相矛盾**（曾输出 `5元(奖池≥8亿)`）。标注现与金额同源动态生成。
- **数据**：历史补至 26097（2915 期）；26097 期验证记录用真实预测 JSON + 真实开奖号诚实回填（第5组熵均衡 七等奖 1 注）。
- **验证**：24 项护栏 22/22 正确性全通过、三方交叉验证 51/51、AST 未定义名 0；包 69 文件 671KB。
- 诚实框架不变：任一组合一等奖概率恒 1/21,425,712，任何方法数学上不优于随机，长期玩必亏。

## 2.1.37 — 2026-08-28（首次上架 ClawHub）

- **门面翻转（A）**：SKILL.md 描述/标题从"诚实理性助手·只讲真话·伤害减损"翻转为"大乐透一站看：专家观点 + 娱乐选号 + 开奖即时核对中奖 + 胆拖省钱"；保留诚实声明为内嵌模块，不再首屏劝退。
- **修复首屏错别字**："不不实中奖许诺" → "不承诺中奖"。
- **对话即出报告（C）**：§1 新增"用户口语请求 → 模型直接 `run_dlt.py` + `present_files` 预览"主路径，降低首用门槛（无需用户自己跑命令）。
- **留存说明（D）**：§2 写清大乐透 `v8` 自动化为 PAUSED（单一执行器 = Windows 计划任务 `DLT_V8_Smart`，周一/三/六 20:10），并诚实说明对新用户最顺滑的激活路径是"对话即出报告"。
- **发布就绪**：补 README.md / CHANGELOG.md / LICENSE(MIT-0)；安全扫描自检通过（SKILL.md 无 curl/wget/二进制下载，scripts 无运行时下载外部可执行程序）。
- 诚实框架不变：任一组合一等奖概率恒 1/21,425,712，任何方法数学上不优于随机，长期玩必亏，仅供娱乐、须量力而行。

> 历史逐版变更（2.1.0 ~ 2.1.36）见 `references/changelog.md`。
