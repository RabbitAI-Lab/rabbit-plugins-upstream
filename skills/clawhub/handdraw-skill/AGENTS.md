# HandDraw Engine principles

Build the rendering engine before planners or UI. Keep rendering deterministic, SVG-first, JSON-DSL-driven, modular, and testable. Do not use text-to-video models or raster imagery for draw animation. Validate DSL input before rendering.
