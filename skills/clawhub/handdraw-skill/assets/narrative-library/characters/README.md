# Narrative character library

Use these SVGs as layered character assets in `narrative-sketch` scenes. Keep one character's hair, facial line weight, clothing palette, and body proportions consistent across poses. Add a new pose only when it preserves the same character identity.
