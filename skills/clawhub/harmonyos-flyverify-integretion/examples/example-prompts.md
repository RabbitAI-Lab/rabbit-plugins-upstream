# Example Prompts

- 帮我在 HarmonyOS NEXT 项目里接入 FlyVerify
- 我要在鸿蒙应用里增加秒验一键登录
- 帮我检查这个 HarmonyOS 工程能不能接入 MobTech 妙验
- 给我生成 FlyVerify 的 HarmonyOS 配置模板
- 帮我把 HarmonyOS 秒验的 `build-profile.json5` 和 `module.json5` 改好
- 帮我把 HarmonyOS 秒验的隐私同意回调和登录落点接进去
- 秒验 HarmonyOS 的 `preVerify` 和 `verify` 怎么调用？
- 帮我定制鸿蒙秒验授权页 UI
- 秒验返回 `6119171` 是什么问题？
