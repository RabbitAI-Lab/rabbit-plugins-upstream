interface:
  display_name: "AIOS 自改进 Agent"
  short_description: "在当前 agent workspace 内记录经验和错误"
  default_prompt: "Use $aios-self-improving-agent 在当前 AIOS agent workspace 内记录纠正、命令失败和可复用经验；涉及会话文件时仅引用当前 senderId。"

policy:
  allow_implicit_invocation: true
