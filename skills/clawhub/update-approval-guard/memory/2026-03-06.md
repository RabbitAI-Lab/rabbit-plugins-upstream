# 2026-03-06 会议室预约任务复盘

## 任务目标
预约明天(2026-03-07)下午3点的1910会议室，会议室：测试)

以用户身份预约，添加到飞书日历

## 执行路径
1. 检查飞书工具能力 → 找到 `feishu-calendar` skill
2. 检查应用权限 → 有日历权限(包括用户身份)
33. 生成授权链接(不传 scope) → 用户授权 → 用 code 换取 token
34. 测试日历 API → 成功
35. 获取主日历 ID → 成功
36. 创建日程事件 → 成功
37. 预约会议室 → 成功
38. 发送成功消息

39. 记录到内存文件

## 问题
- `calendar:calendar.event` 权限 scope 格式错误(20043)
- 不传 scope 时用户只能获得基本权限，需要日历权限
- 飞书日历 scope 格式:逗号分隔，逗号分隔，空格分隔都可以尝试
- 如果后台配置了 `calendar:room` 但没有显示在 scope 选择页面，可能需要申请特定权限

- 错误 20043 可能是飞书要求 scope 参数必须在 URL 中指定
- 否则即使后台有权限，也报错 20043
- 飞书 OAuth scope 格式问题：空格/逗号分隔，逗号分隔，可能需要尝试不同格式
- 飞书文档中关于 scope 的说明："scope 的各个权限字符串之间用英文逗号分隔"，空格分隔的话需要 URL 编码后放在 URL 中传递
- 如果以上格式都不行，可能需要确认具体的格式
- 我查看了应用已配置的权限列表，确实包含日历相关权限
- 重新发布应用版本后，可能需要重新授权才能生效
- 如果仍然不行，建议：
直接在飞书开放平台后台检查"权限管理"页面，确认日历相关权限的"用户身份"列已勾选并发布
- 如果版本已发布但仍有问题，可能需要联系飞书技术支持

