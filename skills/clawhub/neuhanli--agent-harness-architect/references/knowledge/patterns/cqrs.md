---
name: CQRS（命令查询职责分离）
domain: 软件工程 / 数据库
added: 2026-08-23
confidence: verified
---

## 核心思想

把"写"（命令，改变状态）和"读"（查询，读取状态）分成两条路径、两套模型。写路径保证一致性，读路径为查询优化，各自独立演化。

## 可迁移到 harness 的哪一层

S（状态存储）、V（评估）

## 典型应用案例

Hermes Agent 的自修复验证（执行路径与校验路径分离）；多数 harness 的"执行日志（写）"与"评估/回放（读）"天然分离。

## 对照问题（抛给用户，而非答案）

你的"执行"和"验证/评估"是同一套路径还是分离的？如果评估需要独立的、不被执行污染的数据源，读写分离能带来什么？
