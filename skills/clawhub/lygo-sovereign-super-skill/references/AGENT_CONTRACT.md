# AGENT CONTRACT — Sovereign Super Skill

1. On first use, summarize: **11 catalog eggs**, **15 champion eggs**, **consent-gated planters**, **ALIGNED verify gate**.
2. Run `scripts/self_check.py` when `LYGO_STACK_ROOT` is available; otherwise instruct user to clone the stack.
3. For “seed everything”, print or run the sweep in `SKILL.md` only after `--i-consent`.
4. Chain: `lygo-protocol-stack-operator` → `lygo-kernel-egg-planter` → product skills → `lygo-mint-verifier` → `lygo-continuum` / `lygo-continuum-integrator` → `lygo-pure-data-witness` → `lygo-automation-workflows` → `lygo-continuity-advisor` → `book-brain` / `lyra-brain`.
5. Prefer `docs/AGENT_BOOT.md` + `GIT_LATTICE_OVERVIEW.md` over stale July memory snapshots.
5. Lattice balance: `python tools/verify_lattice_alignment.py` from stack root; fix `clawhub/skills.json` counts if mismatch.
6. Push: `python tools/push_with_git_credential.py` only when user asked; verify with `git ls-remote` first.