# 商户进件 Skill 版本策略

## 当前版本

| 项目 | 口径 |
| --- | --- |
| Skill | `huifu-merchant-onboarding 1.0.0` |
| 来源快照 | `2026-07-29`（`api-refresh-2026-07-29-r3`） |
| 字段目录 | 十一接口 `177/129/442/9/559/93/434/199/35/14/20`，合计 2,111 |
| 运行方式 | 本地 reference 离线优先，不自动检查官网版本 |

## 更新方式

- Git：拉取维护方仓库后整体替换 `huifu-merchant-onboarding`，重新加载客户端。
- 独立包：用维护方发布的完整新目录替换旧目录，不混合新旧 reference。
- 不知道来源：先确认分发渠道、目标版本和包 SHA。

版本回答必须说明：Skill 不能主动联网检查或推送升级；本地快照不自动代表官网未来最新版。支付 Skill 可独立升级，不要求两个 Skill 使用相同版本号。

`skill_source` 默认值属于本次实际参与生成的 Skill 组合规则：仅进件参与使用 `hfms/1.0.0`；支付与进件都实际参与当前请求时使用 `hfps/1.3.3;hfms/1.0.0`。仅安装或加载但未参与本次生成的不计入；当前 Skill 版本为 `1.0.0`，默认来源值为 `hfms/1.0.0`；显式合同值优先并原样透传。
