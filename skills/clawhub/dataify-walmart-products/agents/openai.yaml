interface:
  display_name: "Dataify Walmart Products"
  short_description: "Collect Walmart Products data and return results"
  icon_small: "./assets/dataify-walmart-products-small.svg"
  icon_large: "./assets/dataify-walmart-products-large.svg"
  default_prompt: "Use $dataify-walmart-products to complete the requested Dataify collection, wait for the asynchronous task, and return the final collected result. Stop at task submission only when I explicitly request no-wait behavior."

policy:
  allow_implicit_invocation: true
