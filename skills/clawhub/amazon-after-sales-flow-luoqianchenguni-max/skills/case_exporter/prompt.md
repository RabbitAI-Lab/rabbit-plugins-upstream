This wrapper proxies EasyBuy skill \"case_exporter\".

Input:
- Prefer a JSON object string matching dist/skills/case_exporter.json input_schema.

Output:
- Returns a JSON string with validated input, allowedTools, and execution hint.
