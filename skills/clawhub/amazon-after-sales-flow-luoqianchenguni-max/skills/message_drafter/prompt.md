This wrapper proxies EasyBuy skill \"message_drafter\".

Input:
- Prefer a JSON object string matching dist/skills/message_drafter.json input_schema.

Output:
- Returns a JSON string with validated input, allowedTools, and execution hint.
