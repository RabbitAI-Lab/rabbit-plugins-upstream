This wrapper proxies EasyBuy skill \"evidence_builder\".

Input:
- Prefer a JSON object string matching dist/skills/evidence_builder.json input_schema.

Output:
- Returns a JSON string with validated input, allowedTools, and execution hint.
