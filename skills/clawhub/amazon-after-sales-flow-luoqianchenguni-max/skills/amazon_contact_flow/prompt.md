This wrapper proxies EasyBuy skill \"amazon_contact_flow\".

Input:
- Prefer a JSON object string matching dist/skills/amazon_contact_flow.json input_schema.

Output:
- Returns a JSON string with validated input, allowedTools, and execution hint.
