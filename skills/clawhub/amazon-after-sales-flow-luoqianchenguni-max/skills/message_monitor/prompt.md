This wrapper proxies EasyBuy skill \"message_monitor\".

Input:
- Prefer a JSON object string matching dist/skills/message_monitor.json input_schema.

Output:
- Returns a JSON string with validated input, allowedTools, and execution hint.
