This wrapper proxies EasyBuy skill \"amazon_price_checker\".

Input:
- Prefer a JSON object string matching dist/skills/amazon_price_checker.json input_schema.

Output:
- Returns a JSON string with validated input, allowedTools, and execution hint.
