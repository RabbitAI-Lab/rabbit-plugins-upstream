This wrapper proxies EasyBuy skill \"order_reader\".

Input:
- Prefer a JSON object string matching dist/skills/order_reader.json input_schema.

Output:
- Returns a JSON string with validated input, allowedTools, and execution hint.
