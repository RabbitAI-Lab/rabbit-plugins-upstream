This wrapper proxies EasyBuy skill \"amazon_orders_opener\".

Input:
- Prefer a JSON object string matching dist/skills/amazon_orders_opener.json input_schema.

Output:
- Returns a JSON string with validated input, allowedTools, and execution hint.
