# LAN setup
1. Install the `lan` binary for your platform.
2. Run `lan networks` to confirm discovery works.
3. If the list is empty, move closer or switch to a network that allows local discovery.
4. You do not need an account; networks are anonymous and local-first.
