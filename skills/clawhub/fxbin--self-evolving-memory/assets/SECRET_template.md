# Secret Handles

> 本文件禁止保存 API Key、密码、Token、私钥或任何可直接使用的凭证。
> 仅保存外部 secret store 的 handle/locator 与脱敏元数据；文件权限必须为 `0600`。
> 本文件不自动加载、不进入 RAG、不进入普通巩固快照。

## Locators
- **用途**：`secret://provider/item` | owner=用户 | rotated_at=YYYY-MM-DD | last4=****
