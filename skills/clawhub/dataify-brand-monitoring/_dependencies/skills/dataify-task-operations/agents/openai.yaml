interface:
  display_name: "Dataify Task Operations"
  short_description: "Wait for Dataify tasks and return final results"
  default_prompt: "Use $dataify-task-operations to monitor this Dataify task and return its final result without resubmitting it."
policy:
  allow_implicit_invocation: true
