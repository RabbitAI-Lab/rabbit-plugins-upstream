Fixes the installed-branch LDM OS dogfood path by making `ldm status` bounded and visible. Status now prints the local installed-state summary before running npm update checks, uses per-check and total update-check timeouts, and reports slow probes as skipped rows instead of blocking the command. This keeps the install prompt from drifting to `ldm install --dry-run` when status is the required primitive.
