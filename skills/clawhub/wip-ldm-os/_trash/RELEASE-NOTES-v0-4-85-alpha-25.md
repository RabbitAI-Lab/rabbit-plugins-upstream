Tightens the LDM OS install prompt after dogfood showed Codex trying to fetch release-note details from GitHub during install-state detection. The install flow now explicitly blocks GitHub release, API, and search lookups unless the user asks for release notes, and it tells agents to report unavailable release notes from local or npm metadata instead of inventing or fetching them.
