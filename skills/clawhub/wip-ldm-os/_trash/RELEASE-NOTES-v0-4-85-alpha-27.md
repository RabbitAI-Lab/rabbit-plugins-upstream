Fixes the alpha.26 `ldm status` smoke failure where every npm probe timed out even though concurrency was active. Status update checks now read npm registry metadata directly instead of spawning one `npm view` child process per package, keeping the bounded concurrency behavior while avoiding slow npm CLI process startup. The status timeout and concurrency tests now use a local registry fixture so they exercise the production probe path.
