# OPC导师Skill互引推荐引擎
## 推荐算法设计文档 v1.0

---

## 一、核心公式

```
推荐得分 = 固定关联权重 × 0.6 + 标签相似度 × 0.3 + 随机因子 × 0.1
```

### 1.1 权重配置（固化，不可随意修改）
| 权重因子 | 值 | 说明 |
|----------|------|------|
| 固定关联 | 0.6 | 人工定义的强关联 |
| 标签相似度 | 0.3 | domain/stage/triggers重叠 |
| 随机因子 | 0.1 | 曝光平衡，避免马太效应 |

### 1.2 附加规则
- **低频Skill曝光提升**: 使用次数<3的Skill +0.2
- **TopK**: 每次推荐返回3个结果

---

## 二、Python实现

```python
"""
OPC Skill互引推荐引擎
调用方式: python recommend.py <current_skill> [top_k]
"""
import json
import random
from datetime import datetime
from typing import List, Dict, Tuple

class OPCRecommendationEngine:
    def __init__(self, network_path: str = "skill_network.json"):
        self.network = self._load_network(network_path)
        self.config = self.network["algorithm_config"]
        self.weights = self.config["weights"]
        
    def _load_network(self, path: str) -> dict:
        with open(path, 'r', encoding='utf-8') as f:
            return json.load(f)
    
    def _calc_tag_similarity(self, skill_a: str, skill_b: str) -> float:
        """计算两个Skill之间的标签重叠度"""
        tags_a = self.network["skills"][skill_a]["tags"]
        tags_b = self.network["skills"][skill_b]["tags"]
        
        # Domain匹配权重: 2.0
        domain_overlap = len(set(tags_a["domain"]) & set(tags_b["domain"])) * 2.0
        
        # Stage匹配权重: 1.5
        stage_overlap = len(set(tags_a["stage"]) & set(tags_b["stage"])) * 1.5
        
        # Triggers匹配权重: 1.0
        trigger_overlap = len(set(tags_a["triggers"]) & set(tags_b["triggers"])) * 1.0
        
        # 归一化
        max_possible = 10.0  # 简化计算
        return min((domain_overlap + stage_overlap + trigger_overlap) / max_possible, 1.0)
    
    def _get_fixed_score(self, current: str, target: str) -> float:
        """获取固定关联权重"""
        fixed_refs = self.network["skills"][current]["fixed_refs"]
        for ref in fixed_refs:
            if ref["skill"] == target:
                return ref["weight"]
        return 0.0
    
    def _random_factor(self, target: str, user_id: str) -> float:
        """随机因子（基于用户ID的确定性随机）"""
        seed = hash(user_id + target + str(datetime.date().weekday()))
        random.seed(seed % (2**32))
        return random.uniform(0.1, 0.3)
    
    def recommend(self, current_skill: str, user_id: str = "default", 
                  top_k: int = 3) -> List[Tuple[str, float, dict]]:
        """
        推荐引擎主函数
        返回: [(skill_name, score, skill_info), ...]
        """
        all_skills = [s for s in self.network["skills"].keys() if s != current_skill]
        results = []
        
        for skill in all_skills:
            # 1. 固定关联得分
            fixed = self._get_fixed_score(current_skill, skill) * self.weights["fixed_ref"]
            
            # 2. 标签相似度得分
            tag_sim = self._calc_tag_similarity(current_skill, skill) * self.weights["tag_similarity"]
            
            # 3. 随机因子得分
            rand = self._random_factor(skill, user_id) * self.weights["random_factor"]
            
            # 4. 最终得分
            total_score = fixed + tag_sim + rand
            
            # 5. 标签相似度也作为"弱关联"加入（如果没有固定关联）
            if fixed == 0 and tag_sim > 0.3:
                total_score += tag_sim * 0.5
            
            results.append((skill, total_score, self.network["skills"][skill]))
        
        # 排序并返回TopK
        results.sort(key=lambda x: x[1], reverse=True)
        return results[:top_k]
    
    def format_qa_recommendation(self, current_skill: str, user_id: str = "default") -> str:
        """格式化Q&A场景的Follow-up推荐"""
        recs = self.recommend(current_skill, user_id, top_k=3)
        
        lines = ["👇 **你可能还想了解**\n"]
        
        for i, (skill, score, info) in enumerate(recs, 1):
            if score >= 0.7:
                emoji, prefix = "🔥", "你可能还想"
            elif score >= 0.4:
                emoji, prefix = "💡", "顺带可以"
            else:
                emoji, prefix = "✨", "要不要试试"
            
            trigger = info["tags"]["triggers"][0] if info["tags"]["triggers"] else info["description"]
            lines.append(f"{i}. {emoji} {prefix}：{trigger} → {skill}")
        
        lines.append("\n*回复编号直接进入对应Skill，或说"换一批"刷新*")
        return "\n".join(lines)
    
    def format_document_recommendation(self, current_skill: str, user_id: str = "default") -> str:
        """格式化文档末尾的推荐模块"""
        recs = self.recommend(current_skill, user_id, top_k=3)
        
        lines = [
            "\n---\n",
            "💡 **OPC能力联动推荐**\n",
            f"*已完成：{current_skill}*\n\n"
        ]
        
        for i, (skill, score, info) in enumerate(recs, 1):
            pricing = info.get("pricing", "free")
            pricing_str = "免费" if pricing == "free" else f"¥{pricing.split('_')[1]}/次"
            
            if score >= 0.7:
                ref_type = "【强关联】"
            elif score >= 0.4:
                ref_type = "【关联】"
            else:
                ref_type = "【探索】"
            
            # 获取推荐原因
            reason = ""
            fixed_refs = self.network["skills"][current_skill].get("fixed_refs", [])
            for ref in fixed_refs:
                if ref["skill"] == skill:
                    reason = ref["reason"]
                    break
            
            lines.append(f"{i}. {ref_type} **{skill}** — {reason}（{pricing_str}）")
        
        lines.append("\n*由OPC推荐引擎驱动 v1.0*")
        return "\n".join(lines)


# CLI入口
if __name__ == "__main__":
    import sys
    
    engine = OPCRecommendationEngine()
    
    if len(sys.argv) < 2:
        print("用法: python recommend.py <current_skill> [top_k]")
        print("示例: python recommend.py 技术尽调 3")
        sys.exit(1)
    
    current = sys.argv[1]
    top_k = int(sys.argv[2]) if len(sys.argv) > 2 else 3
    
    print("=== Q&A场景推荐 ===")
    print(engine.format_qa_recommendation(current))
    print("\n=== 文档场景推荐 ===")
    print(engine.format_document_recommendation(current))
```

---

## 三、嵌入SKILL.md的模块模板

### 3.1 版权声明模块（必须）
```markdown
<!-- OPC-COPYRIGHT-START -->
*======================================*
*  OPC导师Skill矩阵 · 版权所有          *
*  © 2024 OPC导师团队 · 保留所有权利    *
*  商业使用请联系：OPC官方渠道          *
*======================================*
<!-- OPC-COPYRIGHT-END -->
```

### 3.2 推荐引擎调用指令（Q&A场景）
```markdown
<!-- FOLLOW_UP_PROMPT-START -->
## 👇 你可能还想了解

完成当前问题后，根据skill_network.json动态生成3个推荐：

**调用方式**：执行recommend.py，传入当前Skill名称

**输出格式**：
```
👇 你可能还想了解

1. 🔍 [动作短语] → [Skill名]
2. 💡 [动作短语] → [Skill名]
3. ✨ [动作短语] → [Skill名]

*回复编号直接进入，或说"换一批"*
```

**交互规则**：
- 用户回复"1"→加载第1个推荐Skill
- 用户回复"换一批"→重新随机生成
- 用户回复"不需要"→隐藏模块
<!-- FOLLOW_UP_PROMPT-END -->
```

### 3.3 推荐引擎调用指令（文档场景）
```markdown
<!-- DOCUMENT_RECOMMENDATION-START -->
## 📎 报告完成 - 能力联动推荐

在报告末尾自动追加：

```
---

💡 **OPC能力联动推荐**

已完成：[当前Skill]

1. 【强关联】[Skill名] — [原因]（[定价]）
2. 【关联】[Skill名] — [原因]（[定价]）
3. 【探索】[Skill名] — [原因]（[定价]）

*回复编号进入对应Skill*
```
<!-- DOCUMENT_RECOMMENDATION-END -->
```

---

## 四、关联强度标注

| 得分范围 | 标注 | 场景 | 语气 |
|----------|------|------|------|
| ≥ 0.7 | 🔥强关联 | 固定关联+高标签匹配 | 确定性强推荐 |
| 0.4-0.7 | 💡关联 | 固定关联或标签匹配 | 中等推荐 |
| < 0.4 | ✨探索 | 纯标签匹配+随机 | 探索性推荐 |

---

## 五、可扩展性

### 5.1 新增Skill入网
只需更新 `skill_network.json`，无需修改算法代码：

```json
{
  "[新Skill名称]": {
    "name": "[名称]",
    "description": "[描述]",
    "tags": {
      "domain": ["领域"],
      "stage": ["阶段"],
      "triggers": ["触发词"]
    },
    "fixed_refs": [
      {"skill": "[关联Skill]", "weight": 0.8, "reason": "[原因]"}
    ],
    "pricing": "free"
  }
}
```

### 5.2 标签同义词扩展
```json
{
  "tag_synonyms": {
    "domain": {
      "IP": ["知识产权", "商标", "专利", "版权"],
      "投资": ["融资", "资本", "金融", "并购"]
    }
  }
}
```

---

*文档版本: v1.0 | 最后更新: 2024-01-15*
