# Spec: {标题}

> **版本**: v1.0 / **日期**: {today} / **状态**: Draft / **Phase**: N / **Priority**: PX

spec 是"唯一真相源",只回答**做什么**和**为什么做**,不含任何技术实现细节。全文 ≤ 150 行。

## 背景与问题陈述

### 现状痛点
{当前存在什么问题,谁受影响,影响程度}

### 目标状态
{功能上线后期望达到的状态,1-2 句}

## 用户故事

### US-01: {标题}
**作为** {角色},**我希望** {行为},**以便** {价值}。

### US-02: {标题}
**作为** {角色},**我希望** {行为},**以便** {价值}。

## 验收标准

采用 Given-When-Then,每个 Then 必须可测试、可观察。

```
Feature: {功能名}

  Scenario: {正常场景}
    Given {前置条件}
    When  {触发动作}
    Then  {可验证的结果}

  Scenario: {边界/异常场景}
    Given {前置条件}
    When  {异常输入}
    Then  {期望的错误处理}
```

## 非功能需求(按需)

- **性能**:{明确数值,如"文件 ≤ 50MB""P95 响应 < 200ms",避免"要快"这类模糊表述}
- **安全**:{认证/授权/数据保护要求}
- **兼容**:{浏览器/平台/版本约束}

## 排除范围(≥ 1 条)

- **不做** {功能/场景}:{原因}
- **不做** {功能/场景}:{原因}

## 关联

- Constitution: {涉及的原则编号}
- 依赖 spec: {其他 NNN-name,若有}
