# Constitution: {项目名} 项目治理原则

> **版本**: v1.0 / **日期**: {today} / **适用范围**: 全仓库

Constitution 是项目的"宪法",定义不可违反的工程原则和质量红线。每个 spec/plan 都必须验证是否违反本文件。变更需团队审批。

## 核心架构原则

### P-01: {从项目结构推断的原则}
{例:所有业务逻辑集中在 service 层,UI 层不直接访问数据库}

### P-02: {原则}
{说明 + 理由}

## 技术栈约束

### 锁定项(不得替换)
| 层级 | 技术 | 理由 |
|------|------|------|
| 语言 | {从依赖配置提取} | {既有代码基础} |
| 框架 | {…} | {…} |
| 数据 | {…} | {…} |

### 禁止引入
| 技术 | 禁止理由 |
|------|----------|
| {技术} | {与现有栈冲突/维护成本/安全} |

## 质量红线

- **测试**:{覆盖率目标 / 必须有单测的模块}
- **安全**:{认证授权要求 / 敏感数据处理 / 输入校验}
- **代码风格**:{格式化工具 / 命名约定 / 由 linter 强制}
- **文档**:{公共 API 必须注释 / 变更需更新对应 spec}

## 架构边界

- 分层方向:{依赖只能自上而下,禁止反向}
- 模块边界:{哪些模块之间禁止直接调用}

---

> 无法从项目上下文推断的部分保持留空,交用户在初始化审核阶段补充。
