{
  "name": "blog-create",
  "description": "A blog creation skill based on the Tradebee Website Builder Open API. It is used to create and publish a new blog under a specified site language and blog group, with support for publisher, publication date, cover image, tags, summary, and HTML description.",
  "version": "2.0.0",
  "input": {
    "type": "object",
    "properties": {
      "language": {
        "type": "string",
        "description": "Required. Exact site language code to publish into, such as `en` or `fr`. Do not guess or translate it. First call `languages-get`, show the language list to the user, then copy one exact `language` value the user confirms."
      },
      "blog": {
        "type": "object",
        "description": "Required. Complete blog payload for a new blog. Treat this as the blog that will actually be published.",
        "properties": {
          "bloggroup_id": {
            "type": "integer",
            "description": "Required. Blog group ID for the new blog. First call `bloggroup-read`, then copy one exact `bloggroup_id`."
          },
          "publisher": {
            "type": "string",
            "description": "Optional. Publisher name, up to 100 characters."
          },
          "publication_date": {
            "type": "string",
            "description": "Optional. Display publication date in `yyyy/M/d` format, for example `2026/4/24`. If omitted, the server uses the current date."
          },
          "title": {
            "type": "string",
            "description": "Required. Blog title shown to users, up to 500 characters."
          },
          "cover_image": {
            "type": "object",
            "description": "Required. Cover image payload. It must be one {name, base64} object with a valid image not larger than 500 kB.",
            "properties": {
              "name": {
                "type": "string",
                "description": "Image file label used to identify this image inside the payload."
              },
              "base64": {
                "type": "string",
                "description": "Base64 image data with MIME prefix, for example `image/jpeg;base64,...`."
              }
            }
          },
          "tags": {
            "type": "array",
            "description": "Required. Provide 1 to 6 search keyword tags. Each tag must contain 3 to 50 characters.",
            "items": {
              "type": "string",
              "description": "One keyword tag."
            }
          },
          "summary": {
            "type": "string",
            "description": "Required. Blog summary as plain text, up to 500 characters."
          },
          "description": {
            "type": "string",
            "description": "Required. HTML fragment only. Do not include any `<h1>` tag, and prefer `<h2>` to `<h6>`. `<img src>` may use a normal URL or `data:image/...;base64,...`. At most 50 `<img>` tags are allowed. Each single `<img>` image must be 500 kB or smaller. The 100,000-character limit is checked after removing `<img>` tags, and the server uploads supported base64 images and replaces them with URL addresses. Before generating this field, call `rule-get` with the exact selected `language` and the exact scene value `blog.description`. Do not guess, translate, normalize, shorten, or rename either `language` or `scene`. The fragment must follow the full rule payload returned by `rule-get`, including the current tenant structure rules such as starting with one root `<section>` element and placing one embedded `<style>` block at the end of the fragment. Use the embedded `<style>` block required by `rule-get`; do not use any inline style attributes or external stylesheet links."
          },
          "seo": {
            "type": "object",
            "description": "Optional. SEO metadata for search engines.",
            "properties": {
              "title": {
                "type": "string",
                "description": "Optional. Optimized title, up to 90 characters."
              },
              "description": {
                "type": "string",
                "description": "Optional. Optimized description, up to 200 characters."
              },
              "keywords": {
                "type": "string",
                "description": "Optional. SEO keywords as one comma-separated string, with up to 6 keywords and total length 120 characters or fewer."
              }
            }
          }
        },
        "required": [
          "bloggroup_id",
          "title",
          "cover_image",
          "tags",
          "summary",
          "description"
        ]
      },
      "confirmation": {
        "type": "object",
        "description": "Required. Proof that the user explicitly approved this exact blog creation request after seeing the final language and payload.",
        "properties": {
          "approved": {
            "type": "boolean",
            "description": "Must be `true` only after the user explicitly confirms the language and exact blog payload to be created. Never set this to `true` based on assumption."
          },
          "summary": {
            "type": "string",
            "description": "Required confirmation note that restates the language and exact blog payload shown to the user before execution."
          }
        },
        "required": [
          "approved",
          "summary"
        ]
      }
    },
    "required": [
      "language",
      "blog",
      "confirmation"
    ]
  },
  "output": {
    "type": "object",
    "properties": {
      "status": {
        "type": "boolean",
        "description": "Request status. `true` indicates success and `false` indicates failure."
      },
      "msg": {
        "type": "string",
        "description": "Response message"
      },
      "data": {
        "type": [
          "object",
          "null"
        ],
        "description": "Returned result, `null` on failure.",
        "properties": {
          "blog_id": {
            "type": "integer",
            "description": "Created blog ID"
          },
          "url": {
            "type": "string",
            "description": "Preview URL for the created blog. Always return this to the user and ask the user to preview it."
          }
        },
        "required": [
          "blog_id",
          "url"
        ]
      }
    },
    "required": [
      "status",
      "msg"
    ]
  },
  "dependencies": {
    "language": {
      "skill": "languages-get",
      "field": "list[].language",
      "mode": "select"
    },
    "blog.bloggroup_id": {
      "skill": "bloggroup-read",
      "field": "list[].bloggroup_id",
      "mode": "select",
      "depends_on": {
        "language": "language"
      },
      "description": "Select a blog group ID from `bloggroup-read` under the same language."
    }
  }
}
