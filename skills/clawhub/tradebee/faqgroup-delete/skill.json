{
  "name": "faqgroup-delete",
  "description": "A FAQ group deletion skill based on the Tradebee Website Builder Open API. It is used to batch delete one or more FAQ groups under a specified site language and returns separate success and failure ID lists.",
  "version": "2.0.0",
  "input": {
    "type": "object",
    "properties": {
      "language": {
        "type": "string",
        "description": "Required. Enabled site language. It must be selected from `languages-get` after user confirmation."
      },
      "id_list": {
        "type": "array",
        "description": "Required. FAQ group ID list, up to 100 items.",
        "items": {
          "type": "integer",
          "description": "FAQ group ID"
        }
      },
      "confirmation": {
        "type": "object",
        "description": "Required. Records the explicit user approval shown before deleting FAQ groups.",
        "properties": {
          "approved": {
            "type": "boolean",
            "description": "Must be `true` only after the user explicitly confirms the language and exact FAQ group IDs."
          },
          "summary": {
            "type": "string",
            "description": "Required confirmation summary that restates the language and exact FAQ group IDs shown to the user before execution."
          }
        },
        "required": [
          "approved",
          "summary"
        ]
      }
    },
    "required": [
      "language",
      "id_list",
      "confirmation"
    ]
  },
  "output": {
    "type": "object",
    "properties": {
      "status": {
        "type": "boolean",
        "description": "Request status."
      },
      "msg": {
        "type": "string",
        "description": "Response message"
      },
      "data": {
        "type": [
          "object",
          "null"
        ],
        "description": "Returned result, `null` on failure."
      }
    },
    "required": [
      "status",
      "msg"
    ]
  },
  "dependencies": {
    "language": {
      "skill": "languages-get",
      "field": "list[].language",
      "mode": "select"
    }
  },
  "notes": [
    "If a FAQ group still contains FAQs, the API can return failures for those IDs."
  ]
}
