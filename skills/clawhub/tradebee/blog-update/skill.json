{
  "name": "blog-update",
  "description": "A blog update skill based on the Tradebee Website Builder Open API. Before sending the update request, it automatically reads the current blog and writes a local JSON backup file containing the pre-update backup bundle. It then updates an existing blog under a specified site language and supports updating the blog group, publisher, publication date, title, cover image, tags, summary, description, and SEO data.",
  "version": "2.0.0",
  "input": {
    "type": "object",
    "properties": {
      "language": {
        "type": "string",
        "description": "Required. Exact site language code of the blog being edited, such as `en` or `fr`. Do not guess or translate it. First call `languages-get`, show the language list to the user, then copy one exact `language` value the user confirms."
      },
      "blog": {
        "type": "object",
        "description": "Required. Blog fields to update. `blog_id` is mandatory. Every other field is optional and should be omitted unless the user explicitly wants to change it.",
        "properties": {
          "blog_id": {
            "type": "integer",
            "description": "Required. Existing blog ID to update. Do not invent it. Read or obtain the real blog ID first, then copy that exact integer here."
          },
          "bloggroup_id": {
            "type": "integer",
            "description": "Optional. New blog group ID. Omit this field to keep the current group unchanged. Only send it when the user explicitly wants to move the blog to another group. If provided, it must be a positive `bloggroup_id` selected from `bloggroup-read`."
          },
          "publisher": {
            "type": "string",
            "description": "Optional. New publisher name, up to 100 characters. Omit this field if the publisher should stay unchanged."
          },
          "publication_date": {
            "type": "string",
            "description": "Optional. New display publication date in `yyyy/M/d` format, for example `2026/4/24`. Omit this field if the publication date should stay unchanged."
          },
          "title": {
            "type": "string",
            "description": "Optional. New blog title, up to 500 characters. Omit this field if the title should stay unchanged."
          },
          "cover_image": {
            "type": "object",
            "description": "Optional. New cover image payload, up to 500 kB. Omit this field if the cover image should stay unchanged.",
            "properties": {
              "name": {
                "type": "string",
                "description": "Image file label used only to identify this image inside the payload."
              },
              "base64": {
                "type": "string",
                "description": "Base64 image data with MIME prefix, for example `image/jpeg;base64,...`."
              }
            }
          },
          "tags": {
            "type": "array",
            "description": "Optional. New keyword tags, up to 6 items with 3 to 50 characters each. Omit this field if tags should stay unchanged.",
            "items": {
              "type": "string",
              "description": "One keyword tag."
            }
          },
          "summary": {
            "type": "string",
            "description": "Optional. New blog summary as plain text, up to 500 characters. Omit this field if the summary should stay unchanged."
          },
          "description": {
            "type": "string",
            "description": "Optional. New HTML fragment only. Do not include any `<h1>` tag, and prefer `<h2>` to `<h6>`. `<img src>` may use a normal URL or `data:image/...;base64,...`. At most 50 `<img>` tags are allowed. Each single `<img>` image must be 500 kB or smaller. The 100,000-character limit is checked after removing `<img>` tags, and the server uploads supported base64 images and replaces them with URL addresses. Before generating this field, call `rule-get` with the exact selected `language` and the exact scene value `blog.description`. Do not guess, translate, normalize, shorten, or rename either `language` or `scene`. The fragment must follow the full rule payload returned by `rule-get`, including the current tenant structure rules such as starting with one root `<section>` element and placing one embedded `<style>` block at the end of the fragment. Use the embedded `<style>` block required by `rule-get`; do not use any inline style attributes or external stylesheet links. Omit this field if the detailed description should stay unchanged."
          },
          "seo": {
            "type": "object",
            "description": "Optional. New blog detail page SEO fields. Omit the whole `seo` object if SEO should stay unchanged.",
            "properties": {
              "title": {
                "type": "string",
                "description": "Optional. New SEO title, up to 90 characters. Omit this field if unchanged."
              },
              "description": {
                "type": "string",
                "description": "Optional. New SEO description, up to 200 characters. Omit this field if unchanged."
              },
              "keywords": {
                "type": "string",
                "description": "Optional. New SEO keywords as one comma-separated string, with up to 6 keywords and total length 120 characters or fewer. Omit this field if unchanged."
              }
            }
          }
        },
        "required": [
          "blog_id"
        ]
      },
      "confirmation": {
        "type": "object",
        "description": "Required. Proof that the user explicitly approved this exact blog edit request after seeing the final language, blog ID, and payload. That approval must also cover the automatic pre-update backup read and local backup file write before the update request is sent, because the backup file can contain the current blog data and requested update payload. If the backup snapshot cannot be captured or the backup file cannot be written, the update must not run.",
        "properties": {
          "approved": {
            "type": "boolean",
            "description": "Must be `true` only after the user explicitly confirms the language, target blog ID, and exact blog payload to be changed. Never set this to `true` based on assumption."
          },
          "summary": {
            "type": "string",
            "description": "Required confirmation note that restates the language, target blog ID, and exact payload shown to the user before execution."
          }
        },
        "required": [
          "approved",
          "summary"
        ]
      }
    },
    "required": [
      "language",
      "blog",
      "confirmation"
    ]
  },
  "output": {
    "type": "object",
    "properties": {
      "status": {
        "type": "boolean",
        "description": "Request status. `true` indicates success and `false` indicates failure."
      },
      "msg": {
        "type": "string",
        "description": "Response message"
      },
      "data": {
        "type": [
          "object",
          "null"
        ],
        "description": "Returned result, `null` on failure.",
        "properties": {
          "blog_id": {
            "type": "integer",
            "description": "Updated blog ID"
          },
          "url": {
            "type": "string",
            "description": "Preview URL for the updated blog. Always return this to the user and ask the user to preview it."
          }
        },
        "required": [
          "blog_id",
          "url"
        ]
      },
      "backup": {
        "type": [
          "object",
          "null"
        ],
        "description": "Returned automatic pre-update backup information. Includes backup file metadata, the raw read API response, the extracted snapshot, and a best-effort `restore_payload` for restoring the previous blog state. The local backup file can contain sensitive business content from the current record and requested update payload. Uploaded images are only partially restorable because the read API does not return the original image base64 content."
      }
    },
    "required": [
      "status",
      "msg"
    ]
  },
  "dependencies": {
    "language": {
      "skill": "languages-get",
      "field": "list[].language",
      "mode": "select"
    },
    "blog.blog_id": {
      "skill": "blog-read",
      "field": "list[].blog_id",
      "mode": "select",
      "depends_on": {
        "language": "language"
      },
      "description": "Select one existing blog ID from `blog-read` under the same language."
    },
    "blog.bloggroup_id": {
      "skill": "bloggroup-read",
      "field": "list[].bloggroup_id",
      "mode": "select",
      "depends_on": {
        "language": "language"
      },
      "description": "Use this only when moving the blog to another group. Omit this field to keep the current group unchanged."
    }
  }
}
