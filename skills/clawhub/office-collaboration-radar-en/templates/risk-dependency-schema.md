# Risk and Dependency Schema

Required fields: `type`, `description`, `impact`, `mitigation`, `owner`, and `evidence`.

Use `risk`, `blocker`, `cross-functional dependency`, or `external dependency` only when the source supports the classification. Do not invent severity or mitigation.
