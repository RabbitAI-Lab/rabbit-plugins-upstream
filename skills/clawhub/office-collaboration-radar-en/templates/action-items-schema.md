# Action Item Schema

Required fields: `task`, `owner`, `department`, `ddl`, `deliverable`, `status`, and `evidence`.

- Copy `owner` and `ddl` verbatim from the source. Otherwise use `Not provided`.
- P0: a conflict requires immediate human review.
- P1: both owner and deadline are explicitly supported.
- P2: owner or deadline is missing or unsupported.
