interface:
  display_name: "Dataify Google Map Details"
  short_description: "Collect Google Map Details data and return results"
  icon_small: "./assets/dataify-google-map-details-small.svg"
  icon_large: "./assets/dataify-google-map-details-large.svg"
  default_prompt: "Use $dataify-google-map-details to complete the requested Dataify collection, wait for the asynchronous task, and return the final collected result. Stop at task submission only when I explicitly request no-wait behavior."

policy:
  allow_implicit_invocation: true
