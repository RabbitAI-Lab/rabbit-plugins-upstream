# docs/cccc

This directory is the working memory and audit trail for `cc-codex-collaborate`.

It contains project discovery, planning, reviews, decisions, risks, and runtime state.

Recommended practice:

- Commit planning documents if you want an auditable project history.
- Ignore `runtime/` and `logs/` if they contain noisy local execution artifacts.
- Never store real secrets here.
